# LucyCare — Historial de frentes cerrados

> Detalle completo extraído de CLAUDE.md para reducir su tamaño.
> Para el estado VIGENTE, ver CLAUDE.md y
> **`docs/HANDOFF_CHATGPT_LUCYCARE_NUEVA_VENTANA_2026-08-24_PATIENT_CRM_P0.md`**
> (punto de entrada canónico). Los handoffs `2026-08-22` y anteriores quedan
> **históricos**.
>
> ⚠️ Este archivo lista solo frentes **CERRADOS**. **`ADMIN-DOCTOR-SEED-P0`
> (PR #348), `PATIENT-CRM-P0` (PR #349), `CRM-CSV-FECHAS-P0` (PR #350) y
> `ADMIN-DOCTOR-EXPORT-P0` (PR #351) y `ADMIN-DOCTOR-EXPORT-URL-P0` (PR #352)
> ya están cerrados** y figuran abajo.

## Frentes cerrados — detalle por PR (#105–#346)

- **🔤 (#346) CLAIM-COPY-HYGIENE-P0 — "enlace" y sin anglicismo en el reclamo (LIVE, 2026-08-20):** solo copy user-facing de `ClaimProfileModal.tsx`. Diff **simétrico 10/10**: sin lógica, JSX ni estilos.
  - **7 × `link` → `enlace`**, alineado con la decisión de #343 para `/reset-password`: título y descripción de "Recibir enlace por email", destinatario, vigencia de 1 hora, botón de envío, aviso del paso `success` y el error de conexión.
  - **2 × `onboarding` → "configuración inicial"**, redactadas leyendo la oración completa: *"después de una breve configuración inicial"* (paso `license`) y *"hacemos una configuración inicial breve"* (paso `success`).
  - **No se tocaron los comentarios técnicos** donde `link` es terminología interna. `ClaimProfilePromptCard` quedó fuera: su única aparición de `link` es un comentario.
  - QA: cero `link`/`onboarding` user-facing en el flujo; tuteo de #345 intacto; verificado sobre el bundle de producción porque los pasos afectados exigen OTP real.

- **🗣️ (#345) CLAIM-COPY-TUTEO-P0 — tuteo en el flujo de reclamo (LIVE, 2026-08-20):** **28 sustituciones** de copy en `ClaimProfileModal.tsx` (26) y `ClaimProfilePromptCard.tsx` (2). Diff **simétrico 28/28**, sin cambio funcional.
  - Cubre `ERROR_COPY`, los mensajes de los handlers de sesión y el JSX de los pasos `otp`, `license`, `password` y `success`. El CTA de entrada dice ahora **"Reclama tu perfil"** en sus dos variantes.
  - **Tres formas que un barrido por terminación `á/é/í` NO detecta** y que sí se corrigieron: el pronombre *"Si **sos** el profesional"* y los imperativos con enclítico *contactanos* (×3), *Verificala* y *escribinos*.

- **♿ (#344) NOTIFICATION-BELL-A11Y-P0 — teclado y foco en la campana (LIVE, 2026-08-20):** un archivo, `src/components/NotificationBell.tsx`. Sin datos, backend, queries, contadores ni lógica de leído/no leído.
  - **`Escape` cierra** y **devuelve el foco** a la campana. Al cerrar por click fuera con el foco dentro del panel, el foco vuelve al botón — **solo en ese caso**; si el usuario clickeó otro control, ese control lo conserva.
  - **`aria-expanded` + `aria-controls`** con `id` en el panel, y el contador entra al nombre accesible (*"Notificaciones (2 sin leer)"*), con el badge en `aria-hidden`.
  - **`useId()` para el `id` del panel:** `PanelLayout` monta el componente **dos veces** (header móvil + barra desktop) y un id fijo podía duplicarse en el DOM. Verificado forzando ambos paneles abiertos: ids distintos, cero duplicados, y la instancia oculta (`display:none`) no recibe foco.
  - **Deliberadamente sin `role="menu"`/`aria-haspopup`** (obligarían a flechas, Home/End y roving tabindex que el componente no implementa) **ni focus trap** (contradice el patrón de un popover no modal sin overlay).
  - **Deudas registradas, no abiertas:** `setTimeout(markAllAsRead, 800)` sin cleanup y las dos suscripciones paralelas de `usePanelNotifications`.

- **🔐 (#342 + #343) PASSWORD-ERROR-COPY-P0 — errores de contraseña en español (LIVE, 2026-08-18/20):** **un solo frente funcional.** #343 es el ajuste post-cierre del mismo frente, **no un frente separado**.
  - **#342 — el frente.** `setPasswordFromRecovery` devolvía `error.message` **crudo de GoTrue**, así que `/reset-password` podía mostrar inglés al usuario (*"New password should be different from the old password."*). `src/lib/passwordErrors.ts` (nuevo) pasa a ser la **fuente única** del copy: clasifica por **`error.code` → HTTP status → `error.message` solo como fallback de clasificación**, y el texto del proveedor **nunca se muestra**. **`same_password` quedó separado de `weak_password`** (antes todo 422 se reportaba como contraseña débil). `/reset-password` quedó en español y tuteo, con `link` → `enlace`.
  - **#343 — ajuste post-merge.** El A/B del check derivaba su baseline del merge-base con `main`, que **después del merge ya contiene el fix**. Se ancló al SHA fijo **`48ec3b0`** (commit previo a #342), con aserción de que es ancestro de HEAD. **En CI un A/B omitido ya no puede pasar por PASS:** con `CI` seteada y sin historial suficiente el check **falla explícitamente**; en local avisa y lo omite. **El script nunca hace fetch por su cuenta.** Incluyó `Tu link de recuperación…` → `Tu enlace de recuperación…`.
  - Instrumento: `scripts/check-password-error-copy.mjs` → **40/40 PASS**, con A/B que reproduce el bug contra `48ec3b0`.
  - **Ninguno de los dos PRs tocó DB, migraciones, Auth ni configuración.**

- **📄 (#341) Handoff canónico `2026-08-18`:** docs-only. Publicó el handoff que hoy es **histórico**, superado por `2026-08-20`.

- **⚖️ (#340) LEGAL-ENTITY-RENAME-P0 — entidad operadora Valux → Divalux (LIVE, 2026-08-17):** decisión expresa del owner: la empresa responsable/operadora de LucyCare es **Divalux**, y donde corresponde la razón social completa, **Divalux, S.A. de C.V.**
  - **Inventario previo, case-insensitive sobre todo el repositorio:** **5 apariciones en 2 archivos**, todas user-facing. **Cero `Luxvalle`.** Cero apariciones en `docs/`, `migrations/`, `scripts/`, `public/`, `CLAUDE.md`, `index.html`, `middleware.ts` ni configuración — es decir, **ninguna referencia histórica que preservar y ninguna en código o constantes**.
  - **Cambios:** `/terminos` (operador, "relación laboral o societaria con", propiedad intelectual) y `/privacidad` (responsable, responsable del tratamiento). Diff final: **5 insertions, 5 deletions**.
  - **Verificado en producción:** `Valux` vigente = 0 · `Luxvalle` = 0 · `Divalux` = las 5 referencias legales.
  - **NO se tocó** `TOS_VERSION` (`tos-2026-08-13`), aceptaciones históricas, DB, RPC, migraciones, lógica, configuración ni la marca **LucyCare**. El trámite societario es responsabilidad del owner y queda fuera del frente técnico.

- **🔗 (#338 + #339) RATING-URL-P0 — URL corta de calificación (`s7_72`, LIVE 2026-08-14):** la URL que recibe el paciente pasó de **64 caracteres hexadecimales** (dos UUIDv4 concatenados) a **20 caracteres Crockford Base32**, por ejemplo `/calificar/GMNSKTKWPKYK2KACEJFW`.
  - **`s7_72` es la migración 93.** Crea `public._review_short_code()` — 20 caracteres, **100 bits exactos**, desde `gen_random_uuid()`/`pg_strong_random`, descartando los bytes 6 y 8 de cada UUID (bits fijos de version/variant) y mapeando con `byte % 32`, **uniforme por construcción** porque 256 = 8 × 32: sin módulo bias ni rejection sampling. Reemplaza **solo la expresión del token** dentro de `generate_review_token()`.
  - **El helper queda acotado:** `SECURITY INVOKER`, `search_path = pg_catalog`, ACL `postgres=X/postgres` — **sin EXECUTE** para `PUBLIC`, `anon`, `authenticated` ni `service_role`. Lo invoca `generate_review_token()` bajo su contexto `SECURITY DEFINER`.
  - **Retrocompatibilidad:** `submit_review` **no se tocó** y sigue comparando `token = p_token` por igualdad exacta. Los enlaces de 64 caracteres siguen canjeables hasta usarse o vencer (7 días). **Sin backfill.** Sin cambios en el esquema o índices de `review_tokens`, el trigger, la ruta, el frontend, el middleware, el SEO ni Analytics.
  - **Aplicación en transacción explícita** con guardas PRE/POST bloqueantes. Verificación: PRE 16/16 · POST catálogo y permisos 15/15 · **10 000 generaciones, 10 000 únicas**, 32 símbolos, desvío máximo 2.82 % (~2.2σ) · sin backfill · `check-s7_72` **81/81** · `_smoke-s7_72` **5/5**.
  - **⚠️ Dos defectos propios detectados antes y durante la aplicación:** `SELECT * INTO` sobre un JOIN contra un `pg_proc%ROWTYPE` (corregido a `pr.*`), y `has_function_privilege('public', …)`, que **lanza** porque PUBLIC no es un rol real — además `proacl IS NULL` significa **ACL por defecto, o sea EXECUTE para PUBLIC**, de modo que un chequeo ingenuo habría dado un PASS falso en la aserción de seguridad central. El primer intento de aplicar falló con `42883` (`name[] = text[]`: faltaba `attname::text`); **la transacción explícita hizo rollback total y nada quedó a medias**.
  - **#339** corrigió `check-s7_72`, que fallaba 61/63 desde `main` porque git entrega los `.md` con **CRLF** y la regex de extracción esperaba `\n`: el defecto era del instrumento, no del documento. El total sano es **81/81** — el `63` era el conteo de la corrida rota, en la que 18 aserciones de los bloques SQL nunca llegaban a ejecutarse.
  - Guías operativas: `docs/OWNER_S7_72_APPLY.md` (tres pasos con valores esperados) y `docs/OWNER_S7_72_SMOKE.md`.

- **🔑 (#337) LOGIN-FIRST-TIME-COPY-P0 (LIVE, 2026-08-14):** una sola cadena. El login genérico tiene `shouldCreateUser=false` por decisión congelada; cuando apunta a un teléfono sin cuenta, GoTrue responde `otp_disabled` y **no envía SMS**. El copy anterior empezaba con *"No pudimos enviarte un código"*, que se leía como falla de LucyCare, de Twilio o del teléfono del usuario. Nuevo texto: *"¿Primera vez en LucyCare? Reserva una cita para crear tu acceso. Si ya tienes cuenta, verifica tu número e inténtalo de nuevo."* El caso ya estaba **aislado** en `sendOtp`: la rama `otp_disabled` se evalúa después de rate limit y teléfono inválido, y cualquier otro error conserva su propio mensaje. Se preserva el criterio **anti-enumeración**. `check-auth-p1b2a-login-gating` **15/15 PASS**, incluido `login → shouldCreateUser=false`.

- **📄 (#336) Cierre documental post-GO:** reconcilió `CLAUDE.md`, el handoff canónico y este historial con el GO del piloto, el Booking E2E, #334, #335, la higiene de perfiles QA y el safeguard de Twilio. Docs-only.

- **🚀 (2026-08-14) GO/NO-GO DEL PILOTO = GO.** Cero FAIL bloqueantes. PASS en repo/producción, Home/directorio, perfil y Booking de Camilo, Auth con contraseña, recovery por email, OTP real con Twilio Verify, Turnstile, Booking E2E real, "Mis atenciones", panel médico y notificaciones, Legal, perfiles QA públicos y safeguard de saldo de Twilio. Todas las deudas restantes quedaron clasificadas como **WARN no bloqueantes**. **Aclaración del directorio:** 36 `is_published=true` en DB vs 35 en el Home — la diferencia es el Dr. Abraham Alfredo Amaya Mendoza (`fe7f4f9c-…`), cuya clínica no tiene dirección, excluido por la regla **D1 de completitud mínima**; es comportamiento esperado, verificado también con el cliente `anon` (la RLS no oculta a nadie). D1 gobierna el listado pero **no** el sitemap, donde la ubicación es opcional por diseño. Detalle en el handoff §7.

- **📱 (2026-08-14) BOOKING E2E REAL CONTROLADO = PASS.** Validada en producción la cadena `booking_intent → auth_creation_grant → consentimiento OTP → Turnstile → Twilio Verify real → Before User Created Hook → auth.user → contraseña obligatoria → login → patient → appointment → intent consumido`, con **un único SMS real**.
  - **Ruta del grant demostrada por descarte:** en el instante de la creación del `auth.user`, las otras cuatro ramas del hook (`patients`, `doctors`, `clinic_invitations`, `platform_admin_invitations`) estaban **vacías** para ese teléfono. La identidad se verificó **nueva** por timestamps: `auth.users.created_at` a ~1.1 s del grant, y una sola fila histórica en `otp_consent_events`.
  - **Decisiones que se apartaron del diseño congelado**, autorizadas en el momento: **Camilo como médico destino** (override puntual; su identidad, perfil, clínica, servicios, disponibilidad y configuración **no se tocaron**), **sin ensayo previo** con Test Phone. Del pedido de código a la cita: **1 min 37 s** de los 15 minutos de TTL.
  - **Cleanup** en el orden de las FKs (`auth_creation_grants` → `booking_intents` → `appointments` → `patients` → identidad **solo por Admin API**), con denylist previa. **Cero residuos operativos** en las seis superficies. **Preservados:** `otp_consent_events` y `audit_log` — este último **imborrable** tras `s7_71b` (`service_role` solo `SELECT`).
  - **⚠️ Aprendizaje:** un intento anterior con otro número falló porque se entró por el **login genérico** (`context='login'`, `shouldCreateUser=false`) → GoTrue respondió `otp_disabled` **sin enviar SMS**. No fue Twilio ni el hook. El E2E debe entrar **por la reserva**, con servicio, fecha y horario ya seleccionados.

- **🧹 (2026-08-14) HIGIENE FINAL DE PERFILES QA = CLOSED.** El **único** perfil QA público (`QA LucyCare — Perfil médico de prueba`, doctor `ac0ba772-…`) fue **despublicado** por el owner desde LucyAdmin: `is_published=false` fue el **único campo modificado**. Salió de Home/directorio y del sitemap (37 URLs); la URL directa responde `noindex,follow` **sin exponer datos del perfil** (HTTP 200 por ser SPA, que es lo esperado). **La identidad se conserva íntegra** — médico, profile, clínica y `auth.user`. **0 otros perfiles QA publicados**, verificado por barrido de los 36 publicados.

- **📞 (2026-08-14) SAFEGUARD DE SALDO TWILIO = CLOSED.** Cuenta **Paid**, Verify operativo, saldo recargado a **~US$50**, **Balance notification en US$25**, **Auto Recharge OFF por decisión deliberada del piloto**. Costo observado: **~US$0.349** por verificación exitosa de un solo SMS (US$0.05 Verify + US$0.299 SMS El Salvador). **Estado reportado y verificado por el owner en la consola** — no es medible desde el repositorio. **Modelo de consumo derivado del código:** un paciente consume SMS **una sola vez**, al crear su cuenta (la contraseña es obligatoria tras el OTP y las reservas siguientes entran por `signInWithPhonePassword`); el gasto escala con **pacientes nuevos**, no con reservas.

- **👤 (#334) Nombre del paciente nuevo en Booking (LIVE, 2026-08-14):** hallazgo directo del E2E — un paciente creado por OTP nacía con `profiles.full_name` vacío y `completeBooking` caía al fallback `|| user.phone`, así que **la ficha en la clínica del médico quedaba con el número de teléfono como nombre**. Solución en **un solo archivo (`BookingCard.tsx`, +131/−2), sin migración ni backend**: se pide **"Nombre completo"** *únicamente* cuando el perfil no lo tiene (obligatorio, `trim`, máximo 150 = tope `P009I` de la RPC); se guarda **primero** con `updateMyProfile()` y **solo si eso pasa** se llama a `createBookingWithIntent` — si el guardado falla **no se crea la cita** y el horario sigue vigente. Persistirlo en `profiles.full_name` es lo que **evita volver a preguntarlo** (`AuthUser.name` sale de esa columna). **Dos protecciones añadidas tras revisión de edge cases:** el paso se descarta si cambian servicio, fecha u horario (intent obsoleto), y un **guard de concurrencia** compara el `pendingIntentId` después del `await` para no reservar un slot anterior si la selección cambió durante el guardado. La RPC **no se tocó** y sigue sin sobrescribir fichas existentes; `MobileBookingSheet` reutiliza `BookingCard`.

- **📝 (#335) CALIFICACIÓN-COPY-P0 (LIVE, 2026-08-14):** solo copy en `src/pages/calificar/CalificarPage.tsx` (3 líneas). Subtítulo → *"Tu opinión es confidencial y completar la calificación toma menos de un minuto."* · NPS → *"¿Recomendarías a este médico a un familiar o amigo?"* · footer → *"LucyCare · Calificación de la atención"*. Los tres estaban hardcodeados en JSX, **ninguno venía de backend ni DB**. **Sin cambios de lógica, submit, backend, DB, RPC, RLS, Auth, Booking ni SEO.** Verificado en producción: los tres textos nuevos presentes, los tres viejos sin rastro, título/NPS 0–10/CTA intactos.

- **⚖️ (#331 + #332) LEGAL-P0 — eje COMPLETO (LIVE, 2026-08-13):** los documentos legales quedaron publicados e integrados, sin agregar fricción a ningún flujo. Ambos PRs son **frontend puro: sin migración, sin backend, sin SQL**.
  - **PR #331 — publicación (4 archivos, +975/−79):** nueva página `/terminos` (`src/pages/terminos/page.tsx`) + ruta, y `/privacidad` con el documento definitivo. Ambos textos son **fuente autoritativa entregada por el owner** (**Versión 1.0 · última actualización julio de 2026 · entrada en vigor julio de 2026**), reproducidos **sin resumir, parafrasear ni ampliar**; única corrección editorial autorizada: el título de Términos venía como `"érminos y Condiciones de LucyCare"`. Se eliminó el descargo provisional *"versión inicial / será revisado por asesoría legal"*. `/terminos` se agregó a `PUBLIC_EXACT` de `PublicAnalytics`, por paridad con `/privacidad`. **Efecto lateral:** cierra el enlace roto de `ClaimProfileModal`, que hasta entonces caía en `NotFound` aunque la aceptación **sí** se registraba. **Fidelidad verificada por conteo contra la fuente y de nuevo en producción:** Términos 24 secciones / 149 viñetas; Privacidad 19 secciones / 7 subsecciones / 187 viñetas.
  - **PR #332 — integración (2 archivos, +40/−6):** `TOS_VERSION` pasa de `'v1.0'` a **`'tos-2026-08-13'`** en `ClaimProfileModal`, solo para aceptaciones **nuevas**; la constante fluye por el camino que ya existía hasta `claim_doctor_profile`, y `claimProfile.service.ts`, su fallback `?? 'v1.0'`, la RPC y `doctors.tos_version` **no se tocaron**. El identificador es **técnico y no se imprime en la UI**. Copy del claim alineado: nombre publicado ("Términos y Condiciones", antes "Términos de Uso") y **tuteo** en todo el bloque. **Línea legal discreta en `BookingCard`** bajo el CTA — *"Al reservar, aceptas los Términos y Condiciones y reconoces la Política de Privacidad"* — con enlaces a `/terminos` y `/privacidad` en **pestaña nueva**, para no perder el servicio y el horario ya seleccionados. `MobileBookingSheet` reutiliza `BookingCard`: **móvil cubierto sin segunda implementación**, validado en producción a 375×812.
  - **El flujo de reserva NO cambió:** el diff de `BookingCard` **no elimina ni una línea** — son solo adiciones — y `handleBooking`, `completeBooking`, `registerBookingIntent` y `createBookingWithIntent` no aparecen en él.
  - **Decisiones vinculantes del frente:** las aceptaciones históricas `v1.0` **no se reinterpretan ni se migran** (se firmaron contra un documento no publicado) · **`AffiliationRequestModal.CONSENT_VERSION` queda en `v1.0`** porque versiona el **consentimiento LOPD** del lead (`doctor_affiliation_requests.consent_version`, `s7_21`) y **no** la Política de Privacidad — verificado en código antes de decidirlo · **`otp_consent_events` no se usó** y `otp-consent-v1` no se tocó · sin checkbox nuevo, sin modal legal, sin consentimiento adicional.
  - **PR 3 (evidencia explícita del paciente en DB) = DIFERIDO** por decisión del owner: no se agregan columnas a `appointments` ni se toca `create_booking_with_intent`. **No bloquea el piloto.** El análisis previo descartó `booking_intents` como soporte probatorio: TTL de 15 minutos por CHECK, `service_role` únicamente, se consume, y existe **antes** de que haya usuario.
  - **Validación:** `tsc` limpio · `npm run build` verde · `git diff --check` limpio · Vercel Preview y producción **pass** · QA visual del owner PASS en ambos PRs · verificación final de #332 hecha **por código** (sin completar claim real, sin tocar Auth/Turnstile/Cloudflare/DB) · rutas y línea legal revalidadas en `lucycare.app` tras cada merge.
  - **📉 Deuda menor registrada, NO urgente:** el bundle principal quedó en **~666 kB** porque las dos páginas legales viajan en el chunk inicial, siguiendo la convención del router de mantener estáticas las rutas públicas/SEO. El arreglo sería `lazy()` en ambas. **No bloqueante; el owner decidió no hacerlo ahora.**

- **📄 (#330) Cierre documental post-#329:** reconcilió `CLAUDE.md`, el handoff canónico y este historial con el cierre de RECOVERY-EMAIL-P0 / ADMIN-JUNIOR / TESTPHONE-CLEANUP-P0. Corrigió además un error de identidad **preexistente** en `CLAUDE.md`: `50378627694` es **LucyAdmin**, no Camilo (`50378056365`); ambos quedaron listados como identidades protegidas.

- **🔐 (#329) ADMIN-JUNIOR — navegación de `operations_admin` a LucyAdmin (LIVE, 2026-08-13):** un `operations_admin` autenticaba bien pero la UI lo trataba como paciente ("Cuenta personal", sin camino a `/admin`).
  - **Causa:** el acceso a LucyAdmin tiene dos capas y solo una estaba migrada a capacidades. El guard (`AdminOnlyRoute` → `my_lucyadmin_access()`) **ya admitía** cualquier `lucyadmin_access` activo; el **redirect post-login** (`destinationForRole`) y el **menú** miraban únicamente `profiles.role`, que para un `operations_admin` es `patient`. **El backend nunca estuvo mal:** el acceso directo a `/admin` fue **PASS antes de tocar código**, aterrizando en `/admin/medicos`.
  - **PR #329 (frontend, 4 archivos, +61/−8, SIN migración, SIN backend, SIN privilegios nuevos):** `destinationAfterLogin(role)` en `auth.service.ts` — envuelve a `destinationForRole` (sin cambios) y solo cuando el destino por rol es `/` consulta **la misma RPC del guard**, devolviendo `/admin` si `can_access_lucyadmin`; si la RPC falla cae al destino por rol y **nunca bloquea el login**. Aplicado en `LoginModal` (teléfono+contraseña, código+creación de contraseña, email+contraseña) y `ResetPasswordPage`. Ítem **"Ir a LucyAdmin"** en `PatientAccountMenu`, visible solo con `can_access_lucyadmin`; qué sección se ve la siguen decidiendo `AdminLayout` y `RequireOwnerAdmin`.
  - **Checks:** `npx tsc --noEmit` limpio · `npm run build` verde · `git diff --check` limpio · Vercel Preview y producción **pass**.
  - **QA del owner (Preview + producción):** login email+contraseña · redirect automático · `/admin/medicos` · capacidades acotadas de `operations_admin` · "Ir a LucyAdmin" visible · navegación y logout intactos. El incidente de Turnstile durante la QA del Preview fue **hostname no autorizado en Cloudflare**, no código del PR.
  - **⚠️ Aprendizaje de instrumento:** para probar que el Home anónimo **no** ejecuta `my_lucyadmin_access()`, el grabador de red del navegador reportó **0 requests a Supabase** — pero el Home mostraba 36 médicos reales, así que las llamadas sí ocurrían: **no captura fetch cross-origin**. Concluir "cero requests" con él habría sido un **falso PASS**. El instrumento válido fue la **Performance API**, que registró las 6 llamadas reales del Home y **0** a la RPC (verificado en producción).
  - **Frentes cerrados en el mismo cierre (2026-08-13):** **RECOVERY-EMAIL-P0** (recovery real por email PASS end-to-end; el parche era #327, que eliminó la realimentación `TOKEN_REFRESHED → useIdleLogout → refresh() → getSession()`) y **TESTPHONE-CLEANUP-P0** (`50377507479` retirado de Test Phones — solo de la lista, sin tocar cuenta, teléfono, email, contraseña, `profiles`, `lucyadmin_access` ni roles — con login posterior por email+contraseña **PASS**). **Quedan exactamente 2 Test Phones:** `50378056365` (Camilo QA) y `50378626108` (paciente QA).

- **🗓 (#310 / `s7_70` + #311) CANCELACIÓN POR EL PACIENTE — eje COMPLETO (LIVE, 2026-08-03):** el paciente puede cancelar su propia cita desde "Mis atenciones", y el médico ve la cancelación en su panel.
  - **PR #310 — PR-A backend (`s7_70`, migración `s7_70_patient_cancel_appointment.sql`, aplicada en producción; la del repo es byte-idéntica a la ejecutada):** **hardening de `UPDATE` sobre `appointments`** con restricciones de columnas diferenciadas por rol (paciente / médico / asistente, cada uno solo toca su subconjunto) · RPC **`cancel_my_appointment`** con validación de propiedad y de estado · tabla **`appointment_patient_cancellations` append-only** con la evidencia de cada cancelación y su motivo · RPC de lectura **`list_recent_patient_cancellations`** para el panel · **liberación del horario** tras cancelar (el slot vuelve a estar disponible) · **el historial conserva la cita** (no se borra; queda *Cancelada*) · **aislamiento entre clínicas** verificado. **Validación: smoke E2E de backend 65/65, exit 0. Cleanup con cero residuos.** Turnstile permaneció activo durante todo el frente; `service_role` dejó de usarse al terminar.
  - **PR #311 — PR-B frontend (merge commit `df0d3b5`, squash desde `claude/s7-70-frontend` / `b6834ab`; 11 archivos, TODOS bajo `src/`; SIN migración, SIN SQL, SIN DB):** modal de cancelación en "Mis atenciones" (`CancelAppointmentPatientModal.tsx`) con selección de motivo · **el historial conserva la cita cancelada**, visible como *Cancelada* · el botón "Cancelar cita" **desaparece** una vez cancelada · **tarjeta "Cancelaciones recientes"** en el home del panel médico (`RecentCancellationsCard.tsx`), alimentada por `list_recent_patient_cancellations`, con el **motivo** visible · **navegación "Ver cita"** hacia la agenda posicionada en la **fecha correcta** · conteos del panel actualizados.
  - **P1 — densidad móvil (`RecentCancellationsCard.tsx`):** la lista interna usa `max-h-[320px] overflow-y-auto` y desde `sm:` se libera con `sm:max-h-none sm:overflow-visible`. El límite afecta **solo a la lista**: encabezado y descripción siguen siempre visibles; escritorio sin límite; **sin scroll horizontal** en ningún breakpoint.
  - **P2 — objetivo táctil:** el enlace "Ver cita" usa `inline-flex min-h-[44px] items-center px-2 -mx-2`, alcanzando el mínimo de **44 px**, sin cambiar copy, navegación ni lógica.
  - **P3 quedó FUERA por decisión del owner: `NotificationBell` NO fue modificado.** Su cierre con Escape + devolución de foco sigue en backlog (ver el handoff vigente).
  - **Validación técnica de la rama:** `check-s7_70` **219/219** · `npx tsc --noEmit` limpio · `npm run build` verde · `git diff --check` limpio · Vercel Deployment **pass** · árbol limpio · harness eliminado.
  - **QA manual realmente ejecutada (owner, en Preview, con paciente sintético + Dr. Camilo Carrillo):** reserva · cancelación desde "Mis atenciones" · cita conservada en el historial · estado *Cancelada* · desaparición del botón "Cancelar cita" · notificación de reserva · notificación de cancelación · tarjeta "Cancelaciones recientes" · motivo · enlace "Ver cita" · navegación a la fecha correcta · conteos correctos en el panel médico. **Límite explícito: la matriz manual completa NO se ejecutó en todos los escenarios**; el resto de controles (seguridad, estados, aislamiento, errores) está cubierto por el smoke backend 65/65. **No se afirma nada más allá de esto.**
  - **⚠️ Sobre las mediciones de densidad:** se hicieron con un **harness local con mocks** (cinco cancelaciones simuladas) que **fue eliminado y nunca se commiteó**. En los datos reales existe **una sola cancelación**, la que probó el owner. **No crear cancelaciones adicionales** para reproducir esa medición.
  - **Cierre verificado post-merge (read-only, sin crear datos):** rama `claude/s7-70-frontend` eliminada en local y remoto · 0 PRs abiertos · migraciones sin cambios, siguen hasta `s7_70` · #311 no tocó `migrations/` · sin harness, fixtures, scripts temporales ni servidores locales · `scripts/check-s7_70.mjs` y `scripts/_smoke-s7_70.mjs` están **versionados** (vienen de #310, por convención de PR con DB) y no son residuos · checks Vercel **pass**, combined status del merge commit **success** · deployment de producción **success** · `https://lucycare.app/` y el perfil público de Camilo responden **200**. **Validación autenticada post-merge no ejecutada desde el entorno del dev; la QA funcional la hizo el owner en Preview.**
- **(#205–#212) Frente "Slugs públicos + SEO del directorio" COMPLETO hasta Fase 3 PR C — detalle vivo en `docs/ANALISIS_SLUGS_SEO.md`:** URLs amigables `/doctor/:slug` + metadata/OG dinámica para crawlers + robots/sitemap. **#205** docs-only (handoff). **Fase 1 (#208 / `s7_52`)** — columna `doctors.slug` (nullable + UNIQUE parcial + CHECK formato) + `slugify_name` (IMMUTABLE, normaliza tildes/ñ) + `_doctor_next_slug` (advisory lock, sufijo `-N`) + trigger `set_doctor_slug` (solo al publicar, **congela**) + backfill de publicados. **Fase 2 (#209, frontend)** — resolver dual UUID/slug en `fetchDoctorDetail`, tarjetas y "Ver perfil público" emiten slug, `history.replace` UUID→slug (Compartir usa la URL con slug). **Fase 3 (#210 diseño / #211 PR B / #212 PR C, infra Vercel Edge, sin DB):** `middleware.ts` (matcher `/doctor/:path*` + `/sitemap.xml`) inyecta title/description/OG/Twitter/canonical/robots por médico (lógica pura en `og-meta.mjs`, env `SUPABASE_*`||`VITE_SUPABASE_*` público/RLS, nunca service role) + `public/robots.txt` (Disallow `/panel`/`/admin`/`/paciente`/`/reset-password`, no bloquea `/doctor/*`) + `/sitemap.xml` dinámico (35 perfiles elegibles, `<loc>` por slug, 0 UUIDs, 0 no publicados, cache éxito `s-maxage=3600`/degradado `300`). **Decisión de producto vinculante:** ubicación estructurada **NO** es requisito para indexar; criterio único `isSitemapEligible` (publicado+slug+nombre+especialidad+clínica; ubicación opcional) gobierna sitemap **y** `robots` del `<head>` (alineados). No publicados/inexistentes → `noindex,follow` sin fuga; sin copy de verificación si no está verificado; `listed_only` indexable con metadata honesta. **Validado en producción `lucycare.app`.** **Pendiente NO iniciado:** PR D (JSON-LD `Physician` + canonical hardening + OG branded 1200×630) · Fase 4 (landings especialidad/ubicación) · Analytics. (Frentes intermedios #206 header CTA púrpura de marca, #207 docs Fase 0 SEO — ver git log.)
- **(#188–#204) Frentes recientes (post-#187) — todos frontend-only o docs-only, SIN migración nueva (`s7_51` sin cambios):**
  - **#188/#189 docs-only:** cierre de #187 + reconciliación de la referencia del handoff vigente.
  - **#190 — fix overflow del botón "Congreso/Capacitación"** en la grilla "Tipo de bloqueo" (`BlockForm.tsx`): `min-w-0` + `break-words` + zero-width space tras la "/" (display-only); sin desborde a 360/390.
  - **#191 — selector de hora propio en "Nuevo bloqueo de agenda"** (`BlockForm.tsx`): los `<input type="time">` → dos `<select>` (hora/minuto cada 5'), valor interno sigue `HH:mm`; evita el reloj nativo de Android descuadrado.
  - **#192 — mapa condicional del perfil público** (`doctor-detail/page.tsx`): el iframe de Google Maps solo si `isUsefulAddress(addressLine)`; sin dirección útil pero con muni/depto → link discreto "Ver zona aproximada en Google Maps"; sin nada → "Sin ubicación". No hay lat/lng; gate explícito sin fail-open. (No se reintrodujo `license_number`.)
  - **#193 — placeholders Depto/Municipio del modal "Soy médico"** (`AffiliationRequestModal.tsx`): "—" → "Elegí departamento" / "Elegí municipio" / "Elegí departamento primero".
  - **#194/#195 docs-only — Pagos SaaS (DISEÑO, sin código):** refresh + §14 pricing configurable APROBADO (mensual **$59.00** IVA incl. / anual **$601.80** −15% / asiento adicional **$6.00**mes·**$61.20**año; centavos; **no hardcodear**, tabla futura `subscription_plan_prices`), §15 autoservicio/customer portal, §16 LucyAdmin Billing/reportería/conciliación, §17 separación Lucy SaaS vs LucyAdmin Billing. **Bloqueado por validación de pasarela (§4.5) + Q1–Q11; pago ≠ verified ≠ claimed.** Todo en `docs/ANALISIS_PAGOS_SAAS_MEDICOS.md`.
  - **#196 — foto del médico** (`AvatarUploader.tsx` + `doctor-detail/page.tsx`): se quita `capture="user"` → el médico puede subir **desde galería** (no se fuerza cámara); + se muestra el **avatar/iniciales** en el header del perfil público (antes no se veía).
  - **#197 — privacidad: `license_number` fuera del payload público** (`directory.service.ts` + `directory.types.ts`): `fetchDoctorDetail` ya **no** trae el JVPM al cliente (no se renderizaba; nadie leía `DoctorDetail.licenseNumber`). Flujos admin/claim/afiliación intactos.
  - **#198 docs-only — credenciales JVPM/NUE:** `docs/ANALISIS_CREDENCIALES_MEDICAS.md`. Hoy solo existe `license_number` (= JVPM/licencia base); **NO existe NUE** ni modelo por tipo ni verificación por credencial; `is_verified` es global. Modelo futuro recomendado: tabla **`doctor_credentials`** (type JVPM/NUE/OTHER · status pending/verified/rejected · is_public · audit). **No mostrar números; el número nunca al payload público.**
  - **#199 — copy "Verificado por LucyCare"** en el badge (`doctor-detail/page.tsx` detalle = texto completo; `DoctorCard.tsx` tarjeta = "Verificado" + `title`/`aria-label`). Solo verificados; sin afirmar JVPM/NUE.
  - **#200 — branding público emerald:** púrpura `#3C2285` (+ gradiente `to-purple-50`) → emerald/teal en `home/page.tsx`, `doctor-detail/page.tsx`, `LoginModal.tsx`, `ReviewsSection.tsx`. Color-only. (Pendiente menor: índigo del stat card del panel + botón púrpura `AdminAffiliationDetailModal` interno; semánticos "Fusionada"/"Congreso" se dejan.)
  - **#201 — logo local (fuera de `static.readdy.ai`):** `public/lucycare-logo.png` (PNG actual, 1200×505) + constante `src/lib/brand.ts` `LUCYCARE_LOGO_SRC`; 11 referencias reemplazadas en 6 archivos vivos. **Favicon NO migrado** (logo apaisado; sigue `/vite.svg`) → pendiente: ícono cuadrado de marca.
  - **#202/#203/#204 — cleanup de dead code (delete-only):** **#202** borró `src/data/svLocations.ts` (catálogo SV hardcodeado sin importadores; la fuente viva es DB vía `useDirectory`). **#203** borró el clúster legacy de registro médico (`DoctorRegistrationModal.tsx` + `DoctorInterestModal.tsx` + `doctorRegistration.service.ts`; reemplazado por `AffiliationRequestModal`, el flujo "Soy médico" actual). **#204** borró el **scaffold anidado completo `lucycare-code/`** (38 archivos; era el esqueleto inicial sin uso, sin secretos, que duplicaba dead code; 0 referencias en build/tsc/scripts/CI). `AffiliationRequestModal` y la fuente DB-backed de ubicación quedan intactos.
  - **Backlog actualizado (post-#204):** **YA HECHO** (borrar del backlog) → branding emerald público (#200), logo local (#201), cleanup `svLocations`/registro legacy/`lucycare-code/` (#202–#204), favicon de marca (#220), **Branding PR-1: Home público hacia paleta oficial morado/menta/gris (#222)**, **Branding PR-2a: perfil público visible `/doctor/*` (#229)**, **Branding PR-2b-1: modales públicos simples (`WaitlistModal` + `AffiliationRequestModal`) (#231)**, **Branding PR-2b-2: modales auth/claim (`LoginModal` + `ClaimProfileModal`) (#233)**, **Branding follow-up: `DoctorAvatar.tsx` avatar compartido fallback menta+morado (#235) — con esto el REBRAND PÚBLICO queda 100% COMPLETO por fases (#222 Home, #229 perfil, #231 modales simples, #233 modales auth/claim, #235 avatar compartido)**. **YA HECHO también** → **Perf P2 code-splitting (#284)** · **limpieza de dependencias muertas `recharts`/`@stripe` (#286)**. **Vigente** → **credenciales `doctor_credentials`: F1-a ✅ (#291/`s7_61`) y F1-b ✅ (#292/`s7_62`) LIVE; sigue F1-c — NO INICIADA, no abrir sin instrucción.** **F1-c es la que CIERRA el riesgo residual de `s7_60`.** ⚠️ **Lo que sigue es un PLAN PRELIMINAR, sujeto a preflight y a rediseño — NO un alcance aprobado. NO hay autorización para tocar código, DB, grants, triggers ni para eliminar la columna;** el alcance real se define al abrir el frente, tras un análisis read-only. Dirección prevista (a validar, no a ejecutar): retirar el fallback de los lectores, re-emitir el claim sin fallback, cortar el dual-write (drop del trigger `trg_sync_license_to_credential`), revocar `SELECT (license_number)` a `authenticated`, dropear `doctors.license_number`, + actualizar los scripts que la leen (`import-doctors.mjs`, `check-s7_13/21/22`, `setup-test-doctor-prb`). **Antes de cualquier DROP: verificar la sincronía columna ↔ credencial** (no dropear con drift). Después: F2 (UI LucyAdmin para verificar/rechazar), F3 (propuesta del médico), F4 (señal pública **sin número**), F5 (reportería). Ver `docs/ANALISIS_CREDENCIALES_MEDICAS.md` §0 (no mostrar JVPM/NUE; copy "Verificado por LucyCare" ya live) · **Perf — segundo paso opcional: vendor splitting con `manualChunks`** (separar React/supabase/react-query del entry de 623 KB; retorno menor que #284) · perf menor (lazy loading de avatares, evaluar Pacifico) · branding interno menor (índigo panel + púrpura admin) · decidir semánticos "Fusionada"/"Congreso" · token de marca · próximo-slot-real en la tarjeta · limpieza de ramas locales `claude/*` viejas (solo con autorización expresa) · Search Console/SEO operativo (owner, manual). Pagos: solo diseño (bloqueado por pasarela). **`doctor_credentials` y todo lo demás: no abrir sin instrucción.** **Las menciones a `lucycare-code/` en este doc son históricas: el árbol fue ELIMINADO en #204.**
- **Backlog nuevo (post PILOTO-P0, ninguno bloquea el piloto — no abrir sin instrucción):** **(1) Entregabilidad de correo (SPF/DKIM/DMARC + plantilla).** Gmail marcó el correo de recuperación con una advertencia de "posible mensaje peligroso"; el correo **llegó** y el flujo funcionó, pero la advertencia daña la confianza justo en el paso de recuperar la cuenta. Frente propio: revisar los registros DNS en Cloudflare y la plantilla de Supabase/Resend. **(2) Correo verificado del paciente como canal secundario de recuperación.** Hoy el paciente depende exclusivamente del teléfono, y el cambio de teléfono está **suspendido** mientras el CAPTCHA esté activo → quien pierda el número queda sin camino self-service (§I del handoff: la recuperación es manual por soporte). **(3) Razón genérica "Otro motivo" en `cancel_reasons`.** El catálogo no la tiene y el modal **exige** elegir una razón antes de confirmar, así que un caso no contemplado obliga a elegir una categoría inexacta — **contamina el dato de cancelaciones** que consume `admin_conversion_summary` (`s7_53`). El campo libre "Detalle adicional" es opcional y no sustituye la razón. **(4) Revisión de `cancel_reasons` como tabla legacy.** **No está versionada en `migrations/`** (viene del esquema inicial, igual que los grants que destapó `s7_60`): antes de tocarla hay que revisar schema, grants y policies reales, y entrar **por migración versionada, nunca por SQL ad hoc**. **(5) UX del widget de Turnstile en móvil** — funcional y sin overflow (QA-17 aprobada), pero con bastante protagonismo y altura; mejora cosmética, no bloqueante.
- **Fuente de verdad:** `origin/main` en GitHub (github.com/amedelvalle/lucycare).
- **HEAD esperado:** `cd965ea031dcefe0b7a0e114e7ce06ade0a64c04` o posterior. **PRs #1–#311 mergeados.** **Migraciones APLICADAS hasta `s7_70`** (`main == origin/main`, árbol limpio, 0 PRs abiertos). **Punto de entrada VIGENTE: `docs/HANDOFF_LUCYCARE_NUEVA_VENTANA_2026-08-03.md`** — autocontenido: estado técnico post-#311, eje de cancelación por el paciente (#310/`s7_70` + #311), QA manual ejecutada y sus límites, **Turnstile ACTIVO en producción y configurado en Preview**, siguiente frente **AUDIT-SEC-P0** (no iniciado), **TWILIO-P0** pausado, backlog vigente, objetivo comercial y reglas operativas. (Todos los handoffs anteriores quedan **históricos**: el `2026-07-30` para el eje Auth #304/#305/#306, `s7_69` y el cierre de PILOTO-P0 (#308); el `2026-07-22` para AUTH-P1B1B/`s7_66`; el `2026-07-18` para credenciales F1-b/F1-c; y el `2026-07-14` como mejor referencia del eje clínico F1–F7 y de SEO/Perf.)
- **🔎 PREFLIGHT F1-c (read-only, análisis de código — SIN DB, SIN `service_role`):** **veredicto = NO APTO para un F1-c de un solo PR con DROP.** El bloqueante es **`admin_approve_and_create_doctor` (`s7_42`, vigente), que INSERTA `license_number` en `doctors`** → dropear la columna **rompe la creación de médicos desde afiliación**, y exige re-emitir sus **430 líneas** (justo el riesgo que F1-a evitó usando un trigger). Además siguen dependiendo de la columna: el **fallback** del claim (`s7_62` §5) y del frontend (`resolveJvpm`/`columnFallback` en panel y receta) · el **trigger `trg_sync_license_to_credential`** (si se corta **antes** de que el approve escriba la credencial, **los médicos nuevos nacen sin credencial**) · `import-doctors.mjs` (la **escribe**) · `setup-test-doctor-prb.mjs` · `check-s7_13.mjs` · los smokes `s7_60/61/62` · `database.types.ts`. **NO dependen** (otra tabla, `doctor_affiliation_requests`): toda la UI/servicios de afiliación y la licencia **tipeada** del claim. **Vistas:** ninguna de `migrations/` referencia la columna (sus `SELECT *` son de `reviews`). **Grants:** `authenticated` conserva `SELECT` mientras la columna exista — **y ese acceso residual desaparece SOLO con el DROP** (no hace falta revocar por columna). **Propuesta PRELIMINAR, NO autorizada y NO iniciada:** **F1-c1** retiro lógico reversible (approve escribe la credencial · claim sin fallback · frontend sin `columnFallback` · actualizar `import-doctors` · cortar el dual-write; la columna queda viva pero muerta en uso) → **F1-c2** DROP físico irreversible. **PENDIENTE ANTES DE DECIDIR ALCANCE:** (a) el **SQL de sincronía** columna ↔ credencial que ejecuta el **owner** (valores esperados = hipótesis, **no confirmados**), y (b) el **preflight de DB** (`pg_depend`, `pg_views`, triggers/índices/constraints/defaults reales del **schema inicial**, que no está versionado) — requiere autorización específica de `service_role` read-only. **No abrir F1-c1 ni F1-c2 sin instrucción.**
- **🔑 (#291 / `s7_61`) `doctor_credentials` F1-a — MODELO (LIVE):** primer paso del cierre estructural del riesgo residual de `s7_60`. **El problema de fondo:** `doctors.license_number` es un **secreto viviendo en una tabla legible por fila** (la RLS de `doctors` filtra filas, no columnas), así que un grant de columna se filtra a todo el rol. El arreglo **no** es parchear permisos: es **mover el secreto** a una tabla cuyas **filas** solo ve su dueño y el owner admin. Crea enums `credential_type` (`JVPM`/`NUE`/`OTHER`) + `credential_status` (`pending`/`verified`/`rejected`), la tabla **`doctor_credentials`** (`value` + `value_normalized` GENERATED —espejo exacto de la normalización de `s7_13`—, `is_public` inerte hasta F4, `rejection_reason`, `verified_by`/`verified_at`), **RLS por fila** (`is_admin() OR doctor_id = get_user_doctor_id()`; `directory_editor` NO ve credenciales), grants (`anon` **sin acceso**; `authenticated` SELECT filtrado por RLS, **sin INSERT/UPDATE/DELETE** — nadie escribe directo), **índice antifraude parcial** `UNIQUE(type, value_normalized) WHERE type IN ('JVPM','NUE') AND status <> 'rejected'` (el `status <> 'rejected'` es **crítico**: sin él, un JVPM ajeno **rechazado** bloquearía para siempre al dueño legítimo — el antifraude protegería al fraude), **audit con el `value` REDACTADO** (`to_jsonb(NEW) - 'value' - 'value_normalized'` + booleano `value_changed`; el patrón habitual habría metido el JVPM en texto plano en `audit_log`), **backfill de 114** credenciales JVPM en `pending` (nunca existió verificación por credencial; `is_verified` global es otra cosa y no se toca), y **trigger de sync** `doctors.license_number → doctor_credentials` (dual-write; se eligió trigger y **no** dual-write dentro de la RPC de afiliación porque esa RPC **no es el único escritor** —también `import-doctors.mjs` y scripts con `service_role`— y re-emitir `s7_42` (430 líneas) arriesgaba regresión a cambio de menos cobertura; precedente: `sync_phone_to_identity`/`s7_34`). **Aditiva, sin lectores nuevos.** check `check-s7_61` 5/5 + smoke `_smoke-s7_61` 22/22 (incl. F11/F12: `verified`/`rejected` + cambio de licencia → reset a `pending` limpiando los sellos; F3: re-escribir la misma licencia no genera churn ni fila de audit). **Fix durante la aplicación:** `audit_log.user_id` es NOT NULL y `auth.uid()` es NULL en el SQL Editor/`service_role` → el audit atribuye al **sujeto** (`COALESCE(auth.uid(), doctors.profile_id)`) y marca el origen en **`actor_source`** (`session`/`system`) para no fingir una acción humana; sin eso `s7_61` habría roto `import-doctors.mjs`.
- **🔑 (#292 / `s7_62`) `doctor_credentials` F1-b — CUTOVER DE LECTORES (LIVE):** mueve la **lectura** del JVPM desde la columna hacia `doctor_credentials`, con la columna como **fallback temporal**. Dos `CREATE OR REPLACE` (mismas firmas, retrocompatibles): **(1) `claim_doctor_profile`** — cuerpo **verbatim de `s7_13`** salvo la sección 5 (verificado con diff: solo esa sección + 2 variables nuevas; `s7_13` es el único `CREATE OR REPLACE` del claim en `migrations/`, las menciones en `s7_24`/`s7_42`/`s7_60`/`s7_61` son comentarios). Valida la licencia tipeada contra la credencial, **sin devolver nunca el valor**; `P0001`–`P0005`, OTP, TOS, roles, vinculación y audit **idénticos**. **(2) `sync_license_to_credential`** — mapea **solo** la colisión de `doctor_credentials_registry_uniq` a un P-code propio y estable, **`P0091`** (vía `GET STACKED DIAGNOSTICS … CONSTRAINT_NAME` — ojo: el item es `CONSTRAINT_NAME`, **sin** prefijo `PG_`); **cualquier otro `unique_violation` se re-lanza intacto**. **Frontend (3 archivos):** `getMyDoctorProfile` (panel) y `getConsultationContext` (receta) leen el embed `doctor_credentials(value, type, status)` y resuelven con el helper compartido **`resolveJvpm`**; `mapCreateError` traduce `P0091` a copy claro en la bandeja de afiliación. **`RecetaPrint` NO se tocó.** **REGLA VINCULANTE de lectura:** credencial **`pending`/`verified` → se usa**; **`rejected` → NO se usa y NO cae a la columna** (existe una fila, su estado manda); **fallback a `doctors.license_number` SOLO si no existe fila JVPM** (nunca para evadir un estado). **NO cierra el riesgo residual** — la columna, el dual-write, el fallback y el grant de `authenticated` **siguen vivos**: eso es **F1-c**. Validación: `check-s7_62` 2/2 · `_smoke-s7_62` **E2E 13/13** (R1/R2 embed+RLS · C1a `claim(typed=COLUMNA)`→`P0007` / C1b `claim(typed=CREDENCIAL)`→éxito, el **desync** que prueba de qué tabla lee · J1 `rejected`→`P0006` aunque la columna coincida · J2–J5 regla+datos · P1 duplicado→`P0091` / P2 otra constraint→23505 propio) · **QA visual 7/7 en superficies reales** (panel y receta × `pending`/`verified`/`rejected`/fallback, con valores distintos por fuente para distinguirlas a simple vista) · cleanup **0 residuos operativos**, Test Phone liberado, `doctor_credentials` en **114**, **auditoría append-only preservada** (no se borra: es inmutable por diseño).
- **🔒 (#289 / `s7_60`) SEGURIDAD — hardening de grants sobre `public.doctors` (LIVE, aplicada por el owner):** `doctors` conservaba el **`GRANT ALL` del schema inicial** (Sprint 1-2, anterior a `migrations/`, que arranca en `s4_01`). `profiles` recibió esta cirugía en `s7_16`; **`doctors` nunca la recibió** y el Security Gate (#35) no lo cazó. **Tres vectores, los tres cerrados:** **(1) fuga de credenciales** — `anon` tenía SELECT sobre TODAS las columnas, incluida `license_number`; la RLS (`doctors_select_public`) filtra **filas, no columnas**, así que `GET /rest/v1/doctors?select=license_number&is_published=eq.true` con la anon key (pública por diseño) devolvía el JVPM de todos los publicados. **El fix de #197 sacó el campo del payload de la app; eso nunca fue una barrera de seguridad.** **(2) auto-verificación** — `authenticated` tenía UPDATE sobre todas las columnas y `doctors_update` es `USING (profile_id = auth.uid())` con `WITH CHECK` nulo (hereda el USING); como **`is_verified` es GENERATED de `lucy_status`** (`s7_03`), un médico podía `PATCH {"lucy_status":"verified"}` sobre su propia fila y quedar con el badge "Verificado por LucyCare" **sin LucyAdmin**; también reescribir su `license_number` (el dato contra el que valida el claim) y auto-habilitar `is_operational`. **(3) médico falso** — `doctors_insert` es `WITH CHECK (profile_id = auth.uid())` y un INSERT solo exige `clinic_id`+`profile_id` (el `clinic_id` sale del directorio público) → cualquier autenticado **sin fila de `doctors` (o sea, cualquier paciente)** podía insertarse como médico verificado y publicado. Existe `UNIQUE(profile_id)`: acota el vector a pacientes, no lo cierra. **Fix = cirugía de GRANTS, no de producto** (patrón de `s7_16`): `anon` → SELECT de **16 columnas públicas** (fuera: `license_number`, `is_operational`, `tos_accepted_at`, `tos_version`), sin INSERT/UPDATE; `authenticated` → SELECT completo (sin cambios) + **UPDATE acotado a 8** (`bio`, `specialty_id`, `experience_years`, `consultation_fee`, `languages`, `is_published`, `booking_enabled`, `updated_at`), **sin INSERT**. **La lista salió de leer el código:** `specialty_id` **se incluye** (PerfilPage lo manda en cada guardado; revocarlo rompía "Guardar") y `education` **se excluye** (nadie lo escribe en todo el repo). **NO tocó** RLS/policies (filtran filas y están bien) · `is_verified` · `lucy_status` · el claim · las RPCs admin (`SECURITY DEFINER` → corren como owner, ajenas a los grants) · datos · esquema · `src/`. Verificado con **A/B del mismo instrumento**: `check-s7_60` **2/6 → 6/6** y `_smoke-s7_60` **13/25 → 25/25** (los 12 fallos previos reproducían los tres vectores). **Los scripts no usan `service_role`**: prueban comportamiento con la anon key + sesión de médico real, y **todas las pruebas filtran por un UUID inexistente** — el privilegio de columna se evalúa al planificar, ANTES de filtrar filas, así que `42501` vs. cero-filas distingue los dos estados **sin leer ni modificar un dato real** (ningún JVPM cruzó la red; cero fixtures). Prod validada sin regresión (directorio 35 publicados, perfil, booking con slots reales, panel, JVPM del panel visible, 0 errores de consola).
- **⚠️ RIESGO RESIDUAL VIVO (PR-B, DIFERIDO — no abrir sin instrucción):** **`authenticated` conserva `SELECT (license_number)`.** Los grants de columna son **por rol, no por fila**, y la RLS deja ver las filas de los publicados → **cualquier usuario con cuenta puede leer la licencia de un médico publicado**. `s7_60` bajó el público de *"cualquiera en internet"* a *"cualquiera con cuenta"*; no lo cerró. **Severidad medida: moderada, no crítica.** `license_number` **no es solo un número de registro: es uno de los DOS factores del claim** (`s7_13` exige teléfono OTP-verificado que coincida —`P0005`— **y** licencia tipeada —`P0007`—), y **36 de los 37 médicos publicados están `listed_only`** (sin reclamar; solo Camilo está `verified`) → para esos 36 el factor licencia queda anulado. **Pero NO habilita el robo de perfil por sí solo: el factor teléfono sigue en pie.** **Decisión (post-#289, análisis read-only): PR-B NO se implementa como parche de grants/RPC.** Razones: **(a)** revocar la columna obliga a `REVOKE SELECT` de tabla + re-`GRANT` de las otras 19 columnas (**en Postgres no se puede revocar una columna de un grant de tabla** — sería un no-op silencioso), lo que instala un **impuesto permanente**: cada columna nueva de `doctors` hay que otorgarla o el panel se rompe en silencio (no es hipotético: `s7_52` agregó `slug`); **(b)** una RPC `my_doctor_license()` self-scoped **no alcanza para la receta**, que necesita la licencia del **médico de la consulta**, no la del **caller** — hoy coinciden solo porque la RLS de `consultations` es doctor-scoped (`s7_26`), una equivalencia accidental que, si un asistente accediera, imprimiría la licencia equivocada = **bug de corrección clínica/legal, peor que el problema de privacidad**; **(c)** **F1 lo demolería**: el diseño de `doctor_credentials` ya manda que el `value` nunca salga del servidor. **El fondo: `license_number` es un secreto viviendo en una tabla legible por fila; el arreglo correcto es sacarlo de ahí, no parchear permisos de columna.** → **el cierre estructural se plegó a `doctor_credentials`** (ver `docs/ANALISIS_CREDENCIALES_MEDICAS.md` §0). **ESTADO (post-#292):** **F1-a ✅** (`s7_61`, el secreto ya vive en una tabla con RLS por fila) y **F1-b ✅** (`s7_62`, panel/receta/claim ya LEEN de ahí). **Pero el riesgo residual SIGUE ABIERTO:** la columna `doctors.license_number` **existe todavía**, con dual-write y fallback activos, y `authenticated` **conserva su `SELECT`**. **Lo cierra F1-c** (retirar fallback + cortar dual-write + revocar el grant + DROP de la columna) — **no iniciada, no abrir sin instrucción**. La idea de un PR-B de parche queda **definitivamente descartada**: F1-a/F1-b ya construyeron el camino correcto.
- **(#286–#288) Frentes menores post-#285 (frontend/deps, SIN migración):** **#286 — limpieza de dependencias muertas (delete-only):** `recharts@3.2.0` y `@stripe/react-stripe-js@4.0.2` fuera de `dependencies` (0 imports en `src`/`scripts`/config; único dependiente en el lock = la raíz; los dashboards `/admin/analytics` y `/admin/analytics/farma` son KPI cards + tablas, **sin librería de gráficos**). `@stripe/stripe-js` **no estaba en `package.json`**: entraba como *peer* y se fue sola. Salen **46 entradas del lockfile** (~11 MB): recharts 3 arrastraba todo `d3-*`, `victory-vendor` y **Redux completo**. También sale de `.env.example` la línea `VITE_STRIPE_PUBLISHABLE_KEY` (**ningún archivo del repo la lee**). **Delete-only: no toca una línea de código** (`src/types/database.types.ts` tiene `stripe_payment_id`, pero es el **nombre de una columna de DB** en tipos generados, no un import → intacto). **Verificado con A/B de build: el bundle post-remoción es byte-idéntico** (mismos 46 chunks, mismos hashes de contenido, entry 623.10 kB / **180.29 kB gzip**) → prueba que **nunca entraban al bundle** (Vite ya las excluía por tree-shaking); el ahorro es de instalación/mantenimiento, **no de bundle**. `npm ci` limpio + tsc + build verdes + `_smoke-og-meta` 33/33. **#287 — receta corregida sobria y apta para impresión en grises** (`RecetaPrint.tsx`, solo `className`): el bloque "RECETA CORREGIDA" (borde `teal-600` + fondo `teal-50` + 14px **bold MAYÚSCULAS**) leía como alerta y **comunicaba con color** en un documento que se imprime en B/N — en grises el `teal-50` colapsa contra el papel (**luminancia 0.957**). Pasó a nota sobria: borde `gray-300` 1px, fondo blanco, 12px semi-bold `gray-900` (17.7:1), secundario 11px `gray-500` (4.8:1). El badge por medicamento (`· CORRECCIÓN V2` teal bold mayúsculas) → gris `font-medium`. **Franja teal superior y label "Receta médica" conservados** (marca de la hoja, no comunicación de corrección). **#288 — receta corregida MINIMAL** (`RecetaPrint.tsx`): la hoja corregida acumulaba **seis señales de lo mismo** (`FECHA DE EMISIÓN`+fecha, `CORREGIDA`+fecha, el bloque, y `· Corrección v2` **pegado al nombre del medicamento** — el peor lugar para sembrar una duda: quien lee la receta está leyendo **qué tomar**). Ahora: **UNA sola fecha** (la de la corrección, que es la vigente) + una línea gris de 11px "**Receta corregida**". El rótulo pasa de `FECHA DE EMISIÓN` a **`FECHA`** *solo* si hubo corrección (llamarla "de emisión" sería inexacto); **la receta normal no cambió** (HTML renderizado **byte-idéntico**, 3.601 chars, mismo `sha256`). Se eliminan el bloque inferior y el distintivo por medicamento. **La fecha de emisión original ya NO se imprime en recetas corregidas** — decisión explícita del owner; el historial vive en `consultation_amendments`/`prescriptions.version`/`audit_log`. **Sin cambios de lógica** en #287/#288: mismas condiciones de render, mismo versionado, mismas fechas; `hidden print:block` intacto. **Frentes no clínicos recientes (frontend/asset, SIN migración): #282 SEO PR-D1 (JSON-LD `Physician` en `/doctor/*` indexables + `noindex,follow` en `/calificar/*` vía middleware; Home mantiene `Organization+WebSite`; nunca datos sensibles), #283 SEO PR-D2 (OG cover branded `public/lucycare-og.png` 1200×630; `OG_COVER_PATH` para og:image de Home/genérico/fallback-sin-avatar; `ORG_LOGO_PATH=/lucycare-logo.png` sigue como `Organization.logo`; perfil con avatar usa su avatar), #284 Perf P2 (route-level code-splitting: `/panel/*`·`/admin/*`·`/paciente/*`·`/calificar`·`/reset-password` lazy; Home/`/doctor/*`/`/privacidad`/`NotFound`/guards estáticos; `<Suspense>` en `App.tsx`; bundle inicial 313→180 KB gzip / 1→46 chunks; verificado en prod por Network; SIN `manualChunks`). Toda la lógica SEO vive en `og-meta.mjs` (pura) + `middleware.ts` (Edge) + smoke `_smoke-og-meta.mjs` (33/33).**

**EJE CLÍNICO F1–F7 — CERRADO (#272–#279).** Serie nacida de una auditoría read-only de integridad de formularios clínicos (post-#269). Detalle completo en el handoff vigente. **(F1 / #272)** `duration_value` vacío en receta de borrador persiste como **`NULL`**: el `undefined` se perdía al serializar el UPDATE, la DB conservaba la duración vieja y **la receta impresa la seguía mostrando**. Semántica: `undefined`=no tocar · `null`=borrar · número=guardar. **(F2 / #273)** **No se puede firmar con guardados clínicos en vuelo o fallidos**: receta/diagnósticos/antecedentes autoguardan en `onBlur` con `mutate()` sin `onError`, y si la firma les ganaba la carrera el UPDATE rebotaba contra la inmutabilidad (`s7_28`) → el último campo editado **se perdía en silencio**. Las 9 mutaciones clínicas comparten `mutationKey` (`consultationKeys.clinicalWrite`); el hook **`useClinicalWrites`** expone `isPending`/`hasFailed` (render) e **`isBlockedNow()`** (lectura **síncrona** del cache — el `click` de "Firmar" corre **~130 ms antes** de que React re-renderice el botón, así que el guard de render **no alcanza**). Un guardado **fallido** también bloquea (ese cambio no está en la DB), con salida: **"Reintentar"** o corregir el campo; el error se limpia **por fila** (`clearFailedClinicalWrite`), no globalmente. **(F3 / #274)** Notas de diagnóstico/antecedentes ya **no revierten** al valor viejo tras el `onBlur` (patrón **`boundIdRef`**, el mismo de receta en #269): el efecto re-sembraba desde props todavía viejas. Medido A/B: sin fix la nota revertía ~2.5 s; con fix, 0 transiciones. `AntecedenteRow` = fix **latente** (hoy se monta `readOnly`). **(F4 / #275)** El **texto clínico escrito mientras "Guardar borrador" está en vuelo ya no se pierde**: el refetch (nuevo `updated_at`) rehidrataba el form y borraba lo tipeado tras el click (medido: **631 ms**), sin haberlo persistido nunca. `seededConsultationRef` + `hasLocalEditsRef`; el flag se apaga tras un guardado exitoso **solo si el form no cambió** mientras guardábamos (así el modo lectura **sí** se rehidrata tras un amend). Cambiar de consulta **siempre** resiembra → no se arrastra texto (la ruta es la misma, el componente **no** se desmonta). **(F5 / #276)** **Vitales:** "Guardar borrador" llamaba **siempre** a `upsertVitals` con el objeto completo → creaba filas de `vitals` **enteramente en `NULL`** que, al ser las más recientes, **ocultaban los últimos vitales reales del paciente** en el resumen (mostraba `—`), y reescribía `recorded_at`/`recorded_by` en cada guardado de texto. **Regla vinculante del gate: guardar vitales SOLO si el form difiere del snapshot sembrado** (`vitalsSnapshotRef`) — **NO** vale "hay fila o hay valores". Borrar todos los campos de una fila existente **sigue funcionando** (acción explícita → `NULL`). Bonus: ya no se arrastran vitales entre consultas. **Verificado: el rol `assistant` NO puede leer ni escribir `vitals` desde `s7_26`** (gate doctor-scoped) → el riesgo de "pisar la captura de la asistente" **no aplica**; y **no existe política `vitals_delete`** (la app no puede borrar filas de vitales). **(Limpieza manual post-F5, ejecutada por el owner)** Inventario read-only: **7 filas `vitals` vacías** heredadas (mayo 2026, entorno demo de Camilo; 1 paciente con medición real oculta). Borradas por el owner en el SQL Editor con **doble candado** (`id IN (<7 ids>)` **Y** los 9 campos clínicos en `NULL`) + `RETURNING`. Verificación posterior: **0 vacías**, **12 reales intactas**, **0 pacientes con vitales ocultos**, consultas firmadas intactas, panel sin errores. ⚠️ **El SQL Editor corre cada ejecución en su propia sesión: un `BEGIN; DELETE …;` separado del `COMMIT` hace ROLLBACK automático** (el primer intento no borró nada) → borrados manuales **en una sola corrida**. **(F6 / #277 / `s7_58`)** **La corrección post-firma ya puede BORRAR de verdad** en la receta: el bloque de receta de `amend_consultation` decidía por el **valor** y no por la **presencia de la clave** (`COALESCE(NULLIF(...), viejo)`) y el modal **omitía** `duration_value` al vaciarlo → el médico borraba la duración, se creaba la adenda, se versionaba (**v2**), se marcaba **"RECETA CORREGIDA"**… y **la duración vieja sobrevivía**: *el sistema afirmaba haber corregido algo que no corrigió, con sello de corrección*. Ahora `duration_value`/`duration_unit`/`dosage`/`frequency`/`instructions` quedan realmente en **`NULL`** (no `''`), y **`duration_unit='permanente'` fuerza `duration_value = NULL` server-side**. Verificado: `check-s7_58` + **`_smoke-s7_58` 28/28** (el mismo smoke daba **8 fallas** contra la RPC vieja, reproduciendo el bug) + UI en preview sobre **fixture propia firmada**. Intacto: gates, versionado (`is_current`/`replaces_id`/`version`), snapshots, `consultation_amendments`, `affects_prescriptions`, `audit_log`, diagnósticos/antecedentes/vitales. **`alternatives` NO se tocó (= F7).** Sin backfill. **(F7 / #279 / `s7_59`)** **Alternativas terapéuticas ya corregibles post-firma:** era el último campo de receta incorregible — en `replace` se arrastraba tal cual (`v_old.alternatives`, no editable) y en `add` **ni figuraba en el `INSERT`** (un medicamento agregado en una corrección nunca podía llevar alternativas), pese a que `RecetaPrint` **sí las imprime** → unas alternativas equivocadas en una receta firmada eran **incorregibles**. `s7_59` (`CREATE OR REPLACE`, misma firma, **copia byte a byte de `s7_58` con dos puntos de diff**): `replace` pasa a `CASE WHEN v_op ? 'alternatives' …` (misma regla de presencia de clave) y `add` agrega la columna `alternatives` al `INSERT`. Frontend: campo "Opciones alternativas" en el editor del modal (existentes y nuevos), la clave viaja siempre (`null` al borrarla). **`RecetaPrint` NO se tocó** (ya imprimía alternativas). Verificado: `check-s7_59` + **`_smoke-s7_59` 27/27** (el mismo smoke daba **4 fallas** contra la RPC vieja `s7_58`, e incluye **no regresión de F6**) + UI en preview sobre fixture propia firmada (modificar → borrar → agregar con alternativas; v1/v2 históricas, v3 vigente `alternatives=null`). Intacto: gates, versionado, snapshots, adendas, `audit_log`, y toda la semántica de nulls/`permanente` de F6. Sin backfill. **→ Con F7 el eje clínico F1–F7 queda CERRADO: la corrección post-firma corrige fielmente TODOS los campos de receta.**

**⚠️ REGLA TÉCNICA VINCULANTE — `amend_consultation` (y todo cambio futuro de la RPC, aplica a TODOS los campos de receta desde `s7_59`):** **clave ausente = conservar** el valor anterior · **clave presente con `null` o `''` = limpiar (`NULL`)** · **clave presente con valor = actualizar**. Implementar con `CASE WHEN v_op ? 'campo' THEN NULLIF(...) ELSE v_old.campo END`; **NUNCA `COALESCE(NULLIF(...), viejo)`**, que decide por el valor y hace **imposible borrar**. Ese patrón ya era el de vitales/diagnósticos/antecedentes; `s7_58` alineó la receta (que era la excepción) y `s7_59` cerró el último campo, `alternatives`. **El frontend debe expresar la intención:** mandar la clave con `null` cuando el médico borró el campo — **omitir la clave significa "no tocar", no "borrar"**.

**Pendientes clínicos (NO abrir sin instrucción):** **(F7 CERRADO en #279 / `s7_59` — ver arriba.)** **F8 — optimistic updates / refactor estructural: DIFERIDO conscientemente** (decisión post-#280 tras análisis read-only; **no es un pendiente abierto, es una decisión tomada — no reabrir por inercia**). El repo tiene **cero `onMutate` en `src`** (toda mutación clínica es `invalidate → refetch`), pero **F1–F7 ya instalaron las protecciones locales suficientes** por sección (receta guards/`dirty`/`null`-intent · diagnósticos y antecedentes `boundIdRef` · texto `seededConsultationRef`+`hasLocalEditsRef` · vitales `vitalsSnapshotRef` · firma gate de clinical writes · correcciones RPC atómica). **No queda bug clínico visible ni reproducible por flujo humano normal**; el riesgo residual es una carrera extrema/no-humana (~60 ms) ya mitigada por `boundIdRef`. Optimistic updates mal hechos introducirían **su propia clase de bugs de cache/rollback** en el eje más delicado = **alto riesgo, bajo retorno**. **Solo se reabre si** hay un reporte real de pérdida/reversión por concurrencia **o** el volumen por consulta crece y el refetch degrada UX/perf. **Si se reabre:** nunca global; **un PR por sección empezando por `useUpdatePrescription`**; validar rollback `onMutate`/`onError`/`onSettled`; fixture de borrador (no firmada); sin DB/SQL. **Con F8 diferido, NO queda pendiente clínico activo — los siguientes frentes deben ser no clínicos salvo bug real.** **Frentes recientes post-#263 (todos frontend/docs, SIN migración nueva; `s7_57` sin cambios):** **(#266/#267)** SEO Brand — JSON-LD `Organization`+`WebSite` (`areaServed=El Salvador`) + Home `/` en el sitemap + title comercial del Home (`LucyCare El Salvador — Encuentra al médico perfecto para ti`), solo `og-meta.mjs`+smoke, validado en prod. **(#268)** Rediseño visual de la receta impresa/PDF (`RecetaPrint.tsx` + `index.css`): tarjeta limpia, franja teal, encabezado médico/fecha, bloque gris del paciente, tabla de medicamentos con indicaciones en fila ancha, **footer/firma + datos duplicados eliminados**, receta corregida preservada, `print-color-adjust: exact` scoped; sin DB/backend/lógica clínica. **(#269)** Fix del race de receta (`PrescriptionsSection.tsx`): dosis/frecuencia/duración/unidad/indicaciones/alternativas **ya no se borran** al cambiar de campo — el estado local se siembra solo al cambiar `p.id` (`useRef`), sin rehidratar desde props tras cada guardado; sin tocar receta impresa/DB/backend/firma/correcciones/versionado. **(#270)** Manejo de error de sesión/cuenta (`auth.service.ts` + `PanelLayout.tsx`): distingue **sesión no recuperable** (`SessionExpiredError` vía `isAuthError`) de **conexión temporal/timeout** — "Tu sesión venció → Ingresar nuevamente" (limpieza dura, sin loop) vs "No pudimos conectar → Reintentar"; salida robusta `hardLocalSignOut` (borra `sb-*`+`lc_last_activity`, no depende de que `signOut()` resuelva); sin DB/`auth.users`/config Supabase/roles/SEO/Analytics. **(#263) Editor de directorio — FRONTEND LimitADO (PR-B) LIVE:** capa UX sobre `s7_57` (frontend-only, sin DB/SQL/migraciones/`database.types.ts`/backend/`auth.users`) para que un usuario con `lucyadmin_access` nivel `directory_editor` use LucyAdmin de forma acotada. Hook `useLucyAdminAccess` (consume `my_lucyadmin_access()`) + service `directoryEditor.service` (`directory_list_doctors`/`directory_get_doctor_detail`/`directory_update_doctor_name`). `AdminOnlyRoute` ahora permite entrar a **owner O nivel con capacidad**; **`RequireOwnerAdmin`** envuelve las secciones owner-only (dashboard/afiliaciones/catálogos/lista-espera/pacientes/administradores/analytics/farma) → un nivel acotado se **redirige a `/admin/medicos`**. `AdminLayout` filtra el nav (editor ve **solo "Médicos"**; badge de afiliaciones gateado a owner). `AdminDoctorsPage`/`AdminDoctorEditPage` = **dispatcher por nivel** → vistas acotadas `DirectoryDoctorsList`/`DirectoryDoctorEdit` (**sin** login email/phone, avatar, verificación/`lucy_status`/`is_operational`/`booking_enabled`, lista de espera, borrar servicio, destructivas). `AdminDoctorServicesSection` con prop `canDelete` (editor=false). **Owner Admin (`profiles.role='admin'`) conserva TODO sin cambios.** **El borde real de seguridad sigue siendo el gate server-side de cada RPC** (`s7_57`); esta capa es UX. Validado: `tsc`+`build` verdes + preview owner (nav 9 ítems, Dashboard, tabla completa) y `directory_editor` (`/admin`→`/admin/medicos`, nav solo Médicos, 7 rutas owner-only redirigen, listado/ficha acotados, móvil 375 sin overflow, consola 0 errores) — sesión de editor vía grant temporal a un test phone, **revocado 0 residuales**, ficha de Camilo (demo) read-only, **jamás Katherine**. 11 archivos, todos `src/`. **`operations_admin` sigue diseñado pero INERTE** (sin RPCs ni pantallas operativas; PR-C futuro). **Pendiente NO iniciado:** paso operativo de otorgar acceso a la persona real = **Test Phone OTP fijo + `INSERT` en `lucyadmin_access`**, solo cuando el owner lo autorice. **(#261 / `s7_57`) Niveles de acceso administrativo LucyAdmin (PR-A backend/DB) — LIVE:** acceso admin **por niveles**, aditivo y de mínimo privilegio, **sin tocar `is_admin()` ni el enum `user_role`**. **Modelo:** `owner_admin` = `profiles.role='admin'` + `is_admin()` (**NO vive en la tabla**, superset absoluto — las ~95 superficies admin históricas intactas) · `directory_editor` y `operations_admin` = filas en **tabla nueva `lucyadmin_access`** (`profile_id` PK · `access_level ∈ {directory_editor, operations_admin}` · `is_active` · `granted_by/at` · `revoked_at` · `notes` · **RLS admin-only**). Niveles anidados por rank (`directory_editor(1) ⊂ operations_admin(2) ⊂ owner_admin(∞)`); toda capacidad = `is_admin() OR (fila activa con rank ≥ umbral)`. **Funciones:** `can_access_lucyadmin()` · `can_manage_directory()` · `can_manage_doctor_operations()` · `can_manage_lucyadmin_users()` (owner-only) · `my_lucyadmin_access()` (snapshot para el guard/nav de PR-B) + helpers `_lucyadmin_rank`/`_lucyadmin_actor`. **Re-gate quirúrgico `is_admin()`→`can_manage_directory()`** de las RPCs de datos **públicos** del médico: `admin_update_doctor_clinic` (nombre/dirección/tel. público/depto/muni) · `admin_update_doctor_info` (especialidad/bio) · `admin_set_doctor_published` (**verificado: toca solo `is_published`**) · `admin_list_doctor_services`/`admin_create_service`/`admin_update_service`/`admin_set_service_active`. **RPCs nuevas acotadas:** `directory_list_doctors` + `directory_get_doctor_detail` (**sin email/phone de login**) + `directory_update_doctor_name` (**solo `profiles.full_name`, jamás `auth.users`**). **Audit diferenciado** `edited_via` = actor efectivo (`admin`/`operations_admin`/`directory_editor`). **`directory_editor` PUEDE:** ver listado/ficha del médico, corregir nombre visible/especialidad/bio/clínica/dirección/depto-muni/tel. público, servicios públicos (crear/editar/activar, **no** hard-delete), publicar/despublicar (solo `is_published`). **`directory_editor` NO puede** (todo owner-only, verificado por smoke con denegación): `auth.users`/email-phone de login/`admin_update_doctor_profile` · roles · `admin_set_lucy_status`/`is_verified` (GENERATED de `lucy_status`; `admin_set_doctor_verified` fue dropeada en `s7_03`) · `is_operational` · `booking_enabled` · avatar (RPC **+ Storage RLS**) · `admin_delete_service` (hard-delete) · pacientes/merge · consultas · recetas · diagnósticos · lista de espera · afiliaciones · administradores · Analytics · Analytics Farma · pagos/facturación · ownership clínico. **`operations_admin` = DISEÑADO pero INERTE:** hereda directorio (herencia limpia) pero **ninguna RPC operativa se le abre en este PR** (sin superficie operativa adicional → PR-C futuro decidirá). **Analytics y Analytics Farma siguen owner-only.** Verificado: `check-s7_57` 15/15 + `_smoke-s7_57` 43/43 (matriz admin/editor/operations/inactivo/normal + sin fuga de login creds + audit por actor + fixtures aisladas, cleanup 0 residuales, jamás Katherine/datos reales). **`database.types.ts` actualizado** (tabla + 8 firmas). **Backend/DB only — NO se tocó frontend.** **PR-B (frontend: guard por nivel + nav filtrado + `/admin/medicos` acotado + ficha sin secciones sensibles + preview) queda PENDIENTE, no iniciado.** **(#259 / `s7_56`) Índices de soporte para Analytics/Farma — LIVE:** migración **additive-only** (solo `CREATE INDEX IF NOT EXISTS`, sin lógica/RPC/UI/RLS/`database.types.ts`) que agrega **7 índices** preventivos de performance sobre las columnas calientes que consumen los dashboards admin de solo-lectura (`s7_53` conversión + `s7_54` farma). **Criterio rector:** *"la analítica debe observar la operación, no competir con ella"* — sin estos índices, a escala (1.000+ médicos) los seq scans de los dashboards competirían por IO/conexiones con login/reserva/panel. Índices: `idx_prescriptions_consultation` (JOIN prescriptions→consultations) · `idx_consultations_signed` parcial `WHERE status='signed'` (filtro de fecha farma) · `idx_audit_claim_self_service` parcial `WHERE new_data->>'edited_via'='claim_self_service'` (la peor query —claims del summary de conversión— sobre `audit_log`, la tabla de mayor crecimiento) · `idx_prescriptions_medication` · `idx_prescriptions_current` parcial `WHERE is_current` · `idx_appointments_created` · `idx_reviews_created`. Verificado por **P0 read-only** contra `pg_indexes` de la DB real (huecos reales: `prescriptions`/`audit_log` solo tenían PK; `consultations`/`appointments`/`reviews` sin índice sobre las columnas de fecha/join analíticas; `waitlist_entries`/`doctor_affiliation_requests` ya bien indexadas → intactas). `CREATE INDEX` normal (no `CONCURRENTLY`) por volumen bajo. **Aplicado en Supabase + verificado en DB real** (los 7 con predicados parciales correctos) + **smokes verdes sin regresión** (`_smoke-s7_53.mjs` 32/32, `_smoke-s7_54.mjs` 27/27 — agregados idénticos, los índices no cambian resultados). **NO** creó `check-s7_56.mjs` (fingiría verificar `pg_indexes`, inaccesible vía supabase-js). **NO** cambió `min_count=1` de Farma; **sin** refactor de claims fuera de `audit_log` (opción B = follow-up separado); **sin** tocar código/frontend/RPC/RLS/auth/UI/`database.types.ts`/Analytics V2/materialized views/Search Console/SEO/Slugs/Brand-Favicon. **Punto de entrada VIGENTE: `docs/HANDOFF_CHATGPT_LUCYCARE_NUEVA_VENTANA_2026-07-03_POST_PR220.md`** (el `2026-06-21` queda histórico). **(#220) Favicon de marca:** `index.html` apuntaba a `/vite.svg` inexistente → favicon roto (el rewrite servía el HTML del SPA); ahora `public/favicon.svg` (isotipo oficial: destello menta `#8AE4CB` de 4 puntas + 4 puntos sobre tile morado `#3C2285`) + `<link rel="icon" .../favicon.svg>` + `<meta name="theme-color" content="#3C2285">`. Validado en prod (`/favicon.svg` 200 `image/svg+xml`, sin `/vite.svg`, robots/sitemap/archivos Google intactos). **Follow-up ✅ HECHO (#257 / Brand-Favicon PR-A):** `public/favicon.ico` (ICO 16/32/48) + `public/apple-touch-icon.png` 180 + `public/icon-192.png` + `public/icon-512.png` + `public/site.webmanifest`, generados desde el **isotipo oficial** (`isotipo con margen.svg`, marca mint `#00e4cb` compuesta sobre tile morado `#3C2285` para legibilidad en chico); **`public/favicon.svg` actualizado al mismo isotipo** (consistencia SVG↔raster); `index.html` enlaza `.ico`/svg/apple-touch/manifest; `theme-color #3C2285` intacto. **Causa raíz corregida:** `/favicon.ico`·`/apple-touch-icon.png`·`/site.webmanifest` NO existían → el rewrite SPA devolvía HTML (Google mostraba el globo genérico); ahora Vercel sirve los estáticos reales antes del rewrite. Rasterizado con `sharp` transitorio (`--no-save`; `package.json`/lock intactos). **Validado en prod `lucycare.app`:** `/favicon.ico` 200 `image/vnd.microsoft.icon`, `/favicon.svg` 200 `image/svg+xml`, `/apple-touch-icon.png`·`/icon-192.png`·`/icon-512.png` 200 `image/png`, `/site.webmanifest` 200 `application/manifest+json`; Home/perfil 200; `robots.txt`·`sitemap.xml` (36 URLs) intactos; **NO tocó** `lucycare-logo.png`/OG/SEO/slugs/middleware/sitemap/robots/SQL/DB/backend. **Nota:** Google recrawlea el favicon en su propio ciclo (días/semanas). Pendiente menor: OG branded 1200×630 (Slugs PR D). **(#222) Branding PR-1 — Home público hacia la paleta oficial LucyCare (frontend-only, acotado al Home + tokens Tailwind; SIN migración, `s7_52` sin cambios):** primera fase (por fases pequeñas, NO rebrand global) del acercamiento del público a la paleta oficial **morado `#3C2285` + menta `#8AE4CB` + gris `#EDEDED`**. Tokens nuevos en `tailwind.config.ts`: `brand.mint #8AE4CB` + `brand.gray #EDEDED` (se conservan `brand.purple #3C2285` / `.dark #2d1a64`). **Morado = CTAs primarios/jerarquía:** "Reservar cita" (`DoctorCard`), "Mostrar más", "Reintentar", "Limpiar filtros", "Mi panel"/"Panel Admin", título del hero, tarjeta-toggle "Solo médicos con agenda en línea" (gradiente morado), knob del toggle, focus del selector, **iconos de filtro** (la menta es demasiado clara sobre blanco → se prioriza contraste sobre la guía de "íconos menta"). **Menta = acento suave:** badges "Verificado"/"Agenda en línea"/"Mejor valorado" (menta 30% + texto morado, contraste ~11:1 AA), fondo del hero (gradiente menta muy suave → blanco), borde de tarjeta reservable, fila "Agenda en línea", hovers de opciones de dropdowns. **Gris = footer/superficies neutras.** **"Soy médico, quiero aparecer" → CTA secundario** (fondo blanco, texto y borde morados, hover menta suave) para no competir con la acción del paciente, on-brand y visible. Solo 4 archivos: `tailwind.config.ts` + `src/pages/home/page.tsx` + `src/pages/home/components/SearchSection.tsx` + `DoctorCard.tsx`. Validado: `npm run build` verde (CSS `index-ChTVc9N0.css`) · preview desktop + móvil 360/390/430 sin overflow horizontal · contraste AA de CTAs y badge "Verificado" · **prod `lucycare.app` OK** (Home 200, tokens de marca en el CSS, `/favicon.svg` 200 `image/svg+xml`, `/robots.txt`, `/sitemap.xml` 35 URLs y archivos Google **intactos**). **NO tocó** doctor-detail · `AffiliationRequestModal` · modales públicos (`ClaimProfileModal`/`LoginModal`/`WaitlistModal`) · panel · admin · paciente · DB/SQL/migraciones/`database.types.ts` · Analytics · SEO dinámico/middleware/OG/JSON-LD · sitemap · robots · archivos Google. **Pendiente NO iniciado (no abrir sin instrucción explícita):** alinear el emerald/teal restante **fuera del Home** — doctor-detail, `AffiliationRequestModal`, `ClaimProfileModal`, `LoginModal`, `WaitlistModal` y demás modales públicos; panel/admin solo si se autoriza después. **(#224) Perf PR-1 — carga percibida del Home público (frontend/config, bajo riesgo; SIN migración, `s7_52` sin cambios):** dos cambios derivados de un diagnóstico read-only (Paso 0) que **exoneró a #222** (delta de bundle ~0.19 KB gzip; la percepción venía de factores pre-existentes). **(1) `index.html`:** se elimina el `<link>` render-blocking a **Font Awesome 6.4.0** (`all.min.css`, ~18.75 KB + webfonts) — confirmado **0 usos** en todo el repo (`fa-`, `FontAwesome`, `all.min.css`, sin dependencia npm); los iconos son **RemixIcon** (`ri-*`, 196 usos), que queda intacto; Google Fonts/Pacifico y los `preconnect` intactos. **(2) `vercel.json`:** bloque `headers` que sirve **solo `/assets/(.*)`** (bundles hasheados de Vite) con `Cache-Control: public, max-age=31536000, immutable` (antes heredaban `max-age=0, must-revalidate` → el navegador revalidaba ~305 KB de JS en cada visita); el **`index.html` NO recibe long-cache** (sigue `max-age=0, must-revalidate` para tomar nuevos deploys al instante); el **rewrite SPA intacto**. **Validado en prod `lucycare.app`:** `/` 200, HTML sin Font Awesome, RemixIcon presente, `/assets/*.js` y `*.css` con cache immutable, `index.html` sin immutable, y `/favicon.svg`·`/robots.txt`·`/sitemap.xml` (35 URLs)·archivos Google **intactos**. **NO tocó** React/Home (código)/Tailwind/DB/SQL/migraciones/`database.types.ts`/Analytics/SEO dinámico/OG/JSON-LD/sitemap/robots/favicon/archivos Google. **Pendiente NO iniciado (no abrir sin instrucción):** **Perf P2** = code-splitting del JS monolítico (~305 KB gzip en un chunk); **Perf menor** = lazy loading de avatares + evaluar Pacifico si no aporta. **(#226 / #227) SEO Home OG — preview social/indexable del Home `/` (frontend/SSR-meta, SIN migración, `s7_52` sin cambios):** el Home servía solo `<title>LucyCare</title>` (sin description/OG/Twitter/canonical) → preview pobre al compartir la URL directa; los perfiles `/doctor/*` ya se veían bien porque el middleware les inyecta metadata. **(#226) SEO Home OG:** `buildHomeMeta(origin)` en `og-meta.mjs` (indexable) + `middleware.ts` con `matcher += '/'` y **rama propia para `/`** que inyecta la meta con `injectMeta` (misma máquina que `/doctor/*`); **`/doctor/*` intacto**; smoke `_smoke-og-meta.mjs` T18/T18b (21/21). **Decisión: middleware, NO estático en `index.html`** — `injectMeta` **inserta** (no reemplaza) los `og:*`, así que meter OG del Home en el shell duplicaría los tags en cada perfil (riesgo de romper su preview). Metadata del Home: canonical=`https://lucycare.app/`, `robots index,follow`, `og:type=website`, `og:site_name=LucyCare`, `og:url=https://lucycare.app/`, `og:image=https://lucycare.app/lucycare-logo.png`, Twitter `summary_large_image` (+ title/description/image); **sin duplicados** de `<title>`/canonical/`og:title`/`twitter:card`. **(#227) Copy final del Home OG** (solo `og-meta.mjs` + smoke, middleware intacto): **title** `LucyCare — Encuentra al médico perfecto para ti` (= `<h1>` del Home) + **description** `Busca por especialidad, ubicación y disponibilidad. Reserva en línea con LucyCare.` (tuteo consistente con el title y el Home). **Validado en prod `lucycare.app`:** Home con metadata completa + copy nuevo, `/doctor/dr-camilo-carrillo` sin regresión (`og:type=profile`, sin duplicados), y `/lucycare-logo.png`·`/favicon.svg`·`/robots.txt`·`/sitemap.xml` (35 URLs)·archivos Google **intactos**. **Nota operativa:** para el preview social usar la **URL directa** `https://lucycare.app/`, **no** enlaces `share.google/…` (wrapper externo de Google/Chrome, ajeno al código de LucyCare); si WhatsApp/Facebook muestran un preview viejo es caché → refrescar con el Facebook Sharing Debugger o esperar. **Pendiente NO iniciado (no abrir sin instrucción):** **OG branded 1200×630** para reemplazar el `lucycare-logo.png` (1200×505) temporal como `og:image` — pertenece a **Slugs+SEO PR D** junto con JSON-LD `Physician` + canonical hardening. **(#229) Branding PR-2a — perfil público visible `/doctor/*` hacia la paleta oficial (frontend-only, solo color; SIN migración, `s7_52` sin cambios):** continuación de Branding PR-1 (Home), acotada al **perfil visible del médico** (NO modales, reservados para PR-2b). Reusa los tokens `brand.*` ya existentes. **Edición por intención (NO replace global de emerald):** **morado** = CTAs/interacción/acento ("Reservar cita"/"Reservar ahora", "Llamar para agendar", "Reclamar mi perfil", "Reintentar"/"Volver al inicio", servicio/slot seleccionados, focus de inputs, spinners, iconos de sección, links, barra de rating); **menta** = acento suave/confianza/**éxito** (badges "Verificado por LucyCare"/"Agenda en línea"/"Mejor valorado" = menta 30% + texto morado AA ~11:1, chips de reseñas, superficie/borde de `ClaimProfilePromptCard`, y el estado de éxito "¡Cita agendada!" = menta suave + texto morado); **gris** = footer (`bg-brand-gray`). **Conservados por decisión del owner:** verde de **WhatsApp** (`bg-green-500`, color de canal), **blue/info** (user-info del BookingCard), red (errores), amber (avisos), yellow (estrellas), gris neutro. Solo 4 archivos: `doctor-detail/page.tsx` + `components/{BookingCard,ClaimProfilePromptCard,ReviewsSection}.tsx`. Validado: `npm run build` verde · preview del perfil de Camilo (badges menta+morado, "Reservar cita" morado, WhatsApp verde) · móvil 360/390/430 sin overflow · **prod `lucycare.app` OK** (`/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, WhatsApp verde, **metadata/OG del doctor intacta** `og:type=profile`, Home `/`·`/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **NO tocó** `ClaimProfileModal`/`LoginModal`/`WaitlistModal`/otros modales · `DoctorAvatar.tsx` · Home · panel · admin · paciente · DB/SQL/migraciones/`database.types.ts` · Analytics · SEO runtime/middleware/OG/JSON-LD · sitemap · robots · favicon · archivos Google. **Pendientes NO iniciados (no abrir sin instrucción):** **Branding PR-2b** = modales públicos (`ClaimProfileModal`/`LoginModal`/`WaitlistModal`) · **`DoctorAvatar.tsx`** (iniciales aún en emerald; componente **compartido** con el Home → follow-up separado) · branding menor (splash verde `#047857` de `index.html` + interno panel/admin). **(#231) Branding PR-2b-1 — modales públicos simples hacia la paleta oficial (frontend-only, solo color; SIN migración, `s7_52` sin cambios):** primera mitad de PR-2b, acotada a los modales públicos **sin auth/claim**: `WaitlistModal` (doctor-detail) + `AffiliationRequestModal` (home). Reusa los tokens `brand.*`. **Edición por intención (NO replace global):** **morado** = CTAs (enviar/confirmar), focus de inputs (`border`/`ring`), links de acento (política de privacidad), accent del checkbox, icono de éxito; **menta** = estado de éxito (círculo de check `bg-brand-mint/30` + icono morado); **conservados:** red (errores/validación), gris/neutros (no hay amber/blue/green en estos dos). Solo 2 archivos. Validado: `npm run build` verde · preview en vivo de ambos modales (AffiliationRequestModal desde "Soy médico", WaitlistModal desde médico no reservable) — acentos morados, éxito menta, `*` requeridos en rojo, móvil 360 sin overflow, 0 errores de consola; **NO se enviaron formularios** (evitar datos reales); **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **NO tocó** `LoginModal`/`ClaimProfileModal` (PR-2b-2) · lógica de waitlist/afiliación/validaciones/auth/OTP/claim · `DoctorAvatar.tsx` · DB/SQL/migraciones · Analytics · SEO runtime/middleware/OG/JSON-LD · sitemap · robots · favicon · archivos Google. **Pendientes NO iniciados (no abrir sin instrucción):** **Branding PR-2b-2** = modales auth/claim (`LoginModal`, `ClaimProfileModal`) — validación visual cuidadosa, sin tocar lógica · **`DoctorAvatar.tsx`** (follow-up compartido) · branding menor (splash + panel/admin). **(#233) Branding PR-2b-2 — modales auth/claim hacia la paleta oficial (frontend-only, SOLO color/className; SIN migración, `s7_52` sin cambios):** segunda mitad de PR-2b, acotada a los modales de **auth/claim**: `LoginModal` + `ClaimProfileModal`. **NO toca** auth/OTP/login/claim/password/validaciones/estados/timers/Supabase/RPCs/navegación/roles; sin cambiar copy/labels. Reusa los tokens `brand.*`. **Edición por intención (NO replace global):** **morado** = CTAs (Continuar/Verificar/Ingresar/Enviar/Reclamar/Guardar/"Contactar a Lucy"), **tabs activas** (Teléfono/Email), **step-indicator del claim** (barra de progreso + número), focus de inputs, links ("Reenviar código"/"¿Olvidaste tu contraseña?"/Términos), accent del checkbox, iconos, borde de la tarjeta-opción "Crear contraseña ahora"; **menta** = estados de éxito (círculos de check OTP/reset/"¡Perfil reclamado!", caja "Contraseña creada" = menta suave + texto morado, fondo/hover de la tarjeta-opción); **conservados:** red (errores/validación), amber (avisos: "Mínimo N caracteres", "No encontramos un correo registrado"), blue (info: "Revisá tu correo"), gris/neutros. (El CTA "Contactar a Lucy" enlaza a WhatsApp pero usaba `bg-emerald-700` —botón morado con ícono, no el patrón verde de canal— → morado como CTA.) Solo 2 archivos. Validado: `npm run build` verde · preview en vivo de ambos modales (LoginModal desde "Iniciar sesión", ClaimProfileModal desde la entrada de reclamo de un perfil `listed_only`) — tab/step-indicator/CTAs morados, focus morado, éxito menta, semánticos intactos; desktop + móvil 360/390/430 sin overflow; 0 errores de consola; **NO se enviaron formularios** (sin OTP/SMS/datos reales); **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **(#235) Branding follow-up — `DoctorAvatar.tsx` (frontend-only, visual-only, 1 archivo/1 línea; SIN migración, `s7_52` sin cambios):** cierra el último resto emerald del rebrand público: el círculo de iniciales del avatar (fallback sin foto) pasa de `bg-emerald-100 text-emerald-700` → **`bg-brand-mint/30 text-brand-purple`** (fondo menta suave + iniciales moradas, contraste AA). **Ruta real: `src/components/DoctorAvatar.tsx`** (componente **compartido** Home + doctor-detail; el pedido mencionaba `home/components/` que no existe). **NO tocó** lógica del avatar/cálculo de iniciales/branch `<img>` (byte-idéntico → avatares con foto intactos)/props/tipos/tamaños/layout/responsive/Tailwind config. Validado: `npm run build` verde · preview del fallback "CC" en Home y perfil de Camilo (menta@30% + iniciales moradas, legible) · Home + doctor-detail móvil 360/390/430 sin overflow · 0 errores de consola · **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, `.bg-brand-mint/30` en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **REBRAND PÚBLICO PRINCIPAL COMPLETO por fases:** **#222** Home (tokens + morado/menta/gris + "Soy médico" secundario) · **#229** perfil visible `/doctor/*` (badges/éxito menta, CTAs morados, WhatsApp verde y blue/info conservados) · **#231** modales simples (`WaitlistModal` + `AffiliationRequestModal`) · **#233** modales auth/claim (`LoginModal` + `ClaimProfileModal`) · **#235** avatar compartido (`DoctorAvatar.tsx`, fallback menta+morado). **Con esto el rebrand público queda 100% en sus superficies principales.** **(#237) Branding menor — splash inicial de `index.html` (frontend/HTML, visual-only, 1 archivo/2 líneas; SIN migración, `s7_52` sin cambios):** el splash estático (visible durante el bootstrap antes de que React monte) usaba verde emerald → alineado a la paleta oficial. Diagnóstico (Paso 0): `#047857` aparecía **solo** en el splash de `index.html` (2 ocurrencias: color del texto "LucyCare" + acento del spinner), y el único otro verde relacionado era `#d1fae5` (anillo del spinner). Cambios: `#047857` → **`#3C2285`** (morado, texto + `border-top-color`); `#d1fae5` → **`#8AE4CB`** (menta, `border`/anillo). Sin tocar textos/estructura/comportamiento; `theme-color`/favicon/RemixIcon/Pacifico/preconnect/script de bootstrap intactos. Validado: `npm run build` verde (`out/index.html` `#3C2285`×3 + `#8AE4CB`×1, 0 verdes) · preview del splash renderizado (texto morado + spinner anillo menta/acento morado, legible) · Home + móvil 360/390/430 sin overflow · **prod `lucycare.app` OK** (splash con hex de marca, Home 200, `/doctor/dr-camilo-carrillo` 200, metadata/OG intactas, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **Pendientes NO iniciados (no abrir sin instrucción):** **branding interno** restante (stat card índigo del panel + botón púrpura de `AdminAffiliationDetailModal` + token de marca) · **OG branded 1200×630** (Slugs PR D) · **Perf P2** (code-splitting del JS) · **Perf menor** (lazy avatares, evaluar Pacifico). **(#180–#184, #187) Frente "Perfil público / visibilidad comercial" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** mejoras públicas de conversión en directorio/Home + perfil del médico. **#180 — CTA "Reservar cita" en la tarjeta del directorio (`DoctorCard.tsx`):** médico reservable → fila "Agenda en línea" + CTA emerald "Reservar cita" (navega a `/doctor/:id`, donde la reserva ya funciona); no reservable → "Ver perfil"; se eliminó el copy contradictorio ("Reserva online disponible solo con agenda activa") y el `$0` (precio solo si >0). Fix de fondo: el CTA dependía de `nextAvailableSlot` hardcodeado a `undefined` → la rama nunca se cumplía. **#181 — contador honesto del Home (`home/page.tsx`):** título neutro "Médicos en Lucy" + conteo secundario discreto "Mostrando X de Y médicos publicados · Z con agenda en línea"; variantes para búsqueda ('… resultado(s) para "…"') y toggle agenda ("Médicos con agenda en línea"); sin claims inflados; `PAGE_SIZE=12` y "Mostrar más médicos" intactos. **#182 — limpieza copy/footer del Home (`home/page.tsx`):** hero honesto ("Reserva en línea con los médicos que tienen agenda activa", sin "al instante con médicos verificados"); "Acceso Gratuito… sin costo" → "Búsqueda sin costo: …el directorio es gratis; el precio de la consulta lo define cada médico"; footer con **año dinámico** (`new Date().getFullYear()`, sin "© 2024"), sin backlink "Website Builder" (readdy), sin links muertos `href="#"`, Privacidad → `/privacidad`, columna "Síguenos" eliminada (grid 4→3). **#183 — perfil público móvil más liviano (`doctor-detail/page.tsx` + `BookingCard.tsx`):** en `/doctor/:id` móvil, el médico **reservable** ya NO muestra el `BookingCard` inline arriba (lidera la info/confianza del médico); reserva desde la **barra fija inferior "Reservar cita" → `MobileBookingSheet`** (flujo real intacto); **desktop mantiene el `BookingCard` sticky**; los **no reservables** conservan su `BookingCard` inline (contacto: "Sin agenda en línea" + Llamar/WhatsApp/Lista de espera, sin barra fija ni promesa de reserva); eliminado el claim **"Pago seguro • Confirmación inmediata"** → "Reserva en línea · el pago se coordina con el médico" (Lucy no cobra en línea). La página de Camilo bajó de ~4165px a ~3200px en móvil. **#184 — footer del perfil + "Compartir" funcional (`doctor-detail/page.tsx`):** footer del perfil alineado con el del Home (año dinámico, sin links muertos, Privacidad → `/privacidad`, sin "Síguenos"); **"Guardar" eliminado** (requiere favoritos/persistencia, no se simula); **"Compartir" funcional diferenciado por contexto** (detección por `window.matchMedia('(pointer: coarse)')` en el click): móvil/touch + `navigator.share` → **share sheet nativo** (WhatsApp/SMS/contactos/correo…); desktop o móvil sin share nativo → **copiar la URL del perfil** + toast "Enlace copiado" (NO abre el panel del sistema en desktop); etiqueta desktop "Copiar enlace", ícono discreto en móvil. **#187 — card "Reclamar perfil" visible por viewer en perfiles `listed_only` (`ClaimProfilePromptCard.tsx` + `doctor-detail/page.tsx`):** en `/doctor/:id` con `lucy_status='listed_only'`, la card de reclamo se mostraba **idéntica a todos** (anónimo, paciente, admin) y como bloque emerald **destacado arriba del fold** — ruido/desconfianza para el paciente (el grueso del tráfico), que podía leerlo como "perfil no validado". Ahora la visibilidad **depende del viewer** (mismo patrón `getSessionWithTimeout` que `ClaimedProfileNoticeCard`): **dueño logueado** (`auth.uid() === doctor.profileId`) → card completa arriba, igual que antes; **visitante anónimo** → entrada **discreta** (link "¿Eres este profesional? Reclamá tu perfil", label gris + link emerald) al final de la columna, debajo de Calificaciones, **fuera del primer pantallazo**, abre el mismo `ClaimProfileModal`; **cualquier otro autenticado que no es dueño** / mientras resuelve la sesión → **nada** (sin flash). Además, gate **explícito** `isListedOnly` (`=== 'LISTED_ONLY'`) para que un `lucy_status` faltante/null **ya NO haga fail-open** a la card (se eliminó el `|| 'LISTED_ONLY'` solo para este gate; el `lucyStatus` general por compat de badges/BookingCard queda). Solo 2 `.tsx`; **frontend-only, sin DB/SQL/migraciones/`database.types.ts`/fixtures; no toca reserva/mapa/credenciales/Home/branding/`lucycare-code/`**. Validación: tsc+build verdes · preview desktop + móvil 360/390/430 (0 overflow, 0 errores de consola) contra médicos `listed_only` reales del directorio + Camilo demo; estados en vivo (anónimo sin card grande/discreta abajo, paciente logueado no-dueño sin card, Camilo verified+bookable sin regresión); **estado "dueño logueado" validado por paridad de código** con `ClaimedProfileNoticeCard` (sin fixture DB, por decisión de alcance frontend-only); **0 datos reales tocados, jamás Katherine**. **Validación de todo el frente:** tsc+build verdes por PR · preview en vivo desktop + móvil 360/390/430 sin overflow · 0 errores de consola · 0 residuales · **regla de validación vinculante: directorio público / paciente demo (Camilo/Pepe Toro), jamás Katherine ni datos reales sensibles**. **Pendientes explícitos del frente (backlog, NO implementado):** mapa condicional (hoy 320px siempre, aún con dirección pobre); señales de confianza / credenciales (decisión de privacidad sobre `license_number`); branding emerald unificado (quitar púrpura `#3C2285`); migrar assets del logo fuera de `static.readdy.ai`; próximo-slot-real en la tarjeta. *(La card "Reclamar perfil" visible a cualquier visitante quedó CERRADA en #187 — ver arriba.)* **(Nota: NO existe un duplicado real `src/pages/Home/` — fue un artefacto de filesystem case-insensitive; descartado del backlog por verificación: `git ls-files 'src/pages/Home/'` vacío + `core.ignorecase=true`; el único Home es `src/pages/home/`, que usa el router.)** **(#177–#178) Frente "Onboarding médico operativo V1" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** ayuda dentro del panel para que un médico **ya operativo** entienda qué le falta para quedar **visible y reservable**. **Alcance V1 = solo Gate 2 (configuración en panel); Gate 1 (reclamo/habilitación por LucyAdmin) queda FUERA de V1.** **#177 (PR-1) read model `doctorActivation.service.ts` + hook `useDoctorActivation`** — `getDoctorActivation(doctorId, clinicId, isDoctorContext)` → `{ steps, completedCount, total, allDone, clinicStatus }`; **read-only, sin UI, sin DB, sin migración, sin `database.types.ts`**. **Checklist principal = 5 pasos self-service** (cuentan para el progreso): (1) **completar perfil** (foto+especialidad), (2) **configurar agenda/disponibilidad**, (3) **agregar servicios**, (4) **activar reserva en línea** (`booking_enabled`), (5) **revisar publicación / cómo lo ven los pacientes** (`is_published`). Cada paso `{ key, label, done, href }`, `done` por **señal real** (reusa `getMyDoctorProfile`/`getMyAvailabilityRules`/`listDoctorServices` + query read-only de `clinics`). **`clinicStatus` aparte = info NO bloqueante** (`{ label, complete, actionable:false, href:null, message }`): la ubicación de la clínica (nombre/dirección/depto/muni) **solo la edita LucyAdmin** (`admin_update_doctor_clinic`), así que **no es un paso del médico y NO suma ni resta progreso** (`allDone` depende solo de los 5). **Scoping (igual que el resumen rápido):** gate de ROL `isDoctorContext` → `emptyActivation()` **antes de cualquier query** si no es médico (`doctorId` solo NO basta — el asistente lo trae); verdad de la **sesión** (`getMyDoctorProfile`, RLS doctor-scoped), los ids del caller solo como verificación de consistencia (mismatch → vacío, sin leer clínica/agenda/servicios); nunca cross-médico/cross-clínica. Validado: tsc+build + smoke 17/17 (no-médico/asistente → vacío sin queries; mismatch → vacío sin leer aux; clínica incompleta NO bloquea `allDone`; clínica completa no suma; sesión real de Camilo/RLS = 4/5 le falta foto). **#178 (PR-2) tarjeta UI `DoctorActivationCard.tsx`** montada en el dashboard (`/panel`, **arriba de las métricas**) — "Primeros pasos para activar tu perfil": progreso "**N de 5 pasos listos**" + barra, lista de los 5 pasos (listo/pendiente por señal real, **sin checkbox manual**; pendiente → link directo a su pantalla), y `clinicStatus` **discreto** (solo el mensaje "Contactá a soporte…" si está incompleta; si completa se omite). **Gate VISUAL:** solo `role==='doctor'` (nunca asistente/admin/paciente/null; `doctorId` solo NO es criterio), **se oculta si `allDone`** (V1: no dejar 5/5 fijo), payload vacío, error o carga (nunca error técnico). **Reemplaza el banner de "Completá tu perfil con una foto"** (la foto es parte del paso "Completá tu perfil") para no duplicar. Validado: tsc+build + smoke SSR 13/13 (componente real: médico+progreso → tarjeta; asistente/rol undefined/`allDone`/vacío/error/carga → no se renderiza; clínica incompleta → mensaje discreto sin bloquear) + preview en vivo bajo sesión real de Camilo (RLS) → 4/5 arriba de las métricas, banner sin duplicar, móvil 360/390/430 sin overflow, 0 errores de consola, 0 residuales. **Regla de validación vinculante: fixtures aisladas / paciente demo (Pepe Toro/Camilo), jamás Katherine ni datos reales sensibles.** **(#174–#175) Frente "Resumen rápido del paciente" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** (#173 fue docs-only de cierre de la serie de correcciones #167–#172, **no** parte de este frente.) **#174 (PR-1) read model `PatientQuickSummary`** — servicio (`src/services/patientQuickSummary.service.ts`) + hook (`src/hooks/usePatientQuickSummary.ts`); **read-only desde datos, sin UI, sin DB, sin migración, sin `database.types.ts`**. Agrega la historia clínica DOCUMENTADA del paciente **con este médico**: total de consultas **firmadas** previas, última atención + motivo, diagnósticos previos (distinct por recencia), **medicamentos permanentes/vigentes/relevantes** (reusa `getPermanentPrescriptionsForPatient`), notas relevantes (alergias/tipo de sangre/notas/`family_history_notes` reciente), últimos vitales (kg→lb) y brief de las últimas 3. **Scoping crítico:** gate de ROL **`isDoctorContext`** → devuelve `emptySummary()` **ANTES de cualquier query** si no es médico (un asistente también trae `doctorId`, así que `doctorId` por sí solo **NO** basta y **no se consulta `patients`/alergias/sangre/notas**), filtro **EXPLÍCITO** por `patient_id` + `doctor_id` (defensa en profundidad sobre la RLS doctor-scoped), **SOLO `status='signed'`**, **excluye la consulta actual**, **payload vacío seguro** sin historia. Validado: tsc+build + smoke 24/24 contra el módulo real (no-médico → 0 queries, no toca `patients`; médico bajo sesión real de Camilo/RLS). **#175 (PR-2) tarjeta UI `QuickSummaryCard.tsx`** montada en `ConsultaPage.tsx` **debajo del header del paciente, antes de la evaluación clínica** — tarjeta compacta (total de atenciones, última atención fecha+motivo, chips de diagnósticos, chips de medicamentos vigentes, **alergias en amber** = único color de alerta, tipo de sangre, antecedente familiar, últimos vitales en **lb**, **"Ver últimas atenciones" colapsado** por defecto). **Gate VISUAL:** se muestra **solo si `role==='doctor'`** (nunca asistente/admin/paciente/rol desconocido; `doctorId` solo NO es criterio), **solo si `hasHistory===true`**, y **se oculta ante payload vacío/inseguro o error/carga** (nunca un error técnico al médico). **Regla de seguridad: nunca mezclar pacientes, médicos, clínicas ni perfiles** (lo garantiza el scoping del read model). Validado: tsc+build + smoke SSR 16/16 (componente real: médico+historia → tarjeta con todo el contenido; asistente/rol undefined/`hasHistory=false`/error/carga → no se renderiza) + preview en vivo bajo sesión real de Camilo (RLS) sobre **paciente demo (Pepe Toro)** + móvil 360/390/430 sin overflow + 0 errores de consola + 0 residuales. **Regla de validación vinculante: fixtures aisladas / paciente demo, jamás Katherine ni datos reales sensibles.** **(#167–#172) Serie correcciones clínicas / receta / PDF COMPLETA — frontend-only, SIN migración nueva (`s7_51` sin cambios):** **#167 (PR-A) PDF de receta limpio** — helper `doctorTitleName` evita "Dr. Dr." / "Dra. Dra." (antepone el título solo si falta, respeta el existente, no infiere género), fechas de emisión/corrección **una sola vez** en el encabezado (footer "Firmado digitalmente" sin fecha duplicada, banner "RECETA CORREGIDA" compacto), y dosis **rotulada** (Dosis / Frecuencia / Duración / Indicaciones) — un "1" suelto lee "Dosis: 1", **sin inventar** datos clínicos (texto libre tal cual); solo `RecetaPrint.tsx`. **#168 (PR-B) Diagnósticos sin Tipo/Estado en la UI** — se quitan los `<select>` de `diagnosis_type`/`diagnosis_status` de la sección de consulta y del editor del modal de corrección; queda **nombre (catálogo) + Notas**; los defaults internos `presuntivo`/`activo` los sigue aplicando `addConsultationDiagnosis`; columnas y datos históricos intactos; `DiagnosesSection.tsx` + `CorrectConsultationModal.tsx`. **#169 (PR-C) Orden en el modal de corrección de receta** — bloque "Agregar medicamento" **arriba** + nuevos arriba (más reciente primero), existentes debajo; **solo display del modal** (`[...added].reverse()`), el orden persistido y el del PDF **NO cambian** (`prescriptions.id` es UUID, `getPrescriptions` ordena por id como siempre); `CorrectConsultationModal.tsx`. **#170 (PR-D) Peso en libras con almacenamiento interno en kg** — el médico digita **lb**, se guarda `weight_kg` (helpers `lbToKg`/`kgToLb` en `vitals.service.ts`, único punto de conversión), IMC con kg (fórmula intacta), validadores/labels en lb; sin reinterpretar históricos, sin migración; `vitals.service.ts` + `ConsultaPage.tsx` + `CorrectConsultationModal.tsx`. **#171 (PR-E1) Antecedentes familiares como TEXTO LIBRE en consulta normal/borrador** — textarea ligada a `consultations.family_history_notes` (la columna y su save en `ConsultationUpdate`/`saveConsultationDraft` ya existían; estaba huérfana en UI), se oculta el catálogo estructurado para nuevos; los estructurados previos quedan **histórico/solo lectura** (`AntecedentesSection readOnly`), sin borrar/migrar; solo `ConsultaPage.tsx`. **#172 (PR-E2) Corrección POST-FIRMA de antecedentes texto libre** — el modal de corrección expone `family_history_notes` como texto libre editable (hidratado, con diff, vía `p_consultation_changes`); **SIN migración** porque el RPC `amend_consultation` vigente (`s7_31`) ya tenía `family_history_notes` en su whitelist y `UPDATE` (era infra anticipada); los estructurados quedan read-only y ya **no se generan `familyHistoryOps`**; `consultations.service.ts` (tipo `ConsultationTextField`) + `ConsultaPage.tsx` + `CorrectConsultationModal.tsx`. **Validación de toda la serie:** `tsc`+`build` verdes por PR · fixtures aisladas / paciente demo (Pepe Toro) — **regla vigente: jamás Katherine ni datos reales sensibles, ni en read-only; usar fixtures o paciente demo** · cleanup 0 residuales · preview médico/asistente + móvil 360/390/430 · OK visual del owner por PR. **(#163 / `s7_51`) "Mi lista de espera" en panel médico/asistente LIVE — gestión del directorio sin pasar por LucyAdmin:** ruta **`/panel/lista-espera`** (NavLink en `PanelLayout`, **visible para médico Y asistente**) + página mobile-first + 2 RPCs `SECURITY DEFINER` **paralelas** (NO tocan/ensanchan las `admin_*`): `clinic_list_waitlist(p_doctor_id, p_status, p_limit, p_offset)` (pendientes primero + `total_count` vía `count(*) OVER()`) y `clinic_update_waitlist_entry(p_entry_id, p_status, p_notes)` (estado/nota + audit `edited_via='clinic'`). **Gate server-side** (helper `_clinic_waitlist_authorize`): el caller debe ser el **médico dueño** (`get_user_doctor_id() = doctor_id`) **o miembro de la clínica** del médico (`is_clinic_member`) — **NO confía en el `doctor_id` del cliente**, sin cross-clínica/cross-médico; estados `pending`/`contacted`/`cancelled` (P0001/P0105/P0106/P0107). Médico y asistente pueden: **ver** la lista autorizada, **filtrar por estado**, **marcar contactado**, **volver a pendiente**, **cancelar/descartar**, **editar nota interna**, y **contacto manual** (Llamar `tel:` / WhatsApp `wa.me` / copiar teléfono) sobre el teléfono ya registrado. **V1 SIN agendar** (crear cita / walk-in prefilled = follow-up futuro), **sin mensajería automática**; **admin global (`/admin/lista-espera`, RPCs `admin_*`) y `submit_waitlist_entry` (público) intactos**; **sin columnas nuevas**, sin tocar el esquema de `waitlist_entries` / RLS / dedup / identidad / `auth.users`. **Fix destapado en la validación:** `clinic_update_waitlist_entry` declaraba `v_old record` y el `SELECT … INTO v_old.status/…` abortaba **toda** actualización con `55000 record "v_old" is not assigned yet` → corregido a **`v_old waitlist_entries%ROWTYPE`** (idempotente `CREATE OR REPLACE`, re-aplicada). **Validación:** `check-s7_51` 3/3 + `_smoke-s7_51` 9/9 (T1–T8: médico ve su lista / aislamiento cross-doctor / no-miembro bloqueado / update + audit / asistente ve+actualiza / estado inválido) + cleanup **0 residuales** (`F51_FIXTURE`) + **preview médico Y asistente con sesión real** (NavLink visible, página carga sin error, listado autorizado, cambio de estado, audit `edited_via='clinic'` con `contacted_by` correcto) + **móvil 360/390/430 sin overflow** + desktop sin regresión + tsc/build verdes + **0 datos reales tocados** (test user `50375000001` usado como asistente temporal con rol restaurado a `patient`; jamás Camilo ni Katherine). **Pendiente (futuro, no V1):** "agendar" desde la lista (walk-in prefilled) + sección equivalente en panel + paginación visible si crece. **(#162) Paginación/Home LIVE — render incremental "Mostrar más médicos":** `PAGE_SIZE=12`, contador "Mostrando N de M", reset por filtros/búsqueda/orden/toggle; frontend-only (NO paginación server-side; el fetch sigue trayendo el set completo, tope ~1000 → server-side real = frente mayor futuro). **F4 merge/unmerge + F4-3b cerrados; Pendiente F4: F4-D (diferido, no arrancado).** **(#160) Seguridad de sesión LIVE — cierre por inactividad + tope absoluto por rol (frontend-only):** wrapper central `SessionGuard` (montado 1 vez en `App.tsx` dentro de `BrowserRouter`; sin tocar PanelLayout/AdminLayout/área paciente) + hook `useIdleLogout` + `SessionTimeoutModal` + `sessionPolicy`. Política por rol: **admin 15min/8h · médico/asistente 30min/12h · paciente 60min/24h · aviso 90s**. `deadline = min(inactividad, tope absoluto)`; aviso con cuenta regresiva → **"Mantener sesión"** (resetea SOLO inactividad, NO el tope absoluto) / **"Cerrar sesión ahora"**; al vencer → logout. **Fallback ESTRICTO** para rol desconocido/nulo (= admin, no paciente). Tope absoluto anclado a `last_sign_in_at`. La actividad **no revive** una sesión ya vencida (al volver de background se evalúa el vencimiento antes de registrar actividad; re-evaluación en `visibilitychange`/`focus`). **Logout robusto:** `signOut({scope:'local'})` best-effort + limpieza dura de tokens `sb-*` en localStorage + recarga (evita el cuelgue conocido de `signOut` con el lock no-op). Override de tiempos **solo `import.meta.env.DEV`** (sin lógica por hostname). **Sin DB/RLS/migraciones/`database.types.ts`/identidad/`auth.users`; sin tocar login/OTP/reset.** tsc+build verdes; validación dev (warning/keep/cerrar/auto-logout/tope absoluto/volver-tras-idle) + OK del owner. **(#239) Seguridad de sesión — el idle-logout sobrevive a reload/reapertura/kill de pestaña (frontend-only, 1 archivo `src/hooks/useIdleLogout.ts`, SIN migración, `s7_52` sin cambios):** el gap móvil era que el reloj de inactividad vivía solo en memoria (`useRef`) y se reiniciaba en cada montaje → si el SO móvil mataba/reabría la pestaña en frío, el idle no atrapaba la sesión (solo la cerraba el tope absoluto). Ahora se **persiste SOLO el timestamp de última actividad** en `localStorage` (clave `lc_last_activity`, epoch ms, **sin PII ni datos clínicos**): el idle se **hidrata** del valor persistido al montar, `refresh()` ancla `actividad >= last_sign_in_at` (login nuevo reinicia; restauración en frío conserva), reset **síncrono** en `SIGNED_IN` para evitar el falso logout por la carrera con el tick async, persist throttled (15s) en actividad + flush en `visibilitychange→hidden`/`pagehide`, guarda contra timestamps futuros (clock skew), y **clear en logout y `SIGNED_OUT`**. **NO cambió tiempos de política** (admin 15m/8h · médico/asistente 30m/12h · paciente 60m/24h · desconocido→estricto), tope absoluto, modal 90s, `sessionPolicy.ts`, guards/rutas/layouts, Supabase config, DB/SQL/migraciones/`database.types.ts`, Analytics/SEO/sitemap/robots/favicon/archivos Google/branding/performance. **Validación runtime en prod `lucycare.app` (navegador real, cuenta demo Camilo):** login OK → `/panel` con sesión → `lc_last_activity` presente y **solo epoch ms** → reload **NO borra ni reinicia** la clave (idéntica) → logout limpia `lc_last_activity` **y** tokens `sb-*` → `/panel` sin sesión redirige a `/` → 0 errores de consola; bundle productivo contiene el fix. **(#158–#159) Responsive móvil LIVE (frontend-only):** **#158 Home público** — eliminado el overflow horizontal del header (nav `flex-wrap`+`min-w-0`, logo `h-10` móvil, "Soy médico" oculto en móvil autenticado); **#159 Panel/Citas/Calendario/Bloqueos** — fila de fecha de Citas en 2 filas móvil + KPIs `grid-cols-2`, CalendarHeader con el rango en fila propia (sin truncar), botón "Nueva cita" integrado, WeekView con columnas legibles (scroll interno dentro del card), BlockForm con fechas/horas apiladas y tipos parejos. Desktop intacto; `scrollWidth===clientWidth` en 360/390/430. **(#157)** docs-only: cierre de `s7_50`. **(#156 / `s7_50`) Fix de onboarding de asistentes LIVE — `accept_clinic_invitations` tolerante a formato de teléfono:** el gate (sobre `profiles.phone`) y el loop (sobre `clinic_invitations.phone`) ahora comparan con `normalize_phone_sv(...)` en **ambos lados** → cierra un **bug operativo ACTIVO** (la invitación se guardaba como `+503XXXXXXXX` y `profiles.phone` como `503XXXXXXXX`, así que el gate **literal** fallaba y la invitación no se aceptaba, con el error silenciado en el login). **Misma firma**, misma lógica de cupos/asientos, **`database.types.ts` NO tocado**, sin tocar UI/identidad global/`auth.users`. Validación: `check-s7_50` verde + `_smoke-s7_50` verde (`pass=7 fail=0`; fixtures aisladas `F50_FIXTURE`; **0 residuales**; role del test patient restaurado a `patient`; **0 datos reales tocados** — jamás Camilo ni Katherine). **(#151–#153 / `s7_49`) F4-3b V1 — bandeja de vínculos rechazados LIVE:** sección "Vínculos rechazados" en `/admin/pacientes` sobre la cola `patient_link_rejections` (rechazos paciente-driven "No son mías" de B2). **Backend (#151/`s7_49`):** columna `resolution_note` + `admin_list_patient_link_rejections(p_status)` (read, admin-only, metadatos + estado actual de la ficha, sin contenido clínico/JSON crudo) + `admin_resolve_link_rejection(p_rejection_id, p_resolution_note, p_reopen)` (resolver motivo ≥10 / reabrir; audit en cada transición; el reopen conserva el estado previo en `old_data`; **P0080** inexistente / **P0081** motivo<10); check 5/5 + smoke 11/11. **Fix smoke (#152):** el fixture insertaba sin `date_of_birth` (NOT NULL). **UI (#153, frontend-only, `database.types.ts` NO tocado):** filtro pendientes/resueltos/todos + tabla (ficha/clínica/perfil que rechazó/teléfono/fecha/estado/**estado actual de la ficha**) + **Resolver** (modal, motivo ≥10) + **Reabrir**; invalida la query. **Resolver = bookkeeping administrativo:** NO re-vincula, NO fusiona, NO toca `patients`/`profiles`/`auth.users`; la fila sigue excluyendo el par del auto-link mientras exista (independiente de `status`). **V1 = solo bandeja de rechazos; SIN fuzzy, SIN detección proactiva de pares débiles, SIN F4-D.** Validado con fixtures `F49_FIXTURE` (listar→resolver→reabrir→0 residuales), desktop + móvil, OK visual del owner; **fila real `50375000001` restaurada a `pending_review`** tras el preview, **0 datos reales tocados** (jamás Camilo ni Katherine/`50372608827`). **(#149) F4 Unmerge — UI "Deshacer fusión" LIVE:** acción en el historial de fusiones de `/admin/pacientes` para revertir un merge, sobre las RPCs `s7_48`. Botón **"Deshacer fusión"** solo si `unmerged_at IS NULL` (la autoridad real es `admin_unmerge_patients_preflight`; si bloquea P0070–P0077 no permite confirmar), badge **"Revertida"** si ya se revirtió. Modal de 3 pasos: preflight (resumen del merge, veredicto, conteos a devolver, **warnings como info** — historia nueva en target/E3/merge viejo) → **confirmación reforzada** (motivo ≥10 + teclear `DESHACER FUSIÓN` con tilde, Escape/X bloqueados en submit) → resultado (origen reactivado/destino/`merge_log_id`/devuelto al origen). Errores **P0070–P0077** por `error.code` (`UNMERGE_BLOCK_COPY`/`unmergeBlockMessage`); tras éxito invalida historial + candidatos (la fuente restaurada reaparece como candidata). **Sin contenido clínico, sin JSON crudo. Frontend-only, sin migración/RLS/backend; `database.types.ts` NO tocado** (firmas RPC ya de #147). Solo `src/pages/admin/AdminPacientesPage.tsx` + `src/services/patientMerge.service.ts`. Validado con fixtures `F43B_FIXTURE` (merge UI → unmerge UI → 0 residuales), desktop + móvil, tsc + build, **OK visual del owner**, **0 datos reales tocados**. **(#147 / `s7_48`) Unmerge formal — backend LIVE:** reversa del merge admin sobre `patient_merge_log` — `admin_unmerge_patients_preflight(p_merge_log_id)` (dry-run) + `admin_unmerge_patients(p_merge_log_id, p_reason)` (transaccional, `FOR UPDATE`, revalida server-side). Devuelve destino→fuente **solo** las filas de `moved_ids` que siguen en el target, restaura la fuente desde `source_snapshot`, **el destino conserva** su historia post-merge, rellena `unmerged_at`/`unmerged_by`/`unmerge_reason` (**append-only**) + audit `edited_via='admin_unmerge'`. **Bypass propio `app.unmerging_patients`** añadido a los dos guards (firma `s7_29` + identidad P0030 `s7_33`; los previos intactos). **Reversa atómica** (no parcial), **no hard-delete**, **no toca `profiles`/`auth.users`/identidad global**; el esquema de `patient_merge_log` no cambia (las columnas `unmerge_*` ya existían de `s7_46`). Códigos **P0070–P0077** (P0070 inexistente · P0071 ya revertido · P0072 fuente fuera de estado · P0073 destino en cadena/clínica · **P0074 conflicto de documento** · P0075 `moved_ids` drift · P0076 motivo<10 · **P0077 profile del snapshot inexistente**) + warnings (`W_TARGET_NEW_HISTORY`/`W_OLD_MERGE`/`W_REMERGE_RISK`). Firmas de las 2 RPCs en `database.types.ts`. **Backend-only, sin UI.** check 5/5 + smoke 17/17 (happy path merge→unmerge, P0071/P0073/P0074/P0075/P0076/P0077, historia nueva en target como warning, consulta firmada devuelta sin romper guards, audit, fixtures `F48_FIXTURE` con 0 residuales, 0 datos reales tocados). **(#144) F4-3 UI PR B — merge real en `/admin/pacientes` LIVE:** habilita la fusión real (`admin_merge_patients`, `s7_46`) desde la UI — botón **"Fusionar fichas"** solo si `preflight.eligible`, **confirmación reforzada** (motivo ≥10 + teclear `FUSIONAR FICHAS`), vista de éxito (source/target/`merge_log_id`/conteos movidos), errores **P0060–P0068** por `error.code` (reusa `MERGE_BLOCK_COPY`), Escape/X bloqueados durante el submit, invalidación de candidatos + historial; firma de `admin_merge_patients` agregada a `database.types.ts`; dev-script `scripts/_fixtures-merge-ui.mjs` (fixtures E3 aisladas marcadas `F43B_FIXTURE`, `--apply`/`--clean` con verificación de 0 residuales). **Frontend-only, sin migración/RLS/backend; sin unmerge (reversa futura sobre `patient_merge_log`); sin contenido clínico ni JSON crudo; sin teléfono/nombre como evidencia; sin tocar profiles/auth.users/identidad global.** Validado con fixtures (apply→merge→P0061→clean, **0 residuales**), desktop + móvil, tsc + build verdes, **validación visual del owner OK**, **0 datos reales tocados** (jamás Camilo ni Katherine/`50372608827`). **(#142) F4-3 UI PR A — `/admin/pacientes` (vista previa read-only) LIVE:** ruta `/admin/pacientes` (`AdminOnlyRoute` + `AdminLayout`) + NavLink "Pacientes" + `AdminPacientesPage.tsx` + `patientMerge.service.ts`. Consume `admin_list_patient_merge_candidates()` (lista de grupos `same_profile` con warnings `W_UNCONFIRMED_LINK` + estado vacío) + `admin_merge_patients_preflight` (dry-run: comparación source/target lado a lado con badges, evidencia, conteos, **P0060–P0068 mapeados a copy**, "Intercambiar fuente/destino") + lectura de `patient_merge_log` (historial legible, sin JSON crudo). Firma de `admin_merge_patients_preflight` agregada a `database.types.ts`. **NO ejecuta merge, NO llama `admin_merge_patients`, NO botón "Fusionar", NO contenido clínico, NO JSON crudo, NO F4-3b, NO unmerge, NO toca profiles/auth.users/identidad global.** Decisión A/B: F4-3 UI se dividió en PR A read-only (esto) + **PR B acción destructiva (pendiente, siguiente frente)** por ser la primera UI admin que ejecutará un merge y aún no hay unmerge formal. Frontend-only, sin migración; tsc + build + OK visual del owner (preview desktop/móvil con fixtures E3 limpiadas). El historial aún no muestra "admin ejecutor" (omitido en PR A; dato en `patient_merge_log.merged_by`). **(#140 / `s7_47`) F4-3-search — RPC de candidatos a merge LIVE:** `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`→`P0001`, anon sin EXECUTE). **READ-ONLY** (solo SELECT/agregación: no merge, no escritura, no audit, no toca datos). Lista **grupos** fusionables **V1 = `same_profile`** (E3) por `(clinic_id, profile_id)` con `profile_id IS NOT NULL`, **solo fichas vivas** (`is_active AND merged_into_patient_id IS NULL`), `HAVING count(*) >= 2`. Por grupo: clínica/perfil + nombres + `patient_count` + `confirmed_links_count`/`unconfirmed_links_count`/`has_unconfirmed_links` + `warnings` (**`W_UNCONFIRMED_LINK`** si alguna ficha tiene `link_confirmed_at NULL` — ayuda, no bloqueo). Por ficha: identidad mínima + `counts` (appointments/consultations/vitals). **NO** contenido clínico; **NO** candidatos por teléfono/nombre/walk-in/fuzzy/override; **NO** ejecuta merge; **NO** toca identidad global/`profiles`/`auth.users`. La decisión la toma la UI (PR 2), no la RPC; el gate de evidencia real lo aplica el preflight/merge de `s7_46`. check 2/2 + smoke 10/10. **PR 1 de F4-3; la UI `/admin/pacientes` es PR 2 (siguiente frente).** **(#138 / `s7_46`) F4-2 — backend del merge admin de fichas (intra-clínica) LIVE:** `admin_merge_patients_preflight` (dry-run, no modifica DB) + `admin_merge_patients` (transaccional, `FOR UPDATE`, revalida server-side) + helper interno `_patient_merge_eligibility` + tabla `patient_merge_log` (snapshots/`moved_ids`/`moved_counts`/reason/actor/evidence; campos `unmerge_*` previstos para la reversa formal, RLS admin-only). **Bypass propio `app.merging_patients`** añadido a los dos guards (firma de consulta `s7_29` + identidad P0030 `s7_33`) — sus bypass previos intactos. El merge mueve `appointments`/`consultations`/`vitals` source→target (incl. **consulta firmada** vía bypass), **neutraliza la fuente** (`document_number→NULL`, `profile_id→NULL`, `is_active=false`, `merged_into_patient_id`/`merged_at`; **teléfono conservado**; **no hard-delete**) + audit `edited_via='admin_merge'`. **V1 = evidencia `same_profile`** (E3); el `UNIQUE(clinic,doc)` hace `same_document` inalcanzable intra-clínica (soportado en lógica para legacy, 0 dups hoy); **no merge por teléfono/nombre ni override** (DM3b). Bloqueos **P0060–P0068** (distinta clínica / ya fusionada / source==target / profiles distintos P0065 / dirección inválida P0064 / evidencia insuficiente P0063 / motivo<10 P0066 / **borrador en fuente bloquea P0067** / ficha inactiva P0068); **borrador en target = solo warning** (DM3c). El claim ya ignora fichas merged (`s7_45`). **Sin UI, sin unmerge implementado, sin identidad global, sin `auth.users`.** check 6/6 + smoke 19/19. **Tres cierres previos (2026-06-13):** **(#134) Diseño Paciente Global Fase 4 / merge admin (docs-only)** — DM1–DM9 cerradas, `docs/ANALISIS_PACIENTE_GLOBAL_FASE4_MERGE_ADMIN.md`; alcance = merge de **fichas `patients` intra-clínica** (NO identidades/`profiles`/`auth.users`, NO expedientes cross-clínica); reglas vinculantes (solo LucyAdmin, dry-run + motivo + audit obligatorios, no hard-delete, **unmerge formal** previsto); fases F4-1→F4-2→F4-3 (+F4-3b bandeja `patient_link_rejections`, +F4-D identidades diferida). **(#135 / `s7_45`) F4-1 — claim tolerante fila por fila:** `claim_patient_records()` procesa fichas **fila por fila** (subtransacción `BEGIN/EXCEPTION` por ficha) → una colisión `UNIQUE(clinic_id, document_type, document_number)` ya **no aborta el claim completo** (cierra el hallazgo F4); las fichas válidas se vinculan, la conflictiva queda **intacta** (sin media-copia); skips **auditados** (`claim_skipped_unique` / `claim_skipped_error` con SQLSTATE + fila resumen con `skipped[]`); el cliente recibe **solo conteos** (`linked_count`/`skipped_count`, sin PII por ficha — el detalle vive en `audit_log`); columnas **pasivas** `merged_into_patient_id` (FK a `patients` sin CASCADE + CHECK anti self-reference) + `merged_at` (infra de F4-2, **nada las escribe aún**; el claim **excluye** fichas marcadas como fusionadas); **NO hace merge, NO toca identidad global ni `auth.users`, NO hard-delete**; firma/GRANTs/matching intactos (exclusión de pares rechazados `s7_43` + copia COALESCE `s7_33`); `database.types.ts` actualizado; check 5/5 + smoke 16/16. **(#136) Fix bug público de reserva móvil (frontend-only):** el CTA móvil "Reservar cita" abría un scaffold **mock** (`MobileBookingSheet` con horarios generados en cliente — `doctorId % 2` sobre un UUID = `NaN` —, servicios inventados, `PaymentModal` falso que NO creaba cita en DB) → ahora monta el **flujo real** (`BookingCard`: servicios reales, slots reales vía `useAvailableSlots`, `createBooking`, login/OTP real); **fecha por defecto** (hoy si tiene agenda, o la próxima disponible vía `useAvailableDays` + verificación de slots reales) en hora **local** (helper `localDateStr()`, no `toISOString()` UTC que saltaba al día siguiente después de las 6pm SV); **error de red → "Reintentar"** (`refetch`) distinguido de **médico inexistente**; **splash** estático en `index.html` dentro de `#root` contra pantalla blanca durante el bootstrap; eliminados `PaymentModal.tsx` y `utils/auth.ts` (mocks sin imports reales); sin DB/SQL/migraciones/RLS, sin tocar reglas de agenda; tsc + build + OK visual móvil del owner; fixture de cita de prueba limpiado. **Administración de LucyAdmins Fase 1 (PR #132, `s7_44`) LIVE:** ciclo de vida completo sin SQL manual — tabla `platform_admin_invitations` (`pending/active/revoked`, TTL 14d, trazabilidad completa) + RPCs definer `admin_invite_platform_admin` (admin = **cuenta dedicada**: bloquea teléfonos de paciente real/médico/asistente `P0050`, admin `P0051`, inválido `P0055`; vigente `P0053`; vencida se refresca in-place; cuenta-cáscara creada por invitación vencida es re-invitable vía historial) / `accept_platform_admin_invitation` (activación SOLO al primer OTP del invitado, hook fail-safe en `verifyOtp`; pending sin privilegios; fill de `full_name` desde la invitación si vacío; bloquea si la cuenta se volvió paciente real) / `admin_revoke_platform_admin` (rol→patient, **bloqueo inmediato vía `is_admin()`**, **guard de último admin activo** `P0052`, sin tocar auth.users) / `admin_list_platform_admins`. **`profiles.role='admin'` sigue siendo el ÚNICO bit de autorización; `is_admin()` y sus ~22 superficies intactas.** UI `/admin/administradores` (activos/invitar con teléfono sanitizado+validado/pendientes/revocados + "Editar nombre" solo propio). Audit en cada transición. check 6/6 + smoke 24/24. **Navegación móvil LucyAdmin (PR #130):** `AdminLayout` con header fijo + hamburguesa + drawer en móvil (antes no había NINGUNA nav bajo 768px); desktop intacto; frontend-only. **Análisis administración LucyAdmins (PR #131):** `docs/ANALISIS_ADMINISTRADORES_LUCY.md` (Opción B aprobada, D1–D6 + reglas vinculantes). **Ownership del paciente (análisis #127, D1–D7 aprobadas):** regla central vinculante — *"el médico gestiona una relación clínica local; LucyCare gobierna la identidad global del usuario/paciente"* (`docs/ANALISIS_PACIENTE_GLOBAL_OWNERSHIP.md`). **B2 — Confirmación post-claim (PR #128, `s7_43`) LIVE:** el claim vincula con `patients.link_confirmed_at NULL` → la UI separa esas fichas en "Encontramos atenciones asociadas a tu número" (metadatos mínimos, acción POR FICHA); "Mis atenciones" lista **solo confirmadas**; **"Sí, son mías"** → `confirm_patient_link` (sella+audit, idempotente); **"No son mías"** → `reject_patient_link` (desvincula + `patient_link_rejections` `pending_review` + audit; **el claim no re-vincula el par rechazado**; otro profile legítimo sí puede). Legacy pide confirmación una vez; la ficha creada por el propio paciente al reservar nace confirmada. Sin bandeja admin (tabla+audit). Cierra R1/R2 del análisis de ownership. smoke 14/14. **Identidad múltiple (análisis #124 + Fase 1 #125, frontend-only):** modelo firmado "identidad única + capacidades múltiples" (`docs/ANALISIS_ONBOARDING_IDENTIDAD_MULTIPLE.md`, 5 decisiones aprobadas; `profiles.role` = rol primario/default de navegación). **Fase 1 live:** `PatientOnlyRoute` permite `patient`/`doctor`/`assistant` (excluye admin/anon) → médico/asistente acceden a su lado paciente (`/paciente/mis-atenciones`, `/paciente/perfil`) con menú/logout, "Ir al panel" (copy neutro) y nota azul de "cuenta personal". Sin RLS/DB/selector de modo (Fase 2 pendiente). **2 fixes destapados por Fase 1:** `useDashboard` vía `useClinicContext` (el home del panel reventaba para asistentes — lookup inline de `doctors` por profile_id, bug pre-existente) y `listMyAppointments` con **filtro explícito** `patient.profile_id=uid` (solo-RLS hacía que médico/asistente vieran las citas de su clínica como "Mis atenciones" propias; Camilo 66→11, paciente puro sin cambio). **Afiliación — médico desde paciente existente (PR #122, `s7_42`):** `admin_approve_and_create_doctor` ya no crashea con `users_phone_key` cuando el teléfono ya tiene cuenta — clasifica (helper `_affiliation_classify` + RPC `admin_affiliation_preflight`) y ramifica: `new` (crea auth.user dormant), `reuse_patient` (reusa el profile del paciente **sin nuevo auth.user**, solo con `confirm_reuse`, fill de `full_name` solo si vacío, **sin tocar phone/email/role**), o bloquea (`P0010` médico existente / `P0011` rol sensible / `P0012` identidad ambigua / `P0013` requiere confirmación). UI: banner por clasificación + confirmación + link a ficha. **Decisión: en reuse NO se copia el email del lead** (credencial sensible; se completa/valida en el reclamo). smoke 12/12. **Lista de espera global (PR #120, `s7_41`):** página `/admin/lista-espera` (gate `AdminOnlyRoute`) + NavLink; RPC `admin_list_waitlist` (`SECURITY DEFINER`, `is_admin()`) con join médico/perfil/especialidad/clínica + filtros (estado/médico/especialidad/búsqueda/rango de fechas) + paginación + `total_count`; reutiliza `admin_update_waitlist_entry` para el cambio de estado; smoke 10/10. **RPC F5 tolerante a teléfono crudo (PR #118, `s7_40`):** helper SQL `normalize_phone_sv` (espejo de `normalizePhoneSV`, `IMMUTABLE`, sin EXECUTE para anon/authenticated) + `find_patient_match_candidates` canonicaliza el teléfono en ambos lados → tolera `71234567`/`+503…`/espacios/guiones/paréntesis; firma/salida idénticas, cross-clínica sin PII, sin frontend; smoke 11/11. **Robustez del gate del panel (PR #116):** `useClinicContext` usa `getSession()` (no `getUser()`) → elimina el race de primera carga; `PanelLayout` con timeout 8s + `.catch()` + estado recuperable → sin spinner infinito; errores tipados (`auth`/`no_clinic`/`no_doctor`/`role`/`unknown`) con copy específico para estructurales. Frontend-only. **Catálogos personalizados por médico — EJE 100% COMPLETO** (#105 diseño + #106/`s7_37` snapshot histórico + #107/`s7_38` global+personal+RLS + #108 UI consulta + #109 UI admin médico + #110/`s7_39`+#111 infra/dry-run + **migración de base replicada aplicada**: Base Lucy 127 dx / 736 med, "Míos" solo lo propio, 1650 dx + 9568 med inactivadas, `catalog_migration_map` 11218 filas, sin hard-delete, sin reapuntar FK + **#114 UI de LucyAdmin para administrar la Base Lucy global — `/admin/catalogos`, UI-only sin migración**). Eje Correcciones post-firma COMPLETO (#74–#85, migraciones `s7_28..s7_31`). **Paciente Global Fases 1–3 COMPLETAS:** #87 (`s7_32`) identidad global + #88 UI `/paciente/perfil` + #90 (`s7_33`) sync espejo + guard + #91 UI médico read-only. **Cambio de teléfono por OTP COMPLETO:** #93 (`s7_34`) trigger de sync `auth.users.phone → profiles/patients` + audit + #94 UI paciente + #95 UI médico (`ChangePhoneModal`). **Paciente Global Fase 5 (dedup preventivo) COMPLETA:** #98 diseño + #99 (`s7_35`) backend RPC `find_patient_match_candidates` + #100 UI `PatientMatchHints`. **Mi equipo Fase 2 (ciclo de vida de invitación) COMPLETA:** #102 (`s7_36`) backend (expiración TTL 14d + `resend_invitation`) + #103 UI `EquipoPage` (vigentes/vencidas, Reenviar, badge "Vencida").
- **Sprint 6 — Reputación médica:** ✅ completado (PRs #2–#10).
- **Sprint 7 — Admin SaaS + Robustez pacientes:** ✅ Fases A, B, B2-A, B3-doctor, B3-admin (PRs hasta #30).
- **Pre-piloto (PRs #32–#58):** ✅ reclamo seguro, foto perfil, auth email+password (PR-A y PR-B), Security Gate (5 hallazgos cerrados), directorio informativo, lista de espera real, Paciente Global Fase 1, SMTP externo Resend, dominio público `lucycare.app`, password en flujo de Reclamar perfil, mitigación del flujo público "Soy médico", Afiliación médica Fase 1 (captura de leads + bandeja admin), **Afiliación Fase 2 (admin convierte lead aprobado en doctor `listed_only` con email override)**.
- **Migraciones aplicadas:** `s4_*`, `s5_01..s5_07`, `s6_01..s6_10`, `s7_01..s7_51` (`s7_26` = gate clínico asistente / PR #67; `s7_27` = límite de asistentes / PR #70; `s7_28` = inmutabilidad consultas firmadas Etapa A / PR #74; `s7_29` = corrección controlada Etapa B1 / PR #76; `s7_30` = distinción adenda texto vs receta — `affects_prescriptions` / PR #81; `s7_31` = amend de diagnósticos/antecedentes/vitales + inmutabilidad de `vitals` / PR #84; `s7_32` = Paciente Global Fase 2 — identidad global en `profiles` (DUI/DOB/género/depto/muni) + UNIQUE parcial de documento + UPDATE column-restricted + coherencia muni-depto + audit / PR #87; `s7_33` = Paciente Global Fase 3 — sync espejo `profiles→patients` (trigger) + guard server-side de identidad local (trigger, P0030) + claim copia identidad global→local / PR #90; `s7_34` = Cambio de teléfono por OTP — trigger `AFTER UPDATE OF phone ON auth.users` que sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard) + audit `edited_via='phone_change'` / PR #93; `s7_35` = Paciente Global Fase 5 — RPC `find_patient_match_candidates` (`SECURITY DEFINER`) para dedup preventivo: intra-clínica con detalle, cross-clínica/global solo señal mínima sin PII, gate `is_clinic_member`/`is_admin` / PR #99; `s7_36` = Mi equipo Fase 2 — `clinic_invitations.expires_at` (TTL 14d, `invitation_ttl()`) + `team_seats_used` excluye vencidas + `accept_clinic_invitations` ignora vencidas + RPC `resend_invitation` (titular, revalida cupo si vencida, extiende la misma fila) / PR #102; `s7_37` = Catálogos — snapshot histórico de nombre en `prescriptions`/`consultation_diagnoses` (triggers `BEFORE INSERT` + backfill) / PR #106; `s7_38` = Catálogos — modelo global+personal: `diagnoses`/`medications.doctor_id` nullable (NULL=global) + `doctor_catalog_hidden` + RLS reconstruida + audit / PR #107; `s7_39` = Catálogos — infra de-dup: `catalog_migration_map` + UNIQUE parcial sobre globales / PR #110/#111; `s7_40` = Paciente Global Fase 5 — RPC F5 tolerante a teléfono crudo: helper `normalize_phone_sv` (`IMMUTABLE`, espejo de `normalizePhoneSV`, sin EXECUTE anon/authenticated) + `find_patient_match_candidates` canonicaliza el teléfono en ambos lados / PR #118; `s7_41` = Lista de espera global — RPC `admin_list_waitlist` (`SECURITY DEFINER`, `is_admin()`) cross-médicos con join médico/perfil/especialidad/clínica + filtros + paginación + `total_count` (count(*) OVER) / PR #120; `s7_42` = Afiliación médico desde paciente existente — helper `_affiliation_classify` + RPC `admin_affiliation_preflight` + `admin_approve_and_create_doctor` reescrita (branches new/reuse_patient/block; reuse sin nuevo `auth.user`, fill `full_name` si vacío, sin tocar phone/email/role; P0010–P0013) / PR #122; `s7_43` = B2 Confirmación post-claim — `patients.link_confirmed_at` + tabla `patient_link_rejections` (`pending_review`, RLS admin-only) + RPCs self-gated `confirm_patient_link`/`reject_patient_link` (P0040) + `claim_patient_records` excluye pares rechazados (conserva la copia de identidad global→local de s7_33) / PR #128; `s7_44` = Administración de LucyAdmins Fase 1 — tabla `platform_admin_invitations` (pending/active/revoked, TTL 14d, RLS admin-only) + RPCs `admin_invite_platform_admin` (cuenta dedicada, P0050/P0051/P0053/P0055) / `accept_platform_admin_invitation` (activación al primer OTP, fill full_name) / `admin_revoke_platform_admin` (guard último admin P0052, bloqueo inmediato) / `admin_list_platform_admins`; `profiles.role=admin` sigue siendo el único bit, `is_admin()` intacta / PR #132; `s7_45` = Paciente Global Fase 4 / F4-1 — claim tolerante fila por fila: columnas **pasivas** `merged_into_patient_id` (FK `patients` sin CASCADE + CHECK `<> id`) + `merged_at` (infra F4-2) + `claim_patient_records` reescrita con LOOP `BEGIN/EXCEPTION` por ficha (la colisión `UNIQUE(clinic,doc)` ya no aborta el claim completo; skips `claim_skipped_unique`/`claim_skipped_error` auditados con SQLSTATE; cliente solo conteos; excluye fichas `merged_into_patient_id IS NOT NULL`); sin merge, sin tocar identidad/`auth.users`, sin hard-delete / PR #135; `s7_46` = Paciente Global Fase 4 / F4-2 backend del merge admin de fichas intra-clínica — tabla `patient_merge_log` + `admin_merge_patients_preflight` (dry-run) + `admin_merge_patients` (transaccional) + helper `_patient_merge_eligibility` + bypass propio `app.merging_patients` en los dos guards (firma `s7_29` + identidad P0030 `s7_33`); mueve appointments/consultations/vitals source→target, neutraliza la fuente (sin hard-delete, teléfono conservado), audit `admin_merge`; **V1 = `same_profile`** (same_document inalcanzable intra-clínica por el UNIQUE); bloqueos P0060–P0068; sin UI/unmerge/identidad global / PR #138; `s7_47` = Paciente Global Fase 4 / F4-3-search — RPC read-only `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`/`P0001`, anon sin EXECUTE) que lista **grupos** fusionables V1 `same_profile` por `(clinic_id, profile_id)` con fichas vivas (`is_active AND merged_into IS NULL`) `>= 2`; por grupo identidad mínima + conteos + `confirmed/unconfirmed_links_count` + `has_unconfirmed_links` + warning `W_UNCONFIRMED_LINK`; sin contenido clínico, sin candidatos por teléfono/nombre, sin merge, sin escritura, sin tocar identidad global/`profiles`/`auth.users` / PR #140; `s7_48` = Paciente Global Fase 4 / Unmerge formal — backend de la reversa del merge: bypass propio `app.unmerging_patients` en los dos guards (firma `s7_29` + identidad P0030 `s7_33`, los previos intactos) + helper `_patient_unmerge_eligibility` + `admin_unmerge_patients_preflight(p_merge_log_id)` (dry-run) + `admin_unmerge_patients(p_merge_log_id, p_reason)` (transaccional, `FOR UPDATE`, revalida server-side, devuelve `moved_ids` destino→fuente solo las que siguen en el target, restaura la fuente desde `source_snapshot`, rellena `unmerged_at`/`unmerged_by`/`unmerge_reason` append-only, audit `admin_unmerge`); reversa atómica, no hard-delete, no toca `profiles`/`auth.users`/identidad global; el esquema de `patient_merge_log` NO cambia (`unmerge_*` ya existían de `s7_46`); códigos P0070–P0077 (P0074 conflicto de documento, P0077 profile del snapshot inexistente); check 5/5 + smoke 17/17 / PR #147; `s7_49` = F4-3b V1 / backend de la bandeja de vínculos rechazados — columna `patient_link_rejections.resolution_note` + RPC read `admin_list_patient_link_rejections(p_status)` (admin-only, metadatos + estado actual de la ficha, sin contenido clínico) + RPC `admin_resolve_link_rejection(p_rejection_id, p_resolution_note, p_reopen)` (resolver motivo ≥10 / reabrir; audit en cada transición; el reopen conserva el estado previo; **P0080**/**P0081**); bookkeeping (no toca `patients`/`profiles`/`auth.users`, no re-vincula); la fila sigue excluyendo el par del auto-link mientras exista; UI en #153; check 5/5 + smoke 11/11 / PR #151; `s7_50` = Mi equipo — `accept_clinic_invitations` tolerante a formato de teléfono: el gate (sobre `profiles.phone`) y el loop (sobre `clinic_invitations.phone`) comparan con `normalize_phone_sv(...)` en ambos lados (cierra bug ACTIVO de onboarding de asistentes: invitación `+503XXXXXXXX` vs `profiles.phone` `503XXXXXXXX` → el gate literal fallaba con el error silenciado en el login); **misma firma**, misma lógica de cupos/asientos, sin tocar identidad global/`auth.users`/UI/`database.types.ts`; sin backfill (0 invitaciones pendientes vigentes en prod); check verde + smoke 7/7 (fixtures aisladas `F50_FIXTURE`, 0 residuales) / PR #156; `s7_51` = "Mi lista de espera" en panel médico/asistente — helper `_clinic_waitlist_authorize` (gate: médico dueño `get_user_doctor_id()=doctor_id` **o** miembro `is_clinic_member`; `P0001`/`P0107`) + RPC `clinic_list_waitlist(p_doctor_id, p_status, p_limit, p_offset)` (`SECURITY DEFINER`, pendientes primero + `total_count` vía `count(*) OVER()`; `P0105` estado inválido) + RPC `clinic_update_waitlist_entry(p_entry_id, p_status, p_notes)` (estado/nota + audit `edited_via='clinic'`; `P0105`/`P0106`; **fix `v_old waitlist_entries%ROWTYPE`** — antes `v_old record` abortaba toda actualización con `55000`); paralelas a las `admin_*` (intactas), sin columnas nuevas, sin tocar el esquema de `waitlist_entries` / RLS / dedup / `submit_waitlist_entry` / identidad / `auth.users`; firmas de las 2 RPCs en `database.types.ts`. Checks: `check-s7_51.mjs` (3/3) + `_smoke-s7_51.mjs` (9/9; fixtures `F51_FIXTURE`, 0 residuales) / PR #163). `s7_52` = Slugs Fase 1 — columna `doctors.slug` (nullable + índice UNIQUE parcial `WHERE slug IS NOT NULL` + CHECK de formato `^[a-z0-9]+(-[a-z0-9]+)*$`) + helper `slugify_name(text)` (`IMMUTABLE`, normaliza tildes/ñ) + `_doctor_next_slug(text)` (`SECURITY DEFINER`, `pg_advisory_xact_lock` por base, sufijo `-N`) + trigger `set_doctor_slug` (`BEFORE INSERT/UPDATE`, `WHEN is_published AND slug IS NULL` → genera desde `profiles.full_name`, **congela**) + backfill determinista de publicados; `database.types.ts` actualizado; check `check-s7_52.mjs` + smoke `_smoke-og-meta.mjs` (lógica de meta/sitemap) / PR #208. `s7_53` = Analytics Fase 3 / PR-A backend del dashboard de conversiones (LucyAdmin) — 2 RPCs `SECURITY DEFINER STABLE` de SOLO LECTURA con gate `is_admin()` (→ P0001 anon/médico/asistente/paciente), `SET search_path=public`, REVOKE PUBLIC/anon + GRANT authenticated: `admin_conversion_summary(p_date_from date, p_date_to date, p_specialty_id uuid, p_department_id text, p_municipality_id text) → jsonb` (reservas total + by_source manual/lucy_directorio/lucy_seguimiento con fuentes ausentes=0 + by_status por flags de `appointment_statuses` — completadas=`affects_revenue`, canceladas/no_show por `is_final`+`name`, pendientes=`NOT is_final`; waitlist/afiliaciones por estado; **reclamos desde `audit_log` `edited_via='claim_self_service'`, NO `tos_accepted_at`**; oferta/supply snapshot; reviews total/visibles/avg) + `admin_doctor_conversion_ranking(p_date_from date, p_date_to date, p_limit int, p_specialty_id uuid)` (por médico slug/nombre público/especialidad + reservas totales/directorio/waitlist/reviews/avg). **Solo agregados, 0 PII, 0 clínico; no escribe, no crea tablas, no toca RLS/datos.** Filtros geo `department_id`/`municipality_id` son **TEXT** (clinics/departments/municipalities.id son IDs cortos text, no uuid; corregido en revisión pre-SQL); `specialty_id` uuid. `database.types.ts` con las 2 firmas. **Sin UI (PR-B pendiente).** check `check-s7_53.mjs` + smoke `_smoke-s7_53.mjs` (32/32, fixtures aisladas + geo 8b + 0 PII + cleanup 0 residuales) / PR #245. `s7_54` = Analytics Farma / PR-A backend de inteligencia de prescripción (LucyAdmin) — 3 RPCs `SECURITY DEFINER STABLE` de SOLO LECTURA con gate `is_admin()` (→ P0001), `SET search_path=public`, REVOKE PUBLIC/anon + GRANT authenticated: `admin_pharma_summary(date,date,doctor?,specialty?)→jsonb` (total recetas / medicamentos únicos / médicos prescriptores / consultas firmadas con meds / by_source global-personal / permanentes `duration_unit='permanente'`) + `admin_pharma_medication_ranking(date,date,doctor?,specialty?,limit,min_count)` (por medicamento: snapshots nombre/PA/concentración/presentación + is_global + times_prescribed + consultations_count + distinct_doctors) + `admin_pharma_doctor_ranking(date,date,specialty?,limit,min_count)` (por médico: nombre público/especialidad + total/únicos/global/personal/permanentes). **Base vinculante:** `prescriptions.is_current=true` + `consultations.status='signed'` + fecha `signed_at` + snapshots (`s7_37`, no depende del catálogo actual) + fuente `medications.doctor_id`. **Umbral de privacidad:** los dos rankings aplican `HAVING count(*) >= p_min_count` (default 3) → celda mínima; el summary no se umbraliza. **Solo agregados, 0 PII, 0 clínico** (nunca patient_id/consultation_id/instructions/alternatives/dosage/frequency crudos/relación paciente↔medicamento). Diseño en `docs/ANALISIS_ANALYTICS_FARMA.md`. `database.types.ts` con las 3 firmas. **Sin UI (pendiente).** check `check-s7_54.mjs` + smoke `_smoke-s7_54.mjs` (27/27, fixtures aisladas + 0 PII + cleanup 0 residuales) / PR #250. `s7_55` = **Fix del bug de doble reserva (anti-solapamiento atómico)** — trigger `prevent_appointment_overlap` (`BEFORE INSERT OR UPDATE OF doctor_id, start_time, end_time, status_id ON appointments`, `SECURITY DEFINER` + `SET search_path=public`) que rechaza (`RAISE P0090`) crear/mover una cita que se **solape** con otra cita **activa** del mismo médico. **Causa raíz del incidente (prod):** los 3 flujos (reserva pública `booking.service`, walk-in `walkIn.service`, reschedule `appointments.service`) validaban conflictos **solo client-side** (`check-then-insert/update`), sin guard atómico en DB; el trigger `s6_04` solo valida la ventana de disponibilidad, no el solapamiento → **race condition TOCTOU**: dos reservas concurrentes ambas leen "sin conflicto" y ambas insertan (confirmado: 2 `programada` idénticas para Camilo `2026-07-06 13:00`). **Regla de "activo" alineada al catálogo:** bloquea si `appointment_statuses.is_final = false` (programada/confirmada/en_sala); no bloquea estados finales (cancelada/no_asistio/atendida y futuros); una cita entrante final tampoco se valida. **Atomicidad:** `pg_advisory_xact_lock(doctor)` serializa reservas concurrentes del mismo médico → cierra el race. **Solapamiento** por `tstzrange [start,end) '[)'` (adyacentes no chocan). `SECURITY DEFINER` imprescindible (ver todas las citas del médico saltando RLS). Sin RPC/tablas nuevas/RLS/`database.types.ts`. check `check-s7_55.mjs` + smoke `_smoke-s7_55.mjs` (12/12, fixtures aisladas + respeto a `s6_04` + cleanup 0 residuales) / PR #251. **PR-B UX #252 (frontend/service-layer, sin migración):** `SLOT_TAKEN_MESSAGE` + helper `isSlotOverlapError` (`err.code==='P0090'` con fallback por texto) en `availability.service.ts`, mapeando `P0090` a **"Ese horario acaba de ocuparse. Por favor elegí otro horario disponible."** en los 3 flujos (público/walk-in/reschedule). **Duplicado real de prod = acción operativa manual del owner** (no tocado por el fix). `s7_56` = **Índices de soporte para Analytics/Farma** — migración **additive-only** (solo `CREATE INDEX IF NOT EXISTS`, sin lógica/RPC/UI/RLS/`database.types.ts`) con **7 índices** preventivos de performance sobre las columnas calientes de los dashboards admin de solo-lectura (`s7_53`/`s7_54`). **Criterio rector:** *"la analítica debe observar la operación, no competir con ella"*. Índices: `idx_prescriptions_consultation` · `idx_consultations_signed` (parcial `WHERE status='signed'`) · `idx_audit_claim_self_service` (parcial `WHERE new_data->>'edited_via'='claim_self_service'`, ataca la peor query = claims del summary de conversión sobre `audit_log`, la tabla de mayor crecimiento) · `idx_prescriptions_medication` · `idx_prescriptions_current` (parcial `WHERE is_current`) · `idx_appointments_created` · `idx_reviews_created`. Huecos confirmados por **P0 read-only** contra `pg_indexes` de la DB real (`prescriptions`/`audit_log` solo tenían PK; `consultations`/`appointments`/`reviews` sin índice de fecha/join analítico; `waitlist_entries`/`doctor_affiliation_requests` ya bien indexadas → intactas). `CREATE INDEX` normal (no `CONCURRENTLY`) por volumen bajo. **Aplicado en Supabase + verificado en DB real** (7 índices, predicados parciales correctos) + **smokes verdes sin regresión** (`_smoke-s7_53.mjs` 32/32, `_smoke-s7_54.mjs` 27/27 — agregados idénticos). **NO** hay `check-s7_56.mjs` (una migración solo-índices no tiene superficie observable vía supabase-js; la verificación es la consulta `pg_indexes` + los smokes existentes). `min_count=1` intacto; sin refactor de claims (opción B = follow-up); sin tocar Analytics V2/materialized views/Search Console/SEO/Slugs/Brand-Favicon / PR #259. `s7_57` = **Niveles de acceso administrativo LucyAdmin (PR-A backend/DB)** — acceso admin por niveles aditivo y de mínimo privilegio, sin tocar `is_admin()` ni el enum `user_role`. Tabla `lucyadmin_access` (`access_level ∈ {directory_editor, operations_admin}`, RLS admin-only; **`owner_admin` NO vive en la tabla** = `profiles.role='admin'` vía `is_admin()`, superset absoluto) + funciones `can_access_lucyadmin`/`can_manage_directory`/`can_manage_doctor_operations`/`can_manage_lucyadmin_users`(owner-only)/`my_lucyadmin_access` (+ helpers `_lucyadmin_rank`/`_lucyadmin_actor`) + re-gate quirúrgico `is_admin()`→`can_manage_directory()` de las RPCs de datos públicos del médico (clínica/info/servicios no-destructivos/`is_published`) + RPCs nuevas acotadas `directory_list_doctors`/`directory_get_doctor_detail` (sin email/phone de login) + `directory_update_doctor_name` (solo `profiles.full_name`) + audit `edited_via`=actor efectivo. `directory_editor` = primer uso real; `operations_admin` diseñado pero **inerte** (sin RPC operativa abierta). Owner-only intacto: `auth.users`/login/roles/verify/`lucy_status`/`is_operational`/`booking_enabled`/avatar(Storage RLS)/hard-delete/pacientes/consultas/recetas/diagnósticos/lista de espera/afiliaciones/administradores/Analytics/Analytics Farma/pagos/ownership. `database.types.ts` (tabla + 8 firmas). Backend/DB only (**PR-B frontend pendiente**). check `check-s7_57.mjs` (15/15) + smoke `_smoke-s7_57.mjs` (43/43, fixtures aisladas + cleanup 0 residuales) / PR #261. `s7_58` = **F6 — corrección post-firma de receta que realmente puede LIMPIAR campos** (`CREATE OR REPLACE amend_consultation`, **misma firma de 8 args**). El bloque de receta abandona el `COALESCE(NULLIF(...), viejo)` (decidía por el VALOR) y adopta el patrón de **presencia de clave** que la propia RPC ya usaba para vitales/diagnósticos/antecedentes: **ausente = conservar · `null`/`''` = limpiar (`NULL`) · valor = actualizar**, aplicado a `duration_value`, `duration_unit`, `dosage`, `frequency`, `instructions`. Más **regla clínica server-side: `duration_unit='permanente'` ⇒ `duration_value = NULL` SIEMPRE** (en `replace` y en `add`), sin depender del cliente. Frontend: `CorrectConsultationModal` manda **siempre** la clave `duration_value` (`null` al borrarla o al elegir permanente) y los textos vacíos como `null` en vez de `''`. **NO tocó** gates (`P0011`/`P0012`/`P0013`/`42501`), versionado (`is_current`/`replaces_id`/`version`), snapshots before/after, `consultation_amendments`, `affects_prescriptions`, `audit_log`, ni los bloques de diagnósticos/antecedentes/vitales; **`alternatives` se conserva intacta (= F7)**; **sin backfill** (históricos con `''` o duración fantasma quedan como están). **Aplicada manualmente por el owner en Supabase antes del merge** (la RPC nueva es retrocompatible con el frontend viejo; el frontend nuevo **requiere** la RPC nueva, porque con la vieja un `null` **restauraba** el valor anterior). check `check-s7_58.mjs` + smoke `_smoke-s7_58.mjs` (**28/28**; el mismo smoke daba **8 fallas** contra la RPC vieja, reproduciendo el bug; fixtures **propias** `F6_FIXTURE` — paciente/cita/consulta firmada/recetas creados desde cero, **jamás Pepe Toro ni Katherine**, porque la corrección crea **adendas irreversibles** — cleanup 0 residuales) / PR #277. `s7_59` = **F7 — alternativas terapéuticas corregibles en la receta post-firma** (`CREATE OR REPLACE amend_consultation`, **misma firma de 8 args**; **copia byte a byte de `s7_58` con dos puntos de diff, ambos sobre `alternatives`**). `replace`: `v_old.alternatives` → `CASE WHEN v_op ? 'alternatives' THEN NULLIF(btrim(...),'') ELSE v_old.alternatives END` (misma regla de presencia de clave: ausente=conservar · `null`/`''`=limpiar · valor=actualizar). `add`: se agrega la columna `alternatives` al `INSERT` (sin valor → `NULL`). Frontend: campo "Opciones alternativas" en `CorrectConsultationModal` (existentes y nuevos), la clave viaja siempre (`null` al borrarla), + tipos en `consultations.service`. **NO tocó** gates, versionado, snapshots, `consultation_amendments`, `affects_prescriptions`, `audit_log`, diagnósticos/antecedentes/vitales, ni la semántica de nulls/`permanente` de F6; **`RecetaPrint` NO se tocó** (ya imprimía alternativas); **sin backfill**. **Aplicada manualmente por el owner en Supabase antes del merge** (retrocompatible con el frontend viejo). check `check-s7_59.mjs` + smoke `_smoke-s7_59.mjs` (**27/27**; el mismo smoke daba **4 fallas** contra la RPC vieja `s7_58`, e incluye **no regresión de F6**; fixtures **propias** `F7_FIXTURE`, jamás Pepe Toro ni Katherine — cleanup 0 residuales) / PR #279. **Con F7 el eje clínico F1–F7 queda CERRADO.** `s7_60` = **hardening de grants sobre `public.doctors`** (seguridad; ver el bloque 🔒 del snapshot). Cierra tres vectores nacidos del `GRANT ALL` del schema inicial que `doctors` nunca perdió (a `profiles` se lo quitó `s7_16`): `anon` leía `license_number` de todos los publicados (la RLS filtra filas, no columnas); `authenticated` podía escribir `lucy_status` sobre su propia fila y **auto-verificarse** (`is_verified` es GENERATED de `lucy_status`), además de `license_number` e `is_operational`; y cualquier autenticado sin fila de `doctors` podía **insertarse como médico**. `REVOKE ALL` a `PUBLIC`/`anon`/`authenticated` + `GRANT SELECT` de 16 columnas a `anon` + `GRANT SELECT` completo y `GRANT UPDATE` de 8 columnas a `authenticated`, **sin INSERT para nadie**. **NO toca RLS/policies, `is_verified`, `lucy_status`, el claim, las RPCs admin (`SECURITY DEFINER`), datos ni esquema.** Sin columnas nuevas, sin backfill, sin `src/`. **Riesgo residual conocido: `authenticated` conserva `SELECT (license_number)`** → PR-B **diferido**, se pliega a `doctor_credentials` F1 (ver el bloque ⚠️ del snapshot). check `check-s7_60.mjs` (**2/6 → 6/6**) + smoke `_smoke-s7_60.mjs` (**13/25 → 25/25**), ambos **sin `service_role`** y con pruebas sobre UUID inexistente (no leen ni tocan datos reales) / PR #289. `s7_61` = **`doctor_credentials` F1-a — modelo** (enums `credential_type`/`credential_status` + tabla + RLS **por fila** + grants + índice antifraude parcial + audit con `value` **redactado** + backfill 114 `pending` + trigger de sync `doctors.license_number → doctor_credentials`). Aditiva, sin lectores. Ver el bloque 🔑 del snapshot. check `check-s7_61.mjs` 5/5 + smoke `_smoke-s7_61.mjs` **22/22** (fixtures `F1A_FIXTURE`, cleanup 0 residuales) / PR #291. `s7_62` = **`doctor_credentials` F1-b — cutover de lectores** (`CREATE OR REPLACE` de `claim_doctor_profile` —cuerpo verbatim de `s7_13` salvo la sección 5— y de `sync_license_to_credential` —mapeo de `doctor_credentials_registry_uniq` a **`P0091`** vía `GET STACKED DIAGNOSTICS … CONSTRAINT_NAME`, el resto de `unique_violation` se re-lanza intacto—). Panel, receta y claim leen `doctor_credentials`; **`pending`/`verified` se usan · `rejected` NO y sin fallback · fallback a la columna solo si NO existe fila JVPM**. **NO cierra el riesgo residual** (columna, dual-write, fallback y grant siguen vivos → F1-c). Ver el bloque 🔑 del snapshot. check `check-s7_62.mjs` 2/2 + smoke `_smoke-s7_62.mjs` **E2E 13/13** + **QA visual 7/7** en superficies reales / PR #292. Verificables con `node scripts/check-s7_NN.mjs`.
- **Mi equipo Fase 1 — límite de asistentes (PR #70, `s7_27`) — activo:** plan base = 1 titular + **2 asistentes** (`team_seat_limit()`=2 fijo). El conteo incluye **asistentes activos + invitaciones pendientes**. Enforcement **server-side validado**: trigger `BEFORE INSERT` en `clinic_invitations` (`P0001`) + revalidación en `accept_clinic_invitations`. UI `/panel/equipo` con contador `n/2` + botón deshabilitado al límite. El smoke destapó y **corrigió un bug pre-existente** en `accept_clinic_invitations` (cast `clinic_member_role::user_role` imposible → ahora `::text::user_role`; el accept nunca había funcionado por falta de asistentes). **Usuarios adicionales → fase de pagos/add-ons** (`team_seat_limit()` es el punto de enganche con `subscriptions`).
- **Mi equipo Fase 2 — ciclo de vida de invitación (PR #102 `s7_36` backend + #103 UI) — COMPLETA:** `clinic_invitations.expires_at` (TTL **14 días**, `invitation_ttl()`); "vencida" = estado **derivado** (`expires_at < now()`), sin cron. **Vencidas no consumen cupo** (`team_seats_used` las excluye) y **no se aceptan** (`accept_clinic_invitations` filtra `expires_at > now()`). **`resend_invitation(id)`** RPC (`SECURITY DEFINER`, solo titular): extiende la **misma fila** (no crea otra), y si estaba vencida **revalida cupo** antes de revivirla. UI `/panel/equipo`: pendientes **vigentes** ("expira en N días", Reenviar, Cancelar) vs **vencidas** (badge "Vencida", Reenviar primario), contador alineado (vigentes + activos), copy de seguridad reforzado. Smoke `s7_36` 8/8 + OK visual.
- **Catálogos personalizados por médico (diagnósticos y medicamentos) — EJE 100% COMPLETO (PRs #105–#111 + #114 + migración de datos):** modelo **global (base Lucy, `doctor_id IS NULL`, read-only para médicos, administrable por LucyAdmin desde `/admin/catalogos`) + personal por médico**. `s7_37` snapshot histórico de nombre en receta/diagnóstico (editar el catálogo **no** altera históricos firmados; lectura/impresión usan snapshot, fallback al JOIN). `s7_38` global+personal + `doctor_catalog_hidden` (ocultar globales por médico) + RLS (SELECT propios∪globales∪admin; INSERT/UPDATE solo propios; globales solo `is_admin`; no-médico sin escritura; sin DELETE) + audit `audit_catalog`. UI: consulta busca global∪propios con etiqueta discreta "Base Lucy" y "Crear … propio" (#108); admin del médico `/panel/catalogos` con segmento **Míos | Base Lucy** (read-only + Ocultar/Mostrar + Clonar a propio), guard de rol (#109). **De-dup de la base replicada del seed viejo aplicada** (`migrate-catalog-base.mjs`, seed `s5_03`/`s5_04` como fuente de verdad): **Base Lucy = 127 dx / 736 med canónicos**; "Míos" = solo personalizaciones reales (4 dx + 4 med en Camilo); **1650 dx + 9568 med copias replicadas inactivadas** (nunca hard-delete); `catalog_migration_map` (11218 filas) para reversibilidad; **FK no reapuntados** (snapshot protege históricos); globales de prueba "(base Lucy)" inactivados. **✅ Último cabo cerrado — UI de LucyAdmin para Base Lucy global (#114, UI-only, sin migración):** página **`/admin/catalogos`** (gate `AdminOnlyRoute`) + NavLink "Catálogos" en `AdminLayout`; tabs **Diagnósticos + Medicamentos** (Antecedentes fuera); crear/editar/inactivar/reactivar globales; búsqueda; activos/inactivos; **dedup amable** (`DuplicateCatalogError` + `23505`, refleja el UNIQUE `s7_39`); `usage_count` oculto; **no hard-delete**; candado `.is('doctor_id', null)` en los updates (no toca ítems personales). Servicio `src/services/adminCatalog.service.ts` (directo sobre tablas con sesión admin; sin RPCs nuevas). Backend ya estaba listo: RLS `s7_38` (`*_insert_admin`/`*_update_admin` con `is_admin()`) + `audit_catalog` + UNIQUE `s7_39` + snapshot `s7_37`. **Modelo final:** LucyAdmin administra globales; médicos usan/ocultan/clonan globales y crean propios; médicos **no editan globales directamente** (RLS).
- **Ubicación estructurada del médico (PR #64, `s7_25`) — live en producción:** LucyAdmin > Médicos > Editar > Clínica edita Departamento/Municipio con las listas jerárquicas del Home, guarda IDs en `clinics.department_id/municipality_id` (lo que filtra el directorio). Municipio depende del departamento; **obligatorios para médicos publicados/operativos** (enforcement server-side `P0004` + UI). Los 5 médicos publicados ya tienen ubicación estructurada.
- **Importación de médicos:** 100 cargados + 12 seed `*.lucycare.test` (desactivados en PR #38).
- **Médicos en producción hoy:** 5 publicados, 1 con `booking_enabled=true` (Camilo). Los otros 4 son "informativos" (sin agenda en línea).
- **Deploy:** Vercel auto-deploy desde `main`. `gh` CLI autenticado.
- **Service role:** rotado en PR #36. Scripts admin leen de `.env.local` (ENV vars), `service_role` no está en el repo.
- **Dominio público:** `https://lucycare.app` ✅ live en producción desde 2026-05-26 (PR #48). DNS gestionado en Cloudflare. `www.lucycare.app` redirige 308 a apex. `lucycare.vercel.app` permanece activo como **fallback temporal** (no desactivar). Previews siguen en `lucycare-git-*.vercel.app`.
- **SMTP transaccional:** Resend con dominio `lucycare.app` (verificado en Resend, DNS email — SPF/DKIM/DMARC — en Cloudflare), SMTP custom activo en Supabase Auth (PR #46). El rate limit builtin de ~4 emails/h ya no aplica.


## Frentes cerrados 2026-08 (PR #352)

- **ADMIN-DOCTOR-EXPORT-URL-P0 — #352** ✅ **CLOSED (2026-08-27).** Follow-up de
  `ADMIN-DOCTOR-EXPORT-P0`: el CSV de médicos pasa de **15 a 17 columnas**, con
  **`Slug`** y **`URL pública`**. `main` quedó en
  `f7213d29af0b61ea6104da8a962597bb005e658b`.

  **Backend:** `s7_79` = **migración 100**, **APPLIED / VERIFIED / CLOSED**,
  aplicada por el owner el 2026-08-27 **antes** del PR. Es un
  `CREATE OR REPLACE` de `admin_export_doctors` que añade **una sola clave** a la
  allowlist: `'slug', d.slug`. **No hay JOIN nuevo** —`doctors d` ya estaba
  unido para llegar a `profile_id` y `clinic_id`—, así que el plan de ejecución
  no cambia. Verificado con **A/B estructural**: quitando esa clave, el cuerpo es
  **idéntico** al de `s7_78`.

  **Lo que NO se tocó:** `s7_78` quedó intacta —es el registro de lo ejecutado—
  y **`admin_list_doctors` tampoco**. Podría haberse extendido para devolver el
  slug, pero es la fuente del predicado que comparten listado y export:
  arriesgarla para ahorrar un `d.slug` que ya se tenía no compensa. Una guarda
  POST **verifica que el listado no gane la columna**.

  **La regla de la URL, que es la decisión de producto del frente:**

  | Caso | `Slug` | `URL pública` |
  |---|---|---|
  | publicado + slug | lleno | `https://lucycare.app/doctor/<slug>` |
  | **NO publicado + slug** | **lleno** | **VACÍA** |
  | sin slug | vacío | vacía |

  `fetchDoctorDetail` filtra por `is_published = true`, así que la URL de un
  médico no publicado renderiza **«Médico no encontrado»**. Una columna llamada
  «URL pública» no debe contener enlaces muertos. **El caso existe de verdad:**
  `trg_set_doctor_slug` asigna el slug **al publicar y nunca lo reescribe**, de
  modo que un médico despublicado conserva el suyo — hoy son 3 de 115. Ese slug
  sí se exporta, porque es el valor canónico, pero su URL queda vacía. **El
  export NUNCA reconstruye el slug desde el nombre.**

  **Dominio:** `https://lucycare.app` como **constante literal** del servicio, no
  `window.location.origin`. Exportar desde un Preview habría producido enlaces
  `vercel.app` que fallan en cuanto el archivo sale del navegador que lo generó,
  y el error sería invisible hasta que alguien hiciera clic. La URL se arma en el
  frontend porque el dominio es **presentación, no dato**: guardarlo en la base
  obligaría a una migración para cambiarlo.

  **Sin cambios** en filtros, paginación, auditoría, `MAX_EXPORT = 10000`,
  `P0140`/`P0142`/`P0146`, gate `is_admin()`, grants, orden dentro de
  `jsonb_agg`, formato de fechas, BOM, CRLF ni protección contra formula
  injection. El bloque de auditoría es **idéntico** al de `s7_78`, comprobado por
  A/B: el slug es una columna más del archivo, y la auditoría registra **cuántas**
  filas salieron, no cuáles.

  **Validación:** `check-s7_79` **99/99** (estático, con A/B del instrumento) ·
  `check-admin-doctor-csv` **68/68** (**conductual**, cubriendo publicado+slug, no
  publicado+slug, slug `null`/`''`/espacios/`undefined`, dominio exacto y ausencia
  de `vercel.app`) · `check-s7_78` **117/117** · `check-crm-csv-fechas` **33/33**
  sin regresión · `_qa-crm-paginacion` **35/35** · `tsc` y `build` **PASS** ·
  **Vercel Production PASS**.

  **Smokes del owner:** Preview **PASS** y producción **PASS**, con la **URL real
  abierta manualmente en Internet**. Evidencia del CSV: **115 médicos · 17
  columnas · 39 slugs · 36 URLs públicas · 3 con slug histórico y URL vacía · 76
  sin slug ni URL · 0 inconsistencias · 0 URLs `vercel.app` · 0 duplicados**. Los
  **7** eventos de `audit_log` se verificaron uno por uno: solo metadata
  aprobada, y **ni el slug ni la URL aparecen en el payload**.

  **Verificación del bundle en producción**, tras la minificación: el código
  publicado es `function y(e){const t=e.slug?.trim();return!t||!e.publicado?"":
  `${b}/doctor/${t}`}` con `b = "https://lucycare.app"` — la regla «slug **y**
  publicado» sobrevive literalmente. Buscar la cadena completa
  `https://lucycare.app/doctor/` en el bundle **no sirve**: se arma con un
  template literal y nunca existe concatenada.

  **Nota operativa:** el despliegue de producción de este merge **tardó ~30
  minutos** en dispararse; el webhook de Vercel no creó el deployment hasta mucho
  después del merge, con `statuses = 0` y `check-runs = 0` mientras tanto. No fue
  un fallo del código: el deployment `6125658489` terminó en `success`. Conviene
  **no dar por caído un despliegue solo porque tarde**, y comprobar el bundle
  servido antes de concluir.

  **Turnstile:** el hostname temporal del Preview quedó **REMOVED** y
  `lucycare.app` permanece autorizado.

  **Deuda vigente, no corregida:** `admin_list_doctors` sigue ordenando por
  `created_at DESC` **sin desempate**, lo que afecta a la paginación de la
  PANTALLA, no al export. Y **`ADMIN-DOCTOR-EXPORT-P1`** (actividad y rating) y
  **`ADMIN-DOCTOR-EXPORT-P2`** (exportación asíncrona) siguen sin iniciar.

## Frentes cerrados 2026-08 (PR #351)

- **ADMIN-DOCTOR-EXPORT-P0 — #351** ✅ **CLOSED (2026-08-26).** Exportación CSV
  de la base administrativa de médicos desde `/admin/medicos`, **en producción**.
  `main` quedó en `61da06e372ecb5c8a4f602492f04ce77a72020f4`.

  **Backend:** `s7_78` = **migración 99**, **APPLIED / VERIFIED / CLOSED**,
  aplicada por el owner el 2026-08-26 **antes** del PR; entró como registro
  versionado y **no se ejecutó con el merge**. Crea **una sola función**,
  `admin_export_doctors`: no crea tablas, no altera nada existente y no migra
  datos. Su único efecto sobre datos es la fila de auditoría de cada export.

  **La decisión de diseño central:** en vez de repetir el `WHERE` del listado,
  **invoca `admin_list_doctors`** —que es `RETURNS TABLE` y por eso se puede usar
  en un `FROM`— y solo **enriquece por `id`** las cuatro columnas que esa RPC no
  devuelve: correo, dirección, departamento y municipio. Así el predicado de
  búsqueda y filtros vive en **un solo lugar** (`s7_04`) y **listado y export no
  pueden divergir**. Copiarlo habría creado dos definiciones que se separan al
  primer cambio, con un síntoma silencioso: un CSV que no coincide con lo que el
  admin ve. Verificado antes de escribirlo: `admin_list_doctors` aplica
  `LIMIT GREATEST(p_limit, 1)`, un **piso sin techo**, sin `LEAST` ni clamp, así
  que acepta `MAX_EXPORT + 1 = 10001`. La migración **comprueba ese supuesto en
  su POST** y abortaría si algún día adquiere un límite superior.

  **Orden:** el `ORDER BY` va **dentro de `jsonb_agg`**, la única construcción
  que PostgreSQL garantiza para un agregado; uno en la subconsulta no obliga a
  nada y el `ORDER BY` interno de la función invocada no sobrevive al JOIN. El
  desempate por `id` lo hace determinista entre corridas.

  **15 columnas:** Nombre · Especialidad · Teléfono · Correo · Clínica ·
  Dirección de clínica · Departamento · Municipio · Estado LucyCare · **Perfil
  reclamado** · **Verificado en LucyCare** · Publicado · Agenda habilitada ·
  Operativo · **Fecha de alta en LucyCare**. Los dos booleanos se derivan
  **exclusivamente de `lucy_status`**; los encabezados son explícitos porque
  «Verificado» a secas se confunde con la verificación de la credencial JVPM,
  que es **otro eje**. Fechas en `DD/MM/YYYY HH:mm` con `America/El_Salvador`
  fijada explícitamente. CSV UTF-8 con BOM, CRLF y la protección contra formula
  injection de `src/lib/csv.ts`, reutilizada sin tocarla.

  **Seguridad:** `SECURITY DEFINER` · `VOLATILE` (audita) · `search_path` fijo ·
  gate `is_admin()` con **`P0140`**. Privilegios **explícitos de los cuatro
  roles**, sin heredar defaults: `REVOKE` de `PUBLIC`, `anon` y `service_role`;
  `GRANT` **solo** a `authenticated`. **`directory_editor` es `authenticated`,
  recibe el GRANT y aun así obtiene `P0140`** — autenticado sí, autorizado no —
  y el botón vive solo en `OwnerDoctorsView`. **`MAX_EXPORT = 10000`**: se piden
  10 001 para distinguir «cabe» de «no cabe» y por encima **aborta con `P0146`**.
  **No trunca**: un CSV recortado en silencio es peor que un error, porque quien
  lo recibe no puede notar lo que falta.

  **Auditoría solo de metadata**, escrita **después** de las tres validaciones:
  actor, formato, nº de registros, **si hubo búsqueda (booleano, nunca el
  texto)** y los filtros normalizados. Cero nombres, correos, teléfonos o filas.
  Los **5** eventos generados en los smokes se verificaron uno por uno.

  **Validación:** `check-s7_78` **115/115** (estático, con A/B del instrumento) ·
  `check-admin-doctor-csv` **50/50** (**conductual**: el servicio real
  transpilado con esbuild y `buildDoctorsCsv` ejecutado sobre filas sintéticas —
  15 columnas, fechas incluido UTC que cruza el día y medianoche `00:15`, NULL,
  tildes y eñes, inyección de fórmulas, BOM en bytes, **10 000 filas en 342 ms**)
  · `check-crm-csv-fechas` **33/33** sin regresión · `_qa-crm-paginacion`
  **35/35** · `tsc` y `build` **PASS** · **Vercel Production PASS**.

  **Smokes del owner:** Preview **PASS** con 3 CSV revisados que **particionan el
  universo** —36 publicados + 2 no publicados/operativos + 77 no publicados/no
  operativos = **115**, sin solapes ni huecos— y producción **PASS**. El POST de
  producción confirmó la creación por contraste: la RPC pasó de `PGRST202` a
  **`42501 permission denied`**, que prueba a la vez que existe y que
  `service_role` y `anon` **no** pueden ejecutarla.

  **Turnstile:** el hostname temporal del Preview quedó **REMOVED** y
  `lucycare.app` **permanece autorizado**.

  **Decisiones cerradas:** **CSV en P0, no XLSX** —no hay formato condicional,
  varias hojas ni fórmulas que justifiquen una librería nueva en un bundle que ya
  arrastra un aviso de tamaño— · **sin `license_number` ni JVPM**, protegido por
  RLS **por fila** (`s7_61`) y con su columna en `doctors` en retiro lógico ·
  **sin citas, atenciones, última actividad, próxima cita ni rating** ·
  **fecha de reclamación, de verificación y de afiliación OMITIDAS por no existir
  fuente canónica fiable**, verificado dato a dato: `doctors.claimed_at` **no
  existe** —el `claimed_at` de `s7_13` vive dentro del payload de `audit_log`,
  no es una columna—, `tos_accepted_at` cubre **1/115**,
  `doctor_credentials.verified_at` está en **0/115** porque las 115 credenciales
  son `pending` (y el CHECK impide fecha sin verificación), y
  `clinic_members.joined_at` cubre **15/115**, todas con rol `owner`, que es
  titularidad y no afiliación · **Departamento y Municipio pueden salir vacíos**:
  solo **20/115** clínicas los tienen. Es **dato faltante, no defecto**.

  **Deuda separada, registrada y NO corregida:** `admin_list_doctors` ordena por
  `created_at DESC` **sin desempate**, así que dos médicos creados en el mismo
  instante pueden intercambiarse entre páginas **de la pantalla** — es un defecto
  de la paginación existente, no del export, que aplica su propio orden ·
  **`ADMIN-DOCTOR-EXPORT-P1`**: actividad y rating, si alguna vez se piden ·
  **`ADMIN-DOCTOR-EXPORT-P2`**: exportación asíncrona o por streaming, **solo si
  el volumen supera razonablemente los 10 000**.

  **Numeración:** `s7_78` estaba **reservado especulativamente** para dos frentes
  nunca iniciados; esas referencias pasaron a «migración futura (TBD)». Dos
  menciones quedan **deliberadamente sin tocar** dentro de
  `migrations/s7_76_patient_crm_foundation.sql` y su guía, porque ese archivo ya
  se aplicó y su contenido es el registro de lo ejecutado.

## Frentes cerrados 2026-08 (PR #350)

- **CRM-CSV-FECHAS-P0 — #350** ✅ **CLOSED (2026-08-26).** Las tres columnas de
  fecha/hora del **export CSV** del CRM de pacientes —`Fecha de registro`
  (`created_at`), `Última actividad` (`ultima_actividad`) y `Próxima cita`
  (`proxima_cita`)— se escribían como timestamps ISO crudos
  (`2026-08-02T13:49:49.181225-06:00`), ilegibles al abrir el archivo. Ahora
  salen como **`02/08/2026 13:49`**.

  **Frontend-only: sin SQL, sin migraciones, sin backend, sin configuración.**
  El CSV se genera **en el navegador**; la RPC `admin_export_patients_crm` solo
  devuelve filas JSON y no se tocó. Un único archivo de producto,
  `src/services/patientCrm.service.ts` (**+58 / −0**), más el check nuevo
  `scripts/check-crm-csv-fechas.mjs`.

  **Lo que NO cambió:** las otras **11 columnas quedan byte-equivalentes**, la
  visualización **en pantalla** sigue igual (`PatientsCrmTab` usa su propio
  formateador) y **`src/lib/csv.ts` no se tocó** — la protección contra
  CSV/formula injection y el escape RFC 4180 se aplican igual, porque el valor
  formateado sigue pasando por `csvCell`, y el BOM sigue presente. **NULL y
  `undefined` siguen dando celda vacía**; una fecha ilegible **conserva el valor
  original** en vez de escribir `Invalid Date`.

  **Dos detalles que solo aparecen al probarlo**, y que explican la
  implementación: se ensambla con `Intl.DateTimeFormat().formatToParts()` y no
  con `toLocaleString`, porque **`es-SV` es un locale de 12 horas** —sin
  `hourCycle` devolvería `01:49 p. m.`— y porque **`toLocaleString` intercala
  una coma** entre fecha y hora (`02/08/2026, 13:49`), que no rompería el
  archivo pero no es el formato pedido. Se usa **`hourCycle: 'h23'`** y no
  `hour12: false`: este último puede rendir `24:15` para la medianoche. La zona
  **`America/El_Salvador` se fija explícitamente** porque sin ella `Intl` usa el
  reloj del navegador y un admin conectado desde otro huso vería horas distintas
  para el mismo registro. `SV_TIMEZONE` de `src/utils/date.ts` **no está
  exportada**, así que reutilizarla habría ampliado el alcance.

  **Validación:** `check-crm-csv-fechas` **33/33** —prueba **conductual**: el
  servicio real transpilado con esbuild y `buildPatientsCsv` ejecutado sobre
  filas sintéticas, no inspección de texto—, cubriendo microsegundos con offset,
  UTC convertido a El Salvador (incluido el caso en que **cambia el día**),
  medianoche, NULL, fecha inválida, las 14 columnas y la protección de
  inyección. **Validado A/B**: la versión anterior devuelve el ISO crudo.
  Además `tsc` y `build` **PASS**, `_qa-crm-paginacion` **35/35**, **Vercel
  Production PASS** y **CSV descargado y validado por el owner en producción**.
  `main` quedó en `7f802d3ce1b3fe2c5b4278d063021ee8fd3b0181`.

  **Deuda PREEXISTENTE registrada, no corregida:** `check-s7_76` da `329/353`
  por incompatibilidad con **CRLF en Windows** (`core.autocrlf=true` frente a
  regex anclados en `;\n`). Demostrado A/B contra el archivo original: **no es
  regresión de este PR** y no afecta a producción.

## Frentes cerrados 2026-08 (PRs #348–#349)

- **ADMIN-DOCTOR-SEED-P0 — #348** ✅ **CLOSED (2026-08-24).** Creación de perfil
  médico sembrado desde LucyAdmin: `s7_73`/`s7_74`/`s7_75` (migraciones 94–96) +
  Edge Function `admin-create-seed-doctor` **`ACTIVE` v4**, con idempotencia y
  compensación. E2E real y cleanup **PASS**; **0 seeds en producción**. `main`
  quedó en `b9edf91d135c74404711c2b8f9bffebdc0ad497e`.
- **PATIENT-CRM-P0 — #349** ✅ **CLOSED (2026-08-25).** CRM de pacientes dentro de
  LucyAdmin, **en producción**, con separación estricta del expediente clínico.
  `/admin/pacientes` pasó de ser solo una consola de deduplicación a **tres
  pestañas**: «Base de pacientes» (nueva; la unidad es la **identidad global** de
  `profiles`), «Pendientes de identificar» (nueva; fichas **sin identidad**, donde
  quedan los **walk-ins**, que **no se suman** al universo del CRM) y «Fusión de
  fichas» (**la que ya existía, intacta** — ninguna de sus RPCs de merge, unmerge,
  preflight o rechazos fue modificada).

  **Backend:** `s7_76` (migración **97**, fundación de datos: 4 tablas colgando de
  `profiles`, RLS, 7 índices —incluido `idx_patients_profile_id`, el JOIN central
  que no existía— y auditoría por trigger) y `s7_77` (**98**, vista canónica + 3
  helpers privados + 4 RPCs `SECURITY DEFINER` gateadas con `is_admin()`). Ambas
  **APPLIED / VERIFIED / CLOSED el 2026-08-24**, con PRE, POST de catálogo y POST
  funcional en **0 FAIL**. **Entraron al PR solo como registro versionado: el
  merge no las ejecutó** —el repositorio no tiene ningún automatismo que las
  aplique—.

  **Corrección central de P0:** el orden de operaciones del listado es
  `universo + búsqueda → agregados → FILTRO por estado → TOTAL → PAGINACIÓN`. La
  primera versión filtraba **después** de paginar, lo que rompía el total,
  escondía coincidencias fuera de la primera página y mutilaba la exportación.
  Hay guardas POST y checks estáticos que lo prohíben.

  **Alcance CONGELADO:** búsqueda server-side · filtro por estado · paginación
  **25/50** · exportación **CSV** (UTF-8 con BOM) del conjunto filtrado. Orden por
  defecto `profiles.created_at DESC` con desempate estable por ID; **`Nuevo`** =
  30 días salvo prioridad superior; prioridad `Bloqueado → En seguimiento →
  Recurrente → Nuevo → Activo → Inactivo`.

  **Validación:** `check-s7_76` **353/353** · `_qa-crm-paginacion` **35/35** ·
  `tsc --noEmit` y `npm run build` **PASS** · **smoke del Preview PASS** ·
  **Vercel Production PASS** · **smoke autenticado de producción PASS** con la
  **consola sin errores**. Todos los smokes fueron **read-only**: sin «Exportar
  CSV», sin merge/unmerge/rechazos, **sin ninguna escritura**. El hostname
  temporal del Preview se **retiró** de Cloudflare Turnstile el mismo día, con
  `lucycare.app` intacto. Merge por **squash**; `main` en
  `38fa67c18efbf760c1a8a18d7beccb58b75b81b3`.

  **Frentes/deudas que dejó registrados, ninguno iniciado:**
  `PATIENT-CRM-FILTERS-P1` (filtros y orden; **regla vinculante**: filtros,
  conteo, paginación y exportación comparten la MISMA lógica server-side, y el
  CSV respeta exactamente los filtros activos) · `QA-PATIENT-DATA-CLEANUP-P0`
  (limpiar datos QA ficticios antes de medir pacientes reales; PRE de inventario,
  nunca `auth.users` por SQL, POST de residuos) · `COPY-TUTEO-LOGIN` (voseo
  **preexistente** en `LoginModal` y otras superficies; **no lo introdujo este
  PR**) · `CRM-SEARCH-TRGM` (`pg_trgm` no instalada; el `ILIKE` cae en scan, no
  bloqueante) · `ADMIN-DOCTOR-EXPORT` · `TYPES-RECONCILIATION-P0` · **ventana
  efímera B→C del Seed**.

## Frentes cerrados 2026-08 (PRs #313–#327)

- **AUDIT-SEC-P0 — #313 a #322** ✅ **CLOSED.** `s7_71a` (#313) añadió la cobertura
  server-side de auditoría de `appointments`; #316–#320 corrigieron el instrumento
  del smoke (no la migración); `s7_71b` (#321) cerró la escritura arbitraria sobre
  `audit_log` —cero policies, `anon`/`authenticated` sin privilegios, `service_role`
  solo `SELECT`, `_admin_log_doctor_change` cerrado, escritor de frontend
  eliminado—; #322 documentó el cierre. Prueba QA de continuidad **PASS**.
  ⚠️ El histórico anterior al corte son **trazas operativas observadas**, no
  evidencia infalsificable.
- **BILLING-P0 — #324** ✅ docs-only. `docs/ANALISIS_PAGOS_SAAS_MEDICOS.md` pasó a
  ser la **única fuente canónica** de billing, reconciliada con la arquitectura
  nueva: `billing_account` como entidad independiente, cinco estados sin
  `canceled`, entitlements derivados, **`is_operational` NO es estado de pago**,
  fases P0–P3 con enforcement al final y grilla de evaluación de proveedor.
  **PAUSED** — proveedor no elegido; **no bloquea el piloto**.
- **TWILIO-P0 — #325** ✅ **CLOSED / VALIDATED FOR PILOT.** Supabase pasó a
  **Twilio Verify** (SMS · 6 dígitos · Fraud Guard · Geo Permissions solo El
  Salvador · sin número comprado). **No requirió código.** QA: control con Test
  Phone PASS + un único SMS real con sesión válida y cero efectos colaterales;
  identidad QA temporal eliminada por Admin API. Pendiente operativo: **saldo /
  alerta de recarga** antes de sumar usuarios reales.
- **TESTPHONE-SEC-P0 — #326** ✅ **CLOSED.** `50378056365` era simultáneamente
  teléfono público de soporte, credencial de la única cuenta `role='admin'` y Test
  Phone con código fijo. #326 lo retiró de las **6 superficies públicas**
  (`/privacidad` ×2, `ClaimedProfileNoticeCard`, `ClaimProfileModal` ×2,
  `AffiliationRequestModal`) y sustituyó el canal por
  `lucycare.digital@gmail.com`, porque `hola@lucycare.app` no está operativo.
  Después, un **swap operativo** (sin PR, por Admin API) separó definitivamente
  **LucyAdmin → `50378627694`** (real, no Test Phone) y **Camilo QA →
  `50378056365`** (Test Phone), con las 5 fichas `patients` y las 2 clínicas de
  Camilo sincronizadas.
- **RECOVERY-EMAIL-P0 — #327** 🔴 **ACTIVE.** `src/hooks/useIdleLogout.ts`, **1
  línea**: el listener ignora `TOKEN_REFRESHED`. Elimina la realimentación
  `refresh → TOKEN_REFRESHED → refresh() → getSession() → refresh`, que podía
  agotar el rate limit de Auth; el **429 no es reintentable** para `auth-js`, que
  borra la sesión (`_removeSession`) y hace que `/reset-password` muestre
  falsamente "link expirado". QA A/B: sin el cambio, 3 llamadas nuevas; con el
  cambio, 0. **Falta la prueba real de recovery** — ver el handoff vigente §1.

## Pre-piloto — detalle completo (PRs #53–#61)

- **Mitigación del flujo público "Soy médico"** (PR #53, Hallazgo #7 del Security Gate). El `DoctorRegistrationModal` legacy + `registerDoctor` service creaban automáticamente profile/clinic/clinic_member/doctor con `lucy_status='claimed'` directo, sin validación de identidad. **Neutralizado**: el botón abrió `DoctorInterestModal` (WhatsApp-only) como mitigación temporal. Sin persistencia en DB. Archivos legacy marcados `@deprecated`.
- **Afiliación Fase 1 — captura de leads + bandeja admin** (PR #56, `s7_21`). Reemplaza al `DoctorInterestModal` por `AffiliationRequestModal` con formulario completo. Persiste leads en `doctor_affiliation_requests` con RLS estricto, rate limit 1/IP/24h, UNIQUE phone activo. Bandeja `/admin/afiliaciones` con triage (in_review / approved / rejected) + badge en sidebar. No crea doctor/profile/clinic todavía.
- **Afiliación Fase 2 — convertir lead aprobado en doctor `listed_only`** (PR #58, `s7_22` + `s7_23`). RPC `admin_approve_and_create_doctor` crea auth.users dormant + profile + clinic + doctor en una transacción. Email override aceptado solo si lead no trajo email. UI: botón "Crear médico" en modal admin + pantalla éxito con link a ficha admin. **Claim end-to-end validado ✅ (2026-05-30, smoke con test phone médico real).** El smoke detectó 4 bugs corregidos en PR #61 + `s7_24` (el principal: el profile debe quedar `role='patient'` pre-claim, no `'doctor'`). Ver "Smoke end-to-end de afiliación" abajo.


## Próximas fases cerradas — seguimiento histórico

### Smoke end-to-end de afiliación — ✅ COMPLETADO (2026-05-30)

Validado el claim end-to-end del médico creado vía Fase 2, con test phone
médico real (`50375000099`, OTP de QA no documentado). Resultado en DB: el doctor pasó
de `listed_only` → `claimed`, `tos_accepted_at` seteado, `audit_log` con
`edited_via='claim_self_service'`, y siguió sin `verified` / `is_operational`
/ `booking_enabled`. El claim pasó por el modal correcto y el password se
creó (confirmación visual del step "Contraseña creada").

**El smoke encontró 4 bugs en Fase 2; corregidos en PR #61 + migración `s7_24`:**
1. **Raíz — "Perfil reclamado" falso para `listed_only`.** La RPC `admin_approve_and_create_doctor` dejaba el profile con `role='doctor'` **antes** del reclamo; como ese profile coincide con el `auth.user` que el médico reusa al hacer OTP, entraba al panel pre-claim y veía "Perfil reclamado" sin haber corrido `claim_doctor_profile`. **Fix (`s7_24`):** el profile queda `role='patient'` pre-claim (igual que el login de un importado); `claim_doctor_profile` lo sube a `'doctor'` al reclamar. Un médico `listed_only` ya **no** entra al panel pre-claim.
2. **Nombre de clínica "Consultorio Dr. Dr. X"** cuando el nombre ya traía "Dr."/"Dra." → fix en el fallback.
3. **Modal admin "Crear médico" no precargaba Departamento/Municipio** (la persistencia server-side ya funcionaba; faltaba que `admin_list_affiliation_requests` devolviera los IDs). Fix en `s7_24` + frontend.
4. **Panel requería refresh** si `useClinicContext` fallaba → `PanelLayout` ahora muestra "Reintentar" en vez de spinner colgado.

**Aprendizajes operativos del smoke (conservar):**
- **NO hacer OTP con el teléfono del médico ANTES de que admin cree el doctor.** Supabase crearía un `auth.user` paciente que choca con el `UNIQUE` de phone en la RPC.
- **`listed_only` ≠ `claimed` ≠ `verified`.** Crear desde afiliación deja `listed_only`; reclamar → `claimed`; `verified` lo decide LucyAdmin.
- **"Perfil reclamado" en el panel NO prueba el claim** (con el copy viejo aparecía también para `listed_only`). La prueba es `lucy_status='claimed'` en DB.
- **`auth.admin.listUsers()` pagina mal** en este proyecto (bug GoTrue por fila corrupta en página 2). Para buscar/limpiar por phone, derivar el `auth.user.id` desde `profiles.id` (id compartido) y usar `getUserById`/`deleteUser`.

PR #61 (fix + `s7_24`) ✅ mergeado (commit `b2decba`). Después: opciones en cola (no bloqueantes para piloto) — ver "Pendientes prioritarios a conservar" más abajo.

### Pre-piloto público (operativo, no código)
1. **Smoke 7.3 opcional** — validar capacidad enviando 5 resets seguidos (no bloqueante, el rate limit builtin ya no aplica con SMTP externo).
2. **Cleanup test patients Fase 1** cuando termine la validación (`node scripts/setup-test-patient.mjs --clean`).

### Fase 4 — Auth robusta del médico
- ✅ **PR-A** (PR #39): login email/password + reset por email.
- ✅ **PR-B** (PR #50): password en flujo de Reclamar perfil.
- Próximo (post-piloto): Fase 2 self-service de cambio de email/teléfono. Fase 3 2FA opcional. Ver `docs/ANALISIS_AUTH_MEDICO.md`.

### Paciente Global
- **Fase 1** ✅ live (PR #44, `s7_20`): vinculación retroactiva por phone OTP + "Mis atenciones".
- **Fase 2** ✅ live (PR #87/`s7_32` backend + PR #88 UI): identidad global en `profiles` (DUI/DOB/género/dpto/muni, **DUI progresivo** = todo nullable), **UNIQUE parcial** `(document_type, document_number)`, **UPDATE column-restricted** (paciente no cambia `role`/`is_active`/`phone`), coherencia muni-depto (P0004), audit (`edited_via='profile_identity'`), **privacidad** (anon nunca ve DUI/DOB). UI `/paciente/perfil` (form + teléfono read-only), **banner no bloqueante** "Completá tu perfil" en "Mis atenciones", **prefill con "Usar" por campo** desde fichas locales (sin guardado automático; conflicto entre clínicas sin auto-elegir), DUI duplicado → mensaje amable.
- **Fase 3** ✅ live (PR #90/`s7_33` backend + PR #91 UI): **Opción B (sync espejo)**. `profiles` = verdad canónica; `patients` = **espejo sincronizado** (compat con consulta/receta/listado). **Guard server-side** (trigger BEFORE UPDATE en `patients`, `P0030`): médico/asistente no edita identidad local (`full_name`/`phone`/`email`/`document_*`/`date_of_birth`/`gender`) si `profile_id IS NOT NULL`; locales (sangre/alergias/emergencia/notas/tipo/foto/is_active) editables. **Claim** copia identidad global→local al vincular (global gana; local-only se conserva; no promueve local→global). **Sync ongoing** (trigger AFTER UPDATE en `profiles`): propaga campos cambiados a fichas vinculadas (cross-clínica, sin anular NOT NULL, sin pisar locales; bypass GUC `app.syncing_patient_identity` solo para el sync). `phone` fuera del sync (read-only + guardado). **UI médico**: `EditPatientModal` muestra identidad read-only ("Datos gestionados por el paciente en su perfil Lucy" / "No cargado por el paciente") cuando hay vínculo; badge en el detalle; walk-in sin cambios. Validado smoke OTP 6/6.
- **Fase 4** ⏳: herramienta admin para fusionar duplicados (`admin_merge_patients`, audit + reversibilidad + dry-run) — resuelve los DUI duplicados que el UNIQUE destape. **Nota (de F3):** el sync de Fase 3 puede chocar con `UNIQUE(clinic_id, document_type, document_number)` si existieran **fichas duplicadas del mismo perfil en la misma clínica** con documento no-null (caso raro: el dedup lo previene); falla elegante (error al guardar perfil, no corrupción) → considerarlo en el merge/dedup admin.
- **Fase 5** ✅ live (PR #98 diseño + PR #99/`s7_35` backend + PR #100 UI): **dedup preventivo** en creación de paciente/walk-in. RPC `find_patient_match_candidates` (`SECURITY DEFINER`): **intra-clínica → detalle** (id/nombre/teléfono/activo, "Usar este paciente"); **cross-clínica/global → solo señal mínima** (`weak` por teléfono, `strong` por documento), **sin PII**; gate `is_clinic_member`/`is_admin` (`P0001`); **advisory, no bloquea** (el bloqueo fuerte sigue siendo el `UNIQUE` intra-clínica). `createBasicPatient` con chequeo proactivo de documento → `DuplicateDocumentError` (banner amable, no error crudo). UI: `PatientMatchHints` reusable (debounce 350ms) en `NewPatientModal` + `CreateWalkInModal`. **Normalización del teléfono con `normalizePhoneSV`** (mismo helper que la dedup del INSERT) — el lookup y el submit ven lo mismo. Smoke s7_35 8/8 + OK visual. ✅ **Defensa en profundidad cerrada (PR #118/`s7_40`):** la RPC ahora **canonicaliza el teléfono internamente** (helper `normalize_phone_sv`, espejo de `normalizePhoneSV`, aplicado a input y filas) → ya **no depende** de que el caller normalice; tolera `71234567`/`+503…`/espacios/guiones/paréntesis y cubre filas legacy. Helper `IMMUTABLE` sin EXECUTE para anon/authenticated (`42501`); firma/salida idénticas; cross sin PII; smoke `s7_40` 11/11. Sin frontend.
- **Cambio de teléfono por OTP** ✅ live (PR #93/`s7_34` backend + #94 UI paciente + #95 UI médico): el cambio lo maneja Supabase Auth desde el cliente (`updateUser({phone})` → OTP **solo al número nuevo** → `verifyOtp(type:'phone_change')`, sesión activa; **Secure phone change OFF**). Un **trigger `AFTER UPDATE OF phone ON auth.users`** (`sync_phone_to_identity`, definer) sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard de `s7_33`) + audit `edited_via='phone_change'` (`user_id=NEW.id` porque `auth.uid()` es NULL en contexto GoTrue; auditoría best-effort, no bloquea). UI: **`ChangePhoneModal`** reusable (2 pasos) en `/paciente/perfil` (#94) y `/panel/perfil` (#95), teléfono read-only + botón "Cambiar". **Decisiones:** sesión activa + OTP al nuevo (no se exige OTP al anterior); sin re-auth fuerte en MVP; **recuperación sin sesión** (perdió el teléfono y sin email/password) → **soporte/LucyAdmin manual**. **Mejoras no-MVP:** notificación de seguridad al canal anterior (SMS/email), re-auth reforzada, herramienta formal de recuperación. Smoke automatizado 4/4 (`_smoke-s7_34.mjs`, sin OTP).

### Directorio (follow-ups)
- ~~Vista global `/admin/lista-espera` cross-médicos con filtros.~~ → ✅ **hecho (PR #120, `s7_41`):** `/admin/lista-espera` con RPC `admin_list_waitlist` (cross-médicos, filtros estado/médico/especialidad/búsqueda/fecha, paginación, `total_count`), reutiliza `admin_update_waitlist_entry`. Smoke 10/10.
- Sección "Lista de espera" en panel del médico (RLS ya permite, falta UI).
- Rate limit fuerte para tráfico público real (Cloudflare o counter por IP).
- Notificación automática al activar `booking_enabled` (Edge Function + Twilio).

### Admin SaaS restantes (post-piloto)
- B4: disponibilidad/horarios desde admin.
- C: suspender pacientes/asistentes.
- D: dashboard tracción mensual.
- E: UI sobre `admin_review_traceability` + moderar reseñas.
- F: explorador de `audit_log` con filtros.
- G: catálogos globales + onboarding manual de médico.

### Pendientes prioritarios a conservar (snapshot 2026-05-27)
Backlog vivo de prioridad alta a revisar tras el smoke de afiliación:
- **Paciente Global / DUI**: DUI obligatorio o progresivo + identidad única del paciente (continúa `docs/ANALISIS_PACIENTE_GLOBAL.md` Fase 2+).
- **Ubicación estructurada Departamento/Municipio** en admin médico — ✅ corregido (PR #64, `s7_25`): la ficha admin (Editar médico > Clínica) edita Depto/Municipio con las mismas listas jerárquicas del Home, guarda IDs en `clinics.department_id/municipality_id` (lo que filtra el Home), municipio depende del departamento, y son obligatorios para publicados/operativos (enforcement server-side P0004 + UI). Sin lat/lng.
- **Filtro "Ordenar por" del Home** — ✅ corregido: la opción muerta "Más cercanos" se removió (no había lat/lng ni geolocalización; caía al orden default). Quedan "Disponibilidad/Mejor coincidencia" (orden server: booking_enabled→verified→created_at) y "Mejor valorados" (score ajustado), ambas funcionales. **Backlog:** reintroducir "Más cercanos" cuando exista lat/lng en `clinics` + ubicación del usuario + UX de permisos de geolocalización (no proxy por municipio — no es cercanía real).
- **Paginación / carga dinámica del Home** cuando haya muchos médicos (hoy carga todos).
- ~~**Mi equipo / invitados del médico** — máximo inicial **2 asistentes**~~ → ✅ **hecho (PR #70, `s7_27`)**: límite base 2 activo, conteo activos+pendientes, enforcement server-side (trigger + accept revalida), UI con contador `n/2`. Usuarios adicionales = add-on, pendiente de la fase de pagos. **Fase 2 (ciclo de vida: expiración/reenvío de invitaciones) ✅ hecha (PR #102 `s7_36` + #103 UI).**
- ~~**Catálogos personalizados por médico** para diagnósticos y medicamentos~~ → ✅ **EJE 100% COMPLETO** (PRs #105–#111 + **#114** + migración de datos, `s7_37`/`s7_38`/`s7_39`). Diseño en `docs/ANALISIS_CATALOGOS_PERSONALIZADOS.md`. Modelo global (base Lucy) + personal por médico; snapshot histórico; ocultar/clonar globales; base replicada migrada (Base Lucy 127/736, "Míos" solo lo propio). **✅ Último cabo cerrado — UI de LucyAdmin para administrar la Base Lucy global (PR #114, UI-only, sin migración):** página **`/admin/catalogos`** (gate `AdminOnlyRoute`) + NavLink "Catálogos" en `AdminLayout`; tabs **Diagnósticos + Medicamentos** (Antecedentes fuera); buscar · activos/inactivos · crear · editar · inactivar/reactivar · dedup amable (`DuplicateCatalogError` + `23505`, refleja UNIQUE `s7_39`); `usage_count` oculto; **no hard-delete**; candado `.is('doctor_id', null)` en updates. Servicio `src/services/adminCatalog.service.ts` (directo sobre tablas con sesión admin; sin RPCs nuevas). Backend ya estaba listo: RLS `s7_38` (`is_admin` CRUD globales) + `audit_catalog` + UNIQUE `s7_39` + snapshot `s7_37`. **Modelo final:** LucyAdmin administra globales; médicos usan/ocultan/clonan y crean propios; médicos no editan globales (RLS). Validado visualmente por el owner.
- **Flujo legal/comercial de DUI médico, términos y aceptación formal** pre-verificación (`docs/PLAN_AFILIACION_MEDICO.md §11.bis`).
- **Manejo de email en afiliación/onboarding médico** (override ya existe en Fase 2; falta self-service de cambio post-reclamo, Fase Auth post-piloto).
- ~~**Error handling en PanelLayout / useClinicContext**~~ → ✅ **cerrado (PR #116).** Causa raíz del fallo de primera carga identificada y resuelta: era un **race de sesión** (`useClinicContext` validaba por red con `getUser()` mientras el token se hidrataba). Ahora lee con `getSession()` (misma primitiva que `getCurrentAuthUser`). `PanelLayout` ya no puede quedar en **spinner infinito** por fallo de `getCurrentAuthUser()` (timeout 8s + `.catch()` + estado recuperable con Reintentar/Cerrar sesión). Errores tipados con copy específico para estructurales (`no_clinic`/`no_doctor`/`role`). Frontend-only.

### Otros pendientes conocidos
- **Verificación reforzada de "Sí, son mías" (futuro, decisión owner 2026-06-11):** para MVP/piloto basta confirmación explícita + rechazo + audit (B2). Evaluar después pedir un dato adicional de la ficha (DOB, DUI parcial u otro) para confirmar, especialmente en teléfonos compartidos o mal digitados.
- **Términos/onboarding del paciente (futuro):** al crear o reclamar cuenta, el usuario debe aceptar que solo puede confirmar atenciones propias y que no debe adjudicarse atenciones médicas de terceros. (Documental/legal; aún sin funcionalidad.)
- **Hallazgo F4 confirmado empíricamente (smoke s7_43):** el claim copia el DUI global a las fichas; si la persona ya tiene una ficha con ese DUI en la misma clínica, el `UNIQUE(clinic_id, document_type, document_number)` aborta el claim COMPLETO (todas las clínicas, silencioso por el fail-safe del login). Ya estaba anotado como consideración de Fase 4 (merge/dedup admin); ahora con reproducción concreta.
- **Identidad múltiple Fase 2** (selector "modo paciente / modo médico" + capacidades derivadas + revisar si `claim` deja de sobrescribir el rol) — diseño en `docs/ANALISIS_ONBOARDING_IDENTIDAD_MULTIPLE.md`.
- **`accept_clinic_invitations` compara teléfono literal** (`+503…` ≠ `503…`): una invitación guardada con `+` no matchea un profile sin `+`. En el flujo de la app no se manifiesta (formatos coinciden), pero es candidato a normalizar con `normalize_phone_sv` (hallazgo de QA Fase 1).
- **⚠ OPERATIVO pendiente — registro de Katherine (`50372608827`):** decidir **conservar/corregir o limpiar** ese registro (QA de `s7_42`). Si se conserva: rehacer el nombre desde `/admin/medicos/4bb793d0-44b1-4782-8896-6bf01cd627aa` > Perfil y **verificar que persista** (el intento anterior NO llegó a la DB — sin audit, `updated_at` pre-fix; si se repite, investigar como bug de la ficha). Si se limpia: **solo cuando el owner lo indique**, con dry-run/inventario previo (auth.user, profile, doctor/clinic/membership, lead, fichas, audit) antes de borrar nada.
- S5-01 Estilo de vida (tabaco, alcohol, METs, sueño, alimentación).
- S5-08 SMS automático al invitar asistente.
- Vista `/panel/consultas` global.
- Borradores pendientes de firmar en panel home.
- Vistas históricas del paciente (laboratorios, PDFs).
- Reportes/estadísticas para el médico.
- Extraer modales/diálogos legacy a `src/components/` (refactor).


## Deudas técnicas cerradas


1. ~~**Asistente puede firmar consulta**~~ → ✅ **cerrado / reformulado (PR #67, `s7_26`).** El diagnóstico RLS (2026-06-01) mostró que la premisa era **inexacta**: `consultations`/`prescriptions`/`consultation_diagnoses` (y la **firma**) ya estaban bloqueadas para asistentes vía `doctor_id = get_user_doctor_id()` (= NULL para asistente). La fuga real estaba en **`consultation_family_history` y `vitals`** (usaban `is_clinic_member()`, true para cualquier miembro) → cerradas en `s7_26` (doctor-scoped). Validado con smoke empírico (asistente no lee ni escribe; médico dueño sin regresión).


1. ~~**Asistente puede firmar consulta**~~ → ✅ **cerrado / reformulado (PR #67, `s7_26`).** El diagnóstico RLS (2026-06-01) mostró que la premisa era **inexacta**: `consultations`/`prescriptions`/`consultation_diagnoses` (y la **firma**) ya estaban bloqueadas para asistentes vía `doctor_id = get_user_doctor_id()` (= NULL para asistente). La fuga real estaba en **`consultation_family_history` y `vitals`** (usaban `is_clinic_member()`, true para cualquier miembro) → cerradas en `s7_26` (doctor-scoped). Validado con smoke empírico (asistente no lee ni escribe; médico dueño sin regresión).
2. **Inmutabilidad + corrección controlada de consultas firmadas** — ✅ **EJE COMPLETO (backend + UI) para los 5 bloques clínicos: texto, receta, diagnósticos, antecedentes familiares estructurados y signos vitales.** Etapa A (PR #74, `s7_28`): edición silenciosa por API cerrada (firma vía RPC `sign_consultation()` + RLS endurecida `signed_at IS NULL`). Etapa B1 (PR #76, `s7_29`): corrección controlada — `consultation_amendments` append-only, **motivo obligatorio**, snapshots before/after (JSONB), **versionado de recetas** (anterior `is_current=false` histórica + v2 vigente), **solo médico dueño**, campos prohibidos rechazados (`P0013`), audit, vía RPC `amend_consultation()` (definer + bypass `app.amending`). PR #79: lecturas/impresión de recetas filtran `is_current=true`. PR #80: marca de impresión "RECETA CORREGIDA · fecha" + "Corrección vN" por medicamento. PR #81 (`s7_30`): columna `affects_prescriptions` → la marca se dispara **solo si la corrección tocó receta** (no por texto/diag/antecedentes/vitales). PR #82: UI texto + receta. PR #84 (`s7_31`): backend de **diagnósticos / antecedentes / vitales** (RPC extendida con `p_diagnosis_ops` / `p_family_history_ops` / `p_vitals_changes`, corrección in-place trazada por el snapshot) + **cierre del hueco de inmutabilidad de `vitals`** (RLS rechaza editar vitales de cita con consulta firmada; antes era editable por API directa). Validado con smoke OTP 11/11. PR #85: **UI completa** — modal "Corregir consulta firmada" en accordion con las 5 secciones, "Cambiar diagnóstico", IMC en vivo, **resumen unificado** de cambios (reemplaza el reforzado solo-receta), + fix del dropdown del `Combobox` (portal `fixed`, ya no lo recorta el modal/accordion) + impresión de receta en **2 columnas** para 3+ medicamentos (A4 vertical, sin 2ª página innecesaria). UPDATE directo de firmada sigue bloqueado; asistente también. Análisis `docs/ANALISIS_CORRECCIONES_CONSULTA_FIRMADA.md` + plan `docs/PLAN_CORRECCIONES_FASE0.md`.

## Migraciones SQL — detalle s7_21 en adelante

> Nota: `s7_21`–`s7_31` se aplicaron como parte del Pre-piloto y correcciones post-firma;
> su descripción está en la sección de Sprints completados y Próximas fases arriba.

- `s7_32` Paciente Global Fase 2 — columnas de identidad en `profiles` (DUI/DOB/género/dpto/muni) + CHECK de `document_type` + regla number⇒type + UNIQUE parcial de documento + trigger coherencia muni-depto + GRANT UPDATE column-restricted + trigger audit de identidad.
- `s7_33` Paciente Global Fase 3 — guard BEFORE UPDATE en `patients` (bloquea identidad local si `profile_id` set, P0030) + sync AFTER UPDATE `profiles→patients` (espejo, bypass GUC) + `claim_patient_records` copia identidad global→local.
- `s7_34` Cambio de teléfono por OTP — trigger `AFTER UPDATE OF phone ON auth.users` (`sync_phone_to_identity`, definer) que sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard de `s7_33`) + audit `edited_via='phone_change'`.
- `s7_35` Paciente Global Fase 5 — RPC `find_patient_match_candidates(p_clinic_id, p_phone, p_document_type, p_document_number)` (`SECURITY DEFINER`, `STABLE`) para dedup preventivo en creación: intra-clínica con detalle, cross-clínica/global solo señal mínima (`weak`/`strong`, sin PII), gate `is_clinic_member`/`is_admin` (`P0001`).
- `s7_36` Mi equipo Fase 2 — ciclo de vida de invitación: `invitation_ttl()` (14d), `clinic_invitations.expires_at` (backfill + default + NOT NULL), `team_seats_used()` excluye vencidas, `accept_clinic_invitations()` ignora vencidas, RPC `resend_invitation(p_invitation_id)` (titular; revalida cupo si vencida; extiende la misma fila).
- `s7_37` Catálogos — snapshot histórico: columnas `*_snapshot` en `prescriptions` (medication_name/active_ingredient/concentration/presentation) + `consultation_diagnoses.diagnosis_name_snapshot` + triggers `BEFORE INSERT` que copian del catálogo + backfill. Lectura/impresión usan snapshot (fallback al JOIN).
- `s7_38` Catálogos — global+personal: `diagnoses`/`medications.doctor_id` NULLABLE (NULL=global) + `doctor_catalog_hidden` + RLS reconstruida (propios∪globales∪admin / escritura solo propios / globales solo admin / sin DELETE) + trigger `audit_catalog`.
- `s7_39` Catálogos — infra de-dup: `catalog_migration_map` (mapeo old→global, reversibilidad) + UNIQUE parcial sobre globales (`diagnoses(lower(name))`, `medications(combo)` WHERE `doctor_id IS NULL`). Migración de datos = `scripts/migrate-catalog-base.mjs --apply` (aplicada).
- `s7_40` Paciente Global Fase 5 — RPC F5 tolerante a teléfono crudo (defensa en profundidad): helper `normalize_phone_sv(text)` (`IMMUTABLE`, espejo de `src/lib/phone.ts::normalizePhoneSV`, sin EXECUTE para anon/authenticated) + `CREATE OR REPLACE` de `find_patient_match_candidates` aplicando el helper a ambos lados de cada comparación de teléfono (input y filas). Firma/salida idénticas, cross-clínica sin PII. Checks: `check-s7_40.mjs` + `_smoke-s7_40.mjs` (11/11).
- `s7_41` Lista de espera global cross-médicos — RPC `admin_list_waitlist(p_status, p_doctor_id, p_specialty_id, p_search, p_date_from, p_date_to, p_limit, p_offset)` (`SECURITY DEFINER`, gate `is_admin()`): join a `doctors`/`profiles`/`specialties`/`clinics`, búsqueda por nombre paciente∪teléfono(`phone_normalized`)∪nombre médico, `date_to` inclusivo (`< date_to + 1 día`), `total_count` con `count(*) OVER()`. Reutiliza `admin_update_waitlist_entry` para el cambio de estado. Checks: `check-s7_41.mjs` + `_smoke-s7_41.mjs` (10/10).
- `s7_42` Afiliación — médico desde paciente existente: helper interno `_affiliation_classify(phone, email)` (usa `normalize_phone_sv`; clasifica `new`/`reuse_patient`/`block_doctor`/`block_sensitive`/`block_identity_conflict`; `auth.user` sin profile → `block_sensitive`) + RPC lectura `admin_affiliation_preflight(p_request_id)` + `CREATE OR REPLACE admin_approve_and_create_doctor` (firma idéntica) que re-clasifica en la transacción: `new` crea auth.user dormant (path s7_24); `reuse_patient` reusa el profile (sin nuevo auth.user, `full_name` si vacío, sin tocar phone/email/role, solo con `confirm_reuse`); bloqueos `P0010`/`P0011`/`P0012`; `P0013` requiere confirmación. Audit `reused_existing_user`. **Decisión: en reuse no se copia el email del lead.** Checks: `check-s7_42.mjs` + `_smoke-s7_42.mjs` (12/12).
- `s7_43` B2 Confirmación post-claim — `patients.link_confirmed_at` + tabla `patient_link_rejections` (`pending_review|resolved`, RLS admin-only, UNIQUE par ficha↔profile) + RPCs self-gated `confirm_patient_link`/`reject_patient_link` (P0040) + `claim_patient_records` reescrita excluyendo pares rechazados (conserva la copia de identidad global→local de s7_33). Checks: `check-s7_43.mjs` + `_smoke-s7_43.mjs` (14/14).
- `s7_44` Administración de LucyAdmins Fase 1 — tabla `platform_admin_invitations` (pending/active/revoked, TTL 14d, `invited_by/at`, `activated_at/profile`, `revoked_by/at`, UNIQUE parcial pending por teléfono, RLS admin-only SELECT) + `admin_invite_platform_admin` (cuenta dedicada: P0050 paciente real/médico/asistente, P0051 admin, P0053 vigente, P0055 inválido; vencida se refresca in-place; cuenta-cáscara con historial re-invitable) + `accept_platform_admin_invitation` (activación al primer OTP, pending sin privilegios, fill `full_name` si vacío, bloquea cuentas con fichas vinculadas) + `admin_revoke_platform_admin` (rol→patient, bloqueo inmediato, guard último admin P0052) + `admin_list_platform_admins`. `is_admin()` intacta. Checks: `check-s7_44.mjs` + `_smoke-s7_44.mjs` (24/24).
- `s7_47` Paciente Global Fase 4 / F4-3-search — RPC `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`/`P0001`, anon sin EXECUTE; **READ-ONLY**: solo SELECT/agregación, no merge/escritura/audit). Lista **grupos** candidatos a merge **V1 `same_profile`** (E3): agrupa por `(clinic_id, profile_id)` con `profile_id IS NOT NULL`, contando solo fichas vivas (`is_active = true AND merged_into_patient_id IS NULL`) y `HAVING count(*) >= 2`. Por grupo: `clinic_id`/`clinic_name`/`profile_id`/`profile_full_name`/`patient_count` + `confirmed_links_count`/`unconfirmed_links_count`/`has_unconfirmed_links` + `warnings` (`W_UNCONFIRMED_LINK` si alguna ficha tiene `link_confirmed_at IS NULL` — ayuda, no bloqueo). Por ficha: identidad mínima (nombre/documento/DOB/género/teléfono/`is_active`/`link_confirmed_at`/`created_at`) + `counts` (appointments/consultations/vitals). **NO** contenido clínico, **NO** candidatos por teléfono/nombre/walk-in/fuzzy/override, **NO** merge, **NO** toca identidad global/`profiles`/`auth.users`. Checks: `check-s7_47.mjs` (2/2) + `_smoke-s7_47.mjs` (10/10) / PR #140.
- `s7_46` Paciente Global Fase 4 / F4-2 — backend del merge admin de fichas intra-clínica: tabla `patient_merge_log` (source/target/clinic/merged_by/reason + `evidence` + `source_snapshot`/`target_snapshot` + `moved_counts` + `moved_ids` + `unmerge_*` previstos; RLS admin-only SELECT) + `CREATE OR REPLACE` de los dos guards para reconocer el bypass propio `app.merging_patients` (firma `s7_29` + identidad P0030 `s7_33`, sus bypass previos intactos) + helper interno `_patient_merge_eligibility` (read-only, orden: estructura→identidad/dirección→evidencia→borrador) + `admin_merge_patients_preflight` (dry-run, gate `is_admin`) + `admin_merge_patients` (transaccional, `FOR UPDATE`, revalida server-side, re-point appointments/consultations/vitals, neutraliza fuente — `document_number→NULL`/`profile_id→NULL`/`is_active=false`/`merged_into_patient_id`/`merged_at`, teléfono conservado, no hard-delete — + audit `admin_merge`). **V1 = `same_profile`** (E3); same_document inalcanzable intra-clínica por el `UNIQUE(clinic,doc)`. Bloqueos P0060–P0068; borrador en fuente bloquea (P0067), en target solo warning. Sin UI/unmerge/identidad global/`auth.users`. Checks: `check-s7_46.mjs` (6/6) + `_smoke-s7_46.mjs` (19/19) / PR #138.
- `s7_45` Paciente Global Fase 4 / F4-1 — claim tolerante fila por fila: columnas **pasivas** `patients.merged_into_patient_id` (FK `patients(id)` **sin ON DELETE CASCADE** + CHECK `patients_merged_into_not_self` = `merged_into_patient_id IS NULL OR <> id`) + `patients.merged_at` (infra de F4-2; **nada las escribe en F4-1**) + `CREATE OR REPLACE claim_patient_records()` que reemplaza el UPDATE multi-fila por un **LOOP fila por fila con bloque `BEGIN/EXCEPTION`** (subtransacción por ficha): una colisión `unique_violation` ya **no aborta el claim completo** → skip `unique_conflict`; cualquier otro error de fila → skip `error`; ambos auditados con `SQLSTATE` (fila por ficha `claim_skipped_unique`/`claim_skipped_error` con `record_id` = la ficha + fila resumen con `skipped[]`); errores estructurales fuera del loop (28000, etc.) se siguen propagando; respuesta al cliente = **solo conteos** `{success, linked_count, skipped_count[, reason]}`; el claim **excluye** fichas con `merged_into_patient_id IS NOT NULL`. Firma/nombre/GRANTs intactos; exclusión de pares rechazados (`s7_43`) y copia COALESCE de identidad (`s7_33`) conservadas. **No hace merge, no toca `profiles`/`auth.users`, no resuelve duplicados, no hard-delete.** `database.types.ts` actualizado (2 columnas + FK). Checks: `check-s7_45.mjs` (5/5) + `_smoke-s7_45.mjs` (16/16) / PR #135.

- `s7_70` Cancelación por el paciente + hardening de `appointments` (PR #310, aplicada en producción; archivo del repo byte-idéntico al ejecutado): **hardening de `UPDATE` sobre `appointments`** con restricciones de columnas por rol (paciente / médico / asistente) · RPC **`cancel_my_appointment`** (validación de propiedad y de estado) · tabla **`appointment_patient_cancellations` append-only** con la evidencia y el motivo de cada cancelación hecha por el paciente · RPC de lectura **`list_recent_patient_cancellations`** para el panel del médico · **liberación del horario** tras cancelar · **el historial conserva la cita** como *Cancelada* · **aislamiento entre clínicas** verificado. Validación: **smoke E2E backend 65/65, exit 0**; `check-s7_70` **219/219**; cleanup con **cero residuos**. Frontend en PR #311 (sin migración).

---

## DOCTOR-OWNER-NOTIFICATIONS-P0 — aviso al owner (afiliación y claim)

> Estado: **CLOSED** (2026-08-28). PR **#353 MERGED** por squash; `main` quedó
> en **`55af306746621266cc7c65389e3cd997a5ef5ca2`**.
> `s7_80`, `s7_81` y `s7_82` = **APPLIED / VERIFIED / NO REAPLICAR**
> (migraciones **101**, **102** y **103**), aplicadas por el owner ANTES del PR.

Avisa por correo al owner cuando (1) un médico completa el formulario de
afiliación y (2) un médico reclama un perfil. Nada más.

```
evento → outbox → wakeup (pg_net) → Edge Function → Resend
```

### El punto de disparo, y por qué no es el frontend

`submit_affiliation_request` devuelve `success:true` **también cuando no
inserta**: la rama `unique_violation` de `s7_21` es dedup anti-enumeración, así
que el éxito del frontend no equivale a evento. Y el claim se consuma en el
**paso 2 de 4** del modal, con `checkAlreadyClaimedByMe` convirtiendo timeouts y
`P0003` en `success:true`. Un disparo desde `onSuccess` produciría **falsos
positivos garantizados** y perdería eventos si el navegador se cierra.

- **Afiliación** → trigger `AFTER INSERT` sobre `doctor_affiliation_requests`.
- **Claim** → dentro de `claim_doctor_profile`. Un trigger sobre `doctors` **no
  distingue** el claim self-service de un `admin_set_lucy_status(listed_only →
  claimed)`: ambos producen la misma transición de columna.

### `s7_80` — outbox y encolado *(migración 101)*

Tabla `doctor_owner_notifications`: RLS activa, sin policies de escritura,
`REVOKE` a los cuatro roles, `GRANT SELECT` a `authenticated` (policy
`is_admin()`) y a `service_role` **solo lectura**, para diagnóstico. Toda
mutación pasa obligatoriamente por las RPCs.

**Sin PII.** Guarda tipo de evento, clave de deduplicación, IDs canónicos y
estado. Nombre, teléfono y correo los resuelve `notify_owner_claim_batch` en el
momento del envío, con **allowlist explícita en la base**: fuera quedan
licencia/JVPM, DUI, direcciones, el texto libre `message` del lead y cualquier
dato clínico. **No escribe en `audit_log`** — es inmutable desde `s7_71b` y esto
es estado operativo mutable.

`_enqueue_doctor_owner_notification` es **best-effort por construcción**:
`EXCEPTION WHEN OTHERS` + `RAISE WARNING`. El evento de negocio siempre gana.

**Deduplicación asimétrica, deliberada:** afiliación por `request_id` (una
solicitud, un aviso, siempre); claim por **el propio `outbox.id`** — cero
supresión entre transacciones, para que un claim legítimo posterior (tras
revertir `lucy_status`) **vuelva a notificar**. La unicidad «un correo por claim
real» no depende de esa clave: la da la guarda `lucy_status = 'listed_only'`,
que hace morir el segundo intento en `P0003` antes de llegar al encolado.

**Drenado atómico:** `FOR UPDATE SKIP LOCKED` — dos invocaciones concurrentes
nunca toman la misma fila.

**Ventana de idempotencia de 23 h.** Resend olvida la `Idempotency-Key` a las
24, así que un reintento posterior mandaría un **segundo correo de verdad**.
Dentro de ventana se reintenta con el mismo `outbox.id`; fuera, la fila pasa a
**`needs_reconciliation`** con `idem_window_expired` y **no se reenvía**. El
techo se aplica con `LEAST`, no con `COALESCE`: ningún llamador puede superarlo.
`first_attempt_at` se fija una vez y **nunca se reescribe** — si se actualizara
en cada intento, la ventana se extendería indefinidamente.

El cuerpo de `claim_doctor_profile` es **verbatim de `s7_64`** más un bloque
sentinelado; el A/B de `check-s7_80` demuestra que quitándolo el cuerpo es
byte-idéntico.

### `s7_81` — el aviso representa el evento, no el presente *(migración 102)*

Un aviso de `doctor_profile_claimed` describe un **hecho pasado**. `s7_80` leía
dos datos del presente y podía producir un correo que se contradice a sí mismo:

1. **Nombre.** El CTE no devolvía `subject_profile_id`, así que resolvía por
   `p.id = d.profile_id` — el perfil **actual**. `claim_doctor_profile`
   re-apunta ese campo y un admin puede volver a moverlo.
2. **Estado.** Devolvía `d.lucy_status` leído al enviar. Si un admin lo
   revertía a `listed_only`, el correo diría «Estado LucyCare: listed_only»
   bajo el asunto «Perfil médico reclamado».

**Exactamente tres líneas cambian** frente a `s7_80`, demostrado por A/B. La
**especialidad** se sigue leyendo del médico actual, deliberadamente: es
atributo del profesional, no del claim, y no existe registro histórico suyo.

### `s7_82` — despertador propio *(migración 103)*

**No se usa la Database Webhook del Dashboard.** El proyecto tiene `pg_net
0.20.0` y `net.http_post` ejecutable, pero **no** el esqueleto
`supabase_functions` — crear la hook devuelve `3F000` y la UI ya no ofrece
aprovisionarlo. Reconstruirlo exigiría `CREATE USER … CREATEROLE`,
`REASSIGN OWNED` y alterar funciones de `supabase_admin`: privilegios que el
`postgres` de un proyecto alojado no tiene, y objetos de plataforma creados a
mano que una migración futura de Supabase podría pisar.

Y no hacía falta: lo único que aporta `supabase_functions.http_request()` es un
payload con `old_record`/`record`/`type`/`table`/`schema` que **nuestra Edge
Function descarta**. `s7_82` llama a `net.http_post` con un trigger propio.
**Ventaja lateral:** la configuración del despertador queda versionada en el
repositorio en vez de vivir solo en el Dashboard.

⚠️ **El `EXCEPTION` de `s7_82` no es redundante con el de `s7_80`.** Si el
trigger propagara, la captura de `_enqueue_…` revertiría **su subtransacción y
con ella el INSERT de la outbox**: el aviso pasaría de *retrasado* a
**perdido**. El de `s7_80` protege el **evento de negocio**; este protege la
**durabilidad del aviso**. `check-s7_82` verifica que el bloque **envuelve** la
lectura de Vault y la llamada HTTP, midiendo anidamiento y no orden.

### Edge Function `notify-owner-doctor-events`

Desplegada con `--no-verify-jwt`, bandera **por invocación** que solo afecta a
esa función. **`admin-create-seed-doctor` no se toca ni se redespliega.**

⚠️ **No existe `supabase/config.toml`, y es deliberado.** Se creó y se eliminó:
un config parcial es superficie para `supabase config push`, que empujaría los
defaults de la CLI sobre la configuración remota y podría pisar **Turnstile, el
SMTP de Resend y Twilio**. Un comentario de advertencia dentro del archivo **no
es un control técnico**. `check-s7_80` falla si el archivo reaparece.

El control de acceso real es `X-Lucycare-Notify-Secret`, comparado sobre
digests SHA-256 **en tiempo constante** y fail-closed. La función **ignora el
payload** del wakeup: lo usa como señal de «despierta» y drena la outbox
autoritativa por RPC, así que ni invocándola se puede inyectar un evento.
Sin CORS: no tiene llamador de navegador.

**El secreto vive en dos sitios con papeles distintos:** Vault es lo que el
trigger **envía**, el Edge Function secret es contra lo que se **compara**. Si
difieren, cada wakeup da 401 y no sale ningún correo. **Rotar = actualizar los
dos**, primero el de la función.

### Evidencia de cierre

| | |
|---|---|
| Edge Function | `notify-owner-doctor-events` **ACTIVE v1** |
| Gates | **200 / 401 / 401 PASS** (+ 405 en `GET`) |
| `pg_net` | **0.20.0 habilitado** |
| Vault | **exactamente 1** `DOCTOR_NOTIFICATION_WEBHOOK_SECRET` |
| Trigger | `trg_notify_owner_wakeup`, `AFTER INSERT FOR EACH ROW`, habilitado |
| Smoke SQL `BEGIN…ROLLBACK` | **PASS**, 0 residuales, sin HTTP real |
| **E2E A · afiliación** | **PASS** — correo real enviado y recibido |
| **E2E B · claim** | **PASS** — `sent`, `attempts=1`, `provider_message_id` |
| Cola final | **vacía**, sin `failed` ni `needs_reconciliation` |
| Residuo A | **2 filas inmutables de `audit_log`** (trigger de `s7_21`) |
| Residuo B | **cero** — la outbox no tiene trigger de auditoría |

Validación: `check-s7_80` **223/223**, `check-s7_81` **59/59**, `check-s7_82`
**128/128**, mutation tests **18/18** y **29/29**, regresiones PASS, `build`
PASS. **Sin cambios en `src/`.**

### Lección de método

`pg_proc.prosrc` **incluye los comentarios**. El primer apply de `s7_82` abortó
con «POST falló: el WARNING usa SQLERRM»: la guarda se disparó con **su propio
comentario**. `check-s7_82.mjs` sí quitaba comentarios y daba PASS — **los dos
instrumentos medían textos distintos y solo se notó al aplicar en producción**.

Regla: **una guarda SQL y su equivalente en JS deben normalizar igual.** Y el
mutation test necesita **expectativa invertida** —mutaciones legítimas que el
check NO debe cazar— para probar la ausencia de falsos positivos.

El apply fallido fue **atómico** (DDL transaccional): no dejó función ni
trigger. Se verificó antes de reintentar, no se supuso.
