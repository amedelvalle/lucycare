# LucyCare — Contexto para Claude Code

> ⚠️ **Este archivo es una GUÍA RÁPIDA.** La referencia operativa
> detallada y vigente está en `docs/` (ver abajo). Si algo de este
> archivo contradice a `docs/`, mandan los `docs/`.

## Cómo retomar el proyecto en una sesión nueva

`origin/main` (GitHub) es la **única fuente de verdad**. Antes de
trabajar, sincronizá el repo local:

```bash
git fetch origin
git checkout main
git pull --ff-only origin main
git log --oneline -10
```

Luego leé los documentos oficiales según el objetivo del día:

**Continuidad / base (siempre):**
- `docs/HANDOFF_LUCYCARE_SPRINT7.md` — handoff actualizado, snapshot vigente.
- `docs/HANDOFF_TOMA_DECISIONES_2.md` — handoff corto de decisiones + estado de infra + próximos pasos.
- `docs/ESTADO_TECNICO.md` — ER/BD, matriz de reglas, flujos UI/UX.

**Análisis vivos (cada uno cubre un eje):**
- `docs/ANALISIS_ANALYTICS_LUCYCARE.md` — medición de tráfico/conversión de `lucycare.app`: Q1–Q5 cerradas (Vercel Web Analytics + Speed Insights **cookieless** como primario, GA4 descartado; `doctor_slug` como id de evento; **jamás PII/clínico ni texto libre de búsqueda**; dashboard DB LucyAdmin separado y posterior). **PR-0 diseño ✅ #214. Fase 1A ✅ live #215 + prod + validación owner** — `@vercel/analytics` + `@vercel/speed-insights` + componente `PublicAnalytics` (montaje único en `App.tsx`, **defensa doble**: render allowlist `/`·`/doctor/*`·`/privacidad` + `beforeSend` backstop que retorna `null` fuera del allowlist y recorta query/hash; excluye `/panel`·`/admin`·`/paciente`·`/reset-password`·`/calificar`; sin eventos custom/cookies/PII/dashboard). **Fase 1B ✅ operativa (2026-07-02):** Google Search Console verificado (archivo HTML — `public/google3f2e02a3c176b538.html` #217 + `public/googlef334c08cb9e5d942.html` #218, **permanentes, no borrar**) + sitemap `lucycare.app/sitemap.xml` enviado/procesado (**Correcto**, 35 páginas descubiertas). **Pendiente NO iniciado:** 2 (eventos custom, sujeto a soporte del plan Vercel) · 3 (dashboard conversión DB) · 4 (dashboards por médico/especialidad/canal).
- `docs/SECURITY_GATE_PILOTO.md` — auditoría pre-piloto + hallazgos cerrados.
- `docs/ANALISIS_RECLAMAR_PERFIL.md` — diseño del reclamo (Fase 2 ✅ live).
- `docs/ANALISIS_AUTH_MEDICO.md` — plan auth email+password (PR-A ✅ live, PR-B en cola).
- `docs/ANALISIS_DIRECTORIO_INFORMATIVO.md` — modelo comercial del directorio.
- `docs/ANALISIS_PACIENTE_GLOBAL.md` — modelo de identidad de paciente (Fases 1, 2, 3 y 5 ✅ live; Fase 4: F4-1/F4-2/F4-3 search+UI A+UI B + unmerge backend+UI + **F4-3b bandeja de rechazos** ✅ live #135–#153 (`s7_45`–`s7_49`); F4-D identidades diferido).
- `docs/ANALISIS_ONBOARDING_IDENTIDAD_MULTIPLE.md` — identidad única + capacidades múltiples (5 decisiones aprobadas; Fase 1 ✅ live en #125; Fase 2 selector de contexto en cola; prerequisito de recuperación sin sesión y merge admin).
- `docs/ANALISIS_PACIENTE_GLOBAL_OWNERSHIP.md` — ownership del paciente: "el médico gestiona una relación clínica local; LucyCare gobierna la identidad global" (D1–D7 aprobadas; B2 confirmación post-claim ✅ live en #128/`s7_43`; B1 merge admin con diseño propio cerrado en #134).
- `docs/ANALISIS_PACIENTE_GLOBAL_FASE4_MERGE_ADMIN.md` — diseño del merge admin de fichas duplicadas (Fase 4 / B1), **DM1–DM9 cerradas (#134)**. Alcance = fichas `patients` intra-clínica; reglas vinculantes; fases F4-1 (✅ #135/`s7_45`) → **F4-2 backend (✅ #138/`s7_46`, V1=`same_profile`)** → **F4-3-search RPC candidatos (✅ #140/`s7_47`)** → **F4-3 UI PR A read-only `/admin/pacientes` (✅ #142)** → **F4-3 UI PR B merge real `/admin/pacientes` (✅ #144)** → **unmerge formal backend (✅ #147/`s7_48`)** → **unmerge UI "Deshacer fusión" (✅ #149)** → **F4-3b bandeja de rechazos (`patient_link_rejections`): backend ✅ #151/`s7_49`, UI ✅ #153** → pendiente: F4-D identidades (diferido).
- `docs/ANALISIS_PACIENTE_GLOBAL_F4_UNMERGE.md` — diseño del unmerge formal (reversa del merge), decisiones cerradas; **backend ✅ live en #147/`s7_48`** (`admin_unmerge_patients_preflight` + `admin_unmerge_patients`, códigos P0070–P0077) + **UI "Deshacer fusión" ✅ live en #149** (`/admin/pacientes`, acción en el historial). F4-3b (bandeja `patient_link_rejections`) ✅ live #151/`s7_49`+#153; F4-D pendiente.
- `docs/ANALISIS_ADMINISTRADORES_LUCY.md` — administración de LucyAdmins (Opción B, D1–D6 aprobadas; Fase 1 ✅ live en #132/`s7_44`; owner/superadmin y capacidades granulares = Fase 2).
- `docs/HANDOFF_CHATGPT_LUCYCARE_NUEVA_VENTANA_2026-07-03_POST_PR220.md` — **PUNTO DE ENTRADA VIGENTE** (estado post-#220: Slugs+SEO hasta PR C + Analytics/Search Console Fase 1A/1B + favicon; backlog vivo; protocolo de arranque; reglas operativas). Leer este PRIMERO. `2026-06-21` (y anteriores `2026-06-18`/`2026-06-15`/`2026-06-11`) quedan como históricos.
- `docs/ANALISIS_CATALOGOS_PERSONALIZADOS.md` — catálogo global Lucy + personal por médico + snapshot histórico (✅ COMPLETO: PRs #105–#111 + migración de base replicada, `s7_37`/`s7_38`/`s7_39`).
- `docs/FASE_4_AUTH_EMAIL.md` — guía operativa Supabase URL/email config.
- `docs/SETUP_SMTP_RESEND.md` — Resend como SMTP custom de Supabase Auth (✅ live desde 2026-05-26, dominio `lucycare.app`).
- `docs/CUENTA_DEMO_CAMILO.md` — credenciales y reglas de la cuenta demo oficial.

**Planes:**
- `docs/PLAN_SPRINT7_ADMIN.md` — plan original Sprint 7 (cerrado en mayo).
- `docs/PLAN_PILOTO_5_MEDICOS.md` — checklist de piloto.

⚠️ **No mergear branches `claude/*` viejas.** Las fases se integran a
`main` por **squash-merge**: la rama queda en el remoto pero su
contenido YA está en `main`. Re-mergearlas genera conflictos. Tras un
squash-merge, la rama puede borrarse.

## Estado actual del proyecto (snapshot 2026-07-01 · post-#212)

- **(#205–#212) Frente "Slugs públicos + SEO del directorio" COMPLETO hasta Fase 3 PR C — detalle vivo en `docs/ANALISIS_SLUGS_SEO.md`:** URLs amigables `/doctor/:slug` + metadata/OG dinámica para crawlers + robots/sitemap. **#205** docs-only (handoff). **Fase 1 (#208 / `s7_52`)** — columna `doctors.slug` (nullable + UNIQUE parcial + CHECK formato) + `slugify_name` (IMMUTABLE, normaliza tildes/ñ) + `_doctor_next_slug` (advisory lock, sufijo `-N`) + trigger `set_doctor_slug` (solo al publicar, **congela**) + backfill de publicados. **Fase 2 (#209, frontend)** — resolver dual UUID/slug en `fetchDoctorDetail`, tarjetas y "Ver perfil público" emiten slug, `history.replace` UUID→slug (Compartir usa la URL con slug). **Fase 3 (#210 diseño / #211 PR B / #212 PR C, infra Vercel Edge, sin DB):** `middleware.ts` (matcher `/doctor/:path*` + `/sitemap.xml`) inyecta title/description/OG/Twitter/canonical/robots por médico (lógica pura en `og-meta.mjs`, env `SUPABASE_*`||`VITE_SUPABASE_*` público/RLS, nunca service role) + `public/robots.txt` (Disallow `/panel`/`/admin`/`/paciente`/`/reset-password`, no bloquea `/doctor/*`) + `/sitemap.xml` dinámico (35 perfiles elegibles, `<loc>` por slug, 0 UUIDs, 0 no publicados, cache éxito `s-maxage=3600`/degradado `300`). **Decisión de producto vinculante:** ubicación estructurada **NO** es requisito para indexar; criterio único `isSitemapEligible` (publicado+slug+nombre+especialidad+clínica; ubicación opcional) gobierna sitemap **y** `robots` del `<head>` (alineados). No publicados/inexistentes → `noindex,follow` sin fuga; sin copy de verificación si no está verificado; `listed_only` indexable con metadata honesta. **Validado en producción `lucycare.app`.** **Pendiente NO iniciado:** PR D (JSON-LD `Physician` + canonical hardening + OG branded 1200×630) · Fase 4 (landings especialidad/ubicación) · Analytics. (Frentes intermedios #206 header CTA púrpura de marca, #207 docs Fase 0 SEO — ver git log.)
- **(#188–#204) Frentes recientes (post-#187) — todos frontend-only o docs-only, SIN migración nueva (`s7_51` sin cambios):**
  - **#188/#189 docs-only:** cierre de #187 + reconciliación de la referencia del handoff vigente.
  - **#190 — fix overflow del botón "Congreso/Capacitación"** en la grilla "Tipo de bloqueo" (`BlockForm.tsx`): `min-w-0` + `break-words` + zero-width space tras la "/" (display-only); sin desborde a 360/390.
  - **#191 — selector de hora propio en "Nuevo bloqueo de agenda"** (`BlockForm.tsx`): los `<input type="time">` → dos `<select>` (hora/minuto cada 5'), valor interno sigue `HH:mm`; evita el reloj nativo de Android descuadrado.
  - **#192 — mapa condicional del perfil público** (`doctor-detail/page.tsx`): el iframe de Google Maps solo si `isUsefulAddress(addressLine)`; sin dirección útil pero con muni/depto → link discreto "Ver zona aproximada en Google Maps"; sin nada → "Sin ubicación". No hay lat/lng; gate explícito sin fail-open. (No se reintrodujo `license_number`.)
  - **#193 — placeholders Depto/Municipio del modal "Soy médico"** (`AffiliationRequestModal.tsx`): "—" → "Elegí departamento" / "Elegí municipio" / "Elegí departamento primero".
  - **#194/#195 docs-only — Pagos SaaS (DISEÑO, sin código):** refresh + §14 pricing configurable APROBADO (mensual **$59.00** IVA incl. / anual **$601.80** −15% / asiento adicional **$6.00**mes·**$61.20**año; centavos; **no hardcodear**, tabla futura `subscription_plan_prices`), §15 autoservicio/customer portal, §16 LucyAdmin Billing/reportería/conciliación, §17 separación Lucy SaaS vs LucyAdmin Billing. **Bloqueado por validación de pasarela (§4.5) + Q1–Q11; pago ≠ verified ≠ claimed.** Todo en `docs/ANALISIS_PAGOS_SAAS_MEDICOS.md`.
  - **#196 — foto del médico** (`AvatarUploader.tsx` + `doctor-detail/page.tsx`): se quita `capture="user"` → el médico puede subir **desde galería** (no se fuerza cámara); + se muestra el **avatar/iniciales** en el header del perfil público (antes no se veía).
  - **#197 — privacidad: `license_number` fuera del payload público** (`directory.service.ts` + `directory.types.ts`): `fetchDoctorDetail` ya **no** trae el JVPM al cliente (no se renderizaba; nadie leía `DoctorDetail.licenseNumber`). Flujos admin/claim/afiliación intactos.
  - **#198 docs-only — credenciales JVPM/NUE:** `docs/ANALISIS_CREDENCIALES_MEDICAS.md`. Hoy solo existe `license_number` (= JVPM/licencia base); **NO existe NUE** ni modelo por tipo ni verificación por credencial; `is_verified` es global. Modelo futuro recomendado: tabla **`doctor_credentials`** (type JVPM/NUE/OTHER · status pending/verified/rejected · is_public · audit). **No mostrar números; el número nunca al payload público.**
  - **#199 — copy "Verificado por LucyCare"** en el badge (`doctor-detail/page.tsx` detalle = texto completo; `DoctorCard.tsx` tarjeta = "Verificado" + `title`/`aria-label`). Solo verificados; sin afirmar JVPM/NUE.
  - **#200 — branding público emerald:** púrpura `#3C2285` (+ gradiente `to-purple-50`) → emerald/teal en `home/page.tsx`, `doctor-detail/page.tsx`, `LoginModal.tsx`, `ReviewsSection.tsx`. Color-only. (Pendiente menor: índigo del stat card del panel + botón púrpura `AdminAffiliationDetailModal` interno; semánticos "Fusionada"/"Congreso" se dejan.)
  - **#201 — logo local (fuera de `static.readdy.ai`):** `public/lucycare-logo.png` (PNG actual, 1200×505) + constante `src/lib/brand.ts` `LUCYCARE_LOGO_SRC`; 11 referencias reemplazadas en 6 archivos vivos. **Favicon NO migrado** (logo apaisado; sigue `/vite.svg`) → pendiente: ícono cuadrado de marca.
  - **#202/#203/#204 — cleanup de dead code (delete-only):** **#202** borró `src/data/svLocations.ts` (catálogo SV hardcodeado sin importadores; la fuente viva es DB vía `useDirectory`). **#203** borró el clúster legacy de registro médico (`DoctorRegistrationModal.tsx` + `DoctorInterestModal.tsx` + `doctorRegistration.service.ts`; reemplazado por `AffiliationRequestModal`, el flujo "Soy médico" actual). **#204** borró el **scaffold anidado completo `lucycare-code/`** (38 archivos; era el esqueleto inicial sin uso, sin secretos, que duplicaba dead code; 0 referencias en build/tsc/scripts/CI). `AffiliationRequestModal` y la fuente DB-backed de ubicación quedan intactos.
  - **Backlog actualizado (post-#204):** **YA HECHO** (borrar del backlog) → branding emerald público (#200), logo local (#201), cleanup `svLocations`/registro legacy/`lucycare-code/` (#202–#204), favicon de marca (#220), **Branding PR-1: Home público hacia paleta oficial morado/menta/gris (#222)**, **Branding PR-2a: perfil público visible `/doctor/*` (#229)**, **Branding PR-2b-1: modales públicos simples (`WaitlistModal` + `AffiliationRequestModal`) (#231)**, **Branding PR-2b-2: modales auth/claim (`LoginModal` + `ClaimProfileModal`) (#233)**, **Branding follow-up: `DoctorAvatar.tsx` avatar compartido fallback menta+morado (#235) — con esto el REBRAND PÚBLICO queda 100% COMPLETO por fases (#222 Home, #229 perfil, #231 modales simples, #233 modales auth/claim, #235 avatar compartido)**. **Vigente** → **Perf P2 (code-splitting del JS monolítico ~305 KB gzip) + perf menor (lazy loading de avatares, evaluar Pacifico) — no abrir sin instrucción** · **splash inicial de `index.html` → paleta oficial (morado+menta) YA HECHO (#237)** · branding interno menor (índigo panel + púrpura admin) · decidir semánticos "Fusionada"/"Congreso" · token de marca · credenciales `doctor_credentials` F1–F5 (no mostrar JVPM/NUE; copy "Verificado por LucyCare" ya live) · próximo-slot-real en la tarjeta · limpieza de ramas locales `claude/*` viejas (solo con autorización expresa). Pagos: solo diseño (bloqueado por pasarela). **Las menciones a `lucycare-code/` en este doc son históricas: el árbol fue ELIMINADO en #204.**
- **Fuente de verdad:** `origin/main` en GitHub (github.com/amedelvalle/lucycare).
- **HEAD esperado:** `63cf393` o posterior. **PRs #1–#245 mergeados.** **Migraciones APLICADAS hasta `s7_53`** (`main == origin/main`, árbol limpio, 0 PRs abiertos). **Punto de entrada VIGENTE: `docs/HANDOFF_CHATGPT_LUCYCARE_NUEVA_VENTANA_2026-07-03_POST_PR220.md`** (el `2026-06-21` queda histórico). **(#220) Favicon de marca:** `index.html` apuntaba a `/vite.svg` inexistente → favicon roto (el rewrite servía el HTML del SPA); ahora `public/favicon.svg` (isotipo oficial: destello menta `#8AE4CB` de 4 puntas + 4 puntos sobre tile morado `#3C2285`) + `<link rel="icon" .../favicon.svg>` + `<meta name="theme-color" content="#3C2285">`. Validado en prod (`/favicon.svg` 200 `image/svg+xml`, sin `/vite.svg`, robots/sitemap/archivos Google intactos). **Follow-up (no iniciar sin instrucción):** `apple-touch-icon.png` 180 + `icon-192/512.png` + `site.webmanifest` (requieren export raster confiable del isotipo). **(#222) Branding PR-1 — Home público hacia la paleta oficial LucyCare (frontend-only, acotado al Home + tokens Tailwind; SIN migración, `s7_52` sin cambios):** primera fase (por fases pequeñas, NO rebrand global) del acercamiento del público a la paleta oficial **morado `#3C2285` + menta `#8AE4CB` + gris `#EDEDED`**. Tokens nuevos en `tailwind.config.ts`: `brand.mint #8AE4CB` + `brand.gray #EDEDED` (se conservan `brand.purple #3C2285` / `.dark #2d1a64`). **Morado = CTAs primarios/jerarquía:** "Reservar cita" (`DoctorCard`), "Mostrar más", "Reintentar", "Limpiar filtros", "Mi panel"/"Panel Admin", título del hero, tarjeta-toggle "Solo médicos con agenda en línea" (gradiente morado), knob del toggle, focus del selector, **iconos de filtro** (la menta es demasiado clara sobre blanco → se prioriza contraste sobre la guía de "íconos menta"). **Menta = acento suave:** badges "Verificado"/"Agenda en línea"/"Mejor valorado" (menta 30% + texto morado, contraste ~11:1 AA), fondo del hero (gradiente menta muy suave → blanco), borde de tarjeta reservable, fila "Agenda en línea", hovers de opciones de dropdowns. **Gris = footer/superficies neutras.** **"Soy médico, quiero aparecer" → CTA secundario** (fondo blanco, texto y borde morados, hover menta suave) para no competir con la acción del paciente, on-brand y visible. Solo 4 archivos: `tailwind.config.ts` + `src/pages/home/page.tsx` + `src/pages/home/components/SearchSection.tsx` + `DoctorCard.tsx`. Validado: `npm run build` verde (CSS `index-ChTVc9N0.css`) · preview desktop + móvil 360/390/430 sin overflow horizontal · contraste AA de CTAs y badge "Verificado" · **prod `lucycare.app` OK** (Home 200, tokens de marca en el CSS, `/favicon.svg` 200 `image/svg+xml`, `/robots.txt`, `/sitemap.xml` 35 URLs y archivos Google **intactos**). **NO tocó** doctor-detail · `AffiliationRequestModal` · modales públicos (`ClaimProfileModal`/`LoginModal`/`WaitlistModal`) · panel · admin · paciente · DB/SQL/migraciones/`database.types.ts` · Analytics · SEO dinámico/middleware/OG/JSON-LD · sitemap · robots · archivos Google. **Pendiente NO iniciado (no abrir sin instrucción explícita):** alinear el emerald/teal restante **fuera del Home** — doctor-detail, `AffiliationRequestModal`, `ClaimProfileModal`, `LoginModal`, `WaitlistModal` y demás modales públicos; panel/admin solo si se autoriza después. **(#224) Perf PR-1 — carga percibida del Home público (frontend/config, bajo riesgo; SIN migración, `s7_52` sin cambios):** dos cambios derivados de un diagnóstico read-only (Paso 0) que **exoneró a #222** (delta de bundle ~0.19 KB gzip; la percepción venía de factores pre-existentes). **(1) `index.html`:** se elimina el `<link>` render-blocking a **Font Awesome 6.4.0** (`all.min.css`, ~18.75 KB + webfonts) — confirmado **0 usos** en todo el repo (`fa-`, `FontAwesome`, `all.min.css`, sin dependencia npm); los iconos son **RemixIcon** (`ri-*`, 196 usos), que queda intacto; Google Fonts/Pacifico y los `preconnect` intactos. **(2) `vercel.json`:** bloque `headers` que sirve **solo `/assets/(.*)`** (bundles hasheados de Vite) con `Cache-Control: public, max-age=31536000, immutable` (antes heredaban `max-age=0, must-revalidate` → el navegador revalidaba ~305 KB de JS en cada visita); el **`index.html` NO recibe long-cache** (sigue `max-age=0, must-revalidate` para tomar nuevos deploys al instante); el **rewrite SPA intacto**. **Validado en prod `lucycare.app`:** `/` 200, HTML sin Font Awesome, RemixIcon presente, `/assets/*.js` y `*.css` con cache immutable, `index.html` sin immutable, y `/favicon.svg`·`/robots.txt`·`/sitemap.xml` (35 URLs)·archivos Google **intactos**. **NO tocó** React/Home (código)/Tailwind/DB/SQL/migraciones/`database.types.ts`/Analytics/SEO dinámico/OG/JSON-LD/sitemap/robots/favicon/archivos Google. **Pendiente NO iniciado (no abrir sin instrucción):** **Perf P2** = code-splitting del JS monolítico (~305 KB gzip en un chunk); **Perf menor** = lazy loading de avatares + evaluar Pacifico si no aporta. **(#226 / #227) SEO Home OG — preview social/indexable del Home `/` (frontend/SSR-meta, SIN migración, `s7_52` sin cambios):** el Home servía solo `<title>LucyCare</title>` (sin description/OG/Twitter/canonical) → preview pobre al compartir la URL directa; los perfiles `/doctor/*` ya se veían bien porque el middleware les inyecta metadata. **(#226) SEO Home OG:** `buildHomeMeta(origin)` en `og-meta.mjs` (indexable) + `middleware.ts` con `matcher += '/'` y **rama propia para `/`** que inyecta la meta con `injectMeta` (misma máquina que `/doctor/*`); **`/doctor/*` intacto**; smoke `_smoke-og-meta.mjs` T18/T18b (21/21). **Decisión: middleware, NO estático en `index.html`** — `injectMeta` **inserta** (no reemplaza) los `og:*`, así que meter OG del Home en el shell duplicaría los tags en cada perfil (riesgo de romper su preview). Metadata del Home: canonical=`https://lucycare.app/`, `robots index,follow`, `og:type=website`, `og:site_name=LucyCare`, `og:url=https://lucycare.app/`, `og:image=https://lucycare.app/lucycare-logo.png`, Twitter `summary_large_image` (+ title/description/image); **sin duplicados** de `<title>`/canonical/`og:title`/`twitter:card`. **(#227) Copy final del Home OG** (solo `og-meta.mjs` + smoke, middleware intacto): **title** `LucyCare — Encuentra al médico perfecto para ti` (= `<h1>` del Home) + **description** `Busca por especialidad, ubicación y disponibilidad. Reserva en línea con LucyCare.` (tuteo consistente con el title y el Home). **Validado en prod `lucycare.app`:** Home con metadata completa + copy nuevo, `/doctor/dr-camilo-carrillo` sin regresión (`og:type=profile`, sin duplicados), y `/lucycare-logo.png`·`/favicon.svg`·`/robots.txt`·`/sitemap.xml` (35 URLs)·archivos Google **intactos**. **Nota operativa:** para el preview social usar la **URL directa** `https://lucycare.app/`, **no** enlaces `share.google/…` (wrapper externo de Google/Chrome, ajeno al código de LucyCare); si WhatsApp/Facebook muestran un preview viejo es caché → refrescar con el Facebook Sharing Debugger o esperar. **Pendiente NO iniciado (no abrir sin instrucción):** **OG branded 1200×630** para reemplazar el `lucycare-logo.png` (1200×505) temporal como `og:image` — pertenece a **Slugs+SEO PR D** junto con JSON-LD `Physician` + canonical hardening. **(#229) Branding PR-2a — perfil público visible `/doctor/*` hacia la paleta oficial (frontend-only, solo color; SIN migración, `s7_52` sin cambios):** continuación de Branding PR-1 (Home), acotada al **perfil visible del médico** (NO modales, reservados para PR-2b). Reusa los tokens `brand.*` ya existentes. **Edición por intención (NO replace global de emerald):** **morado** = CTAs/interacción/acento ("Reservar cita"/"Reservar ahora", "Llamar para agendar", "Reclamar mi perfil", "Reintentar"/"Volver al inicio", servicio/slot seleccionados, focus de inputs, spinners, iconos de sección, links, barra de rating); **menta** = acento suave/confianza/**éxito** (badges "Verificado por LucyCare"/"Agenda en línea"/"Mejor valorado" = menta 30% + texto morado AA ~11:1, chips de reseñas, superficie/borde de `ClaimProfilePromptCard`, y el estado de éxito "¡Cita agendada!" = menta suave + texto morado); **gris** = footer (`bg-brand-gray`). **Conservados por decisión del owner:** verde de **WhatsApp** (`bg-green-500`, color de canal), **blue/info** (user-info del BookingCard), red (errores), amber (avisos), yellow (estrellas), gris neutro. Solo 4 archivos: `doctor-detail/page.tsx` + `components/{BookingCard,ClaimProfilePromptCard,ReviewsSection}.tsx`. Validado: `npm run build` verde · preview del perfil de Camilo (badges menta+morado, "Reservar cita" morado, WhatsApp verde) · móvil 360/390/430 sin overflow · **prod `lucycare.app` OK** (`/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, WhatsApp verde, **metadata/OG del doctor intacta** `og:type=profile`, Home `/`·`/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **NO tocó** `ClaimProfileModal`/`LoginModal`/`WaitlistModal`/otros modales · `DoctorAvatar.tsx` · Home · panel · admin · paciente · DB/SQL/migraciones/`database.types.ts` · Analytics · SEO runtime/middleware/OG/JSON-LD · sitemap · robots · favicon · archivos Google. **Pendientes NO iniciados (no abrir sin instrucción):** **Branding PR-2b** = modales públicos (`ClaimProfileModal`/`LoginModal`/`WaitlistModal`) · **`DoctorAvatar.tsx`** (iniciales aún en emerald; componente **compartido** con el Home → follow-up separado) · branding menor (splash verde `#047857` de `index.html` + interno panel/admin). **(#231) Branding PR-2b-1 — modales públicos simples hacia la paleta oficial (frontend-only, solo color; SIN migración, `s7_52` sin cambios):** primera mitad de PR-2b, acotada a los modales públicos **sin auth/claim**: `WaitlistModal` (doctor-detail) + `AffiliationRequestModal` (home). Reusa los tokens `brand.*`. **Edición por intención (NO replace global):** **morado** = CTAs (enviar/confirmar), focus de inputs (`border`/`ring`), links de acento (política de privacidad), accent del checkbox, icono de éxito; **menta** = estado de éxito (círculo de check `bg-brand-mint/30` + icono morado); **conservados:** red (errores/validación), gris/neutros (no hay amber/blue/green en estos dos). Solo 2 archivos. Validado: `npm run build` verde · preview en vivo de ambos modales (AffiliationRequestModal desde "Soy médico", WaitlistModal desde médico no reservable) — acentos morados, éxito menta, `*` requeridos en rojo, móvil 360 sin overflow, 0 errores de consola; **NO se enviaron formularios** (evitar datos reales); **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **NO tocó** `LoginModal`/`ClaimProfileModal` (PR-2b-2) · lógica de waitlist/afiliación/validaciones/auth/OTP/claim · `DoctorAvatar.tsx` · DB/SQL/migraciones · Analytics · SEO runtime/middleware/OG/JSON-LD · sitemap · robots · favicon · archivos Google. **Pendientes NO iniciados (no abrir sin instrucción):** **Branding PR-2b-2** = modales auth/claim (`LoginModal`, `ClaimProfileModal`) — validación visual cuidadosa, sin tocar lógica · **`DoctorAvatar.tsx`** (follow-up compartido) · branding menor (splash + panel/admin). **(#233) Branding PR-2b-2 — modales auth/claim hacia la paleta oficial (frontend-only, SOLO color/className; SIN migración, `s7_52` sin cambios):** segunda mitad de PR-2b, acotada a los modales de **auth/claim**: `LoginModal` + `ClaimProfileModal`. **NO toca** auth/OTP/login/claim/password/validaciones/estados/timers/Supabase/RPCs/navegación/roles; sin cambiar copy/labels. Reusa los tokens `brand.*`. **Edición por intención (NO replace global):** **morado** = CTAs (Continuar/Verificar/Ingresar/Enviar/Reclamar/Guardar/"Contactar a Lucy"), **tabs activas** (Teléfono/Email), **step-indicator del claim** (barra de progreso + número), focus de inputs, links ("Reenviar código"/"¿Olvidaste tu contraseña?"/Términos), accent del checkbox, iconos, borde de la tarjeta-opción "Crear contraseña ahora"; **menta** = estados de éxito (círculos de check OTP/reset/"¡Perfil reclamado!", caja "Contraseña creada" = menta suave + texto morado, fondo/hover de la tarjeta-opción); **conservados:** red (errores/validación), amber (avisos: "Mínimo N caracteres", "No encontramos un correo registrado"), blue (info: "Revisá tu correo"), gris/neutros. (El CTA "Contactar a Lucy" enlaza a WhatsApp pero usaba `bg-emerald-700` —botón morado con ícono, no el patrón verde de canal— → morado como CTA.) Solo 2 archivos. Validado: `npm run build` verde · preview en vivo de ambos modales (LoginModal desde "Iniciar sesión", ClaimProfileModal desde la entrada de reclamo de un perfil `listed_only`) — tab/step-indicator/CTAs morados, focus morado, éxito menta, semánticos intactos; desktop + móvil 360/390/430 sin overflow; 0 errores de consola; **NO se enviaron formularios** (sin OTP/SMS/datos reales); **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, tokens de marca en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **(#235) Branding follow-up — `DoctorAvatar.tsx` (frontend-only, visual-only, 1 archivo/1 línea; SIN migración, `s7_52` sin cambios):** cierra el último resto emerald del rebrand público: el círculo de iniciales del avatar (fallback sin foto) pasa de `bg-emerald-100 text-emerald-700` → **`bg-brand-mint/30 text-brand-purple`** (fondo menta suave + iniciales moradas, contraste AA). **Ruta real: `src/components/DoctorAvatar.tsx`** (componente **compartido** Home + doctor-detail; el pedido mencionaba `home/components/` que no existe). **NO tocó** lógica del avatar/cálculo de iniciales/branch `<img>` (byte-idéntico → avatares con foto intactos)/props/tipos/tamaños/layout/responsive/Tailwind config. Validado: `npm run build` verde · preview del fallback "CC" en Home y perfil de Camilo (menta@30% + iniciales moradas, legible) · Home + doctor-detail móvil 360/390/430 sin overflow · 0 errores de consola · **prod `lucycare.app` OK** (Home 200, `/doctor/dr-camilo-carrillo` 200, `.bg-brand-mint/30` en el CSS, **metadata/OG del Home y doctor intactas**, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **REBRAND PÚBLICO PRINCIPAL COMPLETO por fases:** **#222** Home (tokens + morado/menta/gris + "Soy médico" secundario) · **#229** perfil visible `/doctor/*` (badges/éxito menta, CTAs morados, WhatsApp verde y blue/info conservados) · **#231** modales simples (`WaitlistModal` + `AffiliationRequestModal`) · **#233** modales auth/claim (`LoginModal` + `ClaimProfileModal`) · **#235** avatar compartido (`DoctorAvatar.tsx`, fallback menta+morado). **Con esto el rebrand público queda 100% en sus superficies principales.** **(#237) Branding menor — splash inicial de `index.html` (frontend/HTML, visual-only, 1 archivo/2 líneas; SIN migración, `s7_52` sin cambios):** el splash estático (visible durante el bootstrap antes de que React monte) usaba verde emerald → alineado a la paleta oficial. Diagnóstico (Paso 0): `#047857` aparecía **solo** en el splash de `index.html` (2 ocurrencias: color del texto "LucyCare" + acento del spinner), y el único otro verde relacionado era `#d1fae5` (anillo del spinner). Cambios: `#047857` → **`#3C2285`** (morado, texto + `border-top-color`); `#d1fae5` → **`#8AE4CB`** (menta, `border`/anillo). Sin tocar textos/estructura/comportamiento; `theme-color`/favicon/RemixIcon/Pacifico/preconnect/script de bootstrap intactos. Validado: `npm run build` verde (`out/index.html` `#3C2285`×3 + `#8AE4CB`×1, 0 verdes) · preview del splash renderizado (texto morado + spinner anillo menta/acento morado, legible) · Home + móvil 360/390/430 sin overflow · **prod `lucycare.app` OK** (splash con hex de marca, Home 200, `/doctor/dr-camilo-carrillo` 200, metadata/OG intactas, `/robots.txt`·`/sitemap.xml` 35 URLs·`/favicon.svg`·archivos Google **intactos**). **Pendientes NO iniciados (no abrir sin instrucción):** **branding interno** restante (stat card índigo del panel + botón púrpura de `AdminAffiliationDetailModal` + token de marca) · **OG branded 1200×630** (Slugs PR D) · **Perf P2** (code-splitting del JS) · **Perf menor** (lazy avatares, evaluar Pacifico). **(#180–#184, #187) Frente "Perfil público / visibilidad comercial" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** mejoras públicas de conversión en directorio/Home + perfil del médico. **#180 — CTA "Reservar cita" en la tarjeta del directorio (`DoctorCard.tsx`):** médico reservable → fila "Agenda en línea" + CTA emerald "Reservar cita" (navega a `/doctor/:id`, donde la reserva ya funciona); no reservable → "Ver perfil"; se eliminó el copy contradictorio ("Reserva online disponible solo con agenda activa") y el `$0` (precio solo si >0). Fix de fondo: el CTA dependía de `nextAvailableSlot` hardcodeado a `undefined` → la rama nunca se cumplía. **#181 — contador honesto del Home (`home/page.tsx`):** título neutro "Médicos en Lucy" + conteo secundario discreto "Mostrando X de Y médicos publicados · Z con agenda en línea"; variantes para búsqueda ('… resultado(s) para "…"') y toggle agenda ("Médicos con agenda en línea"); sin claims inflados; `PAGE_SIZE=12` y "Mostrar más médicos" intactos. **#182 — limpieza copy/footer del Home (`home/page.tsx`):** hero honesto ("Reserva en línea con los médicos que tienen agenda activa", sin "al instante con médicos verificados"); "Acceso Gratuito… sin costo" → "Búsqueda sin costo: …el directorio es gratis; el precio de la consulta lo define cada médico"; footer con **año dinámico** (`new Date().getFullYear()`, sin "© 2024"), sin backlink "Website Builder" (readdy), sin links muertos `href="#"`, Privacidad → `/privacidad`, columna "Síguenos" eliminada (grid 4→3). **#183 — perfil público móvil más liviano (`doctor-detail/page.tsx` + `BookingCard.tsx`):** en `/doctor/:id` móvil, el médico **reservable** ya NO muestra el `BookingCard` inline arriba (lidera la info/confianza del médico); reserva desde la **barra fija inferior "Reservar cita" → `MobileBookingSheet`** (flujo real intacto); **desktop mantiene el `BookingCard` sticky**; los **no reservables** conservan su `BookingCard` inline (contacto: "Sin agenda en línea" + Llamar/WhatsApp/Lista de espera, sin barra fija ni promesa de reserva); eliminado el claim **"Pago seguro • Confirmación inmediata"** → "Reserva en línea · el pago se coordina con el médico" (Lucy no cobra en línea). La página de Camilo bajó de ~4165px a ~3200px en móvil. **#184 — footer del perfil + "Compartir" funcional (`doctor-detail/page.tsx`):** footer del perfil alineado con el del Home (año dinámico, sin links muertos, Privacidad → `/privacidad`, sin "Síguenos"); **"Guardar" eliminado** (requiere favoritos/persistencia, no se simula); **"Compartir" funcional diferenciado por contexto** (detección por `window.matchMedia('(pointer: coarse)')` en el click): móvil/touch + `navigator.share` → **share sheet nativo** (WhatsApp/SMS/contactos/correo…); desktop o móvil sin share nativo → **copiar la URL del perfil** + toast "Enlace copiado" (NO abre el panel del sistema en desktop); etiqueta desktop "Copiar enlace", ícono discreto en móvil. **#187 — card "Reclamar perfil" visible por viewer en perfiles `listed_only` (`ClaimProfilePromptCard.tsx` + `doctor-detail/page.tsx`):** en `/doctor/:id` con `lucy_status='listed_only'`, la card de reclamo se mostraba **idéntica a todos** (anónimo, paciente, admin) y como bloque emerald **destacado arriba del fold** — ruido/desconfianza para el paciente (el grueso del tráfico), que podía leerlo como "perfil no validado". Ahora la visibilidad **depende del viewer** (mismo patrón `getSessionWithTimeout` que `ClaimedProfileNoticeCard`): **dueño logueado** (`auth.uid() === doctor.profileId`) → card completa arriba, igual que antes; **visitante anónimo** → entrada **discreta** (link "¿Eres este profesional? Reclamá tu perfil", label gris + link emerald) al final de la columna, debajo de Calificaciones, **fuera del primer pantallazo**, abre el mismo `ClaimProfileModal`; **cualquier otro autenticado que no es dueño** / mientras resuelve la sesión → **nada** (sin flash). Además, gate **explícito** `isListedOnly` (`=== 'LISTED_ONLY'`) para que un `lucy_status` faltante/null **ya NO haga fail-open** a la card (se eliminó el `|| 'LISTED_ONLY'` solo para este gate; el `lucyStatus` general por compat de badges/BookingCard queda). Solo 2 `.tsx`; **frontend-only, sin DB/SQL/migraciones/`database.types.ts`/fixtures; no toca reserva/mapa/credenciales/Home/branding/`lucycare-code/`**. Validación: tsc+build verdes · preview desktop + móvil 360/390/430 (0 overflow, 0 errores de consola) contra médicos `listed_only` reales del directorio + Camilo demo; estados en vivo (anónimo sin card grande/discreta abajo, paciente logueado no-dueño sin card, Camilo verified+bookable sin regresión); **estado "dueño logueado" validado por paridad de código** con `ClaimedProfileNoticeCard` (sin fixture DB, por decisión de alcance frontend-only); **0 datos reales tocados, jamás Katherine**. **Validación de todo el frente:** tsc+build verdes por PR · preview en vivo desktop + móvil 360/390/430 sin overflow · 0 errores de consola · 0 residuales · **regla de validación vinculante: directorio público / paciente demo (Camilo/Pepe Toro), jamás Katherine ni datos reales sensibles**. **Pendientes explícitos del frente (backlog, NO implementado):** mapa condicional (hoy 320px siempre, aún con dirección pobre); señales de confianza / credenciales (decisión de privacidad sobre `license_number`); branding emerald unificado (quitar púrpura `#3C2285`); migrar assets del logo fuera de `static.readdy.ai`; próximo-slot-real en la tarjeta. *(La card "Reclamar perfil" visible a cualquier visitante quedó CERRADA en #187 — ver arriba.)* **(Nota: NO existe un duplicado real `src/pages/Home/` — fue un artefacto de filesystem case-insensitive; descartado del backlog por verificación: `git ls-files 'src/pages/Home/'` vacío + `core.ignorecase=true`; el único Home es `src/pages/home/`, que usa el router.)** **(#177–#178) Frente "Onboarding médico operativo V1" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** ayuda dentro del panel para que un médico **ya operativo** entienda qué le falta para quedar **visible y reservable**. **Alcance V1 = solo Gate 2 (configuración en panel); Gate 1 (reclamo/habilitación por LucyAdmin) queda FUERA de V1.** **#177 (PR-1) read model `doctorActivation.service.ts` + hook `useDoctorActivation`** — `getDoctorActivation(doctorId, clinicId, isDoctorContext)` → `{ steps, completedCount, total, allDone, clinicStatus }`; **read-only, sin UI, sin DB, sin migración, sin `database.types.ts`**. **Checklist principal = 5 pasos self-service** (cuentan para el progreso): (1) **completar perfil** (foto+especialidad), (2) **configurar agenda/disponibilidad**, (3) **agregar servicios**, (4) **activar reserva en línea** (`booking_enabled`), (5) **revisar publicación / cómo lo ven los pacientes** (`is_published`). Cada paso `{ key, label, done, href }`, `done` por **señal real** (reusa `getMyDoctorProfile`/`getMyAvailabilityRules`/`listDoctorServices` + query read-only de `clinics`). **`clinicStatus` aparte = info NO bloqueante** (`{ label, complete, actionable:false, href:null, message }`): la ubicación de la clínica (nombre/dirección/depto/muni) **solo la edita LucyAdmin** (`admin_update_doctor_clinic`), así que **no es un paso del médico y NO suma ni resta progreso** (`allDone` depende solo de los 5). **Scoping (igual que el resumen rápido):** gate de ROL `isDoctorContext` → `emptyActivation()` **antes de cualquier query** si no es médico (`doctorId` solo NO basta — el asistente lo trae); verdad de la **sesión** (`getMyDoctorProfile`, RLS doctor-scoped), los ids del caller solo como verificación de consistencia (mismatch → vacío, sin leer clínica/agenda/servicios); nunca cross-médico/cross-clínica. Validado: tsc+build + smoke 17/17 (no-médico/asistente → vacío sin queries; mismatch → vacío sin leer aux; clínica incompleta NO bloquea `allDone`; clínica completa no suma; sesión real de Camilo/RLS = 4/5 le falta foto). **#178 (PR-2) tarjeta UI `DoctorActivationCard.tsx`** montada en el dashboard (`/panel`, **arriba de las métricas**) — "Primeros pasos para activar tu perfil": progreso "**N de 5 pasos listos**" + barra, lista de los 5 pasos (listo/pendiente por señal real, **sin checkbox manual**; pendiente → link directo a su pantalla), y `clinicStatus` **discreto** (solo el mensaje "Contactá a soporte…" si está incompleta; si completa se omite). **Gate VISUAL:** solo `role==='doctor'` (nunca asistente/admin/paciente/null; `doctorId` solo NO es criterio), **se oculta si `allDone`** (V1: no dejar 5/5 fijo), payload vacío, error o carga (nunca error técnico). **Reemplaza el banner de "Completá tu perfil con una foto"** (la foto es parte del paso "Completá tu perfil") para no duplicar. Validado: tsc+build + smoke SSR 13/13 (componente real: médico+progreso → tarjeta; asistente/rol undefined/`allDone`/vacío/error/carga → no se renderiza; clínica incompleta → mensaje discreto sin bloquear) + preview en vivo bajo sesión real de Camilo (RLS) → 4/5 arriba de las métricas, banner sin duplicar, móvil 360/390/430 sin overflow, 0 errores de consola, 0 residuales. **Regla de validación vinculante: fixtures aisladas / paciente demo (Pepe Toro/Camilo), jamás Katherine ni datos reales sensibles.** **(#174–#175) Frente "Resumen rápido del paciente" COMPLETO — frontend-only, SIN migración nueva (`s7_51` sin cambios):** (#173 fue docs-only de cierre de la serie de correcciones #167–#172, **no** parte de este frente.) **#174 (PR-1) read model `PatientQuickSummary`** — servicio (`src/services/patientQuickSummary.service.ts`) + hook (`src/hooks/usePatientQuickSummary.ts`); **read-only desde datos, sin UI, sin DB, sin migración, sin `database.types.ts`**. Agrega la historia clínica DOCUMENTADA del paciente **con este médico**: total de consultas **firmadas** previas, última atención + motivo, diagnósticos previos (distinct por recencia), **medicamentos permanentes/vigentes/relevantes** (reusa `getPermanentPrescriptionsForPatient`), notas relevantes (alergias/tipo de sangre/notas/`family_history_notes` reciente), últimos vitales (kg→lb) y brief de las últimas 3. **Scoping crítico:** gate de ROL **`isDoctorContext`** → devuelve `emptySummary()` **ANTES de cualquier query** si no es médico (un asistente también trae `doctorId`, así que `doctorId` por sí solo **NO** basta y **no se consulta `patients`/alergias/sangre/notas**), filtro **EXPLÍCITO** por `patient_id` + `doctor_id` (defensa en profundidad sobre la RLS doctor-scoped), **SOLO `status='signed'`**, **excluye la consulta actual**, **payload vacío seguro** sin historia. Validado: tsc+build + smoke 24/24 contra el módulo real (no-médico → 0 queries, no toca `patients`; médico bajo sesión real de Camilo/RLS). **#175 (PR-2) tarjeta UI `QuickSummaryCard.tsx`** montada en `ConsultaPage.tsx` **debajo del header del paciente, antes de la evaluación clínica** — tarjeta compacta (total de atenciones, última atención fecha+motivo, chips de diagnósticos, chips de medicamentos vigentes, **alergias en amber** = único color de alerta, tipo de sangre, antecedente familiar, últimos vitales en **lb**, **"Ver últimas atenciones" colapsado** por defecto). **Gate VISUAL:** se muestra **solo si `role==='doctor'`** (nunca asistente/admin/paciente/rol desconocido; `doctorId` solo NO es criterio), **solo si `hasHistory===true`**, y **se oculta ante payload vacío/inseguro o error/carga** (nunca un error técnico al médico). **Regla de seguridad: nunca mezclar pacientes, médicos, clínicas ni perfiles** (lo garantiza el scoping del read model). Validado: tsc+build + smoke SSR 16/16 (componente real: médico+historia → tarjeta con todo el contenido; asistente/rol undefined/`hasHistory=false`/error/carga → no se renderiza) + preview en vivo bajo sesión real de Camilo (RLS) sobre **paciente demo (Pepe Toro)** + móvil 360/390/430 sin overflow + 0 errores de consola + 0 residuales. **Regla de validación vinculante: fixtures aisladas / paciente demo, jamás Katherine ni datos reales sensibles.** **(#167–#172) Serie correcciones clínicas / receta / PDF COMPLETA — frontend-only, SIN migración nueva (`s7_51` sin cambios):** **#167 (PR-A) PDF de receta limpio** — helper `doctorTitleName` evita "Dr. Dr." / "Dra. Dra." (antepone el título solo si falta, respeta el existente, no infiere género), fechas de emisión/corrección **una sola vez** en el encabezado (footer "Firmado digitalmente" sin fecha duplicada, banner "RECETA CORREGIDA" compacto), y dosis **rotulada** (Dosis / Frecuencia / Duración / Indicaciones) — un "1" suelto lee "Dosis: 1", **sin inventar** datos clínicos (texto libre tal cual); solo `RecetaPrint.tsx`. **#168 (PR-B) Diagnósticos sin Tipo/Estado en la UI** — se quitan los `<select>` de `diagnosis_type`/`diagnosis_status` de la sección de consulta y del editor del modal de corrección; queda **nombre (catálogo) + Notas**; los defaults internos `presuntivo`/`activo` los sigue aplicando `addConsultationDiagnosis`; columnas y datos históricos intactos; `DiagnosesSection.tsx` + `CorrectConsultationModal.tsx`. **#169 (PR-C) Orden en el modal de corrección de receta** — bloque "Agregar medicamento" **arriba** + nuevos arriba (más reciente primero), existentes debajo; **solo display del modal** (`[...added].reverse()`), el orden persistido y el del PDF **NO cambian** (`prescriptions.id` es UUID, `getPrescriptions` ordena por id como siempre); `CorrectConsultationModal.tsx`. **#170 (PR-D) Peso en libras con almacenamiento interno en kg** — el médico digita **lb**, se guarda `weight_kg` (helpers `lbToKg`/`kgToLb` en `vitals.service.ts`, único punto de conversión), IMC con kg (fórmula intacta), validadores/labels en lb; sin reinterpretar históricos, sin migración; `vitals.service.ts` + `ConsultaPage.tsx` + `CorrectConsultationModal.tsx`. **#171 (PR-E1) Antecedentes familiares como TEXTO LIBRE en consulta normal/borrador** — textarea ligada a `consultations.family_history_notes` (la columna y su save en `ConsultationUpdate`/`saveConsultationDraft` ya existían; estaba huérfana en UI), se oculta el catálogo estructurado para nuevos; los estructurados previos quedan **histórico/solo lectura** (`AntecedentesSection readOnly`), sin borrar/migrar; solo `ConsultaPage.tsx`. **#172 (PR-E2) Corrección POST-FIRMA de antecedentes texto libre** — el modal de corrección expone `family_history_notes` como texto libre editable (hidratado, con diff, vía `p_consultation_changes`); **SIN migración** porque el RPC `amend_consultation` vigente (`s7_31`) ya tenía `family_history_notes` en su whitelist y `UPDATE` (era infra anticipada); los estructurados quedan read-only y ya **no se generan `familyHistoryOps`**; `consultations.service.ts` (tipo `ConsultationTextField`) + `ConsultaPage.tsx` + `CorrectConsultationModal.tsx`. **Validación de toda la serie:** `tsc`+`build` verdes por PR · fixtures aisladas / paciente demo (Pepe Toro) — **regla vigente: jamás Katherine ni datos reales sensibles, ni en read-only; usar fixtures o paciente demo** · cleanup 0 residuales · preview médico/asistente + móvil 360/390/430 · OK visual del owner por PR. **(#163 / `s7_51`) "Mi lista de espera" en panel médico/asistente LIVE — gestión del directorio sin pasar por LucyAdmin:** ruta **`/panel/lista-espera`** (NavLink en `PanelLayout`, **visible para médico Y asistente**) + página mobile-first + 2 RPCs `SECURITY DEFINER` **paralelas** (NO tocan/ensanchan las `admin_*`): `clinic_list_waitlist(p_doctor_id, p_status, p_limit, p_offset)` (pendientes primero + `total_count` vía `count(*) OVER()`) y `clinic_update_waitlist_entry(p_entry_id, p_status, p_notes)` (estado/nota + audit `edited_via='clinic'`). **Gate server-side** (helper `_clinic_waitlist_authorize`): el caller debe ser el **médico dueño** (`get_user_doctor_id() = doctor_id`) **o miembro de la clínica** del médico (`is_clinic_member`) — **NO confía en el `doctor_id` del cliente**, sin cross-clínica/cross-médico; estados `pending`/`contacted`/`cancelled` (P0001/P0105/P0106/P0107). Médico y asistente pueden: **ver** la lista autorizada, **filtrar por estado**, **marcar contactado**, **volver a pendiente**, **cancelar/descartar**, **editar nota interna**, y **contacto manual** (Llamar `tel:` / WhatsApp `wa.me` / copiar teléfono) sobre el teléfono ya registrado. **V1 SIN agendar** (crear cita / walk-in prefilled = follow-up futuro), **sin mensajería automática**; **admin global (`/admin/lista-espera`, RPCs `admin_*`) y `submit_waitlist_entry` (público) intactos**; **sin columnas nuevas**, sin tocar el esquema de `waitlist_entries` / RLS / dedup / identidad / `auth.users`. **Fix destapado en la validación:** `clinic_update_waitlist_entry` declaraba `v_old record` y el `SELECT … INTO v_old.status/…` abortaba **toda** actualización con `55000 record "v_old" is not assigned yet` → corregido a **`v_old waitlist_entries%ROWTYPE`** (idempotente `CREATE OR REPLACE`, re-aplicada). **Validación:** `check-s7_51` 3/3 + `_smoke-s7_51` 9/9 (T1–T8: médico ve su lista / aislamiento cross-doctor / no-miembro bloqueado / update + audit / asistente ve+actualiza / estado inválido) + cleanup **0 residuales** (`F51_FIXTURE`) + **preview médico Y asistente con sesión real** (NavLink visible, página carga sin error, listado autorizado, cambio de estado, audit `edited_via='clinic'` con `contacted_by` correcto) + **móvil 360/390/430 sin overflow** + desktop sin regresión + tsc/build verdes + **0 datos reales tocados** (test user `50375000001` usado como asistente temporal con rol restaurado a `patient`; jamás Camilo ni Katherine). **Pendiente (futuro, no V1):** "agendar" desde la lista (walk-in prefilled) + sección equivalente en panel + paginación visible si crece. **(#162) Paginación/Home LIVE — render incremental "Mostrar más médicos":** `PAGE_SIZE=12`, contador "Mostrando N de M", reset por filtros/búsqueda/orden/toggle; frontend-only (NO paginación server-side; el fetch sigue trayendo el set completo, tope ~1000 → server-side real = frente mayor futuro). **F4 merge/unmerge + F4-3b cerrados; Pendiente F4: F4-D (diferido, no arrancado).** **(#160) Seguridad de sesión LIVE — cierre por inactividad + tope absoluto por rol (frontend-only):** wrapper central `SessionGuard` (montado 1 vez en `App.tsx` dentro de `BrowserRouter`; sin tocar PanelLayout/AdminLayout/área paciente) + hook `useIdleLogout` + `SessionTimeoutModal` + `sessionPolicy`. Política por rol: **admin 15min/8h · médico/asistente 30min/12h · paciente 60min/24h · aviso 90s**. `deadline = min(inactividad, tope absoluto)`; aviso con cuenta regresiva → **"Mantener sesión"** (resetea SOLO inactividad, NO el tope absoluto) / **"Cerrar sesión ahora"**; al vencer → logout. **Fallback ESTRICTO** para rol desconocido/nulo (= admin, no paciente). Tope absoluto anclado a `last_sign_in_at`. La actividad **no revive** una sesión ya vencida (al volver de background se evalúa el vencimiento antes de registrar actividad; re-evaluación en `visibilitychange`/`focus`). **Logout robusto:** `signOut({scope:'local'})` best-effort + limpieza dura de tokens `sb-*` en localStorage + recarga (evita el cuelgue conocido de `signOut` con el lock no-op). Override de tiempos **solo `import.meta.env.DEV`** (sin lógica por hostname). **Sin DB/RLS/migraciones/`database.types.ts`/identidad/`auth.users`; sin tocar login/OTP/reset.** tsc+build verdes; validación dev (warning/keep/cerrar/auto-logout/tope absoluto/volver-tras-idle) + OK del owner. **(#239) Seguridad de sesión — el idle-logout sobrevive a reload/reapertura/kill de pestaña (frontend-only, 1 archivo `src/hooks/useIdleLogout.ts`, SIN migración, `s7_52` sin cambios):** el gap móvil era que el reloj de inactividad vivía solo en memoria (`useRef`) y se reiniciaba en cada montaje → si el SO móvil mataba/reabría la pestaña en frío, el idle no atrapaba la sesión (solo la cerraba el tope absoluto). Ahora se **persiste SOLO el timestamp de última actividad** en `localStorage` (clave `lc_last_activity`, epoch ms, **sin PII ni datos clínicos**): el idle se **hidrata** del valor persistido al montar, `refresh()` ancla `actividad >= last_sign_in_at` (login nuevo reinicia; restauración en frío conserva), reset **síncrono** en `SIGNED_IN` para evitar el falso logout por la carrera con el tick async, persist throttled (15s) en actividad + flush en `visibilitychange→hidden`/`pagehide`, guarda contra timestamps futuros (clock skew), y **clear en logout y `SIGNED_OUT`**. **NO cambió tiempos de política** (admin 15m/8h · médico/asistente 30m/12h · paciente 60m/24h · desconocido→estricto), tope absoluto, modal 90s, `sessionPolicy.ts`, guards/rutas/layouts, Supabase config, DB/SQL/migraciones/`database.types.ts`, Analytics/SEO/sitemap/robots/favicon/archivos Google/branding/performance. **Validación runtime en prod `lucycare.app` (navegador real, cuenta demo Camilo):** login OK → `/panel` con sesión → `lc_last_activity` presente y **solo epoch ms** → reload **NO borra ni reinicia** la clave (idéntica) → logout limpia `lc_last_activity` **y** tokens `sb-*` → `/panel` sin sesión redirige a `/` → 0 errores de consola; bundle productivo contiene el fix. **(#158–#159) Responsive móvil LIVE (frontend-only):** **#158 Home público** — eliminado el overflow horizontal del header (nav `flex-wrap`+`min-w-0`, logo `h-10` móvil, "Soy médico" oculto en móvil autenticado); **#159 Panel/Citas/Calendario/Bloqueos** — fila de fecha de Citas en 2 filas móvil + KPIs `grid-cols-2`, CalendarHeader con el rango en fila propia (sin truncar), botón "Nueva cita" integrado, WeekView con columnas legibles (scroll interno dentro del card), BlockForm con fechas/horas apiladas y tipos parejos. Desktop intacto; `scrollWidth===clientWidth` en 360/390/430. **(#157)** docs-only: cierre de `s7_50`. **(#156 / `s7_50`) Fix de onboarding de asistentes LIVE — `accept_clinic_invitations` tolerante a formato de teléfono:** el gate (sobre `profiles.phone`) y el loop (sobre `clinic_invitations.phone`) ahora comparan con `normalize_phone_sv(...)` en **ambos lados** → cierra un **bug operativo ACTIVO** (la invitación se guardaba como `+503XXXXXXXX` y `profiles.phone` como `503XXXXXXXX`, así que el gate **literal** fallaba y la invitación no se aceptaba, con el error silenciado en el login). **Misma firma**, misma lógica de cupos/asientos, **`database.types.ts` NO tocado**, sin tocar UI/identidad global/`auth.users`. Validación: `check-s7_50` verde + `_smoke-s7_50` verde (`pass=7 fail=0`; fixtures aisladas `F50_FIXTURE`; **0 residuales**; role del test patient restaurado a `patient`; **0 datos reales tocados** — jamás Camilo ni Katherine). **(#151–#153 / `s7_49`) F4-3b V1 — bandeja de vínculos rechazados LIVE:** sección "Vínculos rechazados" en `/admin/pacientes` sobre la cola `patient_link_rejections` (rechazos paciente-driven "No son mías" de B2). **Backend (#151/`s7_49`):** columna `resolution_note` + `admin_list_patient_link_rejections(p_status)` (read, admin-only, metadatos + estado actual de la ficha, sin contenido clínico/JSON crudo) + `admin_resolve_link_rejection(p_rejection_id, p_resolution_note, p_reopen)` (resolver motivo ≥10 / reabrir; audit en cada transición; el reopen conserva el estado previo en `old_data`; **P0080** inexistente / **P0081** motivo<10); check 5/5 + smoke 11/11. **Fix smoke (#152):** el fixture insertaba sin `date_of_birth` (NOT NULL). **UI (#153, frontend-only, `database.types.ts` NO tocado):** filtro pendientes/resueltos/todos + tabla (ficha/clínica/perfil que rechazó/teléfono/fecha/estado/**estado actual de la ficha**) + **Resolver** (modal, motivo ≥10) + **Reabrir**; invalida la query. **Resolver = bookkeeping administrativo:** NO re-vincula, NO fusiona, NO toca `patients`/`profiles`/`auth.users`; la fila sigue excluyendo el par del auto-link mientras exista (independiente de `status`). **V1 = solo bandeja de rechazos; SIN fuzzy, SIN detección proactiva de pares débiles, SIN F4-D.** Validado con fixtures `F49_FIXTURE` (listar→resolver→reabrir→0 residuales), desktop + móvil, OK visual del owner; **fila real `50375000001` restaurada a `pending_review`** tras el preview, **0 datos reales tocados** (jamás Camilo ni Katherine/`50372608827`). **(#149) F4 Unmerge — UI "Deshacer fusión" LIVE:** acción en el historial de fusiones de `/admin/pacientes` para revertir un merge, sobre las RPCs `s7_48`. Botón **"Deshacer fusión"** solo si `unmerged_at IS NULL` (la autoridad real es `admin_unmerge_patients_preflight`; si bloquea P0070–P0077 no permite confirmar), badge **"Revertida"** si ya se revirtió. Modal de 3 pasos: preflight (resumen del merge, veredicto, conteos a devolver, **warnings como info** — historia nueva en target/E3/merge viejo) → **confirmación reforzada** (motivo ≥10 + teclear `DESHACER FUSIÓN` con tilde, Escape/X bloqueados en submit) → resultado (origen reactivado/destino/`merge_log_id`/devuelto al origen). Errores **P0070–P0077** por `error.code` (`UNMERGE_BLOCK_COPY`/`unmergeBlockMessage`); tras éxito invalida historial + candidatos (la fuente restaurada reaparece como candidata). **Sin contenido clínico, sin JSON crudo. Frontend-only, sin migración/RLS/backend; `database.types.ts` NO tocado** (firmas RPC ya de #147). Solo `src/pages/admin/AdminPacientesPage.tsx` + `src/services/patientMerge.service.ts`. Validado con fixtures `F43B_FIXTURE` (merge UI → unmerge UI → 0 residuales), desktop + móvil, tsc + build, **OK visual del owner**, **0 datos reales tocados**. **(#147 / `s7_48`) Unmerge formal — backend LIVE:** reversa del merge admin sobre `patient_merge_log` — `admin_unmerge_patients_preflight(p_merge_log_id)` (dry-run) + `admin_unmerge_patients(p_merge_log_id, p_reason)` (transaccional, `FOR UPDATE`, revalida server-side). Devuelve destino→fuente **solo** las filas de `moved_ids` que siguen en el target, restaura la fuente desde `source_snapshot`, **el destino conserva** su historia post-merge, rellena `unmerged_at`/`unmerged_by`/`unmerge_reason` (**append-only**) + audit `edited_via='admin_unmerge'`. **Bypass propio `app.unmerging_patients`** añadido a los dos guards (firma `s7_29` + identidad P0030 `s7_33`; los previos intactos). **Reversa atómica** (no parcial), **no hard-delete**, **no toca `profiles`/`auth.users`/identidad global**; el esquema de `patient_merge_log` no cambia (las columnas `unmerge_*` ya existían de `s7_46`). Códigos **P0070–P0077** (P0070 inexistente · P0071 ya revertido · P0072 fuente fuera de estado · P0073 destino en cadena/clínica · **P0074 conflicto de documento** · P0075 `moved_ids` drift · P0076 motivo<10 · **P0077 profile del snapshot inexistente**) + warnings (`W_TARGET_NEW_HISTORY`/`W_OLD_MERGE`/`W_REMERGE_RISK`). Firmas de las 2 RPCs en `database.types.ts`. **Backend-only, sin UI.** check 5/5 + smoke 17/17 (happy path merge→unmerge, P0071/P0073/P0074/P0075/P0076/P0077, historia nueva en target como warning, consulta firmada devuelta sin romper guards, audit, fixtures `F48_FIXTURE` con 0 residuales, 0 datos reales tocados). **(#144) F4-3 UI PR B — merge real en `/admin/pacientes` LIVE:** habilita la fusión real (`admin_merge_patients`, `s7_46`) desde la UI — botón **"Fusionar fichas"** solo si `preflight.eligible`, **confirmación reforzada** (motivo ≥10 + teclear `FUSIONAR FICHAS`), vista de éxito (source/target/`merge_log_id`/conteos movidos), errores **P0060–P0068** por `error.code` (reusa `MERGE_BLOCK_COPY`), Escape/X bloqueados durante el submit, invalidación de candidatos + historial; firma de `admin_merge_patients` agregada a `database.types.ts`; dev-script `scripts/_fixtures-merge-ui.mjs` (fixtures E3 aisladas marcadas `F43B_FIXTURE`, `--apply`/`--clean` con verificación de 0 residuales). **Frontend-only, sin migración/RLS/backend; sin unmerge (reversa futura sobre `patient_merge_log`); sin contenido clínico ni JSON crudo; sin teléfono/nombre como evidencia; sin tocar profiles/auth.users/identidad global.** Validado con fixtures (apply→merge→P0061→clean, **0 residuales**), desktop + móvil, tsc + build verdes, **validación visual del owner OK**, **0 datos reales tocados** (jamás Camilo ni Katherine/`50372608827`). **(#142) F4-3 UI PR A — `/admin/pacientes` (vista previa read-only) LIVE:** ruta `/admin/pacientes` (`AdminOnlyRoute` + `AdminLayout`) + NavLink "Pacientes" + `AdminPacientesPage.tsx` + `patientMerge.service.ts`. Consume `admin_list_patient_merge_candidates()` (lista de grupos `same_profile` con warnings `W_UNCONFIRMED_LINK` + estado vacío) + `admin_merge_patients_preflight` (dry-run: comparación source/target lado a lado con badges, evidencia, conteos, **P0060–P0068 mapeados a copy**, "Intercambiar fuente/destino") + lectura de `patient_merge_log` (historial legible, sin JSON crudo). Firma de `admin_merge_patients_preflight` agregada a `database.types.ts`. **NO ejecuta merge, NO llama `admin_merge_patients`, NO botón "Fusionar", NO contenido clínico, NO JSON crudo, NO F4-3b, NO unmerge, NO toca profiles/auth.users/identidad global.** Decisión A/B: F4-3 UI se dividió en PR A read-only (esto) + **PR B acción destructiva (pendiente, siguiente frente)** por ser la primera UI admin que ejecutará un merge y aún no hay unmerge formal. Frontend-only, sin migración; tsc + build + OK visual del owner (preview desktop/móvil con fixtures E3 limpiadas). El historial aún no muestra "admin ejecutor" (omitido en PR A; dato en `patient_merge_log.merged_by`). **(#140 / `s7_47`) F4-3-search — RPC de candidatos a merge LIVE:** `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`→`P0001`, anon sin EXECUTE). **READ-ONLY** (solo SELECT/agregación: no merge, no escritura, no audit, no toca datos). Lista **grupos** fusionables **V1 = `same_profile`** (E3) por `(clinic_id, profile_id)` con `profile_id IS NOT NULL`, **solo fichas vivas** (`is_active AND merged_into_patient_id IS NULL`), `HAVING count(*) >= 2`. Por grupo: clínica/perfil + nombres + `patient_count` + `confirmed_links_count`/`unconfirmed_links_count`/`has_unconfirmed_links` + `warnings` (**`W_UNCONFIRMED_LINK`** si alguna ficha tiene `link_confirmed_at NULL` — ayuda, no bloqueo). Por ficha: identidad mínima + `counts` (appointments/consultations/vitals). **NO** contenido clínico; **NO** candidatos por teléfono/nombre/walk-in/fuzzy/override; **NO** ejecuta merge; **NO** toca identidad global/`profiles`/`auth.users`. La decisión la toma la UI (PR 2), no la RPC; el gate de evidencia real lo aplica el preflight/merge de `s7_46`. check 2/2 + smoke 10/10. **PR 1 de F4-3; la UI `/admin/pacientes` es PR 2 (siguiente frente).** **(#138 / `s7_46`) F4-2 — backend del merge admin de fichas (intra-clínica) LIVE:** `admin_merge_patients_preflight` (dry-run, no modifica DB) + `admin_merge_patients` (transaccional, `FOR UPDATE`, revalida server-side) + helper interno `_patient_merge_eligibility` + tabla `patient_merge_log` (snapshots/`moved_ids`/`moved_counts`/reason/actor/evidence; campos `unmerge_*` previstos para la reversa formal, RLS admin-only). **Bypass propio `app.merging_patients`** añadido a los dos guards (firma de consulta `s7_29` + identidad P0030 `s7_33`) — sus bypass previos intactos. El merge mueve `appointments`/`consultations`/`vitals` source→target (incl. **consulta firmada** vía bypass), **neutraliza la fuente** (`document_number→NULL`, `profile_id→NULL`, `is_active=false`, `merged_into_patient_id`/`merged_at`; **teléfono conservado**; **no hard-delete**) + audit `edited_via='admin_merge'`. **V1 = evidencia `same_profile`** (E3); el `UNIQUE(clinic,doc)` hace `same_document` inalcanzable intra-clínica (soportado en lógica para legacy, 0 dups hoy); **no merge por teléfono/nombre ni override** (DM3b). Bloqueos **P0060–P0068** (distinta clínica / ya fusionada / source==target / profiles distintos P0065 / dirección inválida P0064 / evidencia insuficiente P0063 / motivo<10 P0066 / **borrador en fuente bloquea P0067** / ficha inactiva P0068); **borrador en target = solo warning** (DM3c). El claim ya ignora fichas merged (`s7_45`). **Sin UI, sin unmerge implementado, sin identidad global, sin `auth.users`.** check 6/6 + smoke 19/19. **Tres cierres previos (2026-06-13):** **(#134) Diseño Paciente Global Fase 4 / merge admin (docs-only)** — DM1–DM9 cerradas, `docs/ANALISIS_PACIENTE_GLOBAL_FASE4_MERGE_ADMIN.md`; alcance = merge de **fichas `patients` intra-clínica** (NO identidades/`profiles`/`auth.users`, NO expedientes cross-clínica); reglas vinculantes (solo LucyAdmin, dry-run + motivo + audit obligatorios, no hard-delete, **unmerge formal** previsto); fases F4-1→F4-2→F4-3 (+F4-3b bandeja `patient_link_rejections`, +F4-D identidades diferida). **(#135 / `s7_45`) F4-1 — claim tolerante fila por fila:** `claim_patient_records()` procesa fichas **fila por fila** (subtransacción `BEGIN/EXCEPTION` por ficha) → una colisión `UNIQUE(clinic_id, document_type, document_number)` ya **no aborta el claim completo** (cierra el hallazgo F4); las fichas válidas se vinculan, la conflictiva queda **intacta** (sin media-copia); skips **auditados** (`claim_skipped_unique` / `claim_skipped_error` con SQLSTATE + fila resumen con `skipped[]`); el cliente recibe **solo conteos** (`linked_count`/`skipped_count`, sin PII por ficha — el detalle vive en `audit_log`); columnas **pasivas** `merged_into_patient_id` (FK a `patients` sin CASCADE + CHECK anti self-reference) + `merged_at` (infra de F4-2, **nada las escribe aún**; el claim **excluye** fichas marcadas como fusionadas); **NO hace merge, NO toca identidad global ni `auth.users`, NO hard-delete**; firma/GRANTs/matching intactos (exclusión de pares rechazados `s7_43` + copia COALESCE `s7_33`); `database.types.ts` actualizado; check 5/5 + smoke 16/16. **(#136) Fix bug público de reserva móvil (frontend-only):** el CTA móvil "Reservar cita" abría un scaffold **mock** (`MobileBookingSheet` con horarios generados en cliente — `doctorId % 2` sobre un UUID = `NaN` —, servicios inventados, `PaymentModal` falso que NO creaba cita en DB) → ahora monta el **flujo real** (`BookingCard`: servicios reales, slots reales vía `useAvailableSlots`, `createBooking`, login/OTP real); **fecha por defecto** (hoy si tiene agenda, o la próxima disponible vía `useAvailableDays` + verificación de slots reales) en hora **local** (helper `localDateStr()`, no `toISOString()` UTC que saltaba al día siguiente después de las 6pm SV); **error de red → "Reintentar"** (`refetch`) distinguido de **médico inexistente**; **splash** estático en `index.html` dentro de `#root` contra pantalla blanca durante el bootstrap; eliminados `PaymentModal.tsx` y `utils/auth.ts` (mocks sin imports reales); sin DB/SQL/migraciones/RLS, sin tocar reglas de agenda; tsc + build + OK visual móvil del owner; fixture de cita de prueba limpiado. **Administración de LucyAdmins Fase 1 (PR #132, `s7_44`) LIVE:** ciclo de vida completo sin SQL manual — tabla `platform_admin_invitations` (`pending/active/revoked`, TTL 14d, trazabilidad completa) + RPCs definer `admin_invite_platform_admin` (admin = **cuenta dedicada**: bloquea teléfonos de paciente real/médico/asistente `P0050`, admin `P0051`, inválido `P0055`; vigente `P0053`; vencida se refresca in-place; cuenta-cáscara creada por invitación vencida es re-invitable vía historial) / `accept_platform_admin_invitation` (activación SOLO al primer OTP del invitado, hook fail-safe en `verifyOtp`; pending sin privilegios; fill de `full_name` desde la invitación si vacío; bloquea si la cuenta se volvió paciente real) / `admin_revoke_platform_admin` (rol→patient, **bloqueo inmediato vía `is_admin()`**, **guard de último admin activo** `P0052`, sin tocar auth.users) / `admin_list_platform_admins`. **`profiles.role='admin'` sigue siendo el ÚNICO bit de autorización; `is_admin()` y sus ~22 superficies intactas.** UI `/admin/administradores` (activos/invitar con teléfono sanitizado+validado/pendientes/revocados + "Editar nombre" solo propio). Audit en cada transición. check 6/6 + smoke 24/24. **Navegación móvil LucyAdmin (PR #130):** `AdminLayout` con header fijo + hamburguesa + drawer en móvil (antes no había NINGUNA nav bajo 768px); desktop intacto; frontend-only. **Análisis administración LucyAdmins (PR #131):** `docs/ANALISIS_ADMINISTRADORES_LUCY.md` (Opción B aprobada, D1–D6 + reglas vinculantes). **Ownership del paciente (análisis #127, D1–D7 aprobadas):** regla central vinculante — *"el médico gestiona una relación clínica local; LucyCare gobierna la identidad global del usuario/paciente"* (`docs/ANALISIS_PACIENTE_GLOBAL_OWNERSHIP.md`). **B2 — Confirmación post-claim (PR #128, `s7_43`) LIVE:** el claim vincula con `patients.link_confirmed_at NULL` → la UI separa esas fichas en "Encontramos atenciones asociadas a tu número" (metadatos mínimos, acción POR FICHA); "Mis atenciones" lista **solo confirmadas**; **"Sí, son mías"** → `confirm_patient_link` (sella+audit, idempotente); **"No son mías"** → `reject_patient_link` (desvincula + `patient_link_rejections` `pending_review` + audit; **el claim no re-vincula el par rechazado**; otro profile legítimo sí puede). Legacy pide confirmación una vez; la ficha creada por el propio paciente al reservar nace confirmada. Sin bandeja admin (tabla+audit). Cierra R1/R2 del análisis de ownership. smoke 14/14. **Identidad múltiple (análisis #124 + Fase 1 #125, frontend-only):** modelo firmado "identidad única + capacidades múltiples" (`docs/ANALISIS_ONBOARDING_IDENTIDAD_MULTIPLE.md`, 5 decisiones aprobadas; `profiles.role` = rol primario/default de navegación). **Fase 1 live:** `PatientOnlyRoute` permite `patient`/`doctor`/`assistant` (excluye admin/anon) → médico/asistente acceden a su lado paciente (`/paciente/mis-atenciones`, `/paciente/perfil`) con menú/logout, "Ir al panel" (copy neutro) y nota azul de "cuenta personal". Sin RLS/DB/selector de modo (Fase 2 pendiente). **2 fixes destapados por Fase 1:** `useDashboard` vía `useClinicContext` (el home del panel reventaba para asistentes — lookup inline de `doctors` por profile_id, bug pre-existente) y `listMyAppointments` con **filtro explícito** `patient.profile_id=uid` (solo-RLS hacía que médico/asistente vieran las citas de su clínica como "Mis atenciones" propias; Camilo 66→11, paciente puro sin cambio). **Afiliación — médico desde paciente existente (PR #122, `s7_42`):** `admin_approve_and_create_doctor` ya no crashea con `users_phone_key` cuando el teléfono ya tiene cuenta — clasifica (helper `_affiliation_classify` + RPC `admin_affiliation_preflight`) y ramifica: `new` (crea auth.user dormant), `reuse_patient` (reusa el profile del paciente **sin nuevo auth.user**, solo con `confirm_reuse`, fill de `full_name` solo si vacío, **sin tocar phone/email/role**), o bloquea (`P0010` médico existente / `P0011` rol sensible / `P0012` identidad ambigua / `P0013` requiere confirmación). UI: banner por clasificación + confirmación + link a ficha. **Decisión: en reuse NO se copia el email del lead** (credencial sensible; se completa/valida en el reclamo). smoke 12/12. **Lista de espera global (PR #120, `s7_41`):** página `/admin/lista-espera` (gate `AdminOnlyRoute`) + NavLink; RPC `admin_list_waitlist` (`SECURITY DEFINER`, `is_admin()`) con join médico/perfil/especialidad/clínica + filtros (estado/médico/especialidad/búsqueda/rango de fechas) + paginación + `total_count`; reutiliza `admin_update_waitlist_entry` para el cambio de estado; smoke 10/10. **RPC F5 tolerante a teléfono crudo (PR #118, `s7_40`):** helper SQL `normalize_phone_sv` (espejo de `normalizePhoneSV`, `IMMUTABLE`, sin EXECUTE para anon/authenticated) + `find_patient_match_candidates` canonicaliza el teléfono en ambos lados → tolera `71234567`/`+503…`/espacios/guiones/paréntesis; firma/salida idénticas, cross-clínica sin PII, sin frontend; smoke 11/11. **Robustez del gate del panel (PR #116):** `useClinicContext` usa `getSession()` (no `getUser()`) → elimina el race de primera carga; `PanelLayout` con timeout 8s + `.catch()` + estado recuperable → sin spinner infinito; errores tipados (`auth`/`no_clinic`/`no_doctor`/`role`/`unknown`) con copy específico para estructurales. Frontend-only. **Catálogos personalizados por médico — EJE 100% COMPLETO** (#105 diseño + #106/`s7_37` snapshot histórico + #107/`s7_38` global+personal+RLS + #108 UI consulta + #109 UI admin médico + #110/`s7_39`+#111 infra/dry-run + **migración de base replicada aplicada**: Base Lucy 127 dx / 736 med, "Míos" solo lo propio, 1650 dx + 9568 med inactivadas, `catalog_migration_map` 11218 filas, sin hard-delete, sin reapuntar FK + **#114 UI de LucyAdmin para administrar la Base Lucy global — `/admin/catalogos`, UI-only sin migración**). Eje Correcciones post-firma COMPLETO (#74–#85, migraciones `s7_28..s7_31`). **Paciente Global Fases 1–3 COMPLETAS:** #87 (`s7_32`) identidad global + #88 UI `/paciente/perfil` + #90 (`s7_33`) sync espejo + guard + #91 UI médico read-only. **Cambio de teléfono por OTP COMPLETO:** #93 (`s7_34`) trigger de sync `auth.users.phone → profiles/patients` + audit + #94 UI paciente + #95 UI médico (`ChangePhoneModal`). **Paciente Global Fase 5 (dedup preventivo) COMPLETA:** #98 diseño + #99 (`s7_35`) backend RPC `find_patient_match_candidates` + #100 UI `PatientMatchHints`. **Mi equipo Fase 2 (ciclo de vida de invitación) COMPLETA:** #102 (`s7_36`) backend (expiración TTL 14d + `resend_invitation`) + #103 UI `EquipoPage` (vigentes/vencidas, Reenviar, badge "Vencida").
- **Sprint 6 — Reputación médica:** ✅ completado (PRs #2–#10).
- **Sprint 7 — Admin SaaS + Robustez pacientes:** ✅ Fases A, B, B2-A, B3-doctor, B3-admin (PRs hasta #30).
- **Pre-piloto (PRs #32–#58):** ✅ reclamo seguro, foto perfil, auth email+password (PR-A y PR-B), Security Gate (5 hallazgos cerrados), directorio informativo, lista de espera real, Paciente Global Fase 1, SMTP externo Resend, dominio público `lucycare.app`, password en flujo de Reclamar perfil, mitigación del flujo público "Soy médico", Afiliación médica Fase 1 (captura de leads + bandeja admin), **Afiliación Fase 2 (admin convierte lead aprobado en doctor `listed_only` con email override)**.
- **Migraciones aplicadas:** `s4_*`, `s5_01..s5_07`, `s6_01..s6_10`, `s7_01..s7_51` (`s7_26` = gate clínico asistente / PR #67; `s7_27` = límite de asistentes / PR #70; `s7_28` = inmutabilidad consultas firmadas Etapa A / PR #74; `s7_29` = corrección controlada Etapa B1 / PR #76; `s7_30` = distinción adenda texto vs receta — `affects_prescriptions` / PR #81; `s7_31` = amend de diagnósticos/antecedentes/vitales + inmutabilidad de `vitals` / PR #84; `s7_32` = Paciente Global Fase 2 — identidad global en `profiles` (DUI/DOB/género/depto/muni) + UNIQUE parcial de documento + UPDATE column-restricted + coherencia muni-depto + audit / PR #87; `s7_33` = Paciente Global Fase 3 — sync espejo `profiles→patients` (trigger) + guard server-side de identidad local (trigger, P0030) + claim copia identidad global→local / PR #90; `s7_34` = Cambio de teléfono por OTP — trigger `AFTER UPDATE OF phone ON auth.users` que sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard) + audit `edited_via='phone_change'` / PR #93; `s7_35` = Paciente Global Fase 5 — RPC `find_patient_match_candidates` (`SECURITY DEFINER`) para dedup preventivo: intra-clínica con detalle, cross-clínica/global solo señal mínima sin PII, gate `is_clinic_member`/`is_admin` / PR #99; `s7_36` = Mi equipo Fase 2 — `clinic_invitations.expires_at` (TTL 14d, `invitation_ttl()`) + `team_seats_used` excluye vencidas + `accept_clinic_invitations` ignora vencidas + RPC `resend_invitation` (titular, revalida cupo si vencida, extiende la misma fila) / PR #102; `s7_37` = Catálogos — snapshot histórico de nombre en `prescriptions`/`consultation_diagnoses` (triggers `BEFORE INSERT` + backfill) / PR #106; `s7_38` = Catálogos — modelo global+personal: `diagnoses`/`medications.doctor_id` nullable (NULL=global) + `doctor_catalog_hidden` + RLS reconstruida + audit / PR #107; `s7_39` = Catálogos — infra de-dup: `catalog_migration_map` + UNIQUE parcial sobre globales / PR #110/#111; `s7_40` = Paciente Global Fase 5 — RPC F5 tolerante a teléfono crudo: helper `normalize_phone_sv` (`IMMUTABLE`, espejo de `normalizePhoneSV`, sin EXECUTE anon/authenticated) + `find_patient_match_candidates` canonicaliza el teléfono en ambos lados / PR #118; `s7_41` = Lista de espera global — RPC `admin_list_waitlist` (`SECURITY DEFINER`, `is_admin()`) cross-médicos con join médico/perfil/especialidad/clínica + filtros + paginación + `total_count` (count(*) OVER) / PR #120; `s7_42` = Afiliación médico desde paciente existente — helper `_affiliation_classify` + RPC `admin_affiliation_preflight` + `admin_approve_and_create_doctor` reescrita (branches new/reuse_patient/block; reuse sin nuevo `auth.user`, fill `full_name` si vacío, sin tocar phone/email/role; P0010–P0013) / PR #122; `s7_43` = B2 Confirmación post-claim — `patients.link_confirmed_at` + tabla `patient_link_rejections` (`pending_review`, RLS admin-only) + RPCs self-gated `confirm_patient_link`/`reject_patient_link` (P0040) + `claim_patient_records` excluye pares rechazados (conserva la copia de identidad global→local de s7_33) / PR #128; `s7_44` = Administración de LucyAdmins Fase 1 — tabla `platform_admin_invitations` (pending/active/revoked, TTL 14d, RLS admin-only) + RPCs `admin_invite_platform_admin` (cuenta dedicada, P0050/P0051/P0053/P0055) / `accept_platform_admin_invitation` (activación al primer OTP, fill full_name) / `admin_revoke_platform_admin` (guard último admin P0052, bloqueo inmediato) / `admin_list_platform_admins`; `profiles.role=admin` sigue siendo el único bit, `is_admin()` intacta / PR #132; `s7_45` = Paciente Global Fase 4 / F4-1 — claim tolerante fila por fila: columnas **pasivas** `merged_into_patient_id` (FK `patients` sin CASCADE + CHECK `<> id`) + `merged_at` (infra F4-2) + `claim_patient_records` reescrita con LOOP `BEGIN/EXCEPTION` por ficha (la colisión `UNIQUE(clinic,doc)` ya no aborta el claim completo; skips `claim_skipped_unique`/`claim_skipped_error` auditados con SQLSTATE; cliente solo conteos; excluye fichas `merged_into_patient_id IS NOT NULL`); sin merge, sin tocar identidad/`auth.users`, sin hard-delete / PR #135; `s7_46` = Paciente Global Fase 4 / F4-2 backend del merge admin de fichas intra-clínica — tabla `patient_merge_log` + `admin_merge_patients_preflight` (dry-run) + `admin_merge_patients` (transaccional) + helper `_patient_merge_eligibility` + bypass propio `app.merging_patients` en los dos guards (firma `s7_29` + identidad P0030 `s7_33`); mueve appointments/consultations/vitals source→target, neutraliza la fuente (sin hard-delete, teléfono conservado), audit `admin_merge`; **V1 = `same_profile`** (same_document inalcanzable intra-clínica por el UNIQUE); bloqueos P0060–P0068; sin UI/unmerge/identidad global / PR #138; `s7_47` = Paciente Global Fase 4 / F4-3-search — RPC read-only `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`/`P0001`, anon sin EXECUTE) que lista **grupos** fusionables V1 `same_profile` por `(clinic_id, profile_id)` con fichas vivas (`is_active AND merged_into IS NULL`) `>= 2`; por grupo identidad mínima + conteos + `confirmed/unconfirmed_links_count` + `has_unconfirmed_links` + warning `W_UNCONFIRMED_LINK`; sin contenido clínico, sin candidatos por teléfono/nombre, sin merge, sin escritura, sin tocar identidad global/`profiles`/`auth.users` / PR #140; `s7_48` = Paciente Global Fase 4 / Unmerge formal — backend de la reversa del merge: bypass propio `app.unmerging_patients` en los dos guards (firma `s7_29` + identidad P0030 `s7_33`, los previos intactos) + helper `_patient_unmerge_eligibility` + `admin_unmerge_patients_preflight(p_merge_log_id)` (dry-run) + `admin_unmerge_patients(p_merge_log_id, p_reason)` (transaccional, `FOR UPDATE`, revalida server-side, devuelve `moved_ids` destino→fuente solo las que siguen en el target, restaura la fuente desde `source_snapshot`, rellena `unmerged_at`/`unmerged_by`/`unmerge_reason` append-only, audit `admin_unmerge`); reversa atómica, no hard-delete, no toca `profiles`/`auth.users`/identidad global; el esquema de `patient_merge_log` NO cambia (`unmerge_*` ya existían de `s7_46`); códigos P0070–P0077 (P0074 conflicto de documento, P0077 profile del snapshot inexistente); check 5/5 + smoke 17/17 / PR #147; `s7_49` = F4-3b V1 / backend de la bandeja de vínculos rechazados — columna `patient_link_rejections.resolution_note` + RPC read `admin_list_patient_link_rejections(p_status)` (admin-only, metadatos + estado actual de la ficha, sin contenido clínico) + RPC `admin_resolve_link_rejection(p_rejection_id, p_resolution_note, p_reopen)` (resolver motivo ≥10 / reabrir; audit en cada transición; el reopen conserva el estado previo; **P0080**/**P0081**); bookkeeping (no toca `patients`/`profiles`/`auth.users`, no re-vincula); la fila sigue excluyendo el par del auto-link mientras exista; UI en #153; check 5/5 + smoke 11/11 / PR #151; `s7_50` = Mi equipo — `accept_clinic_invitations` tolerante a formato de teléfono: el gate (sobre `profiles.phone`) y el loop (sobre `clinic_invitations.phone`) comparan con `normalize_phone_sv(...)` en ambos lados (cierra bug ACTIVO de onboarding de asistentes: invitación `+503XXXXXXXX` vs `profiles.phone` `503XXXXXXXX` → el gate literal fallaba con el error silenciado en el login); **misma firma**, misma lógica de cupos/asientos, sin tocar identidad global/`auth.users`/UI/`database.types.ts`; sin backfill (0 invitaciones pendientes vigentes en prod); check verde + smoke 7/7 (fixtures aisladas `F50_FIXTURE`, 0 residuales) / PR #156; `s7_51` = "Mi lista de espera" en panel médico/asistente — helper `_clinic_waitlist_authorize` (gate: médico dueño `get_user_doctor_id()=doctor_id` **o** miembro `is_clinic_member`; `P0001`/`P0107`) + RPC `clinic_list_waitlist(p_doctor_id, p_status, p_limit, p_offset)` (`SECURITY DEFINER`, pendientes primero + `total_count` vía `count(*) OVER()`; `P0105` estado inválido) + RPC `clinic_update_waitlist_entry(p_entry_id, p_status, p_notes)` (estado/nota + audit `edited_via='clinic'`; `P0105`/`P0106`; **fix `v_old waitlist_entries%ROWTYPE`** — antes `v_old record` abortaba toda actualización con `55000`); paralelas a las `admin_*` (intactas), sin columnas nuevas, sin tocar el esquema de `waitlist_entries` / RLS / dedup / `submit_waitlist_entry` / identidad / `auth.users`; firmas de las 2 RPCs en `database.types.ts`. Checks: `check-s7_51.mjs` (3/3) + `_smoke-s7_51.mjs` (9/9; fixtures `F51_FIXTURE`, 0 residuales) / PR #163). `s7_52` = Slugs Fase 1 — columna `doctors.slug` (nullable + índice UNIQUE parcial `WHERE slug IS NOT NULL` + CHECK de formato `^[a-z0-9]+(-[a-z0-9]+)*$`) + helper `slugify_name(text)` (`IMMUTABLE`, normaliza tildes/ñ) + `_doctor_next_slug(text)` (`SECURITY DEFINER`, `pg_advisory_xact_lock` por base, sufijo `-N`) + trigger `set_doctor_slug` (`BEFORE INSERT/UPDATE`, `WHEN is_published AND slug IS NULL` → genera desde `profiles.full_name`, **congela**) + backfill determinista de publicados; `database.types.ts` actualizado; check `check-s7_52.mjs` + smoke `_smoke-og-meta.mjs` (lógica de meta/sitemap) / PR #208. `s7_53` = Analytics Fase 3 / PR-A backend del dashboard de conversiones (LucyAdmin) — 2 RPCs `SECURITY DEFINER STABLE` de SOLO LECTURA con gate `is_admin()` (→ P0001 anon/médico/asistente/paciente), `SET search_path=public`, REVOKE PUBLIC/anon + GRANT authenticated: `admin_conversion_summary(p_date_from date, p_date_to date, p_specialty_id uuid, p_department_id text, p_municipality_id text) → jsonb` (reservas total + by_source manual/lucy_directorio/lucy_seguimiento con fuentes ausentes=0 + by_status por flags de `appointment_statuses` — completadas=`affects_revenue`, canceladas/no_show por `is_final`+`name`, pendientes=`NOT is_final`; waitlist/afiliaciones por estado; **reclamos desde `audit_log` `edited_via='claim_self_service'`, NO `tos_accepted_at`**; oferta/supply snapshot; reviews total/visibles/avg) + `admin_doctor_conversion_ranking(p_date_from date, p_date_to date, p_limit int, p_specialty_id uuid)` (por médico slug/nombre público/especialidad + reservas totales/directorio/waitlist/reviews/avg). **Solo agregados, 0 PII, 0 clínico; no escribe, no crea tablas, no toca RLS/datos.** Filtros geo `department_id`/`municipality_id` son **TEXT** (clinics/departments/municipalities.id son IDs cortos text, no uuid; corregido en revisión pre-SQL); `specialty_id` uuid. `database.types.ts` con las 2 firmas. **Sin UI (PR-B pendiente).** check `check-s7_53.mjs` + smoke `_smoke-s7_53.mjs` (32/32, fixtures aisladas + geo 8b + 0 PII + cleanup 0 residuales) / PR #245. Verificables con `node scripts/check-s7_NN.mjs`.
- **Mi equipo Fase 1 — límite de asistentes (PR #70, `s7_27`) — activo:** plan base = 1 titular + **2 asistentes** (`team_seat_limit()`=2 fijo). El conteo incluye **asistentes activos + invitaciones pendientes**. Enforcement **server-side validado**: trigger `BEFORE INSERT` en `clinic_invitations` (`P0001`) + revalidación en `accept_clinic_invitations`. UI `/panel/equipo` con contador `n/2` + botón deshabilitado al límite. El smoke destapó y **corrigió un bug pre-existente** en `accept_clinic_invitations` (cast `clinic_member_role::user_role` imposible → ahora `::text::user_role`; el accept nunca había funcionado por falta de asistentes). **Usuarios adicionales → fase de pagos/add-ons** (`team_seat_limit()` es el punto de enganche con `subscriptions`).
- **Mi equipo Fase 2 — ciclo de vida de invitación (PR #102 `s7_36` backend + #103 UI) — COMPLETA:** `clinic_invitations.expires_at` (TTL **14 días**, `invitation_ttl()`); "vencida" = estado **derivado** (`expires_at < now()`), sin cron. **Vencidas no consumen cupo** (`team_seats_used` las excluye) y **no se aceptan** (`accept_clinic_invitations` filtra `expires_at > now()`). **`resend_invitation(id)`** RPC (`SECURITY DEFINER`, solo titular): extiende la **misma fila** (no crea otra), y si estaba vencida **revalida cupo** antes de revivirla. UI `/panel/equipo`: pendientes **vigentes** ("expira en N días", Reenviar, Cancelar) vs **vencidas** (badge "Vencida", Reenviar primario), contador alineado (vigentes + activos), copy de seguridad reforzado. Smoke `s7_36` 8/8 + OK visual.
- **Catálogos personalizados por médico (diagnósticos y medicamentos) — EJE 100% COMPLETO (PRs #105–#111 + #114 + migración de datos):** modelo **global (base Lucy, `doctor_id IS NULL`, read-only para médicos, administrable por LucyAdmin desde `/admin/catalogos`) + personal por médico**. `s7_37` snapshot histórico de nombre en receta/diagnóstico (editar el catálogo **no** altera históricos firmados; lectura/impresión usan snapshot, fallback al JOIN). `s7_38` global+personal + `doctor_catalog_hidden` (ocultar globales por médico) + RLS (SELECT propios∪globales∪admin; INSERT/UPDATE solo propios; globales solo `is_admin`; no-médico sin escritura; sin DELETE) + audit `audit_catalog`. UI: consulta busca global∪propios con etiqueta discreta "Base Lucy" y "Crear … propio" (#108); admin del médico `/panel/catalogos` con segmento **Míos | Base Lucy** (read-only + Ocultar/Mostrar + Clonar a propio), guard de rol (#109). **De-dup de la base replicada del seed viejo aplicada** (`migrate-catalog-base.mjs`, seed `s5_03`/`s5_04` como fuente de verdad): **Base Lucy = 127 dx / 736 med canónicos**; "Míos" = solo personalizaciones reales (4 dx + 4 med en Camilo); **1650 dx + 9568 med copias replicadas inactivadas** (nunca hard-delete); `catalog_migration_map` (11218 filas) para reversibilidad; **FK no reapuntados** (snapshot protege históricos); globales de prueba "(base Lucy)" inactivados. **✅ Último cabo cerrado — UI de LucyAdmin para Base Lucy global (#114, UI-only, sin migración):** página **`/admin/catalogos`** (gate `AdminOnlyRoute`) + NavLink "Catálogos" en `AdminLayout`; tabs **Diagnósticos + Medicamentos** (Antecedentes fuera); crear/editar/inactivar/reactivar globales; búsqueda; activos/inactivos; **dedup amable** (`DuplicateCatalogError` + `23505`, refleja el UNIQUE `s7_39`); `usage_count` oculto; **no hard-delete**; candado `.is('doctor_id', null)` en los updates (no toca ítems personales). Servicio `src/services/adminCatalog.service.ts` (directo sobre tablas con sesión admin; sin RPCs nuevas). Backend ya estaba listo: RLS `s7_38` (`*_insert_admin`/`*_update_admin` con `is_admin()`) + `audit_catalog` + UNIQUE `s7_39` + snapshot `s7_37`. **Modelo final:** LucyAdmin administra globales; médicos usan/ocultan/clonan globales y crean propios; médicos **no editan globales directamente** (RLS).
- **Ubicación estructurada del médico (PR #64, `s7_25`) — live en producción:** LucyAdmin > Médicos > Editar > Clínica edita Departamento/Municipio con las listas jerárquicas del Home, guarda IDs en `clinics.department_id/municipality_id` (lo que filtra el directorio). Municipio depende del departamento; **obligatorios para médicos publicados/operativos** (enforcement server-side `P0004` + UI). Los 5 médicos publicados ya tienen ubicación estructurada.
- **Importación de médicos:** 100 cargados + 12 seed `*.lucycare.test` (desactivados en PR #38).
- **Médicos en producción hoy:** 5 publicados, 1 con `booking_enabled=true` (Camilo). Los otros 4 son "informativos" (sin agenda en línea).
- **Deploy:** Vercel auto-deploy desde `main`. `gh` CLI autenticado.
- **Service role:** rotado en PR #36. Scripts admin leen de `.env.local` (ENV vars), `service_role` no está en el repo.
- **Dominio público:** `https://lucycare.app` ✅ live en producción desde 2026-05-26 (PR #48). DNS gestionado en Cloudflare. `www.lucycare.app` redirige 308 a apex. `lucycare.vercel.app` permanece activo como **fallback temporal** (no desactivar). Previews siguen en `lucycare-git-*.vercel.app`.
- **SMTP transaccional:** Resend con dominio `lucycare.app` (verificado en Resend, DNS email — SPF/DKIM/DMARC — en Cloudflare), SMTP custom activo en Supabase Auth (PR #46). El rate limit builtin de ~4 emails/h ya no aplica.

## Decisiones cerradas (NO reabrir)

### Stack
- Backend: Supabase (Postgres + Auth + Edge Functions + Storage).
- Frontend: React 19 + TypeScript + Tailwind + Vite + React Query.
- Pagos: Stripe Checkout hosted.
- Hosting: Vercel + Supabase.
- Facturación DTE: diferida (API externa, no se desarrolla internamente).

### Modelo de datos / dominio
- Citas: una sola tabla `appointments` con campo `source`.
- Consulta clínica: guardar = borrador, firmar = inmutable.
- Servicios, diagnósticos, medicamentos: catálogos per-doctor (no globales, no CIE-10).
- Rol secretaria/asistente: incluido (agenda sí, ficha clínica no).
- Single-tenant operativo, **diseño tenant-ready** (conservar `clinic_id` siempre).
- **Modelo de paciente global (firmado en `docs/ANALISIS_PACIENTE_GLOBAL.md`):**
  - DA1: identidad global vive en `profiles` extendido. `patients` sigue como ficha por clínica.
  - DA2: vinculación retroactiva automática **solo** con phone OTP-verified.
  - DA3: walk-in editable libremente hasta reclamo.
  - DA4: "Mis atenciones" muestra la clínica.

### Auth
- OTP por SMS con Twilio para todos (paciente, médico, asistente, admin).
- Email + password como **segunda opción** para médico/admin (PR #39 ✅).
- Reset de password por email (PR #39 ✅).
- Activación de password dentro del flujo de Reclamar perfil (PR #50 ✅ — paso obligatorio post-claim con dos caminos: crear ahora / recibir link por email).
- **Ningún flujo público crea médicos `claimed` automáticamente** (PR #53 cerró esa puerta). Quien quiera afiliarse llena el formulario `/admin/afiliaciones` (PR #56 Fase 1) → LucyAdmin valida → en Fase 2 crea el `doctors` row como `listed_only` → médico hace el reclamo estándar (PR #32 + PR #50).
- **Afiliación Fase 1** (PR #56): captura de leads en `doctor_affiliation_requests` con bandeja admin para triage.
- **Afiliación Fase 2** (PR #58 + migraciones s7_22 y s7_23): RPC `admin_approve_and_create_doctor(p_request_id, p_overrides)` que en una transacción crea `auth.users` dormant + `profile` + `clinics` + `clinic_members` (owner) + `doctors` en `lucy_status='listed_only'` con flags conservadores (`is_published=false`, `is_operational=false`, `booking_enabled=false`). Email override solo aceptado si el lead no trajo email (regla server-side en s7_23). UI: botón "Crear médico" en `AdminAffiliationDetailModal` con form de overrides + pantalla éxito con link a ficha admin (no perfil público — el doctor no está publicado). El médico creado entra al flujo de Reclamar perfil estándar (PR #32 + #50) sin código nuevo del lado del médico.
- **Afiliación — médico desde paciente existente** (PR #122, `s7_42`): si el teléfono del lead ya pertenece a un `auth.user` (típico: ya es paciente), `admin_approve_and_create_doctor` ya **no** crashea con `users_phone_key`. Clasifica (`_affiliation_classify` + preflight `admin_affiliation_preflight`) y ramifica: **`reuse_patient`** → reusa el profile del paciente (sin crear otro `auth.user`), crea clinic/doctor sobre ese profile, **completa `full_name` solo si está vacío**, y **NO toca phone/email/role** (decisión cerrada: el email del lead **no** se copia automáticamente; se completa/valida en el reclamo). Requiere confirmación explícita (`confirm_reuse`, si no → `P0013`). Casos sensibles/duplicados **bloqueados**: `P0010` (ya es médico, con link a ficha), `P0011` (rol sensible/membresía o `auth.user` sin profile → revisión manual), `P0012` (email de otra cuenta / identidad ambigua). El médico creado por reuse igual nace `listed_only`/no publicado/no operativo/no verified y reclama con OTP+licencia+TOS. **Aprendizaje (Katherine):** los doctores creados ANTES de `s7_42` sobre un paciente con `full_name=''` quedaron "(sin nombre)"; se corrigen puntualmente desde la ficha admin (no se auto-arreglan al reemplazar la RPC).

### Ejes del médico (independientes)
- `lucy_status` enum: `listed_only | claimed | booking_enabled | verified`.
- `is_published`: aparece en directorio.
- `is_operational`: gate del panel (operar agenda).
- `booking_enabled`: muestra reserva en línea.
- `is_verified`: GENERATED de `lucy_status='verified'`. No editable a mano.
- **Regla clave del PR #41:** `is_published` ya **NO** exige `is_operational` para directorio. Solo controla visibilidad. Esto habilita médicos informativos.

### Directorio informativo (firmado en `docs/ANALISIS_DIRECTORIO_INFORMATIVO.md`)
- D1: completitud mínima — full_name + specialty + clinic.name + address.
- D2: pill "Agenda en línea" / "Sin agenda en línea". Copy ES (sin "booking", "online", "onboarding" en UI pública).
- D3: waitlist idempotente con UNIQUE(doctor_id, phone_norm).
- D4: notificación manual desde admin (sin SMS auto, sin Edge Functions).

### Seguridad
- RLS en todas las tablas, audit_log inmutable.
- `service_role` solo en scripts admin via `.env.local` (NO en frontend).
- `profiles` con RLS columna-level (anon solo (id, full_name, avatar_url) de publicados).
- Modales sensibles (LoginModal, ClaimProfileModal, WaitlistModal) NO cierran al click outside.

## Stack técnico verificado

- Imports usan alias `@/` (ej: `@/lib/supabase`).
- Auth usa `supabase.auth.getSession()` con timeout duro en `src/lib/session.ts` (a prueba del lock interno de supabase-js).
- React Query: `refetchInterval 30s` para citas, `60s` para notificaciones, `staleTime` variable.
- Iconos Remix (`ri-*`) en PanelLayout, SVG inline en componentes nuevos.
- Color acento del panel: emerald.
- Cliente Supabase con **lock no-op** (`src/lib/supabase.ts`) — evita pantallas blancas por locks huérfanos de navigator.locks.
- Bootstrap defensivo en `main.tsx`: valida sesión con timeout 3s antes de renderizar (PR #33).
- División territorial El Salvador 2024 cargada: `municipalities.name` = distrito (antiguo municipio), `municipalities.district` = nuevo municipio agrupador.
- Helpers compartidos:
  - `src/lib/phone.ts` — `normalizePhoneSV`.
  - `src/lib/document.ts` — `validateDocument`, `sanitizeDuiInput`, `formatDuiDisplay`.
  - `src/lib/errors.ts` — `friendlyErrorMessage`.
  - `src/lib/session.ts` — `getSessionWithTimeout` con `Promise.race`.

## Sistema de diseño (Sprint 4 — vinculante)

Establecido al notar inconsistencias acumuladas. **Toda pantalla nueva debe seguir esto. Páginas de referencia: `/panel/catalogos` y `/panel/servicios`.**

### Colores semánticos

- `emerald` → acción primaria, estado positivo (consulta firmada, paciente atendido).
- `blue` → información neutra, badges secundarios (asistente, permanente).
- `amber` → advertencia / borrador / validación inline.
- `red` → destructivo / cancelado / no asistió / errores.
- `gray` → neutro, fondos, texto secundario.

### Spacing y radius

- **Cards principales**: `bg-white rounded-2xl border border-gray-200 p-5`.
- **Sub-items dentro de cards** (filas de lista, items en form): `bg-gray-50 rounded-lg p-3`.
- **Inputs y selects**: `border border-gray-200 rounded-lg px-3 py-2 text-sm`.
- **Pills/badges**: `rounded-full px-2.5 py-0.5 text-xs font-medium`.
- **Modales**: `rounded-2xl shadow-xl p-6`.
- Gap consistente: `gap-3` para grupos relacionados, `gap-5` entre secciones.

### Botones — usar `<Button>` de `src/components/ui/Button.tsx`

4 variants: `primary` (emerald solid), `secondary` (gray solid), `subtle` (emerald soft pill), `danger` (red solid).
3 sizes: `sm`, `md`, `lg`.

### Copy en español (vinculante en UI pública)

- "Agenda en línea" (NO "online booking" / "agenda online").
- "Sin agenda en línea" (NO "no operativo" / "booking disabled").
- "Lista de espera" (NO "waitlist").
- "Reserva en línea" (NO "online booking").

### Patrones de modal

- **NO cerrar al click outside** en modales con form (LoginModal, ClaimProfileModal, WaitlistModal).
- Cerrar solo por: botón X, Escape, o éxito.
- Botón X disabled durante loading.
- `useEffect` con listeners siempre **antes** de cualquier `return null` por estado.

## Patrones de infraestructura (vinculantes)

### Cliente Supabase
- `src/lib/supabase.ts` con `lock: noopLock` (PR #33).
- NUNCA exponer `service_role` en frontend.
- Scripts admin: `import { supabaseAdmin } from './_lib/supabase-admin.mjs'` (lee de `.env.local`).

### useClinicContext (`src/hooks/useClinicContext.ts`)
Hook unificado que devuelve `{ profileId, role, clinicId, doctorId, doctorName, doctorIsOperational, availableDoctors }` para doctor o asistente. **Toda página del panel debe usarlo** — NO hacer lookup inline a `from('doctors')`. **Lee identidad con `getSession()`** (no `getUser()`; PR #116) para alinear con `getCurrentAuthUser` y evitar el race de primera carga. Lanza `ClinicContextError` tipado (`kind`: `auth`/`no_clinic`/`no_doctor`/`role`/`unknown`); la query **no reintenta** errores estructurales. Helpers exportados: `contextErrorKind`, `isStructuralContextError`.

### Audit log
- `src/services/auditLog.service.ts` con `logAuditEntry()` — no bloquea si falla.
- Schema: `audit_log` action ENUM ('select','insert','update','delete') — siempre minúsculas.
- Triggers de audit en: `consultations`, `patients`, `prescriptions`, `consultation_family_history`, `clinic_invitations`, `profiles.avatar_url`, `reviews` (s7_15), `waitlist_entries` (s7_18).
- Las RPCs admin (`admin_*`) escriben audit_log directamente en su cuerpo, con `edited_via: 'admin'` en `new_data`.
- RPCs especiales (claim, waitlist, paciente global) auditan con `edited_via` distintivo.

### Notificaciones del panel
- NO usa la tabla `notifications` (esa es para SMS/email salientes, futuro).
- Se derivan de `appointments` en `src/services/panelNotifications.service.ts`.
- Read/unread con timestamp en `localStorage`.

### Cuenta demo oficial — Dr. Camilo Carrillo / Pepe Toro

Detallada en `docs/CUENTA_DEMO_CAMILO.md`. NO incluir en limpiezas de seed.

| Campo | Valor |
|---|---|
| `profile_id` | `db1fba98-a299-4f25-82f1-7feff01e58fa` |
| `doctor_id` | `783a902a-55fd-407c-9e0a-69568135c7f5` |
| Phone (Test Phone) | `50378627694` / OTP `123456` |
| Email | `carlosmartinezddv@gmail.com` |

Estado: `lucy_status='verified'`, `is_published=true`, `is_operational=true`, `booking_enabled=true`. Con servicios, availability, ~50 appointments, ~15 consultations.

### Test Phones configurados

| Phone | OTP | Uso |
|---|---|---|
| `50378627694` | `123456` | Camilo (médico demo) |
| `50378056365` | `123456` | Admin de plataforma |
| `50375000001` | `123456` | Paciente test Fase 1 (con 3 patients precargados, marker `notes='test_patient_phase1'`) |

## Sprints completados

### Sprint 1-2 ✅
Directorio público + booking público con OTP + panel médico básico (disponibilidad, bloqueos).

### Sprint 3 ✅
Panel del médico: calendario 3 vistas, marcar estado + audit log, walk-in, pacientes, perfil público editable, rol asistente.

### Sprint 4 ✅
MVP de consulta clínica firmable e imprimible. Sistema de diseño documentado.

### Sprint 5 ✅
Antecedentes familiares, plan de seguimiento inline, estados de diagnóstico expandidos, Mi equipo, multi-doctor selector para asistente, paginación admin catálogos.

### Sprint 6 — Reputación médica ✅ (PRs #2–#10)
Encuesta post-cita con token único, NPS, score público ajustado bayesianamente, badges, comentarios anónimos, trazabilidad admin.

### Sprint 7 — Admin SaaS + Robustez ✅ (PRs #16–#30)
Admin SaaS completo (dashboard, listado, edición perfil/clínica/info/servicios). Robustez pacientes (document_number nullable, dedup teléfono, normalización SV, validación DUI).

### Pre-piloto — Bloqueantes cerrados ✅ (PRs #32–#58)
- **Reclamo seguro** (PR #32, s7_13).
- **Estabilización supabase-js lock** (PR #33).
- **Foto de perfil** (PR #34, s7_14).
- **Security Gate audit + trigger reviews** (PR #35, s7_15).
- **Rotar service_role + ENV vars** (PR #36).
- **RLS hardening profiles** (PR #37, s7_16, s7_16b).
- **Desactivar seed *.lucycare.test** (PR #38, s7_17).
- **Auth email + password + reset por email** (PR #39, Fase 4 PR-A).
- **LoginModal no cierra al click outside** (PR #40).
- **Directorio informativo + copy ES** (PR #41).
- **Análisis paciente global + decisiones** (PR #42).
- **Lista de espera real + badge admin** (PR #43, s7_18, s7_19).
- **Paciente Global Fase 1** (PR #44, s7_20).
- **SMTP externo Resend + reset por email validado** (PR #46, doc + setup operativo 2026-05-26, dominio `lucycare.app`).
- **Dominio público `lucycare.app` live en Vercel** (PR #48, setup operativo 2026-05-26, DNS en Cloudflare, www→apex 308, Supabase Site URL + reset email validados).
- **Password en flujo de Reclamar perfil** (PR #50, Fase 4 PR-B — step obligatorio post-claim, dos caminos: crear ahora / recibir link por email; copy "Perfil reclamado" diferenciado de "Cuenta suspendida"; sin migración).
- **Análisis del flujo "Solicitar afiliación"** (PR #52, `docs/ANALISIS_AFILIACION_MEDICO.md` — 10 secciones, 10 preguntas de decisión pendientes Q1-Q10).
- **Mitigación del flujo público "Soy médico"** (PR #53, Hallazgo #7 del Security Gate). El `DoctorRegistrationModal` legacy + `registerDoctor` service creaban automáticamente profile/clinic/clinic_member/doctor con `lucy_status='claimed'` directo, sin validación de identidad. **Neutralizado**: el botón abrió `DoctorInterestModal` (WhatsApp-only) como mitigación temporal. Sin persistencia en DB. Archivos legacy marcados `@deprecated`.
- **Afiliación Fase 1 — captura de leads + bandeja admin** (PR #56, `s7_21`). Reemplaza al `DoctorInterestModal` por `AffiliationRequestModal` con formulario completo. Persiste leads en `doctor_affiliation_requests` con RLS estricto, rate limit 1/IP/24h, UNIQUE phone activo. Bandeja `/admin/afiliaciones` con triage (in_review / approved / rejected) + badge en sidebar. No crea doctor/profile/clinic todavía.
- **Afiliación Fase 2 — convertir lead aprobado en doctor `listed_only`** (PR #58, `s7_22` + `s7_23`). RPC `admin_approve_and_create_doctor` crea auth.users dormant + profile + clinic + doctor en una transacción. Email override aceptado solo si lead no trajo email. UI: botón "Crear médico" en modal admin + pantalla éxito con link a ficha admin. **Claim end-to-end validado ✅ (2026-05-30, smoke con test phone médico real).** El smoke detectó 4 bugs corregidos en PR #61 + `s7_24` (el principal: el profile debe quedar `role='patient'` pre-claim, no `'doctor'`). Ver "Smoke end-to-end de afiliación" abajo.

## Próximas fases (en cola)

### Smoke end-to-end de afiliación — ✅ COMPLETADO (2026-05-30)

Validado el claim end-to-end del médico creado vía Fase 2, con test phone
médico real (`50375000099` / OTP `123456`). Resultado en DB: el doctor pasó
de `listed_only` → `claimed`, `tos_accepted_at` seteado, `audit_log` con
`edited_via='claim_self_service'`, y siguió sin `verified` / `is_operational`
/ `booking_enabled`. El claim pasó por el modal correcto y el password se
creó (confirmación visual del step "Contraseña creada").

**El smoke encontró 4 bugs en Fase 2; corregidos en PR #61 + migración `s7_24`:**
1. **Raíz — "Perfil reclamado" falso para `listed_only`.** La RPC `admin_approve_and_create_doctor` dejaba el profile con `role='doctor'` **antes** del reclamo; como ese profile coincide con el `auth.user` que el médico reusa al hacer OTP, entraba al panel pre-claim y veía "Perfil reclamado" sin haber corrido `claim_doctor_profile`. **Fix (`s7_24`):** el profile queda `role='patient'` pre-claim (igual que el login de un importado); `claim_doctor_profile` lo sube a `'doctor'` al reclamar. Un médico `listed_only` ya **no** entra al panel pre-claim.
2. **Nombre de clínica "Consultorio Dr. Dr. X"** cuando el nombre ya traía "Dr."/"Dra." → fix en el fallback.
3. **Modal admin "Crear médico" no precargaba Departamento/Municipio** (la persistencia server-side ya funcionaba; faltaba que `admin_list_affiliation_requests` devolviera los IDs). Fix en `s7_24` + frontend.
4. **Panel requería refresh** si `useClinicContext` fallaba → `PanelLayout` ahora muestra "Reintentar" en vez de spinner colgado.

**Aprendizajes operativos del smoke (conservar):**
- **NO hacer OTP con el teléfono del médico ANTES de que admin cree el doctor.** Supabase crearía un `auth.user` paciente que choca con el `UNIQUE` de phone en la RPC.
- **`listed_only` ≠ `claimed` ≠ `verified`.** Crear desde afiliación deja `listed_only`; reclamar → `claimed`; `verified` lo decide LucyAdmin.
- **"Perfil reclamado" en el panel NO prueba el claim** (con el copy viejo aparecía también para `listed_only`). La prueba es `lucy_status='claimed'` en DB.
- **`auth.admin.listUsers()` pagina mal** en este proyecto (bug GoTrue por fila corrupta en página 2). Para buscar/limpiar por phone, derivar el `auth.user.id` desde `profiles.id` (id compartido) y usar `getUserById`/`deleteUser`.

PR #61 (fix + `s7_24`) ✅ mergeado (commit `b2decba`). Después: opciones en cola (no bloqueantes para piloto) — ver "Pendientes prioritarios a conservar" más abajo.

### Pre-piloto público (operativo, no código)
1. **Smoke 7.3 opcional** — validar capacidad enviando 5 resets seguidos (no bloqueante, el rate limit builtin ya no aplica con SMTP externo).
2. **Cleanup test patients Fase 1** cuando termine la validación (`node scripts/setup-test-patient.mjs --clean`).

### Fase 4 — Auth robusta del médico
- ✅ **PR-A** (PR #39): login email/password + reset por email.
- ✅ **PR-B** (PR #50): password en flujo de Reclamar perfil.
- Próximo (post-piloto): Fase 2 self-service de cambio de email/teléfono. Fase 3 2FA opcional. Ver `docs/ANALISIS_AUTH_MEDICO.md`.

### Paciente Global
- **Fase 1** ✅ live (PR #44, `s7_20`): vinculación retroactiva por phone OTP + "Mis atenciones".
- **Fase 2** ✅ live (PR #87/`s7_32` backend + PR #88 UI): identidad global en `profiles` (DUI/DOB/género/dpto/muni, **DUI progresivo** = todo nullable), **UNIQUE parcial** `(document_type, document_number)`, **UPDATE column-restricted** (paciente no cambia `role`/`is_active`/`phone`), coherencia muni-depto (P0004), audit (`edited_via='profile_identity'`), **privacidad** (anon nunca ve DUI/DOB). UI `/paciente/perfil` (form + teléfono read-only), **banner no bloqueante** "Completá tu perfil" en "Mis atenciones", **prefill con "Usar" por campo** desde fichas locales (sin guardado automático; conflicto entre clínicas sin auto-elegir), DUI duplicado → mensaje amable.
- **Fase 3** ✅ live (PR #90/`s7_33` backend + PR #91 UI): **Opción B (sync espejo)**. `profiles` = verdad canónica; `patients` = **espejo sincronizado** (compat con consulta/receta/listado). **Guard server-side** (trigger BEFORE UPDATE en `patients`, `P0030`): médico/asistente no edita identidad local (`full_name`/`phone`/`email`/`document_*`/`date_of_birth`/`gender`) si `profile_id IS NOT NULL`; locales (sangre/alergias/emergencia/notas/tipo/foto/is_active) editables. **Claim** copia identidad global→local al vincular (global gana; local-only se conserva; no promueve local→global). **Sync ongoing** (trigger AFTER UPDATE en `profiles`): propaga campos cambiados a fichas vinculadas (cross-clínica, sin anular NOT NULL, sin pisar locales; bypass GUC `app.syncing_patient_identity` solo para el sync). `phone` fuera del sync (read-only + guardado). **UI médico**: `EditPatientModal` muestra identidad read-only ("Datos gestionados por el paciente en su perfil Lucy" / "No cargado por el paciente") cuando hay vínculo; badge en el detalle; walk-in sin cambios. Validado smoke OTP 6/6.
- **Fase 4** ⏳: herramienta admin para fusionar duplicados (`admin_merge_patients`, audit + reversibilidad + dry-run) — resuelve los DUI duplicados que el UNIQUE destape. **Nota (de F3):** el sync de Fase 3 puede chocar con `UNIQUE(clinic_id, document_type, document_number)` si existieran **fichas duplicadas del mismo perfil en la misma clínica** con documento no-null (caso raro: el dedup lo previene); falla elegante (error al guardar perfil, no corrupción) → considerarlo en el merge/dedup admin.
- **Fase 5** ✅ live (PR #98 diseño + PR #99/`s7_35` backend + PR #100 UI): **dedup preventivo** en creación de paciente/walk-in. RPC `find_patient_match_candidates` (`SECURITY DEFINER`): **intra-clínica → detalle** (id/nombre/teléfono/activo, "Usar este paciente"); **cross-clínica/global → solo señal mínima** (`weak` por teléfono, `strong` por documento), **sin PII**; gate `is_clinic_member`/`is_admin` (`P0001`); **advisory, no bloquea** (el bloqueo fuerte sigue siendo el `UNIQUE` intra-clínica). `createBasicPatient` con chequeo proactivo de documento → `DuplicateDocumentError` (banner amable, no error crudo). UI: `PatientMatchHints` reusable (debounce 350ms) en `NewPatientModal` + `CreateWalkInModal`. **Normalización del teléfono con `normalizePhoneSV`** (mismo helper que la dedup del INSERT) — el lookup y el submit ven lo mismo. Smoke s7_35 8/8 + OK visual. ✅ **Defensa en profundidad cerrada (PR #118/`s7_40`):** la RPC ahora **canonicaliza el teléfono internamente** (helper `normalize_phone_sv`, espejo de `normalizePhoneSV`, aplicado a input y filas) → ya **no depende** de que el caller normalice; tolera `71234567`/`+503…`/espacios/guiones/paréntesis y cubre filas legacy. Helper `IMMUTABLE` sin EXECUTE para anon/authenticated (`42501`); firma/salida idénticas; cross sin PII; smoke `s7_40` 11/11. Sin frontend.
- **Cambio de teléfono por OTP** ✅ live (PR #93/`s7_34` backend + #94 UI paciente + #95 UI médico): el cambio lo maneja Supabase Auth desde el cliente (`updateUser({phone})` → OTP **solo al número nuevo** → `verifyOtp(type:'phone_change')`, sesión activa; **Secure phone change OFF**). Un **trigger `AFTER UPDATE OF phone ON auth.users`** (`sync_phone_to_identity`, definer) sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard de `s7_33`) + audit `edited_via='phone_change'` (`user_id=NEW.id` porque `auth.uid()` es NULL en contexto GoTrue; auditoría best-effort, no bloquea). UI: **`ChangePhoneModal`** reusable (2 pasos) en `/paciente/perfil` (#94) y `/panel/perfil` (#95), teléfono read-only + botón "Cambiar". **Decisiones:** sesión activa + OTP al nuevo (no se exige OTP al anterior); sin re-auth fuerte en MVP; **recuperación sin sesión** (perdió el teléfono y sin email/password) → **soporte/LucyAdmin manual**. **Mejoras no-MVP:** notificación de seguridad al canal anterior (SMS/email), re-auth reforzada, herramienta formal de recuperación. Smoke automatizado 4/4 (`_smoke-s7_34.mjs`, sin OTP).

### Directorio (follow-ups)
- ~~Vista global `/admin/lista-espera` cross-médicos con filtros.~~ → ✅ **hecho (PR #120, `s7_41`):** `/admin/lista-espera` con RPC `admin_list_waitlist` (cross-médicos, filtros estado/médico/especialidad/búsqueda/fecha, paginación, `total_count`), reutiliza `admin_update_waitlist_entry`. Smoke 10/10.
- Sección "Lista de espera" en panel del médico (RLS ya permite, falta UI).
- Rate limit fuerte para tráfico público real (Cloudflare o counter por IP).
- Notificación automática al activar `booking_enabled` (Edge Function + Twilio).

### Admin SaaS restantes (post-piloto)
- B4: disponibilidad/horarios desde admin.
- C: suspender pacientes/asistentes.
- D: dashboard tracción mensual.
- E: UI sobre `admin_review_traceability` + moderar reseñas.
- F: explorador de `audit_log` con filtros.
- G: catálogos globales + onboarding manual de médico.

### Pendientes prioritarios a conservar (snapshot 2026-05-27)
Backlog vivo de prioridad alta a revisar tras el smoke de afiliación:
- **Paciente Global / DUI**: DUI obligatorio o progresivo + identidad única del paciente (continúa `docs/ANALISIS_PACIENTE_GLOBAL.md` Fase 2+).
- **Ubicación estructurada Departamento/Municipio** en admin médico — ✅ corregido (PR #64, `s7_25`): la ficha admin (Editar médico > Clínica) edita Depto/Municipio con las mismas listas jerárquicas del Home, guarda IDs en `clinics.department_id/municipality_id` (lo que filtra el Home), municipio depende del departamento, y son obligatorios para publicados/operativos (enforcement server-side P0004 + UI). Sin lat/lng.
- **Filtro "Ordenar por" del Home** — ✅ corregido: la opción muerta "Más cercanos" se removió (no había lat/lng ni geolocalización; caía al orden default). Quedan "Disponibilidad/Mejor coincidencia" (orden server: booking_enabled→verified→created_at) y "Mejor valorados" (score ajustado), ambas funcionales. **Backlog:** reintroducir "Más cercanos" cuando exista lat/lng en `clinics` + ubicación del usuario + UX de permisos de geolocalización (no proxy por municipio — no es cercanía real).
- **Paginación / carga dinámica del Home** cuando haya muchos médicos (hoy carga todos).
- ~~**Mi equipo / invitados del médico** — máximo inicial **2 asistentes**~~ → ✅ **hecho (PR #70, `s7_27`)**: límite base 2 activo, conteo activos+pendientes, enforcement server-side (trigger + accept revalida), UI con contador `n/2`. Usuarios adicionales = add-on, pendiente de la fase de pagos. **Fase 2 (ciclo de vida: expiración/reenvío de invitaciones) ✅ hecha (PR #102 `s7_36` + #103 UI).**
- ~~**Catálogos personalizados por médico** para diagnósticos y medicamentos~~ → ✅ **EJE 100% COMPLETO** (PRs #105–#111 + **#114** + migración de datos, `s7_37`/`s7_38`/`s7_39`). Diseño en `docs/ANALISIS_CATALOGOS_PERSONALIZADOS.md`. Modelo global (base Lucy) + personal por médico; snapshot histórico; ocultar/clonar globales; base replicada migrada (Base Lucy 127/736, "Míos" solo lo propio). **✅ Último cabo cerrado — UI de LucyAdmin para administrar la Base Lucy global (PR #114, UI-only, sin migración):** página **`/admin/catalogos`** (gate `AdminOnlyRoute`) + NavLink "Catálogos" en `AdminLayout`; tabs **Diagnósticos + Medicamentos** (Antecedentes fuera); buscar · activos/inactivos · crear · editar · inactivar/reactivar · dedup amable (`DuplicateCatalogError` + `23505`, refleja UNIQUE `s7_39`); `usage_count` oculto; **no hard-delete**; candado `.is('doctor_id', null)` en updates. Servicio `src/services/adminCatalog.service.ts` (directo sobre tablas con sesión admin; sin RPCs nuevas). Backend ya estaba listo: RLS `s7_38` (`is_admin` CRUD globales) + `audit_catalog` + UNIQUE `s7_39` + snapshot `s7_37`. **Modelo final:** LucyAdmin administra globales; médicos usan/ocultan/clonan y crean propios; médicos no editan globales (RLS). Validado visualmente por el owner.
- **Flujo legal/comercial de DUI médico, términos y aceptación formal** pre-verificación (`docs/PLAN_AFILIACION_MEDICO.md §11.bis`).
- **Manejo de email en afiliación/onboarding médico** (override ya existe en Fase 2; falta self-service de cambio post-reclamo, Fase Auth post-piloto).
- ~~**Error handling en PanelLayout / useClinicContext**~~ → ✅ **cerrado (PR #116).** Causa raíz del fallo de primera carga identificada y resuelta: era un **race de sesión** (`useClinicContext` validaba por red con `getUser()` mientras el token se hidrataba). Ahora lee con `getSession()` (misma primitiva que `getCurrentAuthUser`). `PanelLayout` ya no puede quedar en **spinner infinito** por fallo de `getCurrentAuthUser()` (timeout 8s + `.catch()` + estado recuperable con Reintentar/Cerrar sesión). Errores tipados con copy específico para estructurales (`no_clinic`/`no_doctor`/`role`). Frontend-only.

### Otros pendientes conocidos
- **Verificación reforzada de "Sí, son mías" (futuro, decisión owner 2026-06-11):** para MVP/piloto basta confirmación explícita + rechazo + audit (B2). Evaluar después pedir un dato adicional de la ficha (DOB, DUI parcial u otro) para confirmar, especialmente en teléfonos compartidos o mal digitados.
- **Términos/onboarding del paciente (futuro):** al crear o reclamar cuenta, el usuario debe aceptar que solo puede confirmar atenciones propias y que no debe adjudicarse atenciones médicas de terceros. (Documental/legal; aún sin funcionalidad.)
- **Hallazgo F4 confirmado empíricamente (smoke s7_43):** el claim copia el DUI global a las fichas; si la persona ya tiene una ficha con ese DUI en la misma clínica, el `UNIQUE(clinic_id, document_type, document_number)` aborta el claim COMPLETO (todas las clínicas, silencioso por el fail-safe del login). Ya estaba anotado como consideración de Fase 4 (merge/dedup admin); ahora con reproducción concreta.
- **Identidad múltiple Fase 2** (selector "modo paciente / modo médico" + capacidades derivadas + revisar si `claim` deja de sobrescribir el rol) — diseño en `docs/ANALISIS_ONBOARDING_IDENTIDAD_MULTIPLE.md`.
- **`accept_clinic_invitations` compara teléfono literal** (`+503…` ≠ `503…`): una invitación guardada con `+` no matchea un profile sin `+`. En el flujo de la app no se manifiesta (formatos coinciden), pero es candidato a normalizar con `normalize_phone_sv` (hallazgo de QA Fase 1).
- **⚠ OPERATIVO pendiente — registro de Katherine (`50372608827`):** decidir **conservar/corregir o limpiar** ese registro (QA de `s7_42`). Si se conserva: rehacer el nombre desde `/admin/medicos/4bb793d0-44b1-4782-8896-6bf01cd627aa` > Perfil y **verificar que persista** (el intento anterior NO llegó a la DB — sin audit, `updated_at` pre-fix; si se repite, investigar como bug de la ficha). Si se limpia: **solo cuando el owner lo indique**, con dry-run/inventario previo (auth.user, profile, doctor/clinic/membership, lead, fichas, audit) antes de borrar nada.
- S5-01 Estilo de vida (tabaco, alcohol, METs, sueño, alimentación).
- S5-08 SMS automático al invitar asistente.
- Vista `/panel/consultas` global.
- Borradores pendientes de firmar en panel home.
- Vistas históricas del paciente (laboratorios, PDFs).
- Reportes/estadísticas para el médico.
- Extraer modales/diálogos legacy a `src/components/` (refactor).

## Deudas técnicas conocidas (priorizadas)

1. ~~**Asistente puede firmar consulta**~~ → ✅ **cerrado / reformulado (PR #67, `s7_26`).** El diagnóstico RLS (2026-06-01) mostró que la premisa era **inexacta**: `consultations`/`prescriptions`/`consultation_diagnoses` (y la **firma**) ya estaban bloqueadas para asistentes vía `doctor_id = get_user_doctor_id()` (= NULL para asistente). La fuga real estaba en **`consultation_family_history` y `vitals`** (usaban `is_clinic_member()`, true para cualquier miembro) → cerradas en `s7_26` (doctor-scoped). Validado con smoke empírico (asistente no lee ni escribe; médico dueño sin regresión).
2. **Inmutabilidad + corrección controlada de consultas firmadas** — ✅ **EJE COMPLETO (backend + UI) para los 5 bloques clínicos: texto, receta, diagnósticos, antecedentes familiares estructurados y signos vitales.** Etapa A (PR #74, `s7_28`): edición silenciosa por API cerrada (firma vía RPC `sign_consultation()` + RLS endurecida `signed_at IS NULL`). Etapa B1 (PR #76, `s7_29`): corrección controlada — `consultation_amendments` append-only, **motivo obligatorio**, snapshots before/after (JSONB), **versionado de recetas** (anterior `is_current=false` histórica + v2 vigente), **solo médico dueño**, campos prohibidos rechazados (`P0013`), audit, vía RPC `amend_consultation()` (definer + bypass `app.amending`). PR #79: lecturas/impresión de recetas filtran `is_current=true`. PR #80: marca de impresión "RECETA CORREGIDA · fecha" + "Corrección vN" por medicamento. PR #81 (`s7_30`): columna `affects_prescriptions` → la marca se dispara **solo si la corrección tocó receta** (no por texto/diag/antecedentes/vitales). PR #82: UI texto + receta. PR #84 (`s7_31`): backend de **diagnósticos / antecedentes / vitales** (RPC extendida con `p_diagnosis_ops` / `p_family_history_ops` / `p_vitals_changes`, corrección in-place trazada por el snapshot) + **cierre del hueco de inmutabilidad de `vitals`** (RLS rechaza editar vitales de cita con consulta firmada; antes era editable por API directa). Validado con smoke OTP 11/11. PR #85: **UI completa** — modal "Corregir consulta firmada" en accordion con las 5 secciones, "Cambiar diagnóstico", IMC en vivo, **resumen unificado** de cambios (reemplaza el reforzado solo-receta), + fix del dropdown del `Combobox` (portal `fixed`, ya no lo recorta el modal/accordion) + impresión de receta en **2 columnas** para 3+ medicamentos (A4 vertical, sin 2ª página innecesaria). UPDATE directo de firmada sigue bloqueado; asistente también. Análisis `docs/ANALISIS_CORRECCIONES_CONSULTA_FIRMADA.md` + plan `docs/PLAN_CORRECCIONES_FASE0.md`.
3. **Inconsistencia visual** pantallas legacy (PacientesPage, AppointmentDetailPanel, BloqueosPage) — no usan `<Button>` reusable.
3. **Sin sistema global de toasts** — `friendlyErrorMessage` ayuda, pero algunos flujos siguen sin feedback.
4. **Print de receta minimalista** — sin logo de clínica, sin QR.
5. **No hay vista `/panel/consultas`** — solo se accede via cita o paciente.
6. **WaitlistModal** todavía tiene el patrón legacy de click-outside-cierra; no es bloqueante (ya tiene UX correcta), pero por consistencia con LoginModal valdría hardening idéntico cuando se toque.

## Datos en DB (snapshot 2026-05-27)

- ~113 doctores totales. 5 publicados (4 informativos + 1 con agenda en línea).
- 12 seed `*.lucycare.test` desactivados (`is_active=false`).
- 14 departments, 262 municipalities con `district` (división 2024).
- 20+ especialidades.
- 10 cancel_reasons pre-cargadas.
- ~1651 diagnósticos + ~9568 medicamentos en catálogos per-doctor.
- ~22 pacientes en `patients`. Mismo phone puede aparecer en N clínicas (modelo legacy tenant-aware, en proceso de evolución a global — Fase 1 lo vincula vía profile_id).
- ~54 appointments, ~15 consultations.
- waitlist_entries: tabla activa.

## Migraciones SQL aplicadas

Todas corridas en Supabase. Cada `s6_*`/`s7_*` con `check-*.mjs` cuando aplica.

**Sprint 4-5:** `s4_01..s4_02`, `s5_01..s5_07`.

**Sprint 6 (reputación):** `s6_01..s6_10`.

**Sprint 7 (admin + robustez):**
- `s7_01` admin foundation.
- `s7_02` admin doctors.
- `s7_03` is_verified GENERATED.
- `s7_04` admin search/paginate.
- `s7_05` admin edit doctor.
- `s7_06` fix clinic update.
- `s7_07` clinics updated_at.
- `s7_08` services delete policy.
- `s7_09` block inactive service.
- `s7_10` patient document nullable.
- `s7_11` normalize patient phone.
- `s7_12` admin services RPCs.

**Pre-piloto (PRs #32+):**
- `s7_13` reclamo seguro — RPC `claim_doctor_profile`.
- `s7_14` foto de perfil — bucket avatars + RLS + RPC admin + trigger audit.
- `s7_15` audit log trigger para reviews.
- `s7_16` + `s7_16b` RLS hardening de profiles (column-level + drop policies legacy).
- `s7_17` desactivación seed `*.lucycare.test`.
- `s7_18` waitlist_entries — tabla + RPC público + RPCs admin + triggers audit.
- `s7_19` bulk count waitlist por doctor.
- `s7_20` Paciente Global Fase 1 — RPC `claim_patient_records` + policies SELECT-self.
- `s7_32` Paciente Global Fase 2 — columnas de identidad en `profiles` (DUI/DOB/género/dpto/muni) + CHECK de `document_type` + regla number⇒type + UNIQUE parcial de documento + trigger coherencia muni-depto + GRANT UPDATE column-restricted + trigger audit de identidad.
- `s7_33` Paciente Global Fase 3 — guard BEFORE UPDATE en `patients` (bloquea identidad local si `profile_id` set, P0030) + sync AFTER UPDATE `profiles→patients` (espejo, bypass GUC) + `claim_patient_records` copia identidad global→local.
- `s7_34` Cambio de teléfono por OTP — trigger `AFTER UPDATE OF phone ON auth.users` (`sync_phone_to_identity`, definer) que sincroniza `profiles.phone` + `patients.phone` vinculados (bypass del guard de `s7_33`) + audit `edited_via='phone_change'`.
- `s7_35` Paciente Global Fase 5 — RPC `find_patient_match_candidates(p_clinic_id, p_phone, p_document_type, p_document_number)` (`SECURITY DEFINER`, `STABLE`) para dedup preventivo en creación: intra-clínica con detalle, cross-clínica/global solo señal mínima (`weak`/`strong`, sin PII), gate `is_clinic_member`/`is_admin` (`P0001`).
- `s7_36` Mi equipo Fase 2 — ciclo de vida de invitación: `invitation_ttl()` (14d), `clinic_invitations.expires_at` (backfill + default + NOT NULL), `team_seats_used()` excluye vencidas, `accept_clinic_invitations()` ignora vencidas, RPC `resend_invitation(p_invitation_id)` (titular; revalida cupo si vencida; extiende la misma fila).
- `s7_37` Catálogos — snapshot histórico: columnas `*_snapshot` en `prescriptions` (medication_name/active_ingredient/concentration/presentation) + `consultation_diagnoses.diagnosis_name_snapshot` + triggers `BEFORE INSERT` que copian del catálogo + backfill. Lectura/impresión usan snapshot (fallback al JOIN).
- `s7_38` Catálogos — global+personal: `diagnoses`/`medications.doctor_id` NULLABLE (NULL=global) + `doctor_catalog_hidden` + RLS reconstruida (propios∪globales∪admin / escritura solo propios / globales solo admin / sin DELETE) + trigger `audit_catalog`.
- `s7_39` Catálogos — infra de-dup: `catalog_migration_map` (mapeo old→global, reversibilidad) + UNIQUE parcial sobre globales (`diagnoses(lower(name))`, `medications(combo)` WHERE `doctor_id IS NULL`). Migración de datos = `scripts/migrate-catalog-base.mjs --apply` (aplicada).
- `s7_40` Paciente Global Fase 5 — RPC F5 tolerante a teléfono crudo (defensa en profundidad): helper `normalize_phone_sv(text)` (`IMMUTABLE`, espejo de `src/lib/phone.ts::normalizePhoneSV`, sin EXECUTE para anon/authenticated) + `CREATE OR REPLACE` de `find_patient_match_candidates` aplicando el helper a ambos lados de cada comparación de teléfono (input y filas). Firma/salida idénticas, cross-clínica sin PII. Checks: `check-s7_40.mjs` + `_smoke-s7_40.mjs` (11/11).
- `s7_41` Lista de espera global cross-médicos — RPC `admin_list_waitlist(p_status, p_doctor_id, p_specialty_id, p_search, p_date_from, p_date_to, p_limit, p_offset)` (`SECURITY DEFINER`, gate `is_admin()`): join a `doctors`/`profiles`/`specialties`/`clinics`, búsqueda por nombre paciente∪teléfono(`phone_normalized`)∪nombre médico, `date_to` inclusivo (`< date_to + 1 día`), `total_count` con `count(*) OVER()`. Reutiliza `admin_update_waitlist_entry` para el cambio de estado. Checks: `check-s7_41.mjs` + `_smoke-s7_41.mjs` (10/10).
- `s7_42` Afiliación — médico desde paciente existente: helper interno `_affiliation_classify(phone, email)` (usa `normalize_phone_sv`; clasifica `new`/`reuse_patient`/`block_doctor`/`block_sensitive`/`block_identity_conflict`; `auth.user` sin profile → `block_sensitive`) + RPC lectura `admin_affiliation_preflight(p_request_id)` + `CREATE OR REPLACE admin_approve_and_create_doctor` (firma idéntica) que re-clasifica en la transacción: `new` crea auth.user dormant (path s7_24); `reuse_patient` reusa el profile (sin nuevo auth.user, `full_name` si vacío, sin tocar phone/email/role, solo con `confirm_reuse`); bloqueos `P0010`/`P0011`/`P0012`; `P0013` requiere confirmación. Audit `reused_existing_user`. **Decisión: en reuse no se copia el email del lead.** Checks: `check-s7_42.mjs` + `_smoke-s7_42.mjs` (12/12).
- `s7_43` B2 Confirmación post-claim — `patients.link_confirmed_at` + tabla `patient_link_rejections` (`pending_review|resolved`, RLS admin-only, UNIQUE par ficha↔profile) + RPCs self-gated `confirm_patient_link`/`reject_patient_link` (P0040) + `claim_patient_records` reescrita excluyendo pares rechazados (conserva la copia de identidad global→local de s7_33). Checks: `check-s7_43.mjs` + `_smoke-s7_43.mjs` (14/14).
- `s7_44` Administración de LucyAdmins Fase 1 — tabla `platform_admin_invitations` (pending/active/revoked, TTL 14d, `invited_by/at`, `activated_at/profile`, `revoked_by/at`, UNIQUE parcial pending por teléfono, RLS admin-only SELECT) + `admin_invite_platform_admin` (cuenta dedicada: P0050 paciente real/médico/asistente, P0051 admin, P0053 vigente, P0055 inválido; vencida se refresca in-place; cuenta-cáscara con historial re-invitable) + `accept_platform_admin_invitation` (activación al primer OTP, pending sin privilegios, fill `full_name` si vacío, bloquea cuentas con fichas vinculadas) + `admin_revoke_platform_admin` (rol→patient, bloqueo inmediato, guard último admin P0052) + `admin_list_platform_admins`. `is_admin()` intacta. Checks: `check-s7_44.mjs` + `_smoke-s7_44.mjs` (24/24).
- `s7_47` Paciente Global Fase 4 / F4-3-search — RPC `admin_list_patient_merge_candidates()` (`SECURITY DEFINER`, `STABLE`, gate `is_admin()`/`P0001`, anon sin EXECUTE; **READ-ONLY**: solo SELECT/agregación, no merge/escritura/audit). Lista **grupos** candidatos a merge **V1 `same_profile`** (E3): agrupa por `(clinic_id, profile_id)` con `profile_id IS NOT NULL`, contando solo fichas vivas (`is_active = true AND merged_into_patient_id IS NULL`) y `HAVING count(*) >= 2`. Por grupo: `clinic_id`/`clinic_name`/`profile_id`/`profile_full_name`/`patient_count` + `confirmed_links_count`/`unconfirmed_links_count`/`has_unconfirmed_links` + `warnings` (`W_UNCONFIRMED_LINK` si alguna ficha tiene `link_confirmed_at IS NULL` — ayuda, no bloqueo). Por ficha: identidad mínima (nombre/documento/DOB/género/teléfono/`is_active`/`link_confirmed_at`/`created_at`) + `counts` (appointments/consultations/vitals). **NO** contenido clínico, **NO** candidatos por teléfono/nombre/walk-in/fuzzy/override, **NO** merge, **NO** toca identidad global/`profiles`/`auth.users`. Checks: `check-s7_47.mjs` (2/2) + `_smoke-s7_47.mjs` (10/10) / PR #140.
- `s7_46` Paciente Global Fase 4 / F4-2 — backend del merge admin de fichas intra-clínica: tabla `patient_merge_log` (source/target/clinic/merged_by/reason + `evidence` + `source_snapshot`/`target_snapshot` + `moved_counts` + `moved_ids` + `unmerge_*` previstos; RLS admin-only SELECT) + `CREATE OR REPLACE` de los dos guards para reconocer el bypass propio `app.merging_patients` (firma `s7_29` + identidad P0030 `s7_33`, sus bypass previos intactos) + helper interno `_patient_merge_eligibility` (read-only, orden: estructura→identidad/dirección→evidencia→borrador) + `admin_merge_patients_preflight` (dry-run, gate `is_admin`) + `admin_merge_patients` (transaccional, `FOR UPDATE`, revalida server-side, re-point appointments/consultations/vitals, neutraliza fuente — `document_number→NULL`/`profile_id→NULL`/`is_active=false`/`merged_into_patient_id`/`merged_at`, teléfono conservado, no hard-delete — + audit `admin_merge`). **V1 = `same_profile`** (E3); same_document inalcanzable intra-clínica por el `UNIQUE(clinic,doc)`. Bloqueos P0060–P0068; borrador en fuente bloquea (P0067), en target solo warning. Sin UI/unmerge/identidad global/`auth.users`. Checks: `check-s7_46.mjs` (6/6) + `_smoke-s7_46.mjs` (19/19) / PR #138.
- `s7_45` Paciente Global Fase 4 / F4-1 — claim tolerante fila por fila: columnas **pasivas** `patients.merged_into_patient_id` (FK `patients(id)` **sin ON DELETE CASCADE** + CHECK `patients_merged_into_not_self` = `merged_into_patient_id IS NULL OR <> id`) + `patients.merged_at` (infra de F4-2; **nada las escribe en F4-1**) + `CREATE OR REPLACE claim_patient_records()` que reemplaza el UPDATE multi-fila por un **LOOP fila por fila con bloque `BEGIN/EXCEPTION`** (subtransacción por ficha): una colisión `unique_violation` ya **no aborta el claim completo** → skip `unique_conflict`; cualquier otro error de fila → skip `error`; ambos auditados con `SQLSTATE` (fila por ficha `claim_skipped_unique`/`claim_skipped_error` con `record_id` = la ficha + fila resumen con `skipped[]`); errores estructurales fuera del loop (28000, etc.) se siguen propagando; respuesta al cliente = **solo conteos** `{success, linked_count, skipped_count[, reason]}`; el claim **excluye** fichas con `merged_into_patient_id IS NOT NULL`. Firma/nombre/GRANTs intactos; exclusión de pares rechazados (`s7_43`) y copia COALESCE de identidad (`s7_33`) conservadas. **No hace merge, no toca `profiles`/`auth.users`, no resuelve duplicados, no hard-delete.** `database.types.ts` actualizado (2 columnas + FK). Checks: `check-s7_45.mjs` (5/5) + `_smoke-s7_45.mjs` (16/16) / PR #135.

## Scripts utilitarios (`/scripts/`)

**Setup** (cada máquina, una vez):
- Copiar `.env.local.example` → `.env.local` con `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.
- Los scripts leen de ahí automáticamente vía `scripts/_lib/env.mjs`.

**Helpers compartidos:**
- `scripts/_lib/env.mjs` — carga `.env.local`.
- `scripts/_lib/supabase-admin.mjs` — cliente service_role.
- `scripts/_lib/supabase-anon.mjs` — cliente anon.

**Comandos típicos:**
- `node scripts/check-s7_NN.mjs` — verifica una migración (NN del 13 al 20).
- `node scripts/import-doctors.mjs --file ... --apply` — importador idempotente.
- `node scripts/_deactivate-demos.mjs` — desactiva demos, preserva Camilo.
- `node scripts/setup-test-patient.mjs --apply|--reset|--clean` — datos de prueba paciente global.
- `node scripts/check-patient-documents.mjs` — diagnóstico DUIs.

`scripts/README.md` tiene la guía completa.

## Cómo crear una asistente

Desde `/panel/equipo` → "Invitar asistente" → teléfono → la asistente se loguea con OTP y queda activa automáticamente vía RPC `accept_clinic_invitations`. SMS automático al invitar **no está implementado** (S5-08, en backlog).

## Tags y commits importantes

```
63cf393 feat(analytics): backend del dashboard de conversiones (Fase 3 PR-A / s7_53) (#245)
9eba653 fix(seguridad): el idle-logout sobrevive a reapertura/kill de pestaña (persistencia) (#239)
b5780dd chore(cleanup): eliminar el scaffold anidado lucycare-code/ (dead code) (#204)
817e63c chore(cleanup): eliminar el flujo legacy de registro médico (dead code) (#203)
247abc2 chore(cleanup): eliminar svLocations.ts (catálogo SV hardcodeado, dead code) (#202)
427effe chore(branding): migrar el logo a asset local (fuera de static.readdy.ai) (#201)
d0efa5c style(branding): unificar el púrpura público #3C2285 a emerald (#200)
fc3249e feat(perfil): copy "Verificado por LucyCare" en el badge de confianza (#199)
0225e87 docs: análisis de credenciales médicas JVPM/NUE + modelo doctor_credentials (#198)
7f55f6a fix(perfil): no enviar license_number en el payload público del médico (#197)
7b4dc72 feat(perfil): subir foto desde galería + mostrar avatar en perfil público (#196)
e8c4252 docs: §14 pricing configurable / modelo comercial aprobado (pagos SaaS) (#195)
0606c7f docs: refresh del análisis de pagos SaaS a estado actual (post-#193) (#194)
e048ee4 fix(afiliacion): placeholders claros en Departamento/Municipio del modal (#193)
7d30127 fix(perfil): mapa condicional — evitar mapa de ciudad/país con falsa precisión (#192)
bce10a2 feat(bloqueos): selector de hora propio (selects) en vez del picker nativo (#191)
6501fb2 fix(bloqueos): el botón "Congreso/Capacitación" ya no desborda en móvil (#190)
568be99 docs: reconciliar referencia de handoff vigente en CLAUDE.md (#189)
11cdaf4 docs: cierre de #187 — card "Reclamar perfil" visible por viewer (#188)
ceb7116 fix(perfil): card "Reclamar perfil" visible por viewer en perfiles listed_only (#187)
c78550f docs: quitar ítem fantasma del backlog — no existe duplicado src/pages/Home/ (#186)
4a09c7b docs: cierre del frente "Perfil público / visibilidad comercial" (#180–#184) (#185)
0258328 fix(perfil): limpieza del perfil público — footer + botones muertos (#184)
cb140eb fix(perfil): móvil más liviano — reservar desde la barra fija + copy honesto (#183)
ab34c7b fix(home): limpieza de copy/footer público — honesto y sin residuos de plantilla (#182)
6b2017c fix(directorio): contador honesto del Home (publicados / con agenda en línea) (#181)
e206676 fix(directorio): CTA "Reservar cita" en la tarjeta de médicos reservables (#180)
446a4c4 feat(panel): tarjeta "Primeros pasos" en el dashboard médico (Onboarding PR-2) (#178)
6f251f3 feat(panel): read model de activación del médico (Onboarding PR-1) (#177)
d1314b7 docs: cierre del frente "Resumen rápido del paciente" (#174–#175) (#176)
053346d feat(consulta): tarjeta "Resumen rápido del paciente" (PR-2) (#175)
13fb54d feat(consulta): read model del resumen rápido del paciente (PR-1) (#174)
b318050 docs: consolidación de la serie correcciones clínicas #167–#172 (#173)
409f608 feat(consulta): corrección post-firma de antecedentes texto libre (PR-E2) (#172)
a54d6f4 feat(consulta): antecedentes familiares como texto libre (PR-E1) (#171)
3f52a69 feat(consulta): peso en libras con almacenamiento interno en kg (PR-D) (#170)
9f1e5cd feat(consulta): orden de receta en el modal de corrección (PR-C) (#169)
5a3fbe9 feat(consulta): diagnósticos sin Tipo/Estado en la UI (PR-B) (#168)
e6f4cac fix(receta): PDF limpio — Dr. Dr., fechas duplicadas, dosis rotulada (#167)
352b44a feat(panel): "Mi lista de espera" para médico/asistente (s7_51) (#163)
2309766 docs: handoff de nueva ventana 2026-06-21 + estado post-#162 / PR #163 abierto (#164)
4ee9b65 feat(home): render incremental del directorio — Mostrar más médicos (Opción A) (#162)
c8fe622 docs: consolidación a #160 — responsive móvil + seguridad de sesión (#161)
967e254 feat(seguridad): cierre de sesión por inactividad + tope absoluto por rol (#160)
19a597f feat(admin): UI de fusión de fichas — vista previa read-only (F4-3 PR A) (#142)
84d6831 docs: refresh documental a #140 — F4-3-search RPC de candidatos (s7_47) (#141)
c4dbc2a feat(paciente): RPC de candidatos a merge (F4-3-search / s7_47) (#140)
f26e208 docs: refresh documental a #138 — F4-2 backend del merge (s7_46) (#139)
c559fd7 feat(paciente): backend del merge admin de fichas (F4-2 / s7_46) (#138)
01c6150 docs: refresh documental a #136 — F4 diseño + F4-1 (s7_45) + fix reserva móvil (#137)
43bd0e9 fix(reserva): flujo real de reserva en móvil + fecha por defecto + carga (#136)
aaf891b feat(paciente): claim tolerante fila por fila (F4-1 / s7_45) (#135)
289599e docs: diseño de Paciente Global Fase 4 — merge admin de duplicados, DM1-DM9 cerradas (#134)
a1e0adf docs: handoff de nueva ventana — cierre a #132/s7_44 (admin LucyAdmins F1) (#133)
22bef3b feat(admin): administración de LucyAdmins — Fase 1 (s7_44) (#132)
8d13ee5 docs: análisis de administración de LucyAdmins (alta/ciclo de vida/revocación) (#131)
ea6541c feat(admin): navegación móvil en LucyAdmin — header + drawer (UX) (#130)
b594c7b docs: cierre de B2 — confirmación post-claim (PR #128 / s7_43) (#129)
ef94d99 feat(paciente): confirmación post-claim de fichas vinculadas (B2 / s7_43) (#128)
46ce5db docs: análisis de ownership del paciente — "el paciente es de Lucy" (#127)
1945588 docs: cierre de Identidad múltiple Fase 1 (PR #125) (#126)
a375989 feat(identidad): médico/asistente acceden a su lado paciente (Fase 1) (#125)
f959351 docs: análisis de onboarding médico/paciente + identidad múltiple (#124)
b6c36a1 docs: cierre de afiliación — médico desde paciente existente (PR #122 / s7_42) (#123)
4752274 feat(afiliacion): crear médico cuando el teléfono ya es de un paciente (s7_42) (#122)
57e30b4 docs: cierre de lista de espera global cross-médicos (PR #120 / s7_41) (#121)
5e27d38 feat(admin): lista de espera global cross-médicos (/admin/lista-espera, s7_41) (#120)
c98828a docs: cierre de RPC F5 tolerante a teléfono crudo (PR #118 / s7_40) (#119)
68e3937 feat(paciente): RPC F5 tolerante a teléfono crudo (s7_40) (#118)
132c42f docs: cierre de robustez PanelLayout/useClinicContext (PR #116) (#117)
b4fd300 fix(panel): robustez de carga de contexto del panel (F1+F2+F3) (#116)
cd93513 docs: cierre de UI de LucyAdmin para Base Lucy global (PR #114) (#115)
3946ff1 feat(admin): UI de LucyAdmin para administrar Base Lucy global (#114)
b8b7dee docs: cierre del eje Catálogos personalizados por médico (PRs #105–#111 + migración) (#112)
f4ae553 fix(catalogos): índice global de medicamentos sin cast enum→text (s7_39) (#111)
54c6522 feat(catalogos): infra + dry-run de migración de base replicada (PR-5a) (#110)
673b6ba feat(catalogos): UI admin global + personal por médico (PR-4) (#109)
aa1a1c7 feat(catalogos): consulta busca global + propios con etiquetas (PR-3 UI) (#108)
e489be0 feat(catalogos): modelo global + personal por médico (PR-2 / s7_38) (#107)
5218cf2 feat(catalogos): snapshot histórico de catálogo en receta/diagnóstico (PR-1 / s7_37) (#106)
6965e33 docs: diseño de Catálogos personalizados por médico (PR-0) (#105)
c3a2f0e docs: cierre de Mi equipo Fase 2 — ciclo de vida de invitación (PRs #102/#103, s7_36) (#104)
3fa1604 feat(equipo): UI de ciclo de vida de invitación — expiración + reenvío (Mi equipo F2 / PR-2) (#103)
faac8a1 feat(equipo): ciclo de vida de invitación — expiración + reenvío (Mi equipo F2 / s7_36) (#102)
02798eb docs: cierre de Paciente Global Fase 5 — dedup preventivo (PRs #98/#99/#100, s7_35) (#101)
bede2df feat(paciente): dedup preventivo en creación de paciente/walk-in (F5 / PR-2 UI) (#100)
766b11f feat(paciente): dedup preventivo backend — find_patient_match_candidates (F5 / s7_35) (#99)
08bafd8 docs: diseño de Paciente Global Fase 5 — dedup preventivo en creación (PR-0) (#98)
fa54f8c docs: handoff de cierre de ventana — sync de HANDOFF_SPRINT7 a s7_34 / PR #96 (#97)
1bf62c7 docs: cierre/handoff de ventana — Cambio de teléfono por OTP (PRs #93/#94/#95, s7_34) (#96)
1bd6c9f feat(medico): cambiar teléfono por OTP en /panel/perfil (F-D) (#95)
0cf0bc5 feat(paciente): cambiar teléfono por OTP en /paciente/perfil (F-C) (#94)
e633ff2 feat(auth): sync de cambio de teléfono por OTP — backend (F-B / s7_34) (#93)
be22e71 docs: cierre de Paciente Global Fase 3 — PRs #90/#91 (s7_33) (#92)
2265226 feat(paciente): identidad read-only del paciente vinculado en panel médico (F3.2) (#91)
dafbfde feat(paciente): sync espejo profiles→patients + guard de identidad (F3.1 / s7_33) (#90)
c7acc54 docs: cierre de Paciente Global Fase 2 — PRs #87/#88 (s7_32) (#89)
399452c feat(paciente): página /paciente/perfil + banner — Paciente Global F2.2 (#88)
7a7e38b feat(paciente): identidad global en profiles — Paciente Global F2.1 (s7_32) (#87)
9ca3c08 docs: cierre FINAL del eje Correcciones post-firma — PRs #84/#85 (s7_31) (#86)
b066967 feat(consulta): corrección de diagnósticos/antecedentes/vitales en el modal (B1.5-b) (#85)
9a9b3de feat(clinico): amend diag/antecedentes/vitales + inmutabilidad vitals (B1.5-a / s7_31) (#84)
f22bbed docs: cierre del eje Correcciones post-firma (texto + receta) — PRs #79–#82 (#83)
2a878f1 feat(consulta): corrección de consulta firmada — texto + receta (B2, Opción A) (#82)
c3bba6d feat(receta): distinguir adenda de texto vs receta para la marca de impresión (B3.1 / s7_30) (#81)
596aaf1 feat(receta): marca de impresión para receta corregida (PR-3 / B3) (#80)
432ef91 fix(receta): filtrar is_current=true en lecturas/impresión de recetas (PR-1) (#79)
5775212 docs: handoff de cierre — contexto consolidado para nueva ventana (#78)
611acf8 docs: refresh post-#76 — Etapa B1 corrección controlada de consultas firmadas (#77)
da5bbab feat(clinico): correcciones post-firma — Etapa B/B1 (adendas + amend_consultation) (#76)
cc71e0b feat(clinico): inmutabilidad server-side de consultas firmadas (Etapa A) (#74)
0abaf23 feat(equipo): límite de 2 asistentes incluidos (Mi equipo Fase 1) (#70)
9f6c6bf feat(security): gate clínico del rol asistente — RLS doctor-scoped (Fase 0) (#67)
1a2cc28 docs: análisis de Mi equipo / invitados del médico (#66)
ed5721f feat(admin): ubicación estructurada (Depto/Municipio) en la ficha del médico (#64)
685f0ec fix(directorio): quitar opción muerta "Más cercanos" del orden del Home (#63)
c87e189 docs: análisis de pagos SaaS autoservicio (suscripción del médico) (#62)
cda1556 docs: handoff de ventana — Afiliación Fase 2 + smoke end-to-end completado (#60)
b2decba fix(afiliacion): Fase 2 — role=patient pre-claim + precarga ubicación + panel copy (#61)
8ec813c docs: refresh post-PR #58 — Afiliación Fase 2 live (#59)
f427ed3 feat(afiliacion): Fase 2 — convertir lead aprobado en doctor listed_only (#58)
c69ea91 docs: refresh post-PR #56 — Afiliación Fase 1 live (#57)
6046d6c feat(afiliacion): Fase 1 — captura de leads + bandeja admin (#56)
269131e docs: plan operativo del flujo Solicitar afiliación médica (#55)
1bd9774 docs: refresh post-PR #53 — mitigación flujo "Soy médico" (#54)
f7fa227 fix(security): neutralizar flujo "Soy médico" — reemplaza por captura de interés (#53)
e1db56c docs: análisis del flujo "Solicitar afiliación" del médico (#52)
e66be15 docs: refresh post-PR #50 — Fase 4 PR-B completada (#51)
a2b1f96 feat(auth): password en flujo de Reclamar perfil (Fase 4 PR-B) (#50)
813c2a2 docs: refresh post-PR #48 — lucycare.app como dominio público principal (#49)
d61ae3f chore(infra): configurar dominio público lucycare.app (#48)
518b84e docs(project): registrar SMTP externo Resend como configurado (#47)
f7797a5 docs(auth): documentar setup SMTP externo con Resend (#46)
b303270 docs: refresh CLAUDE.md + HANDOFF post-PR #44 (#45)
4c4fef1 feat(paciente): Paciente Global Fase 1 — vinculación retroactiva + Mis atenciones (#44)
2d4c94a feat(directorio): lista de espera real + badge admin (#43)
4225687 docs: análisis del modelo de paciente global + decisiones (#42)
106f5f0 feat(directorio): modelo informativo + copy ES (#41)
7bcc0dd fix(login-modal): no cerrar al click fuera del modal (UX) (#40)
5c09a4a feat(auth): email + password + reset por email (Fase 4 PR-A) (#39)
5438c9b chore(seed): desactivar médicos seed *.lucycare.test (#38)
ea7a3cc feat(security): RLS hardening de profiles (#37)
721e7e4 chore(security): mover service_role + anon a ENV vars (#36)
254ea94 docs(security): Security Gate piloto + audit_log para reviews (#35)
8075853 feat(perfil): foto de perfil opcional post-reclamo (#34)
babde41 fix(supabase): deshabilitar lock global del cliente (no-op) (#33)
b4702b3 feat(reclamo): flujo seguro de "Reclamar perfil" (Fase 2) (#32)
3e28eff docs: snapshot post-PR #30 + análisis para auth/reclamo + plan piloto (#31)
f6a4f32 feat(admin): B3-admin — administración de servicios del médico (#30)
```

Volver a un punto anterior:
```bash
git reset --hard piloto-v1
git push --force-with-lease origin main
```

## Cómo arrancar en un nuevo chat de Claude Code

1. Sincronizar repo + leer este archivo + los docs oficiales del objetivo de hoy.
2. `git log --oneline -10` para ver el último estado.
3. NO asumir nada — leer el código antes de tocarlo.
4. Decisión grande sin consultar = problema. Cuestionar suposiciones.
5. Seguir el sistema de diseño documentado arriba para toda pantalla nueva.
6. Si el objetivo es algo ya analizado (auth, reclamo, paciente global, directorio), leer el `docs/ANALISIS_*.md` correspondiente — **no re-analizar**.
7. Mantener branches con nombres cortos (`claude/<8-12 chars>`) para que Vercel no las trunque.

## Plantilla para arrancar una sesión nueva

```
Continuamos LucyCare.

git fetch origin && git checkout main && git pull --ff-only
git log --oneline -10

Leé en este orden:
1. CLAUDE.md
2. docs/HANDOFF_LUCYCARE_SPRINT7.md
3. [docs/ANALISIS_*.md o docs/FASE_*.md según objetivo de hoy]

Estado: PRs #1–#77 mergeados (HEAD 611acf8), migraciones hasta s7_29 (aplicadas en Supabase). SMTP Resend + dominio `lucycare.app` + Fase 4 PR-B ✅. Afiliación Fase 1+2 + smoke ✅. Ubicación estructurada admin ✅ (PR #64). Gate clínico asistente ✅ (PR #67). Mi equipo Fase 1 límite 2 asistentes ✅ (PR #70). Correcciones post-firma: análisis (#72) + plan (#73) + **Etapa A inmutabilidad ✅ (#74, `s7_28`) + Etapa B1 corrección controlada ✅ (#76, `s7_29`)** → ⏳ B1.5 (diag/antecedentes/vitales), B2 (UI), B3 (impresión receta corregida). Análisis pagos SaaS ✅ doc base (PR #62).

Hoy hacemos: ___[opciones en cola (smoke afiliación ya cerrado):
                   - análisis de pagos SaaS autoservicio;
                   - vista global /admin/lista-espera cross-médicos;
                   - Paciente Global Fase 2 (DUI/DOB/dpto/muni);
                   - DUI + TOS del médico pre-verificación (PLAN_AFILIACION §11.bis);
                   - ubicación estructurada Depto/Muni en ficha admin del médico;
                   - Admin SaaS B4 (disponibilidad desde admin);
                   - smoke 7.3 opcional capacidad SMTP;
                   - etc.]___
```

## Documentos fuente originales (en raíz)

- `LucyCare_Schema_DB.docx`
- `LucyCare_Backlog_Estrategico.docx`
- `LucyCare_DocTecnico_Completo.docx`
- `Notas_Sprint3.docx`
